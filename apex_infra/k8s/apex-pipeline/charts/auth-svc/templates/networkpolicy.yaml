# Copyright (c) 2026 Gazuua. All rights reserved. Licensed under the MIT License.
{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "apex-common.fullname" . }}
  labels:
    {{- include "apex-common.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "apex-common.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Prometheus metrics scrape only — no inbound app traffic
    - ports:
        - port: 8082
          protocol: TCP
  egress:
    # Kafka (consume/produce)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: apex-infra
      ports:
        - port: 9092
          protocol: TCP
    # Redis (auth session cache)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: apex-infra
      ports:
        - port: 6379
          protocol: TCP
    # PgBouncer (database)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: apex-infra
      ports:
        - port: 6432
          protocol: TCP
    # DNS resolution
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
{{- end }}
