# 테스트 견고성 번들: BACKLOG-118 + BACKLOG-119

## 결과 요약

BACKLOG-118 (reinterpret_cast UB), BACKLOG-119 (타이밍 의존 sleep_for), BACKLOG-115 (이미 수정 확인) 3건 처리 완료.

## 변경 내역

### BACKLOG-118: reinterpret_cast UB 제거
- `test_service_lifecycle.cpp` — aligned_storage + reinterpret_cast 더미 객체 4개를 실제 인스턴스로 교체
- GTest fixture `ServiceLifecycleTest` 도입 (Server, ServiceRegistry, ServiceRegistryView, PeriodicTaskScheduler)
- NOLINT 블록 전체 제거
- 6개 테스트를 `TEST_F`로 전환, 6개는 fixture 불필요하여 `TEST` 유지

### BACKLOG-119: 타이밍 의존 sleep_for 수정 (4파일 5건)

| 파일 | 변경 | 전략 |
|------|------|------|
| `test_cross_core_call.cpp:115` | 의도적 블로킹 주석 보강 | 유지 + 문서화 |
| `test_connection_handler.cpp:156` | `sleep_for(50ms)` → `sleep_for(50ms * timeout_multiplier())` | 부재 증명 스케일링 |
| `test_periodic_task_scheduler.cpp:58` | `sleep_for(80ms)` → `wait_for(count >= 1)` | 이벤트 대기 폴링 전환 |
| `test_periodic_task_scheduler.cpp:80` | `sleep_for(100ms)` → `sleep_for(100ms * timeout_multiplier())` | 부재 증명 스케일링 |
| `test_server_error_paths.cpp:66` | `sleep_for(500ms)` → `wait_for(t2_done \|\| server2.running())` | 이벤트 대기 폴링 전환 |

### BACKLOG-115: 이미 수정 확인
- `test_tcp_acceptor.cpp` StopPreventsNewAccepts — 현재 코드에서 결정적 패턴 확인. 히스토리로 이동.

## 빌드 검증

- MSVC debug: 빌드 성공 (경고 0건)
- 전체 테스트 79/79 통과
