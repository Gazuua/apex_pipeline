# 완료 기록 — auto-review full 코드 품질 개선

## PR

- **PR #4**: https://github.com/Gazuua/apex_pipeline/pull/4 (squash merged → `db81110`)
- **브랜치**: `bugfix/fix-readme-known-issues`

## 작업 내용

### Critical 수정 (4건)

1. **TSAN flaky 테스트 해결** — `test_helpers.hpp`에 `timeout_multiplier()` constexpr 추가. `__SANITIZE_THREAD__`/`__has_feature(thread_sanitizer)` 감지 시 타임아웃 10x 자동 확대. 기존 `wait_for()` 기본 3000ms가 TSAN(5~15x 느림) 환경에서 부족했던 문제 해결.
2. **CoreAwareService 테스트 격리** — `CoreAwareServiceFixture` 추가, `TEST_F`로 전환. static 카운터 수동 리셋 → fixture `SetUp()` 자동 초기화.
3. **cross_core_call 타임아웃 race 문서화** — `@warning` 추가. 타임아웃 시 `~State()`가 타겟 코어에서 실행될 수 있는 패턴 명시.
4. **async_send 동시 호출 경고** — Session에 `@warning` 추가. 동일 세션에 동시 write 금지 명시.

### Important 수정 (8건)

5. **CoreEngine drain 루프 예외 보호** — `drain_callback_`과 cross-core task 실행에 try-catch 추가. 단일 예외로 코어 전체 마비 방지.
6. **CMakePresets tsan configure preset** — TSAN_OPTIONS environment 블록 제거 (test preset에만 있어야 함).
7. **CMakePresets asan-msvc test preset** — 누락된 test preset 추가.
8. **cross_core_call sleep 하드코딩** — `sleep_for(100ms)` → `timeout_multiplier()` 적용.
9. **marketplace.json 버전 동기화** — 1.0.0 → 1.1.0 (plugin.json과 일치).
10. **Apex_Pipeline.md session-context.sh** — 구조 트리에 누락된 항목 추가.

### auto-review 지침 개선 (5건)

11. Phase 0: full 모드 작업 커밋 불필요 명시
12. 자동화 원칙: Phase 간 유저 확인 금지
13. Phase 5: 프로젝트 문서(Apex_Pipeline.md, README.md) 업데이트 단계 추가
14. CI watch: `run_in_background: true` + 15분 타임아웃
15. Phase 5 최종 push: CI 대기 없이 바로 squash merge

## 리뷰 결과

- **모드**: full (전체 프로젝트 리뷰)
- **라운드**: 2회 (Round 1: 33건 발견 → 12건 수정, Round 2: Clean 0건)
- **CI**: 5/5 잡 전체 통과 (root-linux-gcc, linux-gcc, linux-asan, linux-tsan, windows-msvc)
- **상세**: `docs/apex_core/review/20260309_010351_auto-review.md`
