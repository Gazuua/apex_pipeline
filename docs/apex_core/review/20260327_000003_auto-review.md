# Auto-Review: Acceptor Per-IP 연결 제한 (BACKLOG-256)

- **일시**: 2026-03-27 00:00
- **모드**: task (변경 파일만)
- **리뷰어**: logic, systems (병렬 디스패치)

## 변경 파일 (11개)

| 파일 | 변경 |
|------|------|
| `connection_limiter.hpp/cpp` (신규) | owner-shard per-IP 카운터 |
| `server_config.hpp` | `max_connections_per_ip` 추가 |
| `session.hpp` | `remote_ip_` 캐시 |
| `connection_handler.hpp` | ConnectionClosedCallback + accept IP 전달 |
| `listener.hpp` | inject_connection_limiters, dispatch_accept, checked_accept |
| `server.hpp/cpp` | per_core_limiters_ 멤버, shutdown 순서 |
| `config.cpp` | TOML 파싱 |
| `CMakeLists.txt` | connection_limiter.cpp 추가 |

## 리뷰 결과

### Logic 리뷰어 수정 (3건)

| # | 심각도 | 파일 | 이슈 | 수정 |
|---|--------|------|------|------|
| L1 | CRITICAL | connection_limiter.cpp | `owner_core()`: num_cores==0 division by zero UB | 가드 + assert 추가 |
| L2 | MAJOR | connection_limiter.cpp | `decrement()`: uint32_t underflow → 영구 점유 | count==0 체크 + warn 로깅 |
| L3 | MINOR | connection_limiter.cpp | `try_increment()`: 빈 IP 방어 누락 | empty 체크 + false 반환 |

### Systems 리뷰어 수정 (1건)

| # | 심각도 | 파일 | 이슈 | 수정 |
|---|--------|------|------|------|
| S1 | MAJOR | server.cpp/server.hpp | finalize_shutdown Step 6.6에서 조기 clear → ~Server io.poll()에서 UAF | clear를 ~Server로 이동, finalize_shutdown에서는 주석만 |

### 메인 직접 수정 (2건)

| # | 심각도 | 파일 | 이슈 | 수정 |
|---|--------|------|------|------|
| M1 | MAJOR | listener.hpp | cross_core_call 타임아웃 시 카운트 leak — 보상 decrement 미발행 | 타임아웃 시 보상 decrement post 추가 |
| M2 | MAJOR | listener.hpp | shutdown 중 connection_closed_cb에서 limiter null 역참조 (UBSAN) | unique_ptr null 체크 추가 |

### 검증 완료 항목 (4건)

- connection_closed_cb_ 호출 완전성: 모든 read_loop 종료 경로 커버
- cross_core_call 람다 캡처: string_view 혼용 없음, 값 복사만
- checked_accept 코루틴 socket move: coroutine frame 수명 보장
- Graceful shutdown: boost::asio::post 메시지는 io.poll()에서 소진

## 통계

- 리뷰 이슈: 6건 (CRITICAL 1, MAJOR 4, MINOR 1)
- 수정: 6건
- 미수정: 0건
