# Auto-Review: BlockingTaskExecutor + AuthService bcrypt offload (BACKLOG-146)

**브랜치**: `feature/bcrypt-thread-offload`
**PR**: #196
**리뷰어**: 5명 (design, logic, systems, test, docs-spec)

## 리뷰 결과 요약

| 리뷰어 | 이슈 수 | 수정 | 핵심 판정 |
|--------|---------|------|-----------|
| design | 3 (MAJOR 1, MINOR 2) | 전부 수정 | API 일관성 양호, 가이드 미갱신 지적 |
| logic | 0 | - | 캡처 안전성, 에러 경로, timing, shutdown 전부 PASS |
| systems | 1 (MINOR 1) | 수정 완료 | thread pool 수명/shutdown 안전, caller_executor dead code |
| test | 5 (MAJOR 2, MINOR 3) | 전부 수정 | 테스트 4건 추가 + 2건 강화 |
| docs-spec | 5 (MAJOR 1, MINOR 4) | 전부 수정 | 설계 문서↔구현 정합 갱신 |

## 주요 이슈 및 수정 사항

### MAJOR

1. **프레임워크 가이드 미갱신** (design)
   - `docs/apex_core/apex_core_guide.md`에 `blocking_executor()`, `blocking_pool_threads`, Shutdown Step 6.5 등 미반영
   - **수정**: §7에 `blocking_executor()` 섹션 추가, #5 금지사항에 해결 방안 추가, §2.1 ServerConfig에 필드 추가

2. **ResumesOnCallerExecutor 테스트 불충분** (test)
   - thread pool 실행 검증 누락 — run()이 인라인 실행해도 pass
   - **수정**: `pool_thread_id` 캡처 + `EXPECT_NE(pool_thread_id, caller_thread_id)` 추가

3. **shutdown 테스트 부재** (test)
   - 진행 중 작업 완료 대기, double shutdown 안전성 미검증
   - **수정**: `ShutdownWaitsForRunningTasks`, `DoubleShutdownIsSafe` 2건 추가

4. **설계 문서 코드 예시 불일치** (docs-spec)
   - requires 오버로드 분리, thread_count 멤버/접근자 등 실제 구현과 불일치
   - **수정**: 설계 문서 전면 갱신

### MINOR

5. **caller_executor 미사용 변수** (design + systems 공통)
   - `co_await this_coro::executor` 결과 캡처 후 미사용
   - **수정**: 해당 행 제거, 주석으로 resume 메커니즘 설명

6. **테스트 보강** (test)
   - pool saturation, move-only 반환, 예외 메시지 검증, sleep 제거
   - **수정**: 전부 반영

## 최종 상태

- 로컬 빌드: 96/96 테스트 통과
- 리뷰 이슈: 0건 잔여
