# Auto-Review: BACKLOG-40 NUMA 바인딩 + Core Affinity

**브랜치**: feature/numa-core-affinity
**PR**: #218
**리뷰어**: logic, systems, design, test (4명 병렬 디스패치)

## 리뷰 통계

| 리뷰어 | 수정 건수 | CRITICAL | MEDIUM | LOW/MINOR |
|--------|----------|----------|--------|-----------|
| logic | 6 | 1 | 2 | 3 |
| systems | 5 | 0 | 1 | 4 |
| design | 3 | 0 | 2 | 1 |
| test | 11 | 0 | 0 | 11 |
| **총계** | **25** | **1** | **5** | **19** |

## CRITICAL (1건)

### [logic-1] Linux NUMA 비연속 노드 탐지 누락
- **파일**: `cpu_topology.cpp` L309
- **원인**: NUMA 노드 순회에서 `break` 사용 — 노드 0,1,3 (비연속) 환경에서 노드 2 미존재 시 순회 중단, 노드 3+ 미탐지
- **영향**: 멀티소켓 서버에서 일부 코어가 NUMA 노드 0으로 잘못 배정 → 리모트 메모리 접근
- **수정**: `break` → `continue`

## MEDIUM (5건)

| # | 리뷰어 | 파일 | 이슈 |
|---|--------|------|------|
| 1 | logic | `cpu_topology.cpp` | Linux cpulist 파싱 예외 시 전체 topology fallback → 노드별 try-catch 격리 |
| 2 | logic | `cpu_topology.cpp` | sysfs 읽기 실패 시 모든 코어가 동일 CoreKey 매핑 → 읽기 실패 체크 추가 |
| 3 | logic | `config.cpp` | worker_cores 중복/비정수 미검증 → 중복 검출 + 비정수 시 예외 |
| 4 | systems | `thread_affinity.cpp` | `set_mempolicy` maxnode off-by-one → `MAX_NODES + 1` |
| 5 | design | `thread_affinity.hpp` | `[[nodiscard]]` 누락 |

## LOW/MINOR (19건)

### systems (4건)
- `core_engine.cpp`: NUMA 바인딩 성공/실패 로그 추가
- `thread_affinity.cpp`: Windows `SetThreadGroupAffinity` 단일화 (64+ 코어 지원)
- `thread_affinity.cpp`: Linux `CPU_SETSIZE` 상한 체크 추가
- `core_engine.hpp`: `CoreAssignment::logical_core_id` 기본값 `{0}` 추가

### logic (3건)
- `cpu_topology.cpp`: dead code 블록 제거
- `server.cpp`: manual mode NUMA 탐색 `found` flag 추가
- `config.cpp`: `<set>` include 추가

### design (1건)
- `cpu_topology.hpp`: `primary_logical_id()` fallback 주석 보강

### test (11건)
- `test_cpu_topology.cpp`: 테스트명 개선, 파티션 검증 강화, 2건 추가 (NUMA invalid, fallback)
- `test_thread_affinity.cpp`: fixture 도입 (affinity 복원 TearDown), yield 후 핀닝 검증, NUMA invalid 테스트 추가
- `test_config.cpp`: 경계값 3건 추가 (음수, 오버플로, 비정수 throw), 중복 core throw 추가
- 메인 cross-cutting: 비정수 테스트를 throw 기대로 정합성 수정

## 보고만 (수정 불요)

| # | 리뷰어 | 내용 |
|---|--------|------|
| 1 | systems | CoreContext가 main 스레드에서 할당되어 NUMA node 0 배치 — lazy-init이 이상적이나 리팩터링 규모 대비 io_context 수 KB 수준으로 허용 가능 |
| 2 | design | `apex_core_guide.md`에 AffinityConfig 가이드 미기재 — 머지 전 문서 갱신에서 처리 |
| 3 | test | Docker cgroup v2에서 hardware_concurrency()와 sysfs 열거 불일치 가능 — 현재 구현은 hw_concurrency를 상한으로 사용하므로 안전 |
