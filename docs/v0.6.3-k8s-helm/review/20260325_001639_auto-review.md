# v0.6.3 K8s + Helm 차트 — Auto-Review 보고서

**브랜치**: `feature/v0.6.3-k8s-helm` | **PR**: #147 | 2026-03-25

## 리뷰어 구성

| 리뷰어 | 도메인 | 소요 |
|--------|--------|------|
| infra-security | Helm 템플릿, 크레덴셜, 스크립트, Docker | ~5분 |
| docs-spec | 설계서↔구현 정합성, 문서 포맷, 용어 일관성 | ~3분 |
| 메인 (직접) | C++ 코드 (metrics 와이어링 3파일) | 빌드 검증 |

## 발견 이슈 및 조치

### 직접 수정 완료 (6건)

| # | 심각도 | 파일 | 설명 |
|---|--------|------|------|
| 1 | **CRITICAL** | `pgbouncer/values.yaml` | PgBouncer username/database/password가 Bitnami PostgreSQL 설정과 불일치 → 일치시킴 |
| 2 | **CRITICAL** | `_helpers.tpl` + 4개 `secret.yaml` 신규 | 서비스 sub-chart에 Secret 자동 생성 템플릿 누락 → `apex-common.secret` template 추가 + sub-chart 반영 |
| 3 | **CRITICAL** | `_helpers.tpl` | `secrets: {}` (빈 map)일 때 존재하지 않는 Secret 참조하는 envFrom 렌더링 → 조건 강화 |
| 4 | **MAJOR** | `gateway/ingress.yaml` | Ingress backend 포트 8443 하드코딩 → values 참조로 변경 |
| 5 | **MINOR** | `pgbouncer/deployment.yaml` | Pod template labels에 selectorLabels만 사용 → 전체 labels로 변경 |
| 6 | **MINOR** | `design.md:310` | 설계서 Redis DNS 주소 오타 (`apex-pipeline-` → `apex-infra-`) |

### 보고만 (2건)

| # | 심각도 | 설명 |
|---|--------|------|
| 7 | INFO | Gateway TOML `response_topic`과 서비스 response topic 라우팅 — 기존 아키텍처 의도대로 |
| 8 | INFO | Gateway `num_cores = 2` 하드코딩 — 프로덕션 값 조정 시 함께 변경 필요 |

### 이슈 없음 확인 (14개 리뷰 포인트)

- Helm 템플릿 정합성, 크레덴셜 안전성, existingSecret 패턴 일관성
- Bitnami 의존성 버전 범위, 스크립트 안전성, 저작권 헤더
- Ingress WebSocket annotation, TOML ConfigMap K8s DNS, `${VAR:-default}` 패턴
- sub-chart 일관성, RSA 키 관리, Namespace 생성, 2-Release 배포
- 버전 번호 일관성, 로드맵 상태, 설계서↔구현 정합성, 문서 포맷

## 결론

CRITICAL 3건 모두 **배포 실패를 야기하는 이슈**였다 (PgBouncer 접속 불가, Secret 미존재 → Pod CrashLoop, 빈 map 조건 오류). auto-review 없이 머지했다면 minikube에서 첫 배포부터 실패했을 것이다. 전부 수정 완료.
