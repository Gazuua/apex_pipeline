# FSD Backlog Sweep: BACKLOG-150, 152

- **브랜치**: `feature/fsd-backlog-20260323_235625`
- **PR**: #127
- **일시**: 2026-03-24

## 해결 항목

### BACKLOG-150 (BUG, FIXED)
JWT uid 파싱 — `std::stoull` → `std::from_chars` 전환

- `jwt_verifier.cpp`, `jwt_manager.cpp`에서 `std::stoull` 예외 기반 파싱을 `std::from_chars` 비예외 파싱으로 전환
- 비정상 uid 문자열 시 "JWT unexpected error" 대신 정확한 "uid claim parsing failed" 로깅
- `MalformedUidClaim` 테스트 추가 (auto-review에서 발견)

### BACKLOG-152 (DOCS, DOCUMENTED)
ServiceBase `on_start`/`on_stop` CRTP 비대칭 문서화

- `service_base.hpp`에 비대칭 설계 의도 주석 추가
- `start()` → CRTP dispatch (devirtualization), `stop()` → virtual dispatch (shutdown path)

## 드롭 항목

| ID | 사유 |
|----|------|
| BACKLOG-161 | spdlog source location → SPDLOG_LOGGER_CALL 매크로 전환 필요 (대규모 리팩터) |
| BACKLOG-170 | cleanup copies → 워크스페이스 탐색/안전성 설계 결정 필요 |

## 검증

- 로컬 MSVC debug: 84/84 테스트 통과, 경고 0건
- CI: GCC, UBSAN, ASAN, TSAN, E2E 전체 통과
