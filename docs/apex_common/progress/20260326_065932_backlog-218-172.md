# BACKLOG-218 + BACKLOG-172 완료 기록

- **날짜**: 2026-03-26
- **PR**: #177
- **브랜치**: feature/backlog-218-172-metrics-test-docs-cleanup

## BACKLOG-218: MetricsHttpServer in-flight session cancel 테스트

- `apex_core/tests/unit/test_metrics_http_server.cpp`에 `InFlightSessionCancelledOnStop` 테스트 추가
- slow client 패턴 (TCP 연결만, HTTP 요청 미전송) → async_read 대기 → stop()/cancel_all() → 세션 정리 검증
- 클라이언트 측 read_some에서 EOF/에러 관찰로 크로스플랫폼 검증 (Windows EOF, Linux connection_reset 양쪽 커버)
- CI 전체 통과: MSVC, GCC, TSAN, ASAN, UBSAN, E2E

## BACKLOG-172: deprecated tcp_binary_protocol 포워딩 헤더 문서 참조 정리

- 삭제된 `apex/core/tcp_binary_protocol.hpp` 포워딩 헤더를 참조하는 활성 설계 문서 5건 + progress 1건 갱신
- 경로를 `apex/shared/protocols/tcp/tcp_binary_protocol.hpp` (실제 경로)로 치환하고 "이후 삭제됨" 문맥 추가
- 히스토리 문서 3건은 원칙에 따라 미수정
- 코드 변경 없음 (12개 소스 파일 모두 이미 신경로 사용 중)
