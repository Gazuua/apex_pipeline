# v0.4.5 진행 기록

**작성일**: 2026-03-14
**브랜치**: feature/review-improvements
**PR**: #17 (https://github.com/Gazuua/apex_pipeline/pull/17)

---

## 완료 항목

### 구현 (14 tasks 전체 완료)
- **코어 메모리**: BumpAllocator(요청 수명) + ArenaAllocator(트랜잭션 수명) + SlabPool→SlabAllocator 리네임
- **C++20 concepts**: CoreAllocator/Freeable/Resettable + PoolLike concept (CRTP ConnectionPool 제거)
- **Redis**: RedisPool→RedisMultiplexer (코어당 고정 커넥션 + 코루틴 파이프라이닝), RedisReply wrapper
- **PG**: PgPool 자체완결(CRTP 제거), PgTransaction RAII 가드(begin() 필수), PgConnection prepared stmt + BumpAllocator 주입
- **CoreMetrics**: atomic 카운터 + rate-limited 로깅
- **ServerConfig**: PerCoreState 할당기 통합, cross_core_call_timeout, TOML [server.memory] 스키마

### auto-review task
- Round 1: Critical 7 / Important 16 / Minor 14 → 37건 수정
- Round 2: Critical 1 / Important 2 / Minor 2 → 4건 수정
- CI: GCC debug/asan/tsan + MSVC 전체 통과 (cstdint 누락 1회 수정)

### 머지 전 필수 갱신
- [x] `docs/Apex_Pipeline.md` 완료 이력 + 로드맵 현재 버전
- [x] `CLAUDE.md` 로드맵 현재 버전
- [x] `README.md` 현재 상태 + 변경 내역

---

## 다음 세션에서 할 일

### 1. auto-review 시스템 개편 (선택)
- 계획서: `docs/apex_tools/plans/20260314_180000_auto-review-improvements.md`
- 7개 리뷰어 재설계 (reviewer-code → correctness/memory/concurrency/api 분화)
- 파일 기반 컨텍스트 전달 (JSON findings)
- `apex_tools/auto-review/log/` 디렉토리 (.gitignore 대상)
- 크로스 컴파일러 체크 (GCC/MSVC 차이)
- **판단 필요**: auto-review full 전에 개편할지, 현재 시스템으로 full 먼저 돌릴지

### 2. auto-review full → Clean → PR → CI
- 전체 프로젝트 리뷰 (diff 무관)
- 수정 → 재리뷰 반복 → Clean

### 3. main squash merge → push → 정리
- `gh pr merge #17 --squash --delete-branch`
- `git worktree remove .worktrees/review-improvements`

---

## 알려진 이슈 (수정 보류)

| 이슈 | 이유 | 대상 버전 |
|------|------|----------|
| RedisMultiplexer PendingCommand dangling (timeout 시) | heap 할당으로 전환 필요, 설계 수준 리팩토링 | v0.5 |
| RedisMultiplexer pipeline() 순차 실행 | 진짜 파이프라이닝은 모든 cmd 먼저 전송 후 응답 수집 필요 | v0.5 |
| RedisReply ARRAY 타입 미지원 | SMEMBERS/LRANGE 등 배열 응답 파싱 불가 | v0.5 |
| PgTransaction begun_ 경로 unit test 불가 | begin()이 async → test_force_begun() 또는 integration test 필요 | v0.5 |
| CLAUDE.md analysis.json 키 `slab_pool` | 벤치마크 보고서 스크립트와 동시 갱신 필요 | 벤치마크 재실행 시 |
