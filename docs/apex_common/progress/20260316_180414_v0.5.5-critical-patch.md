# v0.5.5 Critical Patch 완료 기록

## 요약
- 기간: 2026-03-16
- PR: [#30](https://github.com/Gazuua/apex_pipeline/pull/30) 리뷰 이슈 수정 + Auth/Chat 서비스 완성
- 결과: 28개 수정 + 7개 신규 = 35개 파일, +2,747/-493줄 (docs 제외)
- 테스트: 56/56 통과
- 작업 단위: Wave A~E 순차 실행

---

## Wave A: PR #30 리뷰 이슈 8건 수정

### kafka_envelope
- `ReplyTopicHeader` uint16_t overflow 보호 추가 + 10개 테스트
- spdlog 의존성 제거 (프로토콜 라이브러리의 외부 로깅 의존성 제거로 독립성 확보)

### jwt_manager / jwt_verifier
- uid claim `double` -> `string` 전환 (정밀도 손실 방지)
- uid parsing `as_number` -> `as_string` + `std::stoull`
- jwt_verifier copy/move = delete (복사/이동 방지)

### gateway_pipeline
- `GatewayConfig&` 의존성 주입 (config 기반 auth exempt 경로 판별)

### gateway_config_parser
- `[auth.exempt]` TOML 테이블 파싱 지원

### gateway.toml
- HS256 secret 제거 + `[auth.exempt]` 섹션 추가

---

## Wave B-C: Auth Service 완성

- `MessageDispatcher` 기반 dispatch 도입 (switch-case 제거)
- `EnvelopeMetadata` 캐시 패턴 구현 (Kafka -> 코루틴 메타데이터 전달)
- **handle_login**: PG user lookup + bcrypt verify + JWT RS256 생성 + Redis session
- **handle_logout**: JWT verify -> blacklist -> session 삭제
- **handle_refresh_token**: 토큰 로테이션 + reuse detection + family revocation
- **main.cpp**: TOML 기반 완전 부트스트랩

---

## Wave C-D: Chat Service 완성

### 8개 핸들러 (FBS + PG + Redis)
- `create_room`, `join_room`, `leave_room`, `list_rooms`
- `send_message`, `whisper`, `global_broadcast`, `chat_history`

### 주요 패턴
- cursor 기반 페이지네이션 (`chat_history`)
- Redis pub/sub (`send_message`, `global_broadcast`)
- **main.cpp**: TOML 기반 완전 부트스트랩 (Kafka + Redis x2 + PG)

---

## Wave E: 테스트 인프라

- RS256 테스트 키페어 생성
- `test_gateway_pipeline`: RS256 전환 + AuthConfig 테스트
- `test_config_reloader`: RS256 + `[auth.exempt]` 전환
- E2E fixture: 서비스 프로세스 launch/teardown (`CreateProcess`/`fork`)
- E2E TOML 설정 3개 생성

---

## 빌드 검증

- 56/56 테스트 전체 통과 (전 Wave 빌드 성공 확인)
