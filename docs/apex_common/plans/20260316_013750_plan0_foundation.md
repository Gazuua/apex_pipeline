# Plan 0: 기반 작업 — WireHeader v2 + Transport Concept + Kafka Envelope

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wave 2 서비스 체인의 기반 인프라 구축 — 와이어 프로토콜 확장, Transport 추상화, Kafka 메시지 포맷 정의

**Architecture:** WireHeader v2(msg_id u32)로 확장하고, Transport concept으로 TLS/Plain 전송 계층을 추상화하며, Gateway↔Service 간 Kafka Envelope 포맷을 정의한다.

**Tech Stack:** C++23, Boost.Asio, FlatBuffers, CMake, Google Test

---

## 의존성 그래프

```
Task 1 (WireHeader v2) ──────────────────────────┐
Task 2 (Transport concept) ── 독립 (Task 1과 병렬 가능) ──┤
Task 3 (Kafka Envelope) ── 독립 (Task 1, 2와 병렬 가능) ──┤
Task 4 (FlatBuffers 스키마) ── 독립 ──────────────────────┤
                                                          ▼
                                                   Task 5 (전체 빌드 + 테스트)
```

Task 1~4는 파일 충돌이 없으면 병렬 가능. Task 5는 모든 Task 완료 후 수행.

---

## Chunk 1: WireHeader v2 마이그레이션 — 구조체 + Codec

### Step 1.1: WireHeader 구조체 수정

**파일**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/wire_header.hpp`

- [ ] 헤더 레이아웃을 v2로 변경:

```cpp
// 변경 전 (v1, 10바이트):
//   [0]     version    (uint8_t)
//   [1..2]  msg_id     (uint16_t, big-endian)
//   [3..6]  body_size  (uint32_t, big-endian)
//   [7..8]  flags      (uint16_t, big-endian)
//   [9]     reserved   (uint8_t, must be 0)

// 변경 후 (v2, 12바이트):
//   [0]       version    (uint8_t)
//   [1]       flags      (uint8_t)
//   [2..5]    msg_id     (uint32_t, big-endian)
//   [6..9]    body_size  (uint32_t, big-endian)
//   [10..11]  reserved   (uint16_t, must be 0)
```

구체적 변경사항:

```cpp
struct WireHeader {
    static constexpr size_t SIZE = 12;                    // 10 → 12
    static constexpr uint8_t CURRENT_VERSION = 2;         // 1 → 2
    static constexpr uint32_t MAX_BODY_SIZE = 16 * 1024 * 1024;

    // 새 오프셋
    static constexpr size_t OFF_VERSION   = 0;
    static constexpr size_t OFF_FLAGS     = 1;            // 이동: 7→1
    static constexpr size_t OFF_MSG_ID    = 2;            // 이동: 1→2, 크기: 2→4
    static constexpr size_t OFF_BODY_SIZE = 6;            // 이동: 3→6
    static constexpr size_t OFF_RESERVED  = 10;           // 이동: 9→10, 크기: 1→2

    uint8_t  version{CURRENT_VERSION};
    uint8_t  flags{0};                                    // uint16_t → uint8_t
    uint32_t msg_id{0};                                   // uint16_t → uint32_t
    uint32_t body_size{0};
    uint16_t reserved{0};                                 // uint8_t → uint16_t

    [[nodiscard]] static std::expected<WireHeader, ParseError>
    parse(std::span<const uint8_t> data);

    void serialize(std::span<uint8_t, SIZE> out) const;
    [[nodiscard]] std::array<uint8_t, SIZE> serialize() const;

    [[nodiscard]] size_t frame_size() const noexcept {
        return SIZE + body_size;
    }
};
```

- [ ] `wire_flags` 네임스페이스를 `uint8_t`로 변경:

```cpp
namespace wire_flags {
    constexpr uint8_t NONE = 0x00;
    constexpr uint8_t COMPRESSED = 0x01;
    constexpr uint8_t HEARTBEAT = 0x02;
    constexpr uint8_t ERROR_RESPONSE = 0x04;
    constexpr uint8_t REQUIRE_AUTH_CHECK = 0x08;
} // namespace wire_flags
```

### Step 1.2: WireHeader serialize/parse 구현 수정

**파일**: `apex_core/src/wire_header.cpp`

- [ ] `parse()` 함수를 v2 레이아웃에 맞게 변경:

```cpp
std::expected<WireHeader, ParseError>
WireHeader::parse(std::span<const uint8_t> data) {
    if (data.size() < SIZE) {
        return std::unexpected(ParseError::InsufficientData);
    }

    WireHeader h;
    h.version = data[OFF_VERSION];

    if (h.version != CURRENT_VERSION) {
        return std::unexpected(ParseError::UnsupportedVersion);
    }

    h.flags = data[OFF_FLAGS];   // u8 직접 읽기

    uint32_t raw32;
    std::memcpy(&raw32, data.data() + OFF_MSG_ID, sizeof(uint32_t));
    h.msg_id = ntoh32(raw32);    // u32로 변경

    std::memcpy(&raw32, data.data() + OFF_BODY_SIZE, sizeof(uint32_t));
    h.body_size = ntoh32(raw32);

    uint16_t raw16;
    std::memcpy(&raw16, data.data() + OFF_RESERVED, sizeof(uint16_t));
    h.reserved = ntoh16(raw16);  // u16으로 변경

    if (h.body_size > MAX_BODY_SIZE) {
        return std::unexpected(ParseError::BodyTooLarge);
    }

    return h;
}
```

- [ ] `serialize()` 함수를 v2 레이아웃에 맞게 변경:

```cpp
void WireHeader::serialize(std::span<uint8_t, SIZE> out) const {
    out[OFF_VERSION] = version;
    out[OFF_FLAGS] = flags;      // u8 직접 쓰기

    uint32_t net32 = hton32(msg_id);   // u32로 변경
    std::memcpy(out.data() + OFF_MSG_ID, &net32, sizeof(uint32_t));

    net32 = hton32(body_size);
    std::memcpy(out.data() + OFF_BODY_SIZE, &net32, sizeof(uint32_t));

    uint16_t net16 = hton16(reserved);  // u16으로 변경
    std::memcpy(out.data() + OFF_RESERVED, &net16, sizeof(uint16_t));
}
```

### Step 1.3: Forwarding 헤더의 wire_flags using 선언 업데이트

**파일**: `apex_core/include/apex/core/wire_header.hpp`

변경 없음 — 이미 `using` 선언과 `namespace wire_flags = ...`를 사용하고 있어서 타입 변경이 자동 전파됨.

### Step 1.4: FrameCodec 수정

**파일**: `apex_core/src/frame_codec.cpp`

- [ ] `try_decode()`에서 `WireHeader::SIZE` 사용 확인 — 이미 상수를 참조하므로 자동 반영됨. 변경 불필요.

**파일**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/frame_codec.hpp`

- [ ] `encode_to()`와 `encode()`에서 `WireHeader::SIZE` 참조 확인 — 자동 반영됨. 변경 불필요.

**검증**: FrameCodec는 `WireHeader::SIZE`와 `header.serialize()`만 사용하므로 WireHeader 변경이 자동 전파됨. 별도 코드 수정 불필요.

---

## Chunk 2: WireHeader v2 마이그레이션 — Dispatcher + ServiceBase + ErrorSender

### Step 2.1: MessageDispatcher uint16_t → uint32_t

**파일**: `apex_core/include/apex/core/message_dispatcher.hpp`

- [ ] Handler 시그니처의 msg_id 파라미터 타입 변경:

```cpp
// 변경 전
using Handler = std::function<
    boost::asio::awaitable<Result<void>>(SessionPtr, uint16_t, std::span<const uint8_t>)>;

// 변경 후
using Handler = std::function<
    boost::asio::awaitable<Result<void>>(SessionPtr, uint32_t, std::span<const uint8_t>)>;
```

- [ ] 모든 메서드 시그니처의 msg_id를 `uint32_t`로 변경:

```cpp
void register_handler(uint32_t msg_id, Handler handler);
void unregister_handler(uint32_t msg_id);

[[nodiscard]] boost::asio::awaitable<Result<void>>
dispatch(SessionPtr session, uint32_t msg_id, std::span<const uint8_t> payload) const;

[[nodiscard]] bool has_handler(uint32_t msg_id) const noexcept;
```

- [ ] 내부 맵 키 타입 변경:

```cpp
// 변경 전
boost::unordered_flat_map<uint16_t, Handler> handlers_;

// 변경 후
boost::unordered_flat_map<uint32_t, Handler> handlers_;
```

**파일**: `apex_core/src/message_dispatcher.cpp`

- [ ] 모든 함수 시그니처에서 `uint16_t` → `uint32_t`:

```cpp
void MessageDispatcher::register_handler(uint32_t msg_id, Handler handler) { ... }
void MessageDispatcher::unregister_handler(uint32_t msg_id) { ... }

boost::asio::awaitable<Result<void>>
MessageDispatcher::dispatch(SessionPtr session, uint32_t msg_id,
                            std::span<const uint8_t> payload) const {
    // ...
    // 로깅 포맷도 업데이트:
    logger->error("MessageDispatcher: handler for msg_id 0x{:08x} threw: {}",
        static_cast<unsigned>(msg_id), e.what());
    // ...
}

bool MessageDispatcher::has_handler(uint32_t msg_id) const noexcept { ... }
```

### Step 2.2: ServiceBase 타입 변경

**파일**: `apex_core/include/apex/core/service_base.hpp`

- [ ] `handle()` 메서드의 msg_id 파라미터 타입 변경:

```cpp
// 변경 전
void handle(uint16_t msg_id,
            boost::asio::awaitable<Result<void>> (Derived::*method)(
                SessionPtr, uint16_t, std::span<const uint8_t>))

// 변경 후
void handle(uint32_t msg_id,
            boost::asio::awaitable<Result<void>> (Derived::*method)(
                SessionPtr, uint32_t, std::span<const uint8_t>))
```

- [ ] `route<T>()` 메서드의 msg_id 파라미터 타입 변경:

```cpp
// 변경 전
template <typename FbsType>
void route(uint16_t msg_id,
           boost::asio::awaitable<Result<void>> (Derived::*method)(
               SessionPtr, uint16_t, const FbsType*))

// 변경 후
template <typename FbsType>
void route(uint32_t msg_id,
           boost::asio::awaitable<Result<void>> (Derived::*method)(
               SessionPtr, uint32_t, const FbsType*))
```

- [ ] 내부 람다의 파라미터 타입 변경 (`handle()`과 `route()` 내부):

```cpp
// handle() 내부 람다
dispatcher_->register_handler(msg_id,
    [self, method](SessionPtr session, uint32_t id,
                   std::span<const uint8_t> payload)
        -> boost::asio::awaitable<Result<void>> {
            co_return co_await (self->*method)(session, id, payload);
        });

// route() 내부 람다
dispatcher_->register_handler(msg_id,
    [self, method](SessionPtr session, uint32_t id,
                   std::span<const uint8_t> payload)
        -> boost::asio::awaitable<Result<void>> {
            flatbuffers::Verifier verifier(payload.data(), payload.size());
            if (!verifier.VerifyBuffer<FbsType>()) {
                co_return apex::core::error(ErrorCode::FlatBuffersVerifyFailed);
            }
            auto* msg = flatbuffers::GetRoot<FbsType>(payload.data());
            co_return co_await (self->*method)(session, id, msg);
        });
```

- [ ] `registered_msg_ids_` 컨테이너 타입 변경:

```cpp
// 변경 전
boost::unordered_flat_set<uint16_t> registered_msg_ids_;

// 변경 후
boost::unordered_flat_set<uint32_t> registered_msg_ids_;
```

### Step 2.3: ErrorSender 타입 변경

**파일**: `apex_core/include/apex/core/error_sender.hpp`

- [ ] `build_error_frame()`의 `original_msg_id` 파라미터 타입 변경:

```cpp
// 변경 전
[[nodiscard]] static std::vector<uint8_t> build_error_frame(
    uint16_t original_msg_id,
    ErrorCode code,
    std::string_view message = "");

// 변경 후
[[nodiscard]] static std::vector<uint8_t> build_error_frame(
    uint32_t original_msg_id,
    ErrorCode code,
    std::string_view message = "");
```

**파일**: `apex_core/src/error_sender.cpp`

- [ ] 함수 시그니처와 WireHeader 생성:

```cpp
std::vector<uint8_t> ErrorSender::build_error_frame(
    uint32_t original_msg_id,
    ErrorCode code,
    std::string_view message)
{
    // ... FlatBufferBuilder 로직 동일 ...

    WireHeader header{
        .version = WireHeader::CURRENT_VERSION,
        .flags = wire_flags::ERROR_RESPONSE,     // flags가 두번째 필드로 이동
        .msg_id = original_msg_id,               // 이미 uint32_t
        .body_size = static_cast<uint32_t>(payload_size),
    };

    // 나머지 직렬화 로직 동일 (WireHeader::SIZE가 12로 자동 반영)
    // ...
}
```

### Step 2.4: ConnectionHandler dispatch 호출 확인

**파일**: `apex_core/include/apex/core/connection_handler.hpp`

- [ ] `process_frames()` 내 dispatch 호출 확인:

```cpp
// 현재 코드 (line 129-130):
auto result = co_await dispatcher_.dispatch(
    session, header.msg_id, payload_span);
```

`header.msg_id`는 이제 `uint32_t`이고, `dispatcher_.dispatch()`도 `uint32_t`를 받으므로 변경 불필요.

- [ ] `ErrorSender::build_error_frame()` 호출 확인 (line 135-136):

```cpp
auto error_frame = ErrorSender::build_error_frame(
    header.msg_id, result.error());
```

`header.msg_id`가 `uint32_t`이고 `build_error_frame()`도 `uint32_t`를 받으므로 변경 불필요.

### Step 2.5: Session async_send 확인

**파일**: `apex_core/include/apex/core/session.hpp`, `apex_core/src/session.cpp`

- [ ] `async_send(const WireHeader&, ...)` — WireHeader 구조체를 직접 사용하므로 변경 불필요.
- [ ] `session.cpp`의 `async_send()` 내 `WireHeader::SIZE` 사용 확인 — 상수 참조이므로 자동 반영.

```cpp
// session.cpp line 46-47 — SIZE 상수 사용 확인
std::array<uint8_t, WireHeader::SIZE> hdr_buf{};  // 12바이트로 자동 변경
header.serialize(hdr_buf);                          // 새 serialize 호출
```

---

## Chunk 3: WireHeader v2 마이그레이션 — Protocol 구현체 + 테스트

### Step 3.1: TcpBinaryProtocol 확인

**파일**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/tcp_binary_protocol.hpp`

- [ ] `try_decode()`, `consume_frame()` — FrameCodec에 위임하는 래퍼이므로 변경 불필요.
- [ ] `Frame` 타입이 `WireHeader`를 포함하므로 msg_id가 자동으로 `uint32_t`로 반영.

### Step 3.2: WebSocketProtocol 확인

**파일**: `apex_shared/lib/protocols/websocket/include/apex/shared/protocols/websocket/websocket_protocol.hpp`

- [ ] WebSocketProtocol은 독자적 Frame 타입을 사용 (WireHeader 미사용). 변경 불필요.

### Step 3.3: 테스트 수정 — test_wire_header.cpp

**파일**: `apex_core/tests/unit/test_wire_header.cpp`

- [ ] SIZE 테스트 수정:

```cpp
TEST(WireHeader, SizeIs12Bytes) {  // 이름도 변경
    EXPECT_EQ(WireHeader::SIZE, 12u);
}
```

- [ ] DefaultValues 테스트 — version 2 확인:

```cpp
TEST(WireHeader, DefaultValues) {
    WireHeader h;
    EXPECT_EQ(h.version, 2u);  // CURRENT_VERSION = 2
    EXPECT_EQ(h.flags, 0u);
    EXPECT_EQ(h.msg_id, 0u);
    EXPECT_EQ(h.body_size, 0u);
    EXPECT_EQ(h.reserved, 0u);
}
```

- [ ] SerializeAndParse 테스트 — u32 msg_id 값 사용:

```cpp
TEST(WireHeader, SerializeAndParse) {
    WireHeader h;
    h.version = 2;
    h.flags = wire_flags::COMPRESSED;
    h.msg_id = 100042;        // u32 범위 값으로 변경
    h.body_size = 1024;
    h.reserved = 0;

    auto bytes = h.serialize();
    auto result = WireHeader::parse(bytes);
    ASSERT_TRUE(result.has_value());

    EXPECT_EQ(result->version, h.version);
    EXPECT_EQ(result->flags, h.flags);
    EXPECT_EQ(result->msg_id, h.msg_id);
    EXPECT_EQ(result->body_size, h.body_size);
    EXPECT_EQ(result->reserved, h.reserved);
}
```

- [ ] BigEndianByteOrder 테스트 — v2 레이아웃에 맞게:

```cpp
TEST(WireHeader, BigEndianByteOrder) {
    WireHeader h;
    h.flags = 0x12;
    h.msg_id = 0x01020304;       // u32 big-endian
    h.body_size = 0x05060708;

    auto bytes = h.serialize();

    // flags at [1] — u8 직접
    EXPECT_EQ(bytes[1], 0x12);

    // msg_id at [2..5] big-endian
    EXPECT_EQ(bytes[2], 0x01);
    EXPECT_EQ(bytes[3], 0x02);
    EXPECT_EQ(bytes[4], 0x03);
    EXPECT_EQ(bytes[5], 0x04);

    // body_size at [6..9] big-endian
    EXPECT_EQ(bytes[6], 0x05);
    EXPECT_EQ(bytes[7], 0x06);
    EXPECT_EQ(bytes[8], 0x07);
    EXPECT_EQ(bytes[9], 0x08);
}
```

- [ ] FrameSize 테스트:

```cpp
TEST(WireHeader, FrameSize) {
    WireHeader h;
    h.body_size = 256;
    EXPECT_EQ(h.frame_size(), 268u);  // 12 + 256 = 268 (이전: 266)
}
```

- [ ] ParseUnsupportedVersion 테스트 — version 0과 1 모두 rejected:

```cpp
TEST(WireHeader, ParseUnsupportedVersion) {
    WireHeader h;
    auto bytes = h.serialize();
    bytes[0] = 0;  // version 0
    auto result = WireHeader::parse(bytes);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), ParseError::UnsupportedVersion);
}

TEST(WireHeader, ParseV1Rejected) {
    WireHeader h;
    auto bytes = h.serialize();
    bytes[0] = 1;  // 구 v1 version
    auto result = WireHeader::parse(bytes);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), ParseError::UnsupportedVersion);
}
```

- [ ] 나머지 테스트 (ParseInsufficientData, ParseBodyTooLarge, ZeroBodySize, MaxValidBodySize, ParseFutureVersion) — msg_id를 `uint32_t` 범위로 변경하고, SIZE 기대값 조정.

- [ ] 새 테스트 추가 — u32 msg_id 대용량 값:

```cpp
TEST(WireHeader, LargeMsgId) {
    WireHeader h;
    h.msg_id = 0xDEADBEEF;
    auto bytes = h.serialize();
    auto result = WireHeader::parse(bytes);
    ASSERT_TRUE(result.has_value());
    EXPECT_EQ(result->msg_id, 0xDEADBEEF);
}
```

### Step 3.4: 테스트 수정 — test_frame_codec.cpp

**파일**: `apex_core/tests/unit/test_frame_codec.cpp`

- [ ] `build_frame()` 헬퍼의 msg_id 파라미터를 `uint32_t`로 변경:

```cpp
std::vector<uint8_t> build_frame(uint32_t msg_id, std::span<const uint8_t> payload) {
    WireHeader h{.msg_id = msg_id, .body_size = static_cast<uint32_t>(payload.size())};
    // ...
}
```

- [ ] 각 테스트의 `WireHeader::SIZE` 기대값은 상수 참조이므로 자동 반영. 변경 불필요.
- [ ] `EncodeWrapAround` 테스트 — 프레임 크기 계산 코멘트 수정: `WireHeader::SIZE(12) + 8 = 20`.
- [ ] `EncodeInsufficientSpace` 테스트 — 코멘트 수정: `WireHeader::SIZE(12) + 4 = 16`.

### Step 3.5: 테스트 수정 — test_message_dispatcher.cpp

**파일**: `apex_core/tests/unit/test_message_dispatcher.cpp`

- [ ] 모든 핸들러 람다의 msg_id 파라미터를 `uint32_t`로 변경:

```cpp
// 변경 전 (반복되는 패턴)
[&](SessionPtr, uint16_t, std::span<const uint8_t>) -> awaitable<Result<void>> { ... }

// 변경 후
[&](SessionPtr, uint32_t, std::span<const uint8_t>) -> awaitable<Result<void>> { ... }
```

- [ ] `MaxMsgId` 테스트를 u32 최대값으로 변경:

```cpp
TEST_F(MessageDispatcherTest, MaxMsgId) {
    bool called = false;
    d->register_handler(0xFFFFFFFF,
        [&](SessionPtr, uint32_t msg_id, std::span<const uint8_t>) -> awaitable<Result<void>> {
            EXPECT_EQ(msg_id, 0xFFFFFFFF);
            called = true;
            co_return apex::core::ok();
        });
    EXPECT_TRUE(d->has_handler(0xFFFFFFFF));

    auto result = run_coro(io_ctx_, d->dispatch(nullptr, 0xFFFFFFFF, {}));
    EXPECT_TRUE(result.has_value());
    EXPECT_TRUE(called);
}
```

### Step 3.6: 테스트 수정 — test_service_base.cpp

**파일**: `apex_core/tests/unit/test_service_base.cpp`

- [ ] 서비스 메서드 시그니처에서 `uint16_t` → `uint32_t`:

```cpp
// EchoService
awaitable<Result<void>> on_echo(SessionPtr, uint32_t msg_id, std::span<const uint8_t> payload) { ... }
uint32_t last_msg_id = 0;   // uint16_t → uint32_t

// DuplicateHandlerService
awaitable<Result<void>> on_msg(SessionPtr, uint32_t, std::span<const uint8_t>) { ... }

// MultiHandlerService
awaitable<Result<void>> on_msg1(SessionPtr, uint32_t, std::span<const uint8_t>) { ... }
awaitable<Result<void>> on_msg2(SessionPtr, uint32_t, std::span<const uint8_t>) { ... }
awaitable<Result<void>> on_msg3(SessionPtr, uint32_t, std::span<const uint8_t>) { ... }
```

### Step 3.7: 테스트 수정 — test_mocks.hpp

**파일**: `apex_core/tests/test_mocks.hpp`

- [ ] `build_test_frame()` 함수의 msg_id 파라미터를 `uint32_t`로 변경:

```cpp
inline std::vector<uint8_t> build_test_frame(uint32_t msg_id,
                                              std::span<const uint8_t> payload) {
    apex::core::WireHeader header{
        .msg_id = msg_id,
        .body_size = static_cast<uint32_t>(payload.size()),
    };
    // ...
}

inline std::vector<uint8_t> build_test_frame(uint32_t msg_id) {
    return build_test_frame(msg_id, {});
}
```

### Step 3.8: 테스트 수정 — test_connection_handler.cpp

**파일**: `apex_core/tests/unit/test_connection_handler.cpp`

- [ ] 핸들러 람다의 msg_id 파라미터를 `uint32_t`로 변경:

```cpp
// TC2 (line 86-87)
dispatcher.register_handler(0x0042,
    [&](SessionPtr, uint32_t msg_id, std::span<const uint8_t> payload)
        -> boost::asio::awaitable<Result<void>> { ... });

// TC3 (line 123-124)
dispatcher.register_handler(0x0010,
    [&](SessionPtr, uint32_t, std::span<const uint8_t>)
        -> boost::asio::awaitable<Result<void>> { ... });
```

- [ ] `uint16_t dispatched_msg_id` → `uint32_t dispatched_msg_id` (line 83)

- [ ] TC7 (line 266):

```cpp
auto make_handler = [&](uint32_t msg_id) {  // uint16_t → uint32_t
    return [&, msg_id](SessionPtr, uint32_t id, std::span<const uint8_t>)
        -> boost::asio::awaitable<Result<void>> { ... };
};
```

- [ ] TC7 dispatched_ids 벡터 타입: `std::vector<uint32_t>` (line 263)

### Step 3.9: 테스트 수정 — test_flatbuffers_dispatch.cpp

**파일**: `apex_core/tests/unit/test_flatbuffers_dispatch.cpp`

- [ ] `TypedEchoService::on_echo()` msg_id 파라미터:

```cpp
awaitable<Result<void>> on_echo(SessionPtr, uint32_t msg_id,
                        const apex::messages::EchoRequest* req) { ... }
```

- [ ] 핸들러 람다 (line 88):

```cpp
svc->dispatcher().register_handler(0x0020,
    [&](SessionPtr, uint32_t, std::span<const uint8_t>) -> awaitable<Result<void>> { ... });
```

### Step 3.10: 테스트 수정 — test_tcp_binary_protocol.cpp

**파일**: `apex_core/tests/unit/test_tcp_binary_protocol.cpp`

- [ ] `write_frame()` 헬퍼의 msg_id 파라미터를 `uint32_t`로 변경:

```cpp
void write_frame(uint32_t msg_id, const std::vector<uint8_t>& body) {
    WireHeader hdr{.msg_id = msg_id,
                   .body_size = static_cast<uint32_t>(body.size())};
    // ...
}
```

### Step 3.11: 테스트 수정 — test_server_e2e.cpp

**파일**: `apex_core/tests/integration/test_server_e2e.cpp`

- [ ] `TestEchoService::on_echo()` msg_id 파라미터:

```cpp
awaitable<Result<void>> on_echo(SessionPtr session, uint32_t msg_id,
                        const apex::messages::EchoRequest* req) { ... }
```

- [ ] `ThrowingService::on_msg()`, `ErrorReturningService::on_msg()`:

```cpp
awaitable<Result<void>> on_msg(SessionPtr, uint32_t, std::span<const uint8_t>) { ... }
```

### Step 3.12: 기타 테스트 파일 검토

영향받을 수 있는 나머지 테스트 파일 — msg_id 타입이나 WireHeader 사용 여부를 확인:

- [ ] `test_session.cpp` — WireHeader 생성 시 designated initializer에서 `flags` 필드 위치 조정 (v2에서는 `flags`가 `msg_id` 앞으로 이동):

```cpp
// 변경 전
WireHeader header{.msg_id = 0x0042,
                  .body_size = static_cast<uint32_t>(payload.size())};

// 변경 후 (flags가 msg_id 앞이므로 순서 맞춰야 함)
WireHeader header{.msg_id = 0x0042,
                  .body_size = static_cast<uint32_t>(payload.size())};
// Note: C++ designated initializers 순서는 선언 순서여야 하지만,
// flags는 default(0)이므로 생략 가능. msg_id 앞에 .flags = 0을 넣거나 생략.
```

실제로 MSVC에서는 designated initializer 순서를 선언 순서대로 요구하므로, v2에서 필드 순서가 `version, flags, msg_id, body_size, reserved`일 때, `.msg_id = X, .body_size = Y` 형태는 중간 `flags`가 default이므로 OK. 단, `.flags = X` 없이 `.msg_id`로 바로 가면 `flags`가 자동 default-init 됨.

- [ ] `test_session.cpp`의 WireHeader 사용 부분: 필드 순서 확인만으로 충분 (변경 불필요).
- [ ] `test_echo_integration.cpp` — WireHeader 사용 부분 검토.
- [ ] `test_pipeline_integration.cpp` — WireHeader 사용 부분 검토.
- [ ] `test_shared_payload.cpp` — msg_id 관련 타입 확인.
- [ ] `test_session_write_queue.cpp` — WireHeader 미사용이면 패스.
- [ ] `test_error_propagation.cpp` — WireHeader/ErrorSender 사용 여부 확인.
- [ ] `test_server_error_paths.cpp` — 서비스 핸들러 시그니처 확인.
- [ ] `test_server_multicore.cpp` — 서비스 핸들러 시그니처 확인.
- [ ] `test_server_adapter.cpp` — 서비스 핸들러 시그니처 확인.
- [ ] `test_cross_core_dispatcher.cpp` — msg_id 관련 확인.
- [ ] `test_drain_tick.cpp` — 서비스 핸들러 시그니처 확인.
- [ ] `test_shutdown_timeout.cpp` — 서비스 핸들러 시그니처 확인.

> **원칙**: `uint16_t`를 msg_id로 사용하거나 핸들러 시그니처에 `uint16_t`를 쓰는 모든 위치를 `uint32_t`로 변경. WireHeader의 designated initializer에서 필드 순서 위반이 없는지 컴파일러가 검증.

### Step 3.13: 빌드 + 테스트 실행

- [ ] 빌드: `cmd.exe //c build.bat debug` (run_in_background)
- [ ] 테스트: `cd build/debug && ctest --output-on-failure`
- [ ] 전체 테스트 통과 확인
- [ ] 커밋: `feat(wire): WireHeader v2 마이그레이션 — msg_id u16→u32, 헤더 12바이트`

---

## Chunk 4: Transport Concept 정의

### Step 4.1: Transport concept 헤더 생성

**파일 (신규)**: `apex_core/include/apex/core/transport.hpp`

- [ ] Transport concept 정의:

```cpp
#pragma once

#include <apex/core/result.hpp>

#include <boost/asio/awaitable.hpp>
#include <boost/asio/ip/tcp.hpp>

#include <concepts>
#include <cstddef>
#include <span>

namespace apex::core {

/// Transport concept — core에서 정의, shared에서 구현.
/// 의존성 역전: core는 concept만, 구체 Transport는 shared가 제공.
///
/// 요구사항:
///   - T::Config       — Transport별 설정 타입
///   - T::Socket       — 소켓 타입 (tcp::socket 또는 ssl::stream<tcp::socket>)
///   - T::make_socket(io_context&) -> Socket
///   - T::async_accept(acceptor, socket) -> awaitable<Result<void>>
///   - T::async_handshake(socket, config) -> awaitable<Result<void>>
///   - T::async_shutdown(socket) -> awaitable<void>
///
/// Note: async_read/write는 Socket이 AsyncReadStream/AsyncWriteStream을
/// 만족하면 Boost.Asio의 async_read/async_write가 직접 사용 가능하므로
/// concept 요구사항에서 제외한다.
template<typename T>
concept Transport = requires {
    typename T::Config;
    typename T::Socket;
} && requires(boost::asio::io_context& io_ctx) {
    { T::make_socket(io_ctx) } -> std::same_as<typename T::Socket>;
} && requires(boost::asio::ip::tcp::acceptor& acceptor, typename T::Socket& sock) {
    { T::async_accept(acceptor, sock) }
        -> std::same_as<boost::asio::awaitable<Result<void>>>;
} && requires(typename T::Socket& sock, const typename T::Config& cfg) {
    { T::async_handshake(sock, cfg) }
        -> std::same_as<boost::asio::awaitable<Result<void>>>;
} && requires(typename T::Socket& sock) {
    { T::async_shutdown(sock) } -> std::same_as<boost::asio::awaitable<void>>;
};

// --- 컴파일타임 검증용 Mock ---
namespace detail {

struct MockTransport {
    struct Config {};
    using Socket = boost::asio::ip::tcp::socket;

    static Socket make_socket(boost::asio::io_context& ctx) {
        return Socket(ctx);
    }

    static boost::asio::awaitable<Result<void>>
    async_accept(boost::asio::ip::tcp::acceptor& acc, Socket& sock) {
        co_return ok();
    }

    static boost::asio::awaitable<Result<void>>
    async_handshake(Socket&, const Config&) {
        co_return ok();
    }

    static boost::asio::awaitable<void>
    async_shutdown(Socket&) {
        co_return;
    }
};

static_assert(Transport<MockTransport>,
    "MockTransport must satisfy Transport concept");

} // namespace detail

} // namespace apex::core
```

### Step 4.2: PlainTcpTransport 구현

**파일 (신규)**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/plain_tcp_transport.hpp`

- [ ] PlainTcpTransport 구현:

```cpp
#pragma once

#include <apex/core/transport.hpp>

#include <boost/asio/as_tuple.hpp>
#include <boost/asio/ip/tcp.hpp>
#include <boost/asio/use_awaitable.hpp>

namespace apex::shared::protocols::tcp {

/// Plain TCP transport — TLS 없는 기본 TCP 연결.
struct PlainTcpTransport {
    struct Config {};
    using Socket = boost::asio::ip::tcp::socket;

    static Socket make_socket(boost::asio::io_context& ctx) {
        return Socket(ctx);
    }

    [[nodiscard]] static boost::asio::awaitable<apex::core::Result<void>>
    async_accept(boost::asio::ip::tcp::acceptor& acceptor, Socket& sock) {
        auto [ec] = co_await acceptor.async_accept(
            sock,
            boost::asio::as_tuple(boost::asio::use_awaitable));
        if (ec) co_return apex::core::error(apex::core::ErrorCode::AcceptFailed);
        co_return apex::core::ok();
    }

    [[nodiscard]] static boost::asio::awaitable<apex::core::Result<void>>
    async_handshake(Socket&, const Config&) {
        // Plain TCP — handshake is no-op
        co_return apex::core::ok();
    }

    static boost::asio::awaitable<void>
    async_shutdown(Socket& sock) {
        boost::system::error_code ec;
        sock.shutdown(boost::asio::ip::tcp::socket::shutdown_both, ec);
        co_return;
    }
};

// Concept 검증
static_assert(apex::core::Transport<PlainTcpTransport>);

} // namespace apex::shared::protocols::tcp
```

### Step 4.3: TlsTcpTransport 스켈레톤

**파일 (신규)**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/tls_tcp_transport.hpp`

- [ ] TLS Transport 스켈레톤 (실제 구현은 Wave 2 Gateway MVP에서):

```cpp
#pragma once

/// TLS TCP Transport — 스켈레톤.
/// 실제 구현은 Gateway MVP (v0.5.2.0)에서 완성한다.
/// 현재는 타입 정의만 제공하여 Transport concept 만족을 검증한다.
///
/// 구현 시 필요한 의존성:
///   - Boost.Asio ssl::context, ssl::stream
///   - OpenSSL (vcpkg: openssl)
///   - per-core SSL_CTX (Seastar 방식, lock contention 제거)

// TODO(v0.5.2.0): OpenSSL 의존성 추가 후 구현
// #include <boost/asio/ssl.hpp>
// #include <apex/core/transport.hpp>
//
// namespace apex::shared::protocols::tcp {
//
// struct TlsTcpTransport {
//     struct Config {
//         std::string cert_file;
//         std::string key_file;
//         std::string ca_file;
//     };
//     using Socket = boost::asio::ssl::stream<boost::asio::ip::tcp::socket>;
//     // ...
// };
//
// static_assert(apex::core::Transport<TlsTcpTransport>);
//
// } // namespace apex::shared::protocols::tcp
```

### Step 4.4: ErrorCode 추가

**파일**: `apex_core/include/apex/core/error_code.hpp`

- [ ] `AcceptFailed` 에러 코드 추가 (기존 enum에 항목 추가):

```cpp
AcceptFailed,   // Transport::async_accept 실패
```

### Step 4.5: Listener<P, T> 전환

**파일**: `apex_core/include/apex/core/listener.hpp`

- [ ] `Listener` 템플릿 파라미터에 Transport 추가:

```cpp
// 변경 전
template<Protocol P>
class Listener : public ListenerBase {

// 변경 후
template<Protocol P, Transport T = detail::MockTransport>
class Listener : public ListenerBase {
```

> **Note**: 기본 Transport를 `detail::MockTransport`로 두면 기존 `Listener<P>` 코드가 호환된다. 하지만 실제로는 `PlainTcpTransport`를 기본값으로 쓰는 것이 의미상 맞다. 다만 core에서 shared 타입을 기본 인자로 참조하면 순환 의존이 생긴다.
>
> **해결**: core의 transport.hpp에 `PlainTransport`(= plain TCP)를 인라인 정의하거나, 기본값 없이 호출부에서 명시하도록 한다. Server::listen<P, T>에서 T의 기본값을 제공하는 방식이 가장 깔끔하다.

**실제 전략**: `Listener` 자체는 기본값 없이 `<P, T>` 2개 필수. `Server::listen<P, T>`에서 기본값 제공은 shared를 include해야 하므로 불가. 대신 `PlainTcpTransport`를 core 내부에 간단히 정의하거나, 호출부에서 항상 명시하도록 강제.

**최종 결정**: core의 `transport.hpp`에 `DefaultTransport` (= PlainTcpTransport와 동일) 구현을 인라인으로 배치. Listener의 기본 Transport로 사용.

- [ ] `transport.hpp`에 `DefaultTransport` 추가:

```cpp
/// 기본 Transport — Plain TCP (TLS 없음).
/// core 내부 정의로 순환 의존 방지. shared::PlainTcpTransport와 동일한 구현.
struct DefaultTransport {
    struct Config {};
    using Socket = boost::asio::ip::tcp::socket;

    static Socket make_socket(boost::asio::io_context& ctx) {
        return Socket(ctx);
    }

    static boost::asio::awaitable<Result<void>>
    async_accept(boost::asio::ip::tcp::acceptor& acc, Socket& sock) {
        auto [ec] = co_await acc.async_accept(
            sock,
            boost::asio::as_tuple(boost::asio::use_awaitable));
        if (ec) co_return error(ErrorCode::AcceptFailed);
        co_return ok();
    }

    static boost::asio::awaitable<Result<void>>
    async_handshake(Socket&, const Config&) {
        co_return ok();
    }

    static boost::asio::awaitable<void>
    async_shutdown(Socket& sock) {
        boost::system::error_code ec;
        sock.shutdown(boost::asio::ip::tcp::socket::shutdown_both, ec);
        co_return;
    }
};

static_assert(Transport<DefaultTransport>,
    "DefaultTransport must satisfy Transport concept");
```

- [ ] `Listener` 템플릿 기본값:

```cpp
template<Protocol P, Transport T = DefaultTransport>
class Listener : public ListenerBase {
```

현재 단계에서는 Listener 내부 로직은 변경하지 않는다. 기존 `TcpAcceptor`와 `tcp::socket`을 그대로 사용한다. Transport::Socket과 Transport::async_accept로의 실제 전환은 Gateway MVP 단계에서 수행한다. 현재는 타입 시스템만 준비한다.

### Step 4.6: Server::listen<P, T> 전환

**파일**: `apex_core/include/apex/core/server.hpp`

- [ ] `listen()` 시그니처에 Transport 템플릿 파라미터 추가:

```cpp
// 변경 전
template<Protocol P>
Server& listen(uint16_t port, typename P::Config config = {}) {

// 변경 후
template<Protocol P, Transport T = DefaultTransport>
Server& listen(uint16_t port, typename P::Config config = {}) {
```

- [ ] Listener 생성부 변경:

```cpp
// 변경 전
auto listener = std::make_unique<Listener<P>>(
    port, std::move(config), *core_engine_, std::move(mgrs),
    handler_config, config_.reuseport);

// 변경 후
auto listener = std::make_unique<Listener<P, T>>(
    port, std::move(config), *core_engine_, std::move(mgrs),
    handler_config, config_.reuseport);
```

- [ ] `#include <apex/core/transport.hpp>` 추가.

### Step 4.7: ConnectionHandler<P, T> 전환

**파일**: `apex_core/include/apex/core/connection_handler.hpp`

- [ ] 템플릿 파라미터 추가:

```cpp
// 변경 전
template<Protocol P>
class ConnectionHandler {

// 변경 후
template<Protocol P, Transport T = DefaultTransport>
class ConnectionHandler {
```

현재 단계에서는 내부 구현을 변경하지 않는다 (기존 `tcp::socket` 직접 사용 유지). Socket 타입 추상화는 Gateway MVP에서 수행.

### Step 4.8: Listener 내부 PerCoreHandler 타입 변경

**파일**: `apex_core/include/apex/core/listener.hpp`

- [ ] PerCoreHandler의 handler 타입 변경:

```cpp
struct PerCoreHandler {
    MessageDispatcher dispatcher;
    ConnectionHandler<P, T> handler;    // <P> → <P, T>

    PerCoreHandler(SessionManager& session_mgr, ConnectionHandlerConfig config)
        : handler(session_mgr, dispatcher, config) {}
};
```

### Step 4.9: 빌드 + 테스트

- [ ] 빌드: `cmd.exe //c build.bat debug` (run_in_background)
- [ ] 테스트: `cd build/debug && ctest --output-on-failure`
- [ ] 기존 테스트가 모두 통과하는지 확인 (기본 Transport 사용)
- [ ] 커밋: `feat(core): Transport concept 정의 + DefaultTransport + Listener<P,T> 전환`

---

## Chunk 5: Kafka Envelope 정의

### Step 5.1: RoutingHeader 구조체

**파일 (신규)**: `apex_shared/lib/protocols/kafka/include/apex/shared/protocols/kafka/kafka_envelope.hpp`

- [ ] 디렉토리 생성: `apex_shared/lib/protocols/kafka/include/apex/shared/protocols/kafka/`

- [ ] 구조체 + 상수 + encode/decode:

```cpp
#pragma once

#include <array>
#include <cstdint>
#include <cstring>
#include <expected>
#include <span>

namespace apex::shared::protocols::kafka {

// ============================================================
// Routing Header (8바이트, 불변)
// ============================================================
// header_version(u16) | flags(u16) | msg_id(u32)

enum class EnvelopeError : uint8_t {
    InsufficientData,
    UnsupportedVersion,
};

struct RoutingHeader {
    static constexpr size_t SIZE = 8;
    static constexpr uint16_t CURRENT_VERSION = 1;

    static constexpr size_t OFF_HEADER_VERSION = 0;
    static constexpr size_t OFF_FLAGS          = 2;
    static constexpr size_t OFF_MSG_ID         = 4;

    uint16_t header_version{CURRENT_VERSION};
    uint16_t flags{0};
    uint32_t msg_id{0};

    [[nodiscard]] static std::expected<RoutingHeader, EnvelopeError>
    parse(std::span<const uint8_t> data);

    void serialize(std::span<uint8_t, SIZE> out) const;
    [[nodiscard]] std::array<uint8_t, SIZE> serialize() const;
};

// ============================================================
// Routing Flags (u16 비트맵)
// ============================================================
namespace routing_flags {
    // Bit 0: 방향
    constexpr uint16_t DIRECTION_RESPONSE = 0x0001;   // 0=요청, 1=응답
    // Bit 1: 에러
    constexpr uint16_t ERROR_BIT          = 0x0002;
    // Bit 2-3: 전달 방식
    constexpr uint16_t DELIVERY_MASK      = 0x000C;   // 2비트 마스크
    constexpr uint16_t DELIVERY_UNICAST   = 0x0000;   // 00=단일
    constexpr uint16_t DELIVERY_CHANNEL   = 0x0004;   // 01=채널
    constexpr uint16_t DELIVERY_BROADCAST = 0x0008;   // 10=전역
    // Bit 4: 압축
    constexpr uint16_t COMPRESSED         = 0x0010;
    // Bit 5: 암호화
    constexpr uint16_t ENCRYPTED          = 0x0020;
    // Bit 6-7: 우선순위
    constexpr uint16_t PRIORITY_MASK      = 0x00C0;
    constexpr uint16_t PRIORITY_NORMAL    = 0x0000;   // 00=일반
    constexpr uint16_t PRIORITY_HIGH      = 0x0040;   // 01=높음
    constexpr uint16_t PRIORITY_URGENT    = 0x0080;   // 10=긴급
    constexpr uint16_t PRIORITY_SYSTEM    = 0x00C0;   // 11=시스템
    // Bit 8-9: TTL 힌트
    constexpr uint16_t TTL_MASK           = 0x0300;
    constexpr uint16_t TTL_DEFAULT        = 0x0000;
    constexpr uint16_t TTL_SHORT          = 0x0100;
    constexpr uint16_t TTL_LONG           = 0x0200;
    constexpr uint16_t TTL_PERMANENT      = 0x0300;
    // Bit 10: 프래그먼트
    constexpr uint16_t FRAGMENT           = 0x0400;
    // Bit 11-15: 예약
} // namespace routing_flags

// ============================================================
// Metadata Prefix (32바이트, 고정)
// ============================================================
// meta_version(u32) | core_id(u16) | corr_id(u64)
// | source_id(u16) | session_id(u64) | timestamp(u64)

struct MetadataPrefix {
    static constexpr size_t SIZE = 32;
    static constexpr uint32_t CURRENT_VERSION = 1;

    static constexpr size_t OFF_META_VERSION = 0;
    static constexpr size_t OFF_CORE_ID      = 4;
    static constexpr size_t OFF_CORR_ID      = 6;
    static constexpr size_t OFF_SOURCE_ID    = 14;
    static constexpr size_t OFF_SESSION_ID   = 16;
    static constexpr size_t OFF_TIMESTAMP    = 24;

    uint32_t meta_version{CURRENT_VERSION};
    uint16_t core_id{0};
    uint64_t corr_id{0};
    uint16_t source_id{0};     // 0=시스템/Gateway, 1=Auth, 2=Chat, ...
    uint64_t session_id{0};
    uint64_t timestamp{0};     // epoch milliseconds

    [[nodiscard]] static std::expected<MetadataPrefix, EnvelopeError>
    parse(std::span<const uint8_t> data);

    void serialize(std::span<uint8_t, SIZE> out) const;
    [[nodiscard]] std::array<uint8_t, SIZE> serialize() const;
};

// ============================================================
// Full Envelope 상수
// ============================================================
/// Payload 시작 오프셋 = RoutingHeader(8) + MetadataPrefix(32) = 40바이트
static constexpr size_t ENVELOPE_HEADER_SIZE =
    RoutingHeader::SIZE + MetadataPrefix::SIZE;  // 40

/// Source ID 상수
namespace source_ids {
    constexpr uint16_t GATEWAY = 0;
    constexpr uint16_t AUTH    = 1;
    constexpr uint16_t CHAT    = 2;
} // namespace source_ids

} // namespace apex::shared::protocols::kafka
```

### Step 5.2: Kafka Envelope 구현

**파일 (신규)**: `apex_shared/lib/protocols/kafka/src/kafka_envelope.cpp`

- [ ] 디렉토리 생성: `apex_shared/lib/protocols/kafka/src/`

```cpp
#include <apex/shared/protocols/kafka/kafka_envelope.hpp>

#include <bit>

#ifdef _MSC_VER
#include <cstdlib>
#endif

namespace apex::shared::protocols::kafka {

static_assert(std::endian::native == std::endian::little,
              "KafkaEnvelope assumes little-endian host");

namespace {
#ifdef _MSC_VER
inline uint16_t hton16(uint16_t v) { return _byteswap_ushort(v); }
inline uint32_t hton32(uint32_t v) { return _byteswap_ulong(v); }
inline uint64_t hton64(uint64_t v) { return _byteswap_uint64(v); }
#else
inline uint16_t hton16(uint16_t v) { return __builtin_bswap16(v); }
inline uint32_t hton32(uint32_t v) { return __builtin_bswap32(v); }
inline uint64_t hton64(uint64_t v) { return __builtin_bswap64(v); }
#endif
inline uint16_t ntoh16(uint16_t v) { return hton16(v); }
inline uint32_t ntoh32(uint32_t v) { return hton32(v); }
inline uint64_t ntoh64(uint64_t v) { return hton64(v); }
} // anonymous namespace

// --- RoutingHeader ---

std::expected<RoutingHeader, EnvelopeError>
RoutingHeader::parse(std::span<const uint8_t> data) {
    if (data.size() < SIZE) {
        return std::unexpected(EnvelopeError::InsufficientData);
    }

    RoutingHeader h;
    uint16_t raw16;
    uint32_t raw32;

    std::memcpy(&raw16, data.data() + OFF_HEADER_VERSION, sizeof(uint16_t));
    h.header_version = ntoh16(raw16);

    if (h.header_version != CURRENT_VERSION) {
        return std::unexpected(EnvelopeError::UnsupportedVersion);
    }

    std::memcpy(&raw16, data.data() + OFF_FLAGS, sizeof(uint16_t));
    h.flags = ntoh16(raw16);

    std::memcpy(&raw32, data.data() + OFF_MSG_ID, sizeof(uint32_t));
    h.msg_id = ntoh32(raw32);

    return h;
}

void RoutingHeader::serialize(std::span<uint8_t, SIZE> out) const {
    uint16_t net16 = hton16(header_version);
    std::memcpy(out.data() + OFF_HEADER_VERSION, &net16, sizeof(uint16_t));

    net16 = hton16(flags);
    std::memcpy(out.data() + OFF_FLAGS, &net16, sizeof(uint16_t));

    uint32_t net32 = hton32(msg_id);
    std::memcpy(out.data() + OFF_MSG_ID, &net32, sizeof(uint32_t));
}

std::array<uint8_t, RoutingHeader::SIZE> RoutingHeader::serialize() const {
    std::array<uint8_t, SIZE> buf{};
    serialize(std::span<uint8_t, SIZE>{buf});
    return buf;
}

// --- MetadataPrefix ---

std::expected<MetadataPrefix, EnvelopeError>
MetadataPrefix::parse(std::span<const uint8_t> data) {
    if (data.size() < SIZE) {
        return std::unexpected(EnvelopeError::InsufficientData);
    }

    MetadataPrefix m;
    uint16_t raw16;
    uint32_t raw32;
    uint64_t raw64;

    std::memcpy(&raw32, data.data() + OFF_META_VERSION, sizeof(uint32_t));
    m.meta_version = ntoh32(raw32);

    if (m.meta_version != CURRENT_VERSION) {
        return std::unexpected(EnvelopeError::UnsupportedVersion);
    }

    std::memcpy(&raw16, data.data() + OFF_CORE_ID, sizeof(uint16_t));
    m.core_id = ntoh16(raw16);

    std::memcpy(&raw64, data.data() + OFF_CORR_ID, sizeof(uint64_t));
    m.corr_id = ntoh64(raw64);

    std::memcpy(&raw16, data.data() + OFF_SOURCE_ID, sizeof(uint16_t));
    m.source_id = ntoh16(raw16);

    std::memcpy(&raw64, data.data() + OFF_SESSION_ID, sizeof(uint64_t));
    m.session_id = ntoh64(raw64);

    std::memcpy(&raw64, data.data() + OFF_TIMESTAMP, sizeof(uint64_t));
    m.timestamp = ntoh64(raw64);

    return m;
}

void MetadataPrefix::serialize(std::span<uint8_t, SIZE> out) const {
    uint32_t net32 = hton32(meta_version);
    std::memcpy(out.data() + OFF_META_VERSION, &net32, sizeof(uint32_t));

    uint16_t net16 = hton16(core_id);
    std::memcpy(out.data() + OFF_CORE_ID, &net16, sizeof(uint16_t));

    uint64_t net64 = hton64(corr_id);
    std::memcpy(out.data() + OFF_CORR_ID, &net64, sizeof(uint64_t));

    net16 = hton16(source_id);
    std::memcpy(out.data() + OFF_SOURCE_ID, &net16, sizeof(uint16_t));

    net64 = hton64(session_id);
    std::memcpy(out.data() + OFF_SESSION_ID, &net64, sizeof(uint64_t));

    net64 = hton64(timestamp);
    std::memcpy(out.data() + OFF_TIMESTAMP, &net64, sizeof(uint64_t));
}

std::array<uint8_t, MetadataPrefix::SIZE> MetadataPrefix::serialize() const {
    std::array<uint8_t, SIZE> buf{};
    serialize(std::span<uint8_t, SIZE>{buf});
    return buf;
}

} // namespace apex::shared::protocols::kafka
```

### Step 5.3: CMakeLists 추가

**파일 (신규)**: `apex_shared/lib/protocols/kafka/CMakeLists.txt`

- [ ] 라이브러리 정의:

```cmake
# apex_shared protocols/kafka — Kafka Envelope (RoutingHeader + MetadataPrefix)

add_library(apex_protocols_kafka STATIC
    src/kafka_envelope.cpp
)
add_library(apex::shared::protocols::kafka ALIAS apex_protocols_kafka)

target_include_directories(apex_protocols_kafka
    PUBLIC
        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
        $<INSTALL_INTERFACE:include>
)

# Byte-order 유틸은 자체 포함, 외부 의존 없음
if(WIN32)
    target_compile_options(apex_protocols_kafka PUBLIC /utf-8)
    target_compile_definitions(apex_protocols_kafka PUBLIC _WIN32_WINNT=0x0A00)
endif()
```

**파일**: `apex_shared/lib/protocols/CMakeLists.txt`

- [ ] kafka 서브디렉토리 추가:

```cmake
add_subdirectory(kafka)
```

### Step 5.4: 단위 테스트

**파일 (신규)**: `apex_shared/tests/unit/test_kafka_envelope.cpp`

- [ ] RoutingHeader + MetadataPrefix 테스트:

```cpp
#include <apex/shared/protocols/kafka/kafka_envelope.hpp>

#include <gtest/gtest.h>

using namespace apex::shared::protocols::kafka;

// === RoutingHeader Tests ===

TEST(RoutingHeader, SizeIs8Bytes) {
    EXPECT_EQ(RoutingHeader::SIZE, 8u);
}

TEST(RoutingHeader, DefaultValues) {
    RoutingHeader h;
    EXPECT_EQ(h.header_version, RoutingHeader::CURRENT_VERSION);
    EXPECT_EQ(h.flags, 0u);
    EXPECT_EQ(h.msg_id, 0u);
}

TEST(RoutingHeader, SerializeAndParse) {
    RoutingHeader h;
    h.flags = routing_flags::DIRECTION_RESPONSE | routing_flags::PRIORITY_HIGH;
    h.msg_id = 0xDEADBEEF;

    auto bytes = h.serialize();
    auto result = RoutingHeader::parse(bytes);
    ASSERT_TRUE(result.has_value());

    EXPECT_EQ(result->header_version, RoutingHeader::CURRENT_VERSION);
    EXPECT_EQ(result->flags, h.flags);
    EXPECT_EQ(result->msg_id, h.msg_id);
}

TEST(RoutingHeader, BigEndianByteOrder) {
    RoutingHeader h;
    h.header_version = 1;
    h.flags = 0x1234;
    h.msg_id = 0xAABBCCDD;

    auto bytes = h.serialize();

    // header_version at [0..1] big-endian
    EXPECT_EQ(bytes[0], 0x00);
    EXPECT_EQ(bytes[1], 0x01);

    // flags at [2..3] big-endian
    EXPECT_EQ(bytes[2], 0x12);
    EXPECT_EQ(bytes[3], 0x34);

    // msg_id at [4..7] big-endian
    EXPECT_EQ(bytes[4], 0xAA);
    EXPECT_EQ(bytes[5], 0xBB);
    EXPECT_EQ(bytes[6], 0xCC);
    EXPECT_EQ(bytes[7], 0xDD);
}

TEST(RoutingHeader, ParseInsufficientData) {
    std::array<uint8_t, 4> data{};
    auto result = RoutingHeader::parse(data);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), EnvelopeError::InsufficientData);
}

TEST(RoutingHeader, ParseUnsupportedVersion) {
    RoutingHeader h;
    auto bytes = h.serialize();
    bytes[0] = 0x00;
    bytes[1] = 0x99;  // version 153
    auto result = RoutingHeader::parse(bytes);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), EnvelopeError::UnsupportedVersion);
}

TEST(RoutingHeader, DeliveryFlags) {
    RoutingHeader h;
    h.flags = routing_flags::DELIVERY_CHANNEL;
    EXPECT_EQ(h.flags & routing_flags::DELIVERY_MASK,
              routing_flags::DELIVERY_CHANNEL);

    h.flags = routing_flags::DELIVERY_BROADCAST;
    EXPECT_EQ(h.flags & routing_flags::DELIVERY_MASK,
              routing_flags::DELIVERY_BROADCAST);
}

// === MetadataPrefix Tests ===

TEST(MetadataPrefix, SizeIs32Bytes) {
    EXPECT_EQ(MetadataPrefix::SIZE, 32u);
}

TEST(MetadataPrefix, EnvelopeHeaderSizeIs40) {
    EXPECT_EQ(ENVELOPE_HEADER_SIZE, 40u);
}

TEST(MetadataPrefix, DefaultValues) {
    MetadataPrefix m;
    EXPECT_EQ(m.meta_version, MetadataPrefix::CURRENT_VERSION);
    EXPECT_EQ(m.core_id, 0u);
    EXPECT_EQ(m.corr_id, 0u);
    EXPECT_EQ(m.source_id, 0u);
    EXPECT_EQ(m.session_id, 0u);
    EXPECT_EQ(m.timestamp, 0u);
}

TEST(MetadataPrefix, SerializeAndParse) {
    MetadataPrefix m;
    m.core_id = 7;
    m.corr_id = 0x0123456789ABCDEFull;
    m.source_id = source_ids::AUTH;
    m.session_id = 0xFEDCBA9876543210ull;
    m.timestamp = 1710547200000ull;

    auto bytes = m.serialize();
    auto result = MetadataPrefix::parse(bytes);
    ASSERT_TRUE(result.has_value());

    EXPECT_EQ(result->meta_version, MetadataPrefix::CURRENT_VERSION);
    EXPECT_EQ(result->core_id, 7u);
    EXPECT_EQ(result->corr_id, 0x0123456789ABCDEFull);
    EXPECT_EQ(result->source_id, source_ids::AUTH);
    EXPECT_EQ(result->session_id, 0xFEDCBA9876543210ull);
    EXPECT_EQ(result->timestamp, 1710547200000ull);
}

TEST(MetadataPrefix, ParseInsufficientData) {
    std::array<uint8_t, 16> data{};
    auto result = MetadataPrefix::parse(data);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), EnvelopeError::InsufficientData);
}

TEST(MetadataPrefix, ParseUnsupportedVersion) {
    MetadataPrefix m;
    auto bytes = m.serialize();
    // 버전 바이트를 99로 변경
    bytes[0] = 0x00; bytes[1] = 0x00; bytes[2] = 0x00; bytes[3] = 0x63;
    auto result = MetadataPrefix::parse(bytes);
    ASSERT_FALSE(result.has_value());
    EXPECT_EQ(result.error(), EnvelopeError::UnsupportedVersion);
}

TEST(MetadataPrefix, SourceIds) {
    EXPECT_EQ(source_ids::GATEWAY, 0u);
    EXPECT_EQ(source_ids::AUTH, 1u);
    EXPECT_EQ(source_ids::CHAT, 2u);
}
```

**파일**: `apex_shared/tests/unit/CMakeLists.txt`

- [ ] `test_kafka_envelope.cpp` 추가하고 `apex_protocols_kafka` 링크.

### Step 5.5: 빌드 + 테스트

- [ ] 빌드: `cmd.exe //c build.bat debug` (run_in_background)
- [ ] 테스트: `cd build/debug && ctest --output-on-failure -R kafka_envelope`
- [ ] 커밋: `feat(shared): Kafka Envelope 정의 — RoutingHeader + MetadataPrefix + 단위 테스트`

---

## Chunk 6: FlatBuffers 스키마 기반 구축

### Step 6.1: 공통 스키마 디렉토리 생성

- [ ] 디렉토리 생성: `apex_shared/schemas/common/`

### Step 6.2: GatewayError enum + SystemResponse table

**파일 (신규)**: `apex_shared/schemas/common/system_response.fbs`

- [ ] 기존 `gateway_envelope.fbs`와 `error_envelope.fbs`는 v0.5 최소 셋이므로 유지. Wave 2용 새 스키마를 common/에 추가:

```fbs
namespace apex.shared.schemas.common;

enum GatewayError : uint16 {
    NONE = 0,
    RATE_LIMITED_IP = 1,
    RATE_LIMITED_USER = 2,
    RATE_LIMITED_ENDPOINT = 3,
    JWT_EXPIRED = 11,
    JWT_INVALID = 12,
    JWT_BLACKLISTED = 13,
    SERVICE_TIMEOUT = 21,
    SERVICE_UNAVAILABLE = 22,
    INVALID_MSG_ID = 31,
}

table SystemResponse {
    error: GatewayError = NONE;
    message: string;
    retry_after_ms: uint32;
    limited_msg_id: uint32;
}

root_type SystemResponse;
```

### Step 6.3: msg_registry.toml 초기 구조

**파일 (신규)**: `apex_shared/schemas/msg_registry.toml`

- [ ] 시스템 msg_id 초기 할당:

```toml
# msg_registry.toml — 중앙 msg_id 레지스트리
# coordinator가 msg_id 할당 독점. 에이전트는 .fbs 스키마만 작성.
# 규칙: 퇴역된 msg_id는 영원히 재사용 금지.

[system]
range = [0, 999]
description = "시스템/Gateway 예약 범위"

[[system.allocated]]
id = 1
name = "Heartbeat"
status = "active"
schema = "apex_core/schemas/heartbeat.fbs"

[[system.allocated]]
id = 2
name = "SystemErrorResponse"
status = "active"
schema = "apex_core/schemas/error_response.fbs"

[[system.allocated]]
id = 10
name = "RefreshTokenRequest"
status = "reserved"

[[system.allocated]]
id = 11
name = "RefreshTokenResponse"
status = "reserved"

[auth]
range = [1000, 1999]
description = "Auth Service 메시지 범위"

[[auth.allocated]]
id = 1000
name = "LoginRequest"
status = "reserved"

[[auth.allocated]]
id = 1001
name = "LoginResponse"
status = "reserved"

[chat]
range = [2000, 2999]
description = "Chat Service 메시지 범위"

[[chat.allocated]]
id = 2000
name = "ChatSendMessage"
status = "reserved"

[[chat.allocated]]
id = 2001
name = "ChatReceiveMessage"
status = "reserved"

[[chat.allocated]]
id = 2010
name = "JoinRoomRequest"
status = "reserved"

[[chat.allocated]]
id = 2011
name = "JoinRoomResponse"
status = "reserved"

[[chat.allocated]]
id = 2020
name = "LeaveRoomRequest"
status = "reserved"

[[chat.allocated]]
id = 2021
name = "LeaveRoomResponse"
status = "reserved"

[[chat.allocated]]
id = 2030
name = "RoomListRequest"
status = "reserved"

[[chat.allocated]]
id = 2031
name = "RoomListResponse"
status = "reserved"

[[chat.allocated]]
id = 2040
name = "CreateRoomRequest"
status = "reserved"

[[chat.allocated]]
id = 2041
name = "CreateRoomResponse"
status = "reserved"

[[chat.allocated]]
id = 2050
name = "ChatHistoryRequest"
status = "reserved"

[[chat.allocated]]
id = 2051
name = "ChatHistoryResponse"
status = "reserved"

[[chat.allocated]]
id = 2060
name = "WhisperRequest"
status = "reserved"

[[chat.allocated]]
id = 2061
name = "WhisperResponse"
status = "reserved"
```

### Step 6.4: CMake에 공통 스키마 빌드 규칙 확인

- [ ] 현재 `apex_core/CMakeLists.txt`에 FlatBuffers 빌드 규칙이 있음. `apex_shared/schemas/common/`의 스키마는 Gateway MVP 단계에서 별도 CMake target으로 추가 예정. 현재는 파일만 생성.

### Step 6.5: 커밋

- [ ] 커밋: `feat(shared): FlatBuffers 공통 스키마 + msg_registry.toml 초기 구조`

---

## Chunk 7: 전체 빌드 + 테스트 통과 확인

### Step 7.1: 전체 빌드

- [ ] 빌드: `cmd.exe //c build.bat debug` (run_in_background)
- [ ] 빌드 성공 확인

### Step 7.2: 전체 테스트

- [ ] 테스트: `cd build/debug && ctest --output-on-failure`
- [ ] 실패 테스트 수집

### Step 7.3: 실패 테스트 수정

- [ ] 각 실패 테스트의 원인을 분석하고 수정
- [ ] 주로 예상되는 실패:
  - `uint16_t` → `uint32_t` 누락된 위치
  - WireHeader designated initializer 필드 순서 (v2 레이아웃에서 `flags`가 `msg_id` 앞)
  - `WireHeader::SIZE` 변경에 따른 하드코딩된 매직 넘버

### Step 7.4: 최종 확인

- [ ] 전체 테스트 통과 확인
- [ ] 커밋: `fix(tests): WireHeader v2 마이그레이션에 따른 테스트 수정`

---

## 수정 대상 파일 요약

### Task 1 (WireHeader v2) — 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `apex_shared/lib/protocols/tcp/include/.../wire_header.hpp` | 구조체 v2 레이아웃 |
| `apex_core/src/wire_header.cpp` | parse/serialize v2 구현 |
| `apex_core/include/apex/core/message_dispatcher.hpp` | uint16_t → uint32_t |
| `apex_core/src/message_dispatcher.cpp` | uint16_t → uint32_t |
| `apex_core/include/apex/core/service_base.hpp` | uint16_t → uint32_t |
| `apex_core/include/apex/core/error_sender.hpp` | uint16_t → uint32_t |
| `apex_core/src/error_sender.cpp` | uint16_t → uint32_t + 필드 순서 |
| `apex_core/tests/unit/test_wire_header.cpp` | v2 테스트 |
| `apex_core/tests/unit/test_frame_codec.cpp` | build_frame u32 |
| `apex_core/tests/unit/test_message_dispatcher.cpp` | 핸들러 시그니처 u32 |
| `apex_core/tests/unit/test_service_base.cpp` | 핸들러 시그니처 u32 |
| `apex_core/tests/unit/test_connection_handler.cpp` | 핸들러 시그니처 u32 |
| `apex_core/tests/unit/test_flatbuffers_dispatch.cpp` | 핸들러 시그니처 u32 |
| `apex_core/tests/unit/test_tcp_binary_protocol.cpp` | write_frame u32 |
| `apex_core/tests/test_mocks.hpp` | build_test_frame u32 |
| `apex_core/tests/integration/test_server_e2e.cpp` | 핸들러 시그니처 u32 |
| 기타 테스트 (확인 필요) | uint16_t → uint32_t |

### Task 2 (Transport concept) — 신규/수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `apex_core/include/apex/core/transport.hpp` **(신규)** | Transport concept + DefaultTransport |
| `apex_core/include/apex/core/error_code.hpp` | AcceptFailed 추가 |
| `apex_shared/lib/protocols/tcp/include/.../plain_tcp_transport.hpp` **(신규)** | PlainTcpTransport |
| `apex_shared/lib/protocols/tcp/include/.../tls_tcp_transport.hpp` **(신규)** | TLS 스켈레톤 |
| `apex_core/include/apex/core/listener.hpp` | Listener<P, T> |
| `apex_core/include/apex/core/server.hpp` | listen<P, T> |
| `apex_core/include/apex/core/connection_handler.hpp` | ConnectionHandler<P, T> |

### Task 3 (Kafka Envelope) — 신규 파일

| 파일 | 변경 내용 |
|------|----------|
| `apex_shared/lib/protocols/kafka/include/.../kafka_envelope.hpp` **(신규)** | RoutingHeader + MetadataPrefix |
| `apex_shared/lib/protocols/kafka/src/kafka_envelope.cpp` **(신규)** | 구현 |
| `apex_shared/lib/protocols/kafka/CMakeLists.txt` **(신규)** | 빌드 설정 |
| `apex_shared/lib/protocols/CMakeLists.txt` | kafka 서브디렉토리 추가 |
| `apex_shared/tests/unit/test_kafka_envelope.cpp` **(신규)** | 단위 테스트 |
| `apex_shared/tests/unit/CMakeLists.txt` | 테스트 추가 |

### Task 4 (FlatBuffers 스키마) — 신규 파일

| 파일 | 변경 내용 |
|------|----------|
| `apex_shared/schemas/common/system_response.fbs` **(신규)** | GatewayError + SystemResponse |
| `apex_shared/schemas/msg_registry.toml` **(신규)** | msg_id 레지스트리 |

---

## 에이전트 병렬 실행 가이드

### Worker 1: WireHeader v2 (Chunk 1-3)
- **수정 가능 파일**: 위 Task 1 파일 목록 전체
- **금지**: transport.hpp, kafka_envelope.hpp, schemas/common/

### Worker 2: Transport Concept (Chunk 4)
- **수정 가능 파일**: transport.hpp, plain_tcp_transport.hpp, tls_tcp_transport.hpp, error_code.hpp, listener.hpp, server.hpp, connection_handler.hpp
- **금지**: wire_header.hpp, message_dispatcher.hpp, service_base.hpp, 테스트 파일

### Worker 3: Kafka Envelope (Chunk 5)
- **수정 가능 파일**: kafka/ 디렉토리 전체, protocols/CMakeLists.txt, test_kafka_envelope.cpp, tests/unit/CMakeLists.txt
- **금지**: wire_header, transport, core 파일

### Worker 4: FlatBuffers 스키마 (Chunk 6)
- **수정 가능 파일**: schemas/common/, msg_registry.toml
- **금지**: 코드 파일

### Coordinator: 전체 빌드 + 테스트 (Chunk 7)
- Worker 1-4 완료 후 빌드 + 통합 테스트
