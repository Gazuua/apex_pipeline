# Phase 5.5 Tier 3: 구조 개선 + I/O 최적화 — 구현 계획

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Server 클래스에서 ConnectionHandler를 분리하고, SO_REUSEPORT per-core acceptor를 도입하며, io_uring CMake 옵션과 SlabPool auto-grow를 추가하여 코드 구조와 I/O 성능을 완성한다.

**Architecture:** 4단계 — (1) ConnectionHandler 추출로 Server 책임 분리, (2) SO_REUSEPORT per-core accept + control_io_ 전환 (Linux/Windows 분기), (3) io_uring 빌드 옵션 추가, (4) SlabPool 자동 확장 + 메트릭.

**Tech Stack:** C++23, Boost.Asio, SO_REUSEPORT (Linux), IOCP (Windows), io_uring (optional), CMake

**v6 계획서 참조**: `docs/apex_common/plans/20260312_070255_phase5_5_v6.md` § 3.1, § 4.1, § 4.2, § 6 Tier 3

**선행 조건**: Tier 2 (메모리 아키텍처) 완료, Tier 1 Task 4 (zero-copy dispatch) 완료

> **참고**: v6 #14 (코루틴 프레임 최적화)는 Tier 1 Task 4에서 `stack_buf` 제거로 자동 해결됨. Tier 3 scope에서 제외.

---

## File Structure

### New Files

| File | Purpose |
|------|---------|
| `apex_core/include/apex/core/connection_handler.hpp` | ConnectionHandler 클래스 — read_loop + process_frames 캡슐화 |
| `apex_core/src/connection_handler.cpp` | ConnectionHandler 구현 |
| `apex_core/tests/unit/test_connection_handler.cpp` | ConnectionHandler 단위 테스트 |

### Modified Files

| File | Change |
|------|--------|
| `apex_core/include/apex/core/server.hpp` | read_loop/process_frames 제거 → ConnectionHandler 위임, `accept_io_` → `control_io_`, per-core acceptor 지원, `active_sessions_` 위치 변경 |
| `apex_core/src/server.cpp` | on_accept → ConnectionHandler 위임, SO_REUSEPORT 분기, control_io_ 전환 |
| `apex_core/include/apex/core/tcp_acceptor.hpp` | `reuseport` 생성자 옵션 추가 |
| `apex_core/src/tcp_acceptor.cpp` | SO_REUSEPORT 소켓 옵션 설정 |
| `apex_core/include/apex/core/slab_pool.hpp` | `auto_grow` 옵션, `grow()` public화, 메트릭 멤버 추가 |
| `apex_core/src/slab_pool.cpp` | allocate() 내 auto-grow 호출, 메트릭 업데이트 |
| `apex_core/CMakeLists.txt` | `connection_handler.cpp` 추가, io_uring 조건부 정의, test 추가 |
| `apex_core/tests/unit/test_slab_pool.cpp` | auto-grow + 메트릭 테스트 추가 |
| `apex_core/tests/unit/test_tcp_acceptor.cpp` | SO_REUSEPORT 테스트 추가 (Linux only) |
| `apex_core/tests/integration/test_server_e2e.cpp` | ConnectionHandler 분리 후 E2E 동작 검증 |

---

## Chunk 1: ConnectionHandler 분리 (Tasks 1–2)

### Task 1: ConnectionHandler 클래스 추출

**Files:**
- Create: `apex_core/include/apex/core/connection_handler.hpp`
- Create: `apex_core/src/connection_handler.cpp`
- Create: `apex_core/tests/unit/test_connection_handler.cpp`
- Modify: `apex_core/CMakeLists.txt`

- [ ] **Step 1: test_connection_handler.cpp — 기본 동작 테스트 작성**

```cpp
#include <apex/core/connection_handler.hpp>
#include <apex/core/session.hpp>
#include <apex/core/session_manager.hpp>
#include <apex/core/message_dispatcher.hpp>
#include "../test_helpers.hpp"

#include <boost/asio/io_context.hpp>
#include <boost/asio/ip/tcp.hpp>
#include <gtest/gtest.h>

using namespace apex::core;
using boost::asio::ip::tcp;

class ConnectionHandlerTest : public ::testing::Test {
protected:
    void SetUp() override {
        session_mgr_ = std::make_unique<SessionManager>(0, 300, 1024, 8192);
    }

    boost::asio::io_context io_ctx_;
    std::unique_ptr<SessionManager> session_mgr_;
    MessageDispatcher dispatcher_;
};

TEST_F(ConnectionHandlerTest, Construction) {
    ConnectionHandlerConfig config{};
    ConnectionHandler handler(*session_mgr_, dispatcher_, config);
    EXPECT_EQ(handler.active_sessions(), 0u);
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug"`
Expected: FAIL — `connection_handler.hpp` 미존재

- [ ] **Step 3: connection_handler.hpp 작성**

```cpp
#pragma once

#include <apex/core/message_dispatcher.hpp>
#include <apex/core/session.hpp>
#include <apex/core/session_manager.hpp>

#include <spdlog/spdlog.h>

#include <boost/asio/awaitable.hpp>
#include <boost/asio/io_context.hpp>
#include <boost/asio/ip/tcp.hpp>

#include <atomic>
#include <cstdint>
#include <memory>

namespace apex::core {

/// Configuration for ConnectionHandler behavior.
struct ConnectionHandlerConfig {
    bool tcp_nodelay = true;
};

/// Handles the lifecycle of a single TCP connection:
/// read_loop (recv → parse → dispatch) and process_frames.
///
/// Extracted from Server to separate connection handling from server lifecycle.
/// Each ConnectionHandler is bound to a single core's PerCoreState.
class ConnectionHandler {
public:
    ConnectionHandler(SessionManager& session_mgr,
                      MessageDispatcher& dispatcher,
                      ConnectionHandlerConfig config);

    /// Accept a new connection — create session + spawn read_loop.
    /// Must be called on the owning core's io_context thread.
    void accept_connection(boost::asio::ip::tcp::socket socket,
                           boost::asio::io_context& io_ctx);

    /// Number of currently active read_loop coroutines.
    [[nodiscard]] uint32_t active_sessions() const noexcept {
        return active_sessions_.load(std::memory_order_acquire);
    }

private:
    boost::asio::awaitable<void> read_loop(SessionPtr session);
    boost::asio::awaitable<void> process_frames(SessionPtr session);

    SessionManager& session_mgr_;
    MessageDispatcher& dispatcher_;
    ConnectionHandlerConfig config_;
    std::atomic<uint32_t> active_sessions_{0};
    std::shared_ptr<spdlog::logger> logger_;
};

} // namespace apex::core
```

> **설계 결정**: `active_sessions_`는 ConnectionHandler에 귀속. Server::active_sessions_는 모든 코어의 합산이 필요하므로, Server가 per-core ConnectionHandler의 active_sessions()를 합산하는 방식으로 전환.
>
> **PerCoreState 참조 방식** (v5 검토사항 해결): ConnectionHandler는 SessionManager&와 MessageDispatcher& 개별 참조. PerCoreState 전체를 전달하지 않음 — 필요한 의존성만 명시적으로 주입.

- [ ] **Step 4: connection_handler.cpp 작성**

```cpp
#include <apex/core/connection_handler.hpp>
#include <apex/core/tcp_binary_protocol.hpp>
#include <apex/core/error_sender.hpp>

#include <boost/asio/as_tuple.hpp>
#include <boost/asio/co_spawn.hpp>
#include <boost/asio/detached.hpp>
#include <boost/asio/use_awaitable.hpp>

#include <cstring>
#include <span>

namespace apex::core {

ConnectionHandler::ConnectionHandler(SessionManager& session_mgr,
                                     MessageDispatcher& dispatcher,
                                     ConnectionHandlerConfig config)
    : session_mgr_(session_mgr)
    , dispatcher_(dispatcher)
    , config_(config)
    , logger_(spdlog::get("apex"))
{
}

void ConnectionHandler::accept_connection(
    boost::asio::ip::tcp::socket socket,
    boost::asio::io_context& io_ctx)
{
    if (config_.tcp_nodelay) {
        boost::system::error_code ec;
        socket.set_option(boost::asio::ip::tcp::no_delay(true), ec);
    }

    auto session = session_mgr_.create_session(std::move(socket));
    boost::asio::co_spawn(io_ctx,
        read_loop(std::move(session)),
        boost::asio::detached);
}

boost::asio::awaitable<void> ConnectionHandler::read_loop(SessionPtr session) {
    active_sessions_.fetch_add(1, std::memory_order_relaxed);

    while (session->is_open()) {
        auto& rb = session->recv_buffer();
        auto writable = rb.writable();
        if (writable.empty()) {
            session->close();
            break;
        }

        auto [ec, n] = co_await session->socket().async_read_some(
            boost::asio::buffer(writable.data(), writable.size()),
            boost::asio::as_tuple(boost::asio::use_awaitable));
        if (ec || n == 0) break;

        rb.commit_write(n);
        co_await process_frames(session);
        if (!session->is_open()) break;

        session_mgr_.touch_session(session->id());
    }

    session_mgr_.remove_session(session->id());
    active_sessions_.fetch_sub(1, std::memory_order_release);
}

boost::asio::awaitable<void> ConnectionHandler::process_frames(SessionPtr session) {
    auto& recv_buf = session->recv_buffer();

    for (;;) {
        auto decode_result = TcpBinaryProtocol::try_decode(recv_buf);
        if (!decode_result) {
            if (decode_result.error() != FrameError::InsufficientData) {
                session->close();
                co_return;
            }
            break;
        }
        auto& frame = *decode_result;
        auto header = frame.header;

        // Tier 1 Task 4에서 zero-copy로 전환됨:
        // payload는 RingBuffer 직접 참조, dispatch 후 consume.
        // 여기서는 Tier 1 완료 후의 코드를 반영.
        // (Tier 1 미완료 시 기존 stack_buf/heap_buf 복사 유지)
        auto payload_span = frame.payload;
        auto result = co_await dispatcher_.dispatch(
            session, header.msg_id, payload_span);
        TcpBinaryProtocol::consume_frame(recv_buf, frame);

        if (!result.has_value()) {
            ErrorCode code = (result.error() == DispatchError::UnknownMessage)
                ? ErrorCode::HandlerNotFound
                : ErrorCode::Unknown;
            auto error_frame = ErrorSender::build_error_frame(
                header.msg_id, code);
            (void)co_await session->async_send_raw(error_frame);
        } else if (!result.value().has_value()) {
            auto error_frame = ErrorSender::build_error_frame(
                header.msg_id, result.value().error());
            (void)co_await session->async_send_raw(error_frame);
        }

        if (!session->is_open()) break;
    }
}

} // namespace apex::core
```

> **주의**: `process_frames`의 dispatch 결과 처리 패턴은 Tier 0.5 (에러 타입 통일) 후 `Result<void>` 단일 반환으로 단순화됨. 위 코드는 Tier 0.5 이전 상태 기준. 구현 시 실제 코드 상태에 맞춰야 함.

- [ ] **Step 5: CMakeLists.txt — connection_handler.cpp 추가**

`apex_core/CMakeLists.txt`의 `add_library(apex_core STATIC ...)` 목록에:
```cmake
    src/connection_handler.cpp
```

`apex_core/tests/unit/CMakeLists.txt`에 테스트 추가:
```cmake
    test_connection_handler.cpp
```

- [ ] **Step 6: 빌드 + 테스트 확인**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug" && ctest --preset debug -V`
Expected: PASS — ConnectionHandler 단독 동작 확인

- [ ] **Step 7: 커밋**

```bash
git add apex_core/include/apex/core/connection_handler.hpp \
        apex_core/src/connection_handler.cpp \
        apex_core/tests/unit/test_connection_handler.cpp \
        apex_core/CMakeLists.txt \
        apex_core/tests/unit/CMakeLists.txt
git commit -m "feat(core): ConnectionHandler 클래스 추출 — read_loop + process_frames 캡슐화"
```

---

### Task 2: Server → ConnectionHandler 위임 + PerCoreState 리팩토링

**Files:**
- Modify: `apex_core/include/apex/core/server.hpp`
- Modify: `apex_core/src/server.cpp`
- Test: `apex_core/tests/integration/test_server_e2e.cpp`

- [ ] **Step 1: server.hpp — ConnectionHandler 통합**

변경 사항:
1. `#include <apex/core/connection_handler.hpp>` 추가
2. PerCoreState에 `ConnectionHandler handler` 멤버 추가
3. Server에서 `read_loop`, `process_frames` 선언 제거
4. `active_sessions_` 원자 변수를 Server에서 제거 (ConnectionHandler로 이관)
5. `total_active_sessions()` 메서드 추가 (per-core 합산)

```cpp
struct PerCoreState {
    uint32_t core_id;
    SessionManager session_mgr;
    MessageDispatcher dispatcher;
    ConnectionHandler handler;  // NEW
    std::vector<std::unique_ptr<ServiceBaseInterface>> services;

    explicit PerCoreState(uint32_t id, uint32_t heartbeat_timeout_ticks,
                          size_t timer_wheel_slots, size_t recv_buf_capacity,
                          ConnectionHandlerConfig handler_config);
};
```

Server 클래스에서 제거:
```cpp
// 제거: read_loop, process_frames 선언
// 제거: std::atomic<uint32_t> active_sessions_{0};
// 제거: static constexpr size_t TMP_BUF_SIZE = 4096;
```

Server 클래스에 추가:
```cpp
/// Total active sessions across all cores.
[[nodiscard]] uint32_t total_active_sessions() const noexcept;
```

- [ ] **Step 2: server.cpp — on_accept 위임 + shutdown 수정**

`on_accept`:
```cpp
void Server::on_accept(boost::asio::ip::tcp::socket socket) {
    uint32_t core_id = next_core_.fetch_add(1, std::memory_order_relaxed)
                       % config_.num_cores;
    auto& core_io = core_engine_->io_context(core_id);
    boost::asio::post(core_io, [this, core_id,
                                s = std::move(socket)]() mutable {
        per_core_[core_id]->handler.accept_connection(
            std::move(s), core_engine_->io_context(core_id));
    });
}
```

`total_active_sessions`:
```cpp
uint32_t Server::total_active_sessions() const noexcept {
    uint32_t total = 0;
    for (const auto& state : per_core_) {
        total += state->handler.active_sessions();
    }
    return total;
}
```

`poll_shutdown`에서 `active_sessions_` → `total_active_sessions()`:
```cpp
void Server::poll_shutdown() {
    if (total_active_sessions() == 0) {
        // ...
    }
    // ...
}
```

`PerCoreState` 생성자:
```cpp
PerCoreState::PerCoreState(uint32_t id, uint32_t heartbeat_timeout_ticks,
                           size_t timer_wheel_slots, size_t recv_buf_capacity,
                           ConnectionHandlerConfig handler_config)
    : core_id(id)
    , session_mgr(id, heartbeat_timeout_ticks, timer_wheel_slots, recv_buf_capacity)
    , handler(session_mgr, dispatcher, handler_config)
{
}
```

- [ ] **Step 3: begin_shutdown — ConnectionHandler 경유 세션 종료**

기존 `begin_shutdown()`의 `for_each(close)` 로직은 유지. SessionManager를 직접 참조하므로 ConnectionHandler 분리와 무관.

```cpp
void Server::begin_shutdown() {
    // 기존 코드 유지 — SessionManager::for_each는 ConnectionHandler와 독립
    for (uint32_t i = 0; i < config_.num_cores; ++i) {
        boost::asio::post(core_engine_->io_context(i),
            [this, i] {
                per_core_[i]->session_mgr.for_each([](SessionPtr session) {
                    session->close();
                });
            });
    }
    // ...
}
```

- [ ] **Step 4: Server 생성자 — PerCoreState 생성 인자 변경**

```cpp
ConnectionHandlerConfig handler_config{
    .tcp_nodelay = config_.tcp_nodelay,
};

for (uint32_t i = 0; i < config_.num_cores; ++i) {
    per_core_.push_back(std::make_unique<PerCoreState>(
        i, config_.heartbeat_timeout_ticks,
        config_.timer_wheel_slots, config_.recv_buf_capacity,
        handler_config));
}
```

- [ ] **Step 5: 전체 빌드 + 테스트**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug" && ctest --preset debug -V`
Expected: PASS — 모든 기존 E2E 테스트 통과 (동작 변경 없음)

- [ ] **Step 6: 커밋**

```bash
git add apex_core/include/apex/core/server.hpp \
        apex_core/src/server.cpp
git commit -m "refactor(server): read_loop/process_frames → ConnectionHandler 위임"
```

---

## Chunk 2: SO_REUSEPORT + control_io_ (Task 3)

### Task 3: SO_REUSEPORT per-core acceptor + accept_io_ → control_io_ 전환

**Files:**
- Modify: `apex_core/include/apex/core/tcp_acceptor.hpp`
- Modify: `apex_core/src/tcp_acceptor.cpp`
- Modify: `apex_core/include/apex/core/server.hpp`
- Modify: `apex_core/src/server.cpp`
- Test: `apex_core/tests/unit/test_tcp_acceptor.cpp`
- Test: `apex_core/tests/integration/test_server_e2e.cpp`

> **플랫폼 분기**:
> - **Linux**: per-core TcpAcceptor + SO_REUSEPORT → 커널이 incoming connection 직접 분배
> - **Windows**: 단일 TcpAcceptor (현재와 동일) — SO_REUSEPORT 미지원, atomic round-robin 유지

- [ ] **Step 1: test_tcp_acceptor.cpp — SO_REUSEPORT 테스트 추가**

```cpp
#ifndef _WIN32
TEST(TcpAcceptor, ReusePortMultipleAcceptors) {
    boost::asio::io_context io1, io2;
    std::atomic<int> count1{0}, count2{0};

    TcpAcceptor acc1(io1, 0, [&](tcp::socket) { ++count1; },
                     tcp::v4(), /*reuseport=*/true);
    acc1.start();
    auto port = acc1.port();

    TcpAcceptor acc2(io2, port, [&](tcp::socket) { ++count2; },
                     tcp::v4(), /*reuseport=*/true);
    acc2.start();

    std::thread t1([&] { io1.run(); });
    std::thread t2([&] { io2.run(); });

    // 여러 연결 시도 — 커널이 두 acceptor에 분배
    boost::asio::io_context client_ctx;
    for (int i = 0; i < 10; ++i) {
        tcp::socket client(client_ctx);
        client.connect(tcp::endpoint(boost::asio::ip::address_v4::loopback(), port));
        client.close();
    }

    apex::test::wait_for([&] { return count1.load() + count2.load() >= 10; },
                          std::chrono::milliseconds(3000));
    EXPECT_EQ(count1.load() + count2.load(), 10);

    acc1.stop(); acc2.stop();
    t1.join(); t2.join();
}
#endif
```

- [ ] **Step 2: 테스트 실패 확인**

Expected: FAIL — TcpAcceptor에 `reuseport` 파라미터 없음

- [ ] **Step 3: tcp_acceptor.hpp — reuseport 옵션 추가**

```cpp
class TcpAcceptor {
public:
    /// @param reuseport  Enable SO_REUSEPORT (Linux only, ignored on Windows).
    TcpAcceptor(boost::asio::io_context& io_ctx, uint16_t port,
                AcceptCallback on_accept,
                boost::asio::ip::tcp protocol = boost::asio::ip::tcp::v4(),
                bool reuseport = false);
    // ... 나머지 동일 ...

private:
    // ...
    bool reuseport_;
};
```

- [ ] **Step 4: tcp_acceptor.cpp — SO_REUSEPORT 설정**

```cpp
TcpAcceptor::TcpAcceptor(boost::asio::io_context& io_ctx, uint16_t port,
                          AcceptCallback on_accept,
                          boost::asio::ip::tcp protocol,
                          bool reuseport)
    : io_ctx_(io_ctx)
    , acceptor_(io_ctx)
    , backoff_timer_(io_ctx)
    , on_accept_(std::move(on_accept))
    , reuseport_(reuseport)
{
    acceptor_.open(protocol);

    // SO_REUSEADDR — 필수 (bind 재사용)
    acceptor_.set_option(boost::asio::socket_base::reuse_address(true));

#if !defined(_WIN32) && defined(SO_REUSEPORT)
    if (reuseport_) {
        using reuseport_option = boost::asio::detail::socket_option::boolean<
            SOL_SOCKET, SO_REUSEPORT>;
        acceptor_.set_option(reuseport_option(true));
    }
#endif

    acceptor_.bind(boost::asio::ip::tcp::endpoint(protocol, port));
    acceptor_.listen();
}
```

> **Windows 분기**: `#if !defined(_WIN32)` 가드로 SO_REUSEPORT 코드 전체를 제외. Windows에서는 reuseport=true를 전달해도 무시됨 (silent no-op).

- [ ] **Step 5: server.hpp — accept_io_ → control_io_ + per-core acceptor**

```cpp
// 변경: accept_io_ → control_io_
boost::asio::io_context control_io_;

// 변경: 단일 acceptor → per-core acceptor (Linux) or 단일 (Windows)
std::vector<std::unique_ptr<TcpAcceptor>> acceptors_;

// ServerConfig 추가:
struct ServerConfig {
    // ... 기존 필드 ...
    bool reuseport = false;  // Linux: per-core SO_REUSEPORT, Windows: ignored
};
```

- [ ] **Step 6: server.cpp — per-core acceptor 생성 (Linux) / 단일 acceptor (Windows)**

```cpp
// Server 생성자 내:
#if !defined(_WIN32)
    if (config_.reuseport) {
        // Linux: per-core acceptor — 각 코어의 io_context에서 accept
        for (uint32_t i = 0; i < config_.num_cores; ++i) {
            auto& core_io = core_engine_->io_context(i);
            auto acc = std::make_unique<TcpAcceptor>(
                core_io, config_.port,
                [this, i](boost::asio::ip::tcp::socket socket) {
                    // 이미 올바른 코어의 io_context에서 실행 중
                    per_core_[i]->handler.accept_connection(
                        std::move(socket), core_engine_->io_context(i));
                },
                boost::asio::ip::tcp::v4(),
                /*reuseport=*/true);
            acceptors_.push_back(std::move(acc));
        }
    } else
#endif
    {
        // Windows 또는 reuseport=false: 단일 acceptor on control_io_
        acceptors_.push_back(std::make_unique<TcpAcceptor>(
            control_io_, config_.port,
            [this](boost::asio::ip::tcp::socket socket) {
                on_accept(std::move(socket));
            }));
    }
```

- [ ] **Step 7: server.cpp — accept_io_ → control_io_ 일괄 치환**

모든 `accept_io_` 참조를 `control_io_`로 변경:
- `Server::run()`: `control_io_.restart()`, `control_io_.run()`, `make_work_guard(control_io_)`
- `Server::stop()`: `boost::asio::post(control_io_, ...)`
- `Server::begin_shutdown()`: `shutdown_timer_ = make_unique<steady_timer>(control_io_)`
- `Server::finalize_shutdown()`: `control_io_.stop()`

> **이름 변경 근거**: control_io_는 더 이상 accept 전용이 아님 (reuseport 시 accept는 core_io에서 실행). 시그널 핸들링 + shutdown 조율 전용 → `control_io_`가 정확한 이름.

- [ ] **Step 8: acceptor start/stop — 벡터 순회**

```cpp
// Server::run() 내:
for (auto& acc : acceptors_) acc->start();

// Server::stop() 내:
for (auto& acc : acceptors_) acc->stop();

// Server::port() — 첫 번째 acceptor의 포트 반환:
uint16_t Server::port() const noexcept {
    return acceptors_.empty() ? 0 : acceptors_[0]->port();
}
```

- [ ] **Step 9: 전체 빌드 + 테스트**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug" && ctest --preset debug -V`
Expected: PASS — Windows에서는 reuseport=false (기본값)이므로 기존 동작 유지

- [ ] **Step 10: 커밋**

```bash
git add apex_core/include/apex/core/tcp_acceptor.hpp \
        apex_core/src/tcp_acceptor.cpp \
        apex_core/include/apex/core/server.hpp \
        apex_core/src/server.cpp \
        apex_core/tests/unit/test_tcp_acceptor.cpp
git commit -m "feat(server): SO_REUSEPORT per-core acceptor + accept_io_ → control_io_ 전환"
```

---

## Chunk 3: io_uring CMake + SlabPool auto-grow (Tasks 4–5)

### Task 4: io_uring CMake 옵션 구현

**Files:**
- Modify: `apex_core/CMakeLists.txt`
- Modify: `CMakePresets.json` (선택 — io_uring preset 추가)

> **범위**: 코드 변경 없음. CMake 빌드 옵션만 추가. Boost.Asio는 `BOOST_ASIO_HAS_IO_URING` 정의 시 자동으로 io_uring 백엔드 사용.

- [ ] **Step 1: CMakeLists.txt — io_uring 조건부 정의**

기존 `option(APEX_USE_IO_URING ...)` 아래에 추가:

```cmake
# io_uring backend (Linux only)
option(APEX_USE_IO_URING "Enable io_uring backend for Boost.Asio" OFF)

if(APEX_USE_IO_URING)
    if(NOT UNIX OR APPLE)
        message(FATAL_ERROR "APEX_USE_IO_URING requires Linux")
    endif()

    # Boost.Asio io_uring 활성화
    target_compile_definitions(apex_core PUBLIC
        BOOST_ASIO_HAS_IO_URING
        BOOST_ASIO_DISABLE_EPOLL  # epoll 비활성화, io_uring 전용
    )

    # liburing 링크
    find_library(URING_LIB uring REQUIRED)
    target_link_libraries(apex_core PUBLIC ${URING_LIB})
endif()
```

- [ ] **Step 2: CMakePresets.json — io_uring preset 추가 (선택)**

```json
{
    "name": "iouring",
    "displayName": "Debug + io_uring",
    "inherits": "debug",
    "condition": {
        "type": "equals",
        "lhs": "${hostSystemName}",
        "rhs": "Linux"
    },
    "cacheVariables": {
        "APEX_USE_IO_URING": "ON"
    }
}
```

- [ ] **Step 3: Windows 빌드 확인 (io_uring OFF)**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug" && ctest --preset debug -V`
Expected: PASS — APEX_USE_IO_URING=OFF (기본값), 기존 동작 무변경

- [ ] **Step 4: 커밋**

```bash
git add apex_core/CMakeLists.txt CMakePresets.json
git commit -m "build(core): io_uring CMake 옵션 구현 (APEX_USE_IO_URING)"
```

---

### Task 5: SlabPool auto-grow + 메트릭

**Files:**
- Modify: `apex_core/include/apex/core/slab_pool.hpp`
- Modify: `apex_core/src/slab_pool.cpp`
- Test: `apex_core/tests/unit/test_slab_pool.cpp`

- [ ] **Step 1: test_slab_pool.cpp — auto-grow 테스트 추가**

```cpp
TEST(SlabPool, AutoGrowOnExhaustion) {
    // auto_grow=true, initial=4, grow_chunk=4, max_total=16
    SlabPool pool(64, 4, SlabPoolConfig{.auto_grow = true, .grow_chunk_size = 4, .max_total_count = 16});
    EXPECT_EQ(pool.total_count(), 4u);

    // 4개 할당 후 exhaustion
    std::vector<void*> ptrs;
    for (int i = 0; i < 4; ++i) {
        ptrs.push_back(pool.allocate());
        ASSERT_NE(ptrs.back(), nullptr);
    }
    EXPECT_EQ(pool.free_count(), 0u);
    EXPECT_EQ(pool.grow_count(), 0u);

    // 5번째 할당 → auto-grow 트리거
    void* p5 = pool.allocate();
    ASSERT_NE(p5, nullptr);
    ptrs.push_back(p5);
    EXPECT_EQ(pool.total_count(), 8u);  // 4 + 4(grow_chunk)
    EXPECT_EQ(pool.grow_count(), 1u);

    // max_total 도달 시 nullptr
    for (int i = 0; i < 3; ++i) ptrs.push_back(pool.allocate());
    // total=8, allocated=8 → 다시 grow → total=12
    for (int i = 0; i < 4; ++i) ptrs.push_back(pool.allocate());
    // total=12, allocated=12 → grow → total=16
    for (int i = 0; i < 4; ++i) ptrs.push_back(pool.allocate());
    // total=16, allocated=16 → max → nullptr
    EXPECT_EQ(pool.allocate(), nullptr);
    EXPECT_EQ(pool.total_count(), 16u);

    for (void* p : ptrs) pool.deallocate(p);
}

TEST(SlabPool, MetricsTracking) {
    SlabPool pool(64, 10, SlabPoolConfig{.auto_grow = false});

    void* p1 = pool.allocate();
    void* p2 = pool.allocate();
    void* p3 = pool.allocate();
    EXPECT_EQ(pool.peak_allocated(), 3u);

    pool.deallocate(p2);
    EXPECT_EQ(pool.peak_allocated(), 3u);  // high water mark 유지

    pool.deallocate(p1);
    pool.deallocate(p3);
    EXPECT_EQ(pool.peak_allocated(), 3u);  // 여전히 3
}

TEST(SlabPool, AutoGrowDisabledByDefault) {
    SlabPool pool(64, 4);  // 기존 생성자 — auto_grow=false
    for (int i = 0; i < 4; ++i) pool.allocate();
    EXPECT_EQ(pool.allocate(), nullptr);  // 기존 동작 유지
}
```

- [ ] **Step 2: 테스트 실패 확인**

Expected: FAIL — `SlabPoolConfig`, `grow_count()`, `peak_allocated()` 미존재

- [ ] **Step 3: slab_pool.hpp — SlabPoolConfig + 메트릭 추가**

```cpp
/// Configuration for SlabPool behavior.
struct SlabPoolConfig {
    bool auto_grow = false;      // true: exhaustion 시 자동 확장
    size_t grow_chunk_size = 0;  // 0: initial_count와 동일한 크기로 grow
    size_t max_total_count = 0;  // 0: 제한 없음
};

class SlabPool {
public:
    /// Original constructor — auto_grow=false (backward compatible).
    SlabPool(size_t slot_size, size_t initial_count);

    /// Extended constructor with config.
    SlabPool(size_t slot_size, size_t initial_count, SlabPoolConfig config);

    // ... 기존 API 유지 ...

    /// Number of times the pool has grown (0 = never grown).
    [[nodiscard]] size_t grow_count() const noexcept;

    /// Peak number of simultaneously allocated slots (high water mark).
    [[nodiscard]] size_t peak_allocated() const noexcept;

private:
    // ... 기존 멤버 유지 ...
    SlabPoolConfig config_;
    size_t initial_count_;   // grow_chunk_size=0 시 사용
    size_t grow_count_{0};
    size_t peak_allocated_{0};
};
```

- [ ] **Step 4: slab_pool.cpp — auto-grow + 메트릭 구현**

기존 생성자 위임:
```cpp
SlabPool::SlabPool(size_t slot_size, size_t initial_count)
    : SlabPool(slot_size, initial_count, SlabPoolConfig{})
{
}

SlabPool::SlabPool(size_t slot_size, size_t initial_count, SlabPoolConfig config)
    : free_list_(nullptr)
    , slot_size_(align_up(std::max(slot_size, sizeof(FreeNode)), alignof(std::max_align_t)))
    , total_count_(0)
    , free_count_(0)
    , config_(config)
    , initial_count_(initial_count)
{
    if (initial_count == 0) {
        throw std::invalid_argument("SlabPool: initial_count must be > 0");
    }
    grow(initial_count);
}
```

allocate() 수정:
```cpp
void* SlabPool::allocate() {
    if (!free_list_) {
        if (!config_.auto_grow) return nullptr;

        // max_total 체크
        if (config_.max_total_count > 0 && total_count_ >= config_.max_total_count) {
            return nullptr;
        }

        size_t chunk = config_.grow_chunk_size > 0
            ? config_.grow_chunk_size
            : initial_count_;

        // max_total 초과 방지
        if (config_.max_total_count > 0) {
            chunk = std::min(chunk, config_.max_total_count - total_count_);
        }

        if (chunk == 0) return nullptr;
        grow(chunk);
        ++grow_count_;
    }

    FreeNode* node = free_list_;
    free_list_ = node->next;
    --free_count_;
    node->magic = SLAB_MAGIC_ALLOCATED;

    // 메트릭 업데이트
    size_t current = allocated_count();
    if (current > peak_allocated_) {
        peak_allocated_ = current;
    }

    return static_cast<void*>(node);
}
```

메트릭 getter:
```cpp
size_t SlabPool::grow_count() const noexcept { return grow_count_; }
size_t SlabPool::peak_allocated() const noexcept { return peak_allocated_; }
```

- [ ] **Step 5: TypedSlabPool — config 전달**

```cpp
template <typename T>
class TypedSlabPool {
public:
    explicit TypedSlabPool(size_t initial_count)
        : pool_(sizeof(T), initial_count) {}

    TypedSlabPool(size_t initial_count, SlabPoolConfig config)
        : pool_(sizeof(T), initial_count, config) {}

    // ... 기존 API + 메트릭 전달 ...
    [[nodiscard]] size_t grow_count() const noexcept { return pool_.grow_count(); }
    [[nodiscard]] size_t peak_allocated() const noexcept { return pool_.peak_allocated(); }

    // ... 나머지 동일 ...
};
```

- [ ] **Step 6: 전체 빌드 + 테스트**

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat debug" && ctest --preset debug -V`
Expected: PASS — 기존 테스트 (auto_grow=false 기본값) + 신규 테스트 전부 통과

- [ ] **Step 7: ASAN 테스트** (grow 후 포인터 유효성 검증)

Run: `cmd.exe //c "D:\.workspace\apex_core\build.bat asan" && ctest --preset asan -V`
Expected: PASS — grow()는 새 chunk 추가 방식, 기존 포인터 불변

- [ ] **Step 8: 커밋**

```bash
git add apex_core/include/apex/core/slab_pool.hpp \
        apex_core/src/slab_pool.cpp \
        apex_core/tests/unit/test_slab_pool.cpp
git commit -m "feat(core): SlabPool auto-grow + peak_allocated/grow_count 메트릭"
```

---

## Summary

### 핵심 변경 4가지
1. **ConnectionHandler 분리**: Server(라이프사이클) ↔ ConnectionHandler(연결 처리) 책임 분리
2. **SO_REUSEPORT**: Linux per-core acceptor (커널 분배) / Windows 단일 acceptor (atomic round-robin) 이중 전략
3. **io_uring CMake**: `APEX_USE_IO_URING=ON` → `BOOST_ASIO_HAS_IO_URING` + liburing 자동 링크
4. **SlabPool auto-grow**: exhaustion 시 자동 확장 + max_total 상한 + peak_allocated/grow_count 메트릭

### 아키텍처 결정
- **ConnectionHandler → PerCoreState 멤버**: SessionManager&, MessageDispatcher& 개별 참조 (최소 의존성 주입)
- **active_sessions_ 이관**: Server → ConnectionHandler per-core. Server는 합산 조회.
- **accept_io_ → control_io_**: 시그널 + shutdown 전용으로 명확화
- **auto_grow 기본 OFF**: 기존 동작 100% 호환. Tier 2의 Session SlabPool에서 필요 시 활성화.

### Task 별 커밋 (5개)
1. `feat(core): ConnectionHandler 클래스 추출 — read_loop + process_frames 캡슐화`
2. `refactor(server): read_loop/process_frames → ConnectionHandler 위임`
3. `feat(server): SO_REUSEPORT per-core acceptor + accept_io_ → control_io_ 전환`
4. `build(core): io_uring CMake 옵션 구현 (APEX_USE_IO_URING)`
5. `feat(core): SlabPool auto-grow + peak_allocated/grow_count 메트릭`
