# v0.4.0.1 공통 추상화 + 코어 변경 — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모든 외부 어댑터의 기반이 되는 공통 추상화(AdapterBase, ConnectionPool, AdapterError)를 구현하고, apex_core에 어댑터 등록/라이프사이클 API를 추가한다.

**Architecture:** AdapterBase와 ConnectionPool은 CRTP 패턴으로 컴파일타임 다형성을 제공한다. Server에 add_adapter API를 추가하여 어댑터를 인프라 컴포넌트로 등록하고, Graceful Shutdown 시퀀스를 재배치한다. 어댑터는 전역 1개 인스턴스로 등록되며 내부적으로 CoreEngine::current_core_id()를 이용해 코어별 리소스를 라우팅한다.

**Tech Stack:** C++23, Boost.Asio, Google Test, CMake

**Spec:** `docs/apex_common/plans/20260313_063601_v0.4_external_adapters_design.md`

---

## File Structure

### 신규 파일

```
apex_core/include/apex/core/
└── adapter_interface.hpp                                   ← AdapterInterface 순수 가상 (순환의존 방지)

apex_shared/lib/adapters/
├── CMakeLists.txt                                          ← 어댑터 서브디렉토리 총괄
└── common/
    ├── CMakeLists.txt                                      ← apex::shared::adapters::common 타겟
    ├── include/apex/shared/adapters/
    │   ├── adapter_base.hpp                                ← AdapterBase<Derived> CRTP + AdapterWrapper
    │   ├── adapter_error.hpp                               ← AdapterError 구조체
    │   └── connection_pool.hpp                             ← ConnectionPool<Derived> CRTP
    └── src/
        └── adapter_error.cpp                               ← error_code_name 확장 등

apex_shared/tests/
├── CMakeLists.txt                                          ← 테스트 총괄
└── unit/
    ├── CMakeLists.txt                                      ← apex_shared 단위 테스트 타겟
    ├── test_adapter_error.cpp                              ← AdapterError 테스트
    ├── test_adapter_base.cpp                               ← AdapterBase 라이프사이클 테스트
    └── test_connection_pool.cpp                            ← ConnectionPool acquire/release/health 테스트
```

### 수정 파일

```
apex_shared/CMakeLists.txt                                  ← add_subdirectory(lib/adapters), add_subdirectory(tests)
apex_core/include/apex/core/error_code.hpp                  ← AdapterError = 2000 추가
apex_core/include/apex/core/server.hpp                      ← add_adapter<T>/adapter<T> API (adapter_interface.hpp만 include)
apex_core/src/server.cpp                                    ← 어댑터 저장소, init, shutdown 재배치
apex_core/tests/unit/CMakeLists.txt                         ← test_server_adapter 추가
apex_core/tests/unit/test_server_adapter.cpp (신규)         ← Server 어댑터 등록 테스트
```

### ⚠️ 순환 의존성 방지 설계

`apex_core` ↔ `apex_shared::adapters::common` 순환을 피하기 위해:
- **AdapterInterface** (순수 가상 인터페이스) → `apex_core`에 배치 (server.hpp가 include)
- **AdapterBase<Derived>**, **AdapterWrapper<Derived>** → `apex_shared`에 배치 (apex_core에 의존)
- `server.hpp`는 `adapter_interface.hpp`만 include → CMake 의존성 단방향 유지
- `add_adapter<T>()`는 template이므로 호출자가 `adapter_base.hpp` include 시 컴파일됨

---

## Task 1: CMake 인프라 구축

**Files:**
- Create: `apex_shared/lib/adapters/CMakeLists.txt`
- Create: `apex_shared/lib/adapters/common/CMakeLists.txt`
- Create: `apex_shared/lib/adapters/common/src/adapter_error.cpp` (placeholder)
- Modify: `apex_shared/CMakeLists.txt`

- [ ] **Step 1: 어댑터 common CMakeLists.txt 작성**

```cmake
# apex_shared/lib/adapters/common/CMakeLists.txt
add_library(apex_shared_adapters_common STATIC
    src/adapter_error.cpp
)
add_library(apex::shared::adapters::common ALIAS apex_shared_adapters_common)

target_include_directories(apex_shared_adapters_common
    PUBLIC
        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
        $<INSTALL_INTERFACE:include>
)

target_link_libraries(apex_shared_adapters_common
    PUBLIC
        apex::core
)

target_compile_features(apex_shared_adapters_common PUBLIC cxx_std_23)

if(WIN32)
    target_compile_options(apex_shared_adapters_common PUBLIC /utf-8)
endif()
```

- [ ] **Step 2: 어댑터 총괄 CMakeLists.txt 작성**

```cmake
# apex_shared/lib/adapters/CMakeLists.txt
add_subdirectory(common)
# add_subdirectory(kafka)   # v0.4.1
# add_subdirectory(redis)   # v0.4.2
# add_subdirectory(pg)      # v0.4.3
```

- [ ] **Step 3: placeholder 소스 생성**

```cpp
// apex_shared/lib/adapters/common/src/adapter_error.cpp
#include <apex/shared/adapters/adapter_error.hpp>

namespace apex::shared::adapters {
// 구현은 header-only. 링커 타겟용 번역 단위.
} // namespace apex::shared::adapters
```

- [ ] **Step 4: 통합 vcpkg.json 미리 생성 (Plans 2-4 충돌 방지)**

> Plans 2-4가 병렬 또는 순차 진행될 때 `apex_shared/vcpkg.json`과 루트 `vcpkg.json` 충돌을 방지하기 위해,
> Plan 1에서 모든 어댑터 의존성을 미리 선언한다.

```json
// apex_shared/vcpkg.json (신규)
{
  "$schema": "https://raw.githubusercontent.com/microsoft/vcpkg-tool/main/docs/vcpkg.schema.json",
  "name": "apex-shared",
  "version-semver": "0.4.0",
  "description": "Apex Shared — Adapters and shared utilities",
  "dependencies": [
    "librdkafka",
    "hiredis",
    {
      "name": "redis-plus-plus",
      "features": ["cxx17"]
    },
    "libpq"
  ]
}
```

루트 `vcpkg.json`에도 추가:
```json
"dependencies": [
    ...기존...,
    "librdkafka",
    "hiredis",
    "redis-plus-plus",
    "libpq"
]
```

> Plans 2-4는 이 파일을 수정하지 않고, 자신의 CMakeLists.txt에서 `find_package()`만 하면 됨.

- [ ] **Step 5: apex_shared/CMakeLists.txt 수정**

기존 파일 끝에 추가:
```cmake
# --- Adapters ---
add_subdirectory(lib/adapters)
```

- [ ] **Step 6: 빌드 확인**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
```
Expected: 빌드 성공 (새 타겟 포함, vcpkg 의존성 설치)

- [ ] **Step 7: 커밋**

```bash
git add apex_shared/lib/adapters/ apex_shared/CMakeLists.txt apex_shared/vcpkg.json vcpkg.json
git commit -m "build: v0.4.0.1 어댑터 공통 CMake 인프라 + 전체 vcpkg 의존성"
```

---

## Task 2: AdapterError 구현 + ErrorCode 확장

**Files:**
- Create: `apex_shared/lib/adapters/common/include/apex/shared/adapters/adapter_error.hpp`
- Modify: `apex_core/include/apex/core/error_code.hpp`

- [ ] **Step 1: AdapterError 헤더 작성**

```cpp
// apex_shared/lib/adapters/common/include/apex/shared/adapters/adapter_error.hpp
#pragma once

#include <apex/core/error_code.hpp>

#include <cstdint>
#include <string>
#include <string_view>

namespace apex::shared::adapters {

/// 어댑터 에러 상세 컨텍스트.
/// ErrorCode::AdapterError와 함께 사용하여 라이브러리별 에러 정보를 전달.
struct AdapterError {
    apex::core::ErrorCode code = apex::core::ErrorCode::AdapterError;
    uint32_t native_error = 0;      ///< 라이브러리 원본 에러코드
    std::string message;            ///< 사람 읽기용 상세

    /// 에러 여부 확인
    [[nodiscard]] bool ok() const noexcept {
        return code == apex::core::ErrorCode::Ok;
    }

    /// 축약 문자열 (로깅용)
    [[nodiscard]] std::string to_string() const;
};

} // namespace apex::shared::adapters
```

- [ ] **Step 2: adapter_error.cpp 구현**

```cpp
// apex_shared/lib/adapters/common/src/adapter_error.cpp
#include <apex/shared/adapters/adapter_error.hpp>

#include <sstream>

namespace apex::shared::adapters {

std::string AdapterError::to_string() const {
    std::ostringstream oss;
    oss << apex::core::error_code_name(code);
    if (native_error != 0) {
        oss << " (native=" << native_error << ")";
    }
    if (!message.empty()) {
        oss << ": " << message;
    }
    return oss.str();
}

} // namespace apex::shared::adapters
```

- [ ] **Step 3: ErrorCode에 AdapterError 추가**

`apex_core/include/apex/core/error_code.hpp` 수정:

```cpp
// Application errors (1000-1999)
AppError = 1000,

// Adapter errors (2000+)
AdapterError = 2000,
```

`error_code_name()` switch에 추가:
```cpp
case ErrorCode::AdapterError: return "AdapterError";
```

- [ ] **Step 4: 빌드 확인**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
```

- [ ] **Step 5: 커밋**

```bash
git add apex_shared/lib/adapters/common/ apex_core/include/apex/core/error_code.hpp
git commit -m "feat: AdapterError 구조체 + ErrorCode::AdapterError 추가"
```

---

## Task 3: AdapterInterface (apex_core) + AdapterBase CRTP (apex_shared) 구현

**Files:**
- Create: `apex_core/include/apex/core/adapter_interface.hpp` ← **순환의존 방지 핵심**
- Create: `apex_shared/lib/adapters/common/include/apex/shared/adapters/adapter_base.hpp`

- [ ] **Step 1: AdapterInterface를 apex_core에 작성**

> **설계 이유**: `server.hpp`가 이 인터페이스를 include해야 하므로 `apex_core` 내부에 배치.
> `apex_shared`에 두면 `apex_core` → `apex_shared` → `apex_core` 순환 의존이 발생한다.

```cpp
// apex_core/include/apex/core/adapter_interface.hpp
#pragma once

#include <string_view>

namespace apex::core {

class CoreEngine;  // forward declaration

/// 어댑터 타입 소거 인터페이스 (Server 내부 저장용).
/// AdapterBase<Derived>의 AdapterWrapper가 이를 구현한다.
/// apex_core에 배치하여 server.hpp가 apex_shared에 의존하지 않게 한다.
class AdapterInterface {
public:
    virtual ~AdapterInterface() = default;
    virtual void init(CoreEngine& engine) = 0;
    virtual void drain() = 0;
    virtual void close() = 0;
    [[nodiscard]] virtual bool is_ready() const noexcept = 0;
    [[nodiscard]] virtual std::string_view name() const noexcept = 0;
};

} // namespace apex::core
```

- [ ] **Step 2: AdapterBase CRTP + AdapterWrapper를 apex_shared에 작성**

```cpp
// apex_shared/lib/adapters/common/include/apex/shared/adapters/adapter_base.hpp
#pragma once

#include <apex/shared/adapters/adapter_error.hpp>
#include <apex/core/adapter_interface.hpp>
#include <apex/core/core_engine.hpp>
#include <apex/core/result.hpp>

#include <atomic>
#include <string_view>

namespace apex::shared::adapters {

/// 모든 어댑터의 공통 라이프사이클을 관리하는 CRTP 기본 클래스.
///
/// Derived가 구현해야 할 메서드:
///   void do_init(apex::core::CoreEngine& engine)
///   void do_drain()
///   void do_close()
///   std::string_view do_name() const noexcept
///
/// 소유 모델: Server에 전역 1개 인스턴스로 등록.
/// 코어별 리소스는 Derived 내부에서 CoreEngine::current_core_id()로 라우팅.
template <typename Derived>
class AdapterBase {
public:
    AdapterBase() = default;
    ~AdapterBase() = default;  // CRTP — virtual 불필요 (타입 소거는 AdapterWrapper가 담당)

    // Non-copyable, non-movable
    AdapterBase(const AdapterBase&) = delete;
    AdapterBase& operator=(const AdapterBase&) = delete;
    AdapterBase(AdapterBase&&) = delete;
    AdapterBase& operator=(AdapterBase&&) = delete;

    /// 어댑터 초기화. Server::run()에서 서비스 시작 전에 호출.
    void init(apex::core::CoreEngine& engine) {
        static_cast<Derived*>(this)->do_init(engine);
        ready_.store(true, std::memory_order_release);
    }

    /// 새 요청 거부 시그널. 진행 중 요청은 허용.
    void drain() {
        ready_.store(false, std::memory_order_release);
        static_cast<Derived*>(this)->do_drain();
    }

    /// 리소스 정리. flush + 커넥션 해제.
    void close() {
        static_cast<Derived*>(this)->do_close();
    }

    /// init 완료 + drain 안 됨
    [[nodiscard]] bool is_ready() const noexcept {
        return ready_.load(std::memory_order_acquire);
    }

    /// 어댑터 이름
    [[nodiscard]] std::string_view name() const noexcept {
        return static_cast<const Derived*>(this)->do_name();
    }

private:
    std::atomic<bool> ready_{false};
};

/// AdapterBase<Derived>를 AdapterInterface(apex_core)로 감싸는 래퍼.
/// Server::add_adapter<T>()에서 생성되어 타입 소거된 인터페이스로 저장된다.
template <typename Derived>
class AdapterWrapper final : public apex::core::AdapterInterface {
public:
    template <typename... Args>
    explicit AdapterWrapper(Args&&... args)
        : adapter_(std::forward<Args>(args)...) {}

    void init(apex::core::CoreEngine& engine) override { adapter_.init(engine); }
    void drain() override { adapter_.drain(); }
    void close() override { adapter_.close(); }
    [[nodiscard]] bool is_ready() const noexcept override { return adapter_.is_ready(); }
    [[nodiscard]] std::string_view name() const noexcept override { return adapter_.name(); }

    Derived& get() noexcept { return adapter_; }
    const Derived& get() const noexcept { return adapter_; }

private:
    Derived adapter_;
};

} // namespace apex::shared::adapters
```

- [ ] **Step 2: 빌드 확인**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
```

- [ ] **Step 3: 커밋**

```bash
git add apex_shared/lib/adapters/common/include/apex/shared/adapters/adapter_base.hpp
git commit -m "feat: AdapterBase CRTP + AdapterInterface 타입 소거"
```

---

## Task 4: ConnectionPool CRTP 구현

**Files:**
- Create: `apex_shared/lib/adapters/common/include/apex/shared/adapters/connection_pool.hpp`

- [ ] **Step 1: ConnectionPool 헤더 작성**

```cpp
// apex_shared/lib/adapters/common/include/apex/shared/adapters/connection_pool.hpp
#pragma once

#include <apex/shared/adapters/adapter_error.hpp>
#include <apex/core/result.hpp>

#include <chrono>
#include <cstddef>
#include <deque>
#include <functional>

namespace apex::shared::adapters {

/// ConnectionPool 설정
struct PoolConfig {
    size_t min_size = 1;                                         ///< 최소 유지 커넥션
    size_t max_size = 8;                                         ///< 최대 확장 한도
    std::chrono::seconds max_idle_time{60};                      ///< 유휴 커넥션 폐기 시간
    std::chrono::seconds health_check_interval{30};              ///< 헬스 체크 주기
};

/// Redis/PG 커넥션 풀 공통 추상화 (CRTP).
/// 코어별 독립 인스턴스로 사용 — 락 불필요.
///
/// Derived가 구현해야 할 메서드:
///   using ConnectionType = ...;
///   ConnectionType do_create_connection()
///   void do_destroy_connection(ConnectionType& conn)
///   bool do_validate(ConnectionType& conn)
template <typename Derived>
class ConnectionPool {
public:
    using Connection = typename Derived::ConnectionType;

    explicit ConnectionPool(PoolConfig config) : config_(std::move(config)) {}
    ~ConnectionPool() { close_all(); }

    // Non-copyable, non-movable
    ConnectionPool(const ConnectionPool&) = delete;
    ConnectionPool& operator=(const ConnectionPool&) = delete;

    /// 풀에서 커넥션 획득
    [[nodiscard]] apex::core::Result<Connection> acquire() {
        // 유휴 커넥션에서 꺼내기
        while (!idle_.empty()) {
            auto conn = std::move(idle_.front().conn);
            idle_.pop_front();
            if (derived().do_validate(conn)) {
                ++active_count_;
                return conn;
            }
            derived().do_destroy_connection(conn);
            --total_count_;
        }
        // 새 커넥션 생성 (한도 내)
        if (total_count_ < config_.max_size) {
            auto conn = derived().do_create_connection();
            ++total_count_;
            ++active_count_;
            return conn;
        }
        return std::unexpected(apex::core::ErrorCode::AdapterError);
    }

    /// 커넥션 반환
    void release(Connection conn) {
        --active_count_;
        idle_.push_back({std::move(conn), std::chrono::steady_clock::now()});
    }

    /// 유휴 커넥션 폐기 (max_idle_time 초과)
    void shrink_idle() {
        auto now = std::chrono::steady_clock::now();
        while (!idle_.empty() && total_count_ > config_.min_size) {
            auto& entry = idle_.front();
            if (now - entry.last_used < config_.max_idle_time) break;
            derived().do_destroy_connection(entry.conn);
            idle_.pop_front();
            --total_count_;
        }
    }

    /// 헬스 체크 — 유휴 커넥션 검증
    void health_check_tick() {
        std::deque<IdleEntry> valid;
        while (!idle_.empty()) {
            auto entry = std::move(idle_.front());
            idle_.pop_front();
            if (derived().do_validate(entry.conn)) {
                valid.push_back(std::move(entry));
            } else {
                derived().do_destroy_connection(entry.conn);
                --total_count_;
            }
        }
        idle_ = std::move(valid);
    }

    /// 모든 커넥션 정리
    void close_all() {
        for (auto& entry : idle_) {
            derived().do_destroy_connection(entry.conn);
        }
        idle_.clear();
        total_count_ = 0;
        active_count_ = 0;
    }

    [[nodiscard]] size_t active_count() const noexcept { return active_count_; }
    [[nodiscard]] size_t idle_count() const noexcept { return idle_.size(); }
    [[nodiscard]] size_t total_count() const noexcept { return total_count_; }
    [[nodiscard]] const PoolConfig& config() const noexcept { return config_; }

private:
    Derived& derived() noexcept { return static_cast<Derived&>(*this); }

    struct IdleEntry {
        Connection conn;
        std::chrono::steady_clock::time_point last_used;
    };

    PoolConfig config_;
    std::deque<IdleEntry> idle_;
    size_t total_count_ = 0;
    size_t active_count_ = 0;
};

} // namespace apex::shared::adapters
```

- [ ] **Step 2: 빌드 확인**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
```

- [ ] **Step 3: 커밋**

```bash
git add apex_shared/lib/adapters/common/include/apex/shared/adapters/connection_pool.hpp
git commit -m "feat: ConnectionPool CRTP (acquire/release/health_check/shrink)"
```

---

## Task 5: 테스트 인프라 + 단위 테스트

**Files:**
- Create: `apex_shared/tests/CMakeLists.txt`
- Create: `apex_shared/tests/unit/CMakeLists.txt`
- Create: `apex_shared/tests/unit/test_adapter_error.cpp`
- Create: `apex_shared/tests/unit/test_adapter_base.cpp`
- Create: `apex_shared/tests/unit/test_connection_pool.cpp`
- Modify: `apex_shared/CMakeLists.txt`

- [ ] **Step 1: 테스트 CMake 인프라**

```cmake
# apex_shared/tests/CMakeLists.txt
add_subdirectory(unit)
```

```cmake
# apex_shared/tests/unit/CMakeLists.txt
find_package(GTest CONFIG REQUIRED)

function(apex_shared_add_unit_test TEST_NAME TEST_SOURCE)
    add_executable(${TEST_NAME} ${TEST_SOURCE})
    target_link_libraries(${TEST_NAME}
        PRIVATE
            apex::shared::adapters::common
            GTest::gtest_main
    )
    add_test(NAME ${TEST_NAME} COMMAND ${TEST_NAME})
    set_tests_properties(${TEST_NAME} PROPERTIES TIMEOUT 30)
endfunction()

apex_shared_add_unit_test(test_adapter_error test_adapter_error.cpp)
apex_shared_add_unit_test(test_adapter_base test_adapter_base.cpp)
apex_shared_add_unit_test(test_connection_pool test_connection_pool.cpp)
```

apex_shared/CMakeLists.txt 끝에 추가:
```cmake
add_subdirectory(tests)
```

- [ ] **Step 2: test_adapter_error.cpp 작성**

```cpp
#include <apex/shared/adapters/adapter_error.hpp>
#include <apex/core/error_code.hpp>
#include <gtest/gtest.h>

using namespace apex::shared::adapters;
using apex::core::ErrorCode;

TEST(AdapterError, DefaultIsAdapterError) {
    AdapterError err;
    EXPECT_EQ(err.code, ErrorCode::AdapterError);
    EXPECT_EQ(err.native_error, 0u);
    EXPECT_TRUE(err.message.empty());
    EXPECT_FALSE(err.ok());
}

TEST(AdapterError, OkWhenCodeIsOk) {
    AdapterError err{.code = ErrorCode::Ok};
    EXPECT_TRUE(err.ok());
}

TEST(AdapterError, ToStringBasic) {
    AdapterError err{.native_error = 42, .message = "connection refused"};
    auto str = err.to_string();
    EXPECT_NE(str.find("42"), std::string::npos);
    EXPECT_NE(str.find("connection refused"), std::string::npos);
}

TEST(AdapterError, ToStringNoNativeError) {
    AdapterError err{.message = "timeout"};
    auto str = err.to_string();
    EXPECT_EQ(str.find("native"), std::string::npos);
    EXPECT_NE(str.find("timeout"), std::string::npos);
}
```

- [ ] **Step 3: test_adapter_base.cpp 작성**

```cpp
#include <apex/shared/adapters/adapter_base.hpp>
#include <apex/core/core_engine.hpp>
#include <gtest/gtest.h>

using namespace apex::shared::adapters;

class MockAdapter : public AdapterBase<MockAdapter> {
public:
    void do_init(apex::core::CoreEngine& /*engine*/) { init_called = true; }
    void do_drain() { drain_called = true; }
    void do_close() { close_called = true; }
    std::string_view do_name() const noexcept { return "mock"; }

    bool init_called = false;
    bool drain_called = false;
    bool close_called = false;
};

TEST(AdapterBase, NotReadyBeforeInit) {
    MockAdapter adapter;
    EXPECT_FALSE(adapter.is_ready());
}

TEST(AdapterBase, ReadyAfterInit) {
    MockAdapter adapter;
    apex::core::CoreEngineConfig config{.num_cores = 1, .mpsc_capacity = 64};
    apex::core::CoreEngine engine(config);
    adapter.init(engine);
    EXPECT_TRUE(adapter.is_ready());
    EXPECT_TRUE(adapter.init_called);
}

TEST(AdapterBase, NotReadyAfterDrain) {
    MockAdapter adapter;
    apex::core::CoreEngineConfig config{.num_cores = 1, .mpsc_capacity = 64};
    apex::core::CoreEngine engine(config);
    adapter.init(engine);
    adapter.drain();
    EXPECT_FALSE(adapter.is_ready());
    EXPECT_TRUE(adapter.drain_called);
}

TEST(AdapterBase, CloseCallsDerived) {
    MockAdapter adapter;
    adapter.close();
    EXPECT_TRUE(adapter.close_called);
}

TEST(AdapterBase, NameReturnsCorrectly) {
    MockAdapter adapter;
    EXPECT_EQ(adapter.name(), "mock");
}

TEST(AdapterWrapper, TypeErasureWorks) {
    auto wrapper = std::make_unique<AdapterWrapper<MockAdapter>>();
    apex::core::AdapterInterface* iface = wrapper.get();

    EXPECT_EQ(iface->name(), "mock");
    EXPECT_FALSE(iface->is_ready());
}
```

- [ ] **Step 4: test_connection_pool.cpp 작성**

```cpp
#include <apex/shared/adapters/connection_pool.hpp>
#include <gtest/gtest.h>

using namespace apex::shared::adapters;

struct FakeConnection {
    int id = 0;
    bool valid = true;
};

class FakePool : public ConnectionPool<FakePool> {
public:
    using ConnectionType = FakeConnection;

    FakePool(PoolConfig config = {.min_size = 1, .max_size = 4})
        : ConnectionPool(config) {}

    FakeConnection do_create_connection() {
        return {next_id_++, true};
    }

    void do_destroy_connection(FakeConnection& /*conn*/) {
        ++destroy_count;
    }

    bool do_validate(FakeConnection& conn) {
        return conn.valid;
    }

    int next_id_ = 1;
    int destroy_count = 0;
};

TEST(ConnectionPool, AcquireCreatesConnection) {
    FakePool pool;
    auto result = pool.acquire();
    ASSERT_TRUE(result.has_value());
    EXPECT_EQ(result->id, 1);
    EXPECT_EQ(pool.active_count(), 1u);
    EXPECT_EQ(pool.total_count(), 1u);
}

TEST(ConnectionPool, ReleaseReturnsToIdle) {
    FakePool pool;
    auto conn = pool.acquire().value();
    pool.release(std::move(conn));
    EXPECT_EQ(pool.active_count(), 0u);
    EXPECT_EQ(pool.idle_count(), 1u);
}

TEST(ConnectionPool, AcquireReusesIdleConnection) {
    FakePool pool;
    auto conn = pool.acquire().value();
    int id = conn.id;
    pool.release(std::move(conn));

    auto conn2 = pool.acquire().value();
    EXPECT_EQ(conn2.id, id);  // 같은 커넥션 재사용
    EXPECT_EQ(pool.total_count(), 1u);
}

TEST(ConnectionPool, AcquireFailsWhenExhausted) {
    FakePool pool({.min_size = 1, .max_size = 1});
    auto conn = pool.acquire().value();
    auto result = pool.acquire();
    EXPECT_FALSE(result.has_value());
}

TEST(ConnectionPool, InvalidConnectionIsDestroyed) {
    FakePool pool;
    auto conn = pool.acquire().value();
    conn.valid = false;
    pool.release(std::move(conn));

    auto conn2 = pool.acquire().value();
    EXPECT_EQ(conn2.id, 2);  // 새 커넥션 생성됨
    EXPECT_EQ(pool.destroy_count, 1);
}

TEST(ConnectionPool, HealthCheckRemovesInvalid) {
    FakePool pool;
    auto conn = pool.acquire().value();
    conn.valid = false;
    pool.release(std::move(conn));

    pool.health_check_tick();
    EXPECT_EQ(pool.idle_count(), 0u);
    EXPECT_EQ(pool.destroy_count, 1);
}

TEST(ConnectionPool, CloseAllCleansUp) {
    FakePool pool;
    auto c1 = pool.acquire().value();
    auto c2 = pool.acquire().value();
    pool.release(std::move(c1));
    pool.release(std::move(c2));
    pool.close_all();
    EXPECT_EQ(pool.idle_count(), 0u);
    EXPECT_EQ(pool.total_count(), 0u);
}
```

- [ ] **Step 5: 빌드 + 테스트 실행**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
ctest --preset debug -R "test_adapter|test_connection_pool" -v
```
Expected: 모든 테스트 PASS

- [ ] **Step 6: 커밋**

```bash
git add apex_shared/tests/ apex_shared/CMakeLists.txt
git commit -m "test: AdapterError + AdapterBase + ConnectionPool 단위 테스트"
```

---

## Task 6: Server add_adapter API + Shutdown 재배치

**Files:**
- Modify: `apex_core/include/apex/core/server.hpp`
- Modify: `apex_core/src/server.cpp`
- Create: `apex_core/tests/unit/test_server_adapter.cpp`
- Modify: `apex_core/tests/unit/CMakeLists.txt`

- [ ] **Step 1: server.hpp에 어댑터 API 추가**

> **핵심**: `server.hpp`는 `apex/core/adapter_interface.hpp`만 include한다 (apex_core 내부).
> `adapter_base.hpp` (apex_shared)는 include하지 않아 순환 의존 없음.
> `add_adapter<T>()`는 template이므로, 호출자가 `adapter_base.hpp`를 include하면 컴파일됨.

server.hpp의 Server 클래스에 추가:

```cpp
// --- 상단 include 추가 ---
#include <apex/core/adapter_interface.hpp>  // apex_core 내부 — 순환 없음
#include <typeindex>
#include <unordered_map>

// --- forward declaration (template 인스턴스화는 호출자에서) ---
namespace apex::shared::adapters {
template <typename Derived> class AdapterWrapper;
} // namespace apex::shared::adapters

// --- Server 클래스 내 public 멤버 ---

/// 어댑터(인프라 컴포넌트) 등록. 체이닝 지원.
/// 등록 순서는 무관 — run()에서 서비스보다 먼저 초기화됨.
/// 주의: 호출자가 <apex/shared/adapters/adapter_base.hpp>를 include해야 함.
///       server.hpp는 순환 의존 방지를 위해 이를 포함하지 않음.
template <typename T, typename... Args>
Server& add_adapter(Args&&... args) {
    auto wrapper = std::make_unique<apex::shared::adapters::AdapterWrapper<T>>(
        std::forward<Args>(args)...);
    auto* raw = wrapper.get();
    auto key = std::type_index(typeid(T));
    adapter_map_[key] = raw;
    adapters_.push_back(std::move(wrapper));
    return *this;
}

/// 등록된 어댑터 접근 (타입 기반).
/// 주의: 호출자가 <apex/shared/adapters/adapter_base.hpp>를 include해야 함.
template <typename T>
T& adapter() const {
    auto key = std::type_index(typeid(T));
    auto it = adapter_map_.find(key);
    assert(it != adapter_map_.end() && "Adapter not registered");
    return static_cast<apex::shared::adapters::AdapterWrapper<T>*>(it->second)->get();
}

// --- Server 클래스 내 private 멤버 ---
std::vector<std::unique_ptr<AdapterInterface>> adapters_;            // apex::core::AdapterInterface
std::unordered_map<std::type_index, AdapterInterface*> adapter_map_; // apex::core::AdapterInterface
```

> `AdapterInterface`가 `apex::core` 네임스페이스에 있으므로, Server 클래스 내에서 네임스페이스 없이 사용 가능.
> `AdapterWrapper<T>`는 `apex::shared::adapters` 네임스페이스이며, template 인스턴스화 시점에 해석됨.

- [ ] **Step 2: server.cpp의 run()에서 어댑터 초기화 추가**

`run()`에서 서비스 시작 전에 삽입:

```cpp
// Initialize adapters before services
for (auto& adapter : adapters_) {
    adapter->init(*core_engine_);
}

// Per-core service instances (기존 코드)
for (uint32_t core_id = 0; ...
```

- [ ] **Step 3: finalize_shutdown() 재배치**

기존:
```cpp
void Server::finalize_shutdown() {
    shutdown_timer_.reset();
    core_engine_->stop();
    core_engine_->join();
    core_engine_->drain_remaining();
    for (auto& state : per_core_) {
        for (auto& svc : state->services) { svc->stop(); }
    }
    control_io_.stop();
}
```

변경:
```cpp
void Server::finalize_shutdown() {
    shutdown_timer_.reset();

    // 1. Adapter drain (새 요청 거부)
    for (auto& adapter : adapters_) {
        adapter->drain();
    }

    // 2. CoreEngine stop (기존: 세션 drain은 begin_shutdown에서 이미 처리)
    // 단, adapter close가 io_context에서 동작해야 하므로
    // 서비스 먼저 정지 후 어댑터 정리, 그 다음 엔진 종료

    // 3. Stop services
    for (auto& state : per_core_) {
        for (auto& svc : state->services) {
            svc->stop();
        }
    }

    // 4. Adapter close (flush + 커넥션 정리)
    for (auto& adapter : adapters_) {
        adapter->close();
    }

    // 5. CoreEngine stop + join + drain
    core_engine_->stop();
    core_engine_->join();
    core_engine_->drain_remaining();

    control_io_.stop();
}
```

- [ ] **Step 4: test_server_adapter.cpp 작성**

```cpp
#include <apex/core/server.hpp>
#include <apex/shared/adapters/adapter_base.hpp>
#include <gtest/gtest.h>

using namespace apex::core;
using namespace apex::shared::adapters;

class TestAdapter : public AdapterBase<TestAdapter> {
public:
    void do_init(CoreEngine& /*engine*/) { init_called = true; }
    void do_drain() { drain_called = true; }
    void do_close() { close_called = true; }
    std::string_view do_name() const noexcept { return "test"; }

    bool init_called = false;
    bool drain_called = false;
    bool close_called = false;
};

TEST(ServerAdapter, AddAdapterChaining) {
    ServerConfig config{.port = 0, .num_cores = 1};
    Server server(config);
    auto& ref = server.add_adapter<TestAdapter>();
    EXPECT_EQ(&ref, &server);  // 체이닝 반환
}

TEST(ServerAdapter, AdapterAccessible) {
    ServerConfig config{.port = 0, .num_cores = 1};
    Server server(config);
    server.add_adapter<TestAdapter>();
    auto& adapter = server.adapter<TestAdapter>();
    EXPECT_EQ(adapter.name(), "test");
}
```

- [ ] **Step 5: tests/unit/CMakeLists.txt에 테스트 추가**

```cmake
apex_add_unit_test(test_server_adapter test_server_adapter.cpp)
```

target_link_libraries에 `apex::shared::adapters::common` 추가 필요.
test_server_adapter는 별도 링크:
```cmake
add_executable(test_server_adapter test_server_adapter.cpp)
target_link_libraries(test_server_adapter
    PRIVATE
        apex::core
        apex::shared::adapters::common
        GTest::gtest_main
)
add_test(NAME test_server_adapter COMMAND test_server_adapter)
```

- [ ] **Step 6: 빌드 + 테스트**

```bash
cmd.exe //c "D:\\.workspace\\.worktrees\\v0.4-adapters\\apex_core\\build.bat debug"
ctest --preset debug -R "test_server_adapter" -v
```
Expected: PASS

- [ ] **Step 7: 전체 기존 테스트 회귀 확인**

```bash
ctest --preset debug -v
```
Expected: 기존 테스트 전부 PASS (shutdown 재배치가 기존 동작에 영향 없음)

- [ ] **Step 8: 커밋**

```bash
git add apex_core/include/apex/core/server.hpp apex_core/src/server.cpp \
        apex_core/tests/unit/test_server_adapter.cpp apex_core/tests/unit/CMakeLists.txt
git commit -m "feat: Server add_adapter API + Graceful Shutdown 어댑터 통합"
```
