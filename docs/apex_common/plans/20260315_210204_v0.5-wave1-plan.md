# v0.5 Wave 1 구현 계획서

> **작성일**: 2026-03-15
> **스펙 문서**: `docs/apex_core/plans/20260315_000000_v0.5-wave1-design.md`
> **브랜치**: `feature/v0.5-wave1`
> **워크트리**: `D:/.workspace/.worktrees/v0.5-wave1`

---

## Agentic Workers 지시

이 계획서는 **서브에이전트가 메인 컨텍스트 없이 독립 실행**할 수 있도록 설계되었다.
각 Chunk(Bundle)는 독립 실행 가능하며, Phase 1 내에서는 병렬 실행한다.

**규칙**:
1. 모든 태스크는 TDD: 실패 테스트 작성 -> 구현 -> 통과 -> 커밋
2. 커밋 메시지는 한국어: `feat(core): 코루틴 할당기 추가` 형태
3. Phase 1 완료 후 **full auto-review (12명)** 게이트 — 빌드+CI만이 아니라 코드 리뷰 포함
4. Phase 2 완료 후 빌드+테스트 -> auto-review -> PR -> main 머지
5. 파일 충돌 방지: 각 Bundle의 터치 영역을 엄격히 준수
6. 빌드는 coordinator만 수행 (워커는 코드 수정만)

---

## Phase 구성

```
Phase 1 (병렬)            Phase 2 (병렬)            최종
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│ Chunk 1 (A)  │         │ Chunk 4      │         │ auto-review  │
│ Chunk 2 (B)  │──gate──>│   (C-main)   │──gate──>│ 이슈 수정     │
│ Chunk 3      │         │ Chunk 5      │         │ PR -> 머지    │
│   (C-prep)   │         │   (E-1)      │         └──────────────┘
└──────────────┘         └──────────────┘

Phase 1 게이트: 빌드 + full auto-review (12명)
Phase 2 게이트: 빌드 + 테스트 전체 통과 + auto-review
```

---

## 파일 충돌 방지 매트릭스

| Bundle | 터치 영역 | 비고 |
|--------|----------|------|
| **A** (Chunk 1) | `apex_core/src/server.cpp`, `apex_core/src/session.cpp`, `apex_core/src/arena_allocator.cpp`, `docs/apex_core/design-rationale.md`, `docs/BACKLOG.md` | .cpp만 수정, .hpp 미접촉 |
| **B** (Chunk 2) | `apex_shared/lib/adapters/` 전체 (common/, redis/, pg/, kafka/), `apex_core/include/apex/core/error_code.hpp` (PoolExhausted, CircuitOpen 추가) | 어댑터 영역 독점 + error_code 확장 |
| **C-prep** (Chunk 3) | `apex_core/include/apex/core/protocol.hpp` (신규), `apex_core/include/apex/core/session.hpp`, `apex_core/include/apex/core/error_code.hpp` (BufferFull 확인만), `apex_core/src/session.cpp` (enqueue_write/write_pump 구현 추가), `apex_shared/schemas/` (신규), `apex_shared/CMakeLists.txt` | session.cpp는 A와 C-prep 공유 파일. A가 먼저 커밋 완료된 후 C-prep이 session.cpp를 수정할 것. A의 clang-tidy는 기존 코드 정리(행위 변경 없음)이므로 C-prep과 의미적 충돌 없음 |
| **C-main** (Chunk 4) | `apex_core/` 전반 (server.hpp/cpp, connection_handler.hpp/cpp, protocol_base.hpp 삭제), `apex_shared/lib/protocols/` (신규), `apex_core/examples/`, `vcpkg.json` | Phase 2에서만 실행 |
| **E-1** (Chunk 5) | `apex_core/tests/`, `apex_shared/tests/` | Phase 2에서만 실행 |

---

## Chunk 1: Phase 1 — Bundle A (코드 위생)

> Bundle A는 행위 변경 없는 코드 위생 작업입니다. TDD 원칙의 예외로, 새 실패 테스트 작성 대신 기존 테스트 회귀 확인으로 대체합니다.

### 목표
clang-tidy ~15건 수정 (실제 건수는 LSP 진단으로 확인 — clang-tidy 버전/설정에 따라 가변), make_socket_pair 반환 순서 통일, ADR-04 갱신, BACKLOG 정리.

### 터치 파일
- `apex_core/src/server.cpp` (2건)
- `apex_core/src/session.cpp` (5건)
- `apex_core/src/arena_allocator.cpp` (8건)
- `apex_core/tests/test_helpers.hpp` (make_socket_pair 반환 순서)
- `docs/apex_core/design-rationale.md` (ADR-04)
- `docs/BACKLOG.md` (MoveConstruction 항목 정리)

### 현재 코드 구조 참조

**네임스페이스**: `apex::core`
**make_socket_pair**: `apex::test` 네임스페이스, `apex_core/tests/test_helpers.hpp`
- 현재 반환: `{server, client}` — `acceptor.accept()` 결과가 첫 번째
- 호출처 전수 확인 필요

### Step A-1: clang-tidy — server.cpp (2건)

**파일**: `apex_core/src/server.cpp`

clang-tidy를 실행하여 정확한 경고를 식별한다:
```bash
# 워크트리에서 clang-tidy 실행 (compile_commands.json 필요)
```

**예상 항목** (LSP로 확인):
1. 미사용 변수 — `on_accept` 등에서 캡처된 미사용 변수 제거
2. 불필요한 복사 — `std::move` 누락 또는 const ref로 변경 가능한 곳

**액션**:
1. LSP `textDocument/diagnostic`로 정확한 위치 확인
2. 수정 적용
3. 빌드 + 기존 테스트 통과 확인
4. 커밋: `fix(core): server.cpp clang-tidy 경고 2건 수정`

### Step A-2: clang-tidy — session.cpp (5건)

**파일**: `apex_core/src/session.cpp`

**현재 구조**:
- `Session::async_send()` — WireHeader serialize + async_write (gather I/O)
- `Session::async_send_raw()` — raw buffer async_write
- `Session::close()` — 상태 전이 + socket shutdown

**예상 항목**:
1. narrowing conversion (size_t -> unsigned 등)
2. 불필요한 `std::move` (이미 rvalue인 곳)
3. const 누락 (멤버 함수, 파라미터)
4. implicit conversion 경고
5. 기타 modernize 제안

**액션**:
1. LSP 진단으로 정확한 경고 확인
2. 수정 (기능 변경 없이 경고만 해소)
3. 빌드 + 기존 `test_session.cpp` 테스트 통과 확인
4. 커밋: `fix(core): session.cpp clang-tidy 경고 5건 수정`

### Step A-3: clang-tidy — arena_allocator.cpp (8건)

**파일**: `apex_core/src/arena_allocator.cpp`
**헤더**: `apex_core/include/apex/core/arena_allocator.hpp`

**예상 항목**:
1. `reinterpret_cast` 정리 (가능한 경우 `static_cast`나 `std::bit_cast`로)
2. alignment 체크 관련 경고
3. 매직 넘버 상수화 (4096 등 → named constant)
4. 불필요한 캐스팅
5-8. 기타 modernize/readability 경고

**액션**:
1. LSP 진단으로 정확한 경고 확인
2. 수정 (arena_allocator.cpp만 — .hpp는 C-prep 영역 아님이므로 필요 시 .hpp도 수정 가능하되 session.hpp는 건드리지 않음)
3. 빌드 + 기존 `test_arena_allocator.cpp` 테스트 통과 확인
4. 커밋: `fix(core): arena_allocator.cpp clang-tidy 경고 8건 수정`

### Step A-4: make_socket_pair 반환 순서 통일

**파일**: `apex_core/tests/test_helpers.hpp`

**현재 구현** (line 37-49):
```cpp
inline std::pair<boost::asio::ip::tcp::socket, boost::asio::ip::tcp::socket>
make_socket_pair(boost::asio::io_context& ctx) {
    // ...
    auto server = acceptor.accept();
    return {std::move(server), std::move(client)};
}
```

현재 이미 `{server, client}` 순서. 호출처에서 순서 가정이 일관된지 확인:

**액션**:
1. 모든 호출처 grep:
   ```bash
   grep -rn "make_socket_pair" apex_core/tests/
   ```
2. `auto [first, second] = make_socket_pair(ctx)` 형태에서 first=server, second=client가 일관된지 확인
3. 불일치 있으면 호출처를 수정 (반환 순서는 {server, client}로 확정)
4. 커밋: `fix(test): make_socket_pair 반환 순서 호출처 통일`

### Step A-5: ADR-04 갱신

**파일**: `docs/apex_core/design-rationale.md`

**현재 상태** (line 110-112):
```
## ADR-04: 디렉토리 구조
> ※ 현행 주석: 아래는 초기 브레인스토밍 당시 결정 기록이다. 현재 구조는 apex_ prefix 기반으로 재편...
```

**액션**:
1. ADR-04 섹션을 읽고 현재 모노레포 구조를 반영
2. 특히 `apex_shared/lib/protocols/` 디렉토리 추가를 명시 (Phase 2에서 생성되지만 ADR에 미리 반영)
3. 기존 `apex_shared/lib/adapters/` 구조도 정확히 기술
4. 커밋: `docs(adr): ADR-04 모노레포 구조 현행화 + protocols 디렉토리 반영`

### Step A-6: BACKLOG 정리

**파일**: `docs/BACKLOG.md`

**액션**:
1. MoveConstruction 관련 항목이 있으면 확인 — 오타 아님으로 확인되었으므로 "오타 아님 확인 완료" 메모와 함께 항목 제거
2. 기타 Wave 1에서 해결되는 항목 있으면 마킹
3. 커밋: `docs(backlog): MoveConstruction 오타 아님 확인, 항목 정리`

### Bundle A 완료 커밋
모든 A 스텝 완료 후 최종 빌드 검증.

---

## Chunk 2: Phase 1 — Bundle B (어댑터 회복력)

### 목표
CircuitBreaker, Redis AUTH + ARRAY, PgPool 재시도, Kafka DLQ + AdapterState.

### 터치 파일
- `apex_shared/lib/adapters/common/` (CircuitBreaker 신규, adapter_error.hpp)
- `apex_shared/lib/adapters/redis/` (redis_connection.hpp, redis_multiplexer.hpp/cpp, redis_reply.hpp, redis_config.hpp)
- `apex_shared/lib/adapters/pg/` (pg_pool.hpp/cpp, pg_config.hpp)
- `apex_shared/lib/adapters/kafka/` (kafka_adapter.hpp/cpp, kafka_consumer.hpp/cpp, kafka_producer.hpp)
- `apex_core/include/apex/core/error_code.hpp` (PoolExhausted 추가)
- `apex_shared/lib/adapters/CMakeLists.txt` (CircuitBreaker 소스 등록)

### 현재 코드 구조 참조

**네임스페이스**:
- `apex::shared::adapters` — 공통
- `apex::shared::adapters::redis` — Redis
- `apex::shared::adapters::pg` — PostgreSQL
- `apex::shared::adapters::kafka` — Kafka

**핵심 타입**:
- `apex::core::Result<T>` = `std::expected<T, ErrorCode>`
- `apex::core::ErrorCode` — 현재 `AdapterError = 2000`이 마지막
- `RedisReply` — 현재 str/integer/type만 있음 (array 없음)
- `RedisConfig` — password 필드 이미 존재
- `PgPool::acquire()` — 동기, 단일 시도
- `MessageCallback` — 현재 `void` 반환
- `KafkaAdapter::draining_` — `bool`

### Step B-1: ErrorCode에 PoolExhausted, CircuitOpen, InsufficientData 추가

**파일**: `apex_core/include/apex/core/error_code.hpp`

**현재 상태**: `AdapterError = 2000`이 마지막 어댑터 에러코드.

**코드 변경**:
```cpp
// Protocol errors
InsufficientData = 13,  // 추가: 버퍼에 완전한 프레임 없음 (더 읽기 신호)

// Adapter errors (2000+)
AdapterError = 2000,
PoolExhausted = 2001,   // 추가: PgPool 재시도 초과
CircuitOpen = 2002,     // 추가: CircuitBreaker OPEN 상태에서 호출 거부
```

`error_code_name()` switch에도 추가:
```cpp
case ErrorCode::InsufficientData: return "InsufficientData";
case ErrorCode::PoolExhausted: return "PoolExhausted";
case ErrorCode::CircuitOpen: return "CircuitOpen";
```

**커밋**: `feat(core): ErrorCode::InsufficientData(13), PoolExhausted(2001), CircuitOpen(2002) 추가`

### Step B-2: CircuitBreaker 구현

**신규 파일**:
- `apex_shared/lib/adapters/common/include/apex/shared/adapters/circuit_breaker.hpp`
- `apex_shared/lib/adapters/common/src/circuit_breaker.cpp`

**네임스페이스**: `apex::shared::adapters`

**테스트 파일** (신규):
- `apex_shared/tests/unit/test_circuit_breaker.cpp`

**구현**:

```cpp
// circuit_breaker.hpp
#pragma once

#include <apex/core/result.hpp>

#include <boost/asio/awaitable.hpp>

#include <chrono>
#include <concepts>
#include <cstdint>

namespace apex::shared::adapters {

struct CircuitBreakerConfig {
    uint32_t failure_threshold = 5;
    std::chrono::milliseconds open_duration{30'000};
    uint32_t half_open_max_calls = 3;
};

enum class CircuitState : uint8_t { CLOSED, OPEN, HALF_OPEN };

class CircuitBreaker {
public:
    explicit CircuitBreaker(CircuitBreakerConfig config);

    /// fn은 Task<Result<void>>를 반환하는 invocable
    template<std::invocable F>
        requires std::same_as<std::invoke_result_t<F>,
                              boost::asio::awaitable<apex::core::Result<void>>>
    boost::asio::awaitable<apex::core::Result<void>> call(F&& fn);

    [[nodiscard]] CircuitState state() const noexcept;
    [[nodiscard]] uint32_t failure_count() const noexcept;
    void reset() noexcept;

private:
    bool should_allow() noexcept;
    void on_success() noexcept;
    void on_failure() noexcept;

    CircuitBreakerConfig config_;
    CircuitState state_ = CircuitState::CLOSED;
    uint32_t failure_count_ = 0;
    uint32_t half_open_calls_ = 0;
    std::chrono::steady_clock::time_point open_since_;
};

// Template implementation
template<std::invocable F>
    requires std::same_as<std::invoke_result_t<F>,
                          boost::asio::awaitable<apex::core::Result<void>>>
boost::asio::awaitable<apex::core::Result<void>> CircuitBreaker::call(F&& fn) {
    if (!should_allow()) {
        co_return std::unexpected(apex::core::ErrorCode::CircuitOpen);
    }

    auto result = co_await std::forward<F>(fn)();
    if (result.has_value()) {
        on_success();
    } else {
        on_failure();
    }
    co_return result;
}

} // namespace apex::shared::adapters
```

```cpp
// circuit_breaker.cpp
#include <apex/shared/adapters/circuit_breaker.hpp>

namespace apex::shared::adapters {

CircuitBreaker::CircuitBreaker(CircuitBreakerConfig config)
    : config_(config) {}

CircuitState CircuitBreaker::state() const noexcept { return state_; }
uint32_t CircuitBreaker::failure_count() const noexcept { return failure_count_; }

void CircuitBreaker::reset() noexcept {
    state_ = CircuitState::CLOSED;
    failure_count_ = 0;
    half_open_calls_ = 0;
}

bool CircuitBreaker::should_allow() noexcept {
    switch (state_) {
        case CircuitState::CLOSED:
            return true;
        case CircuitState::OPEN: {
            auto elapsed = std::chrono::steady_clock::now() - open_since_;
            if (elapsed >= config_.open_duration) {
                state_ = CircuitState::HALF_OPEN;
                half_open_calls_ = 0;
                return true;
            }
            return false;
        }
        case CircuitState::HALF_OPEN:
            return half_open_calls_ < config_.half_open_max_calls;
    }
    return false;
}

void CircuitBreaker::on_success() noexcept {
    switch (state_) {
        case CircuitState::HALF_OPEN:
            ++half_open_calls_;
            if (half_open_calls_ >= config_.half_open_max_calls) {
                state_ = CircuitState::CLOSED;
                failure_count_ = 0;
            }
            break;
        case CircuitState::CLOSED:
            failure_count_ = 0;
            break;
        default:
            break;
    }
}

void CircuitBreaker::on_failure() noexcept {
    switch (state_) {
        case CircuitState::CLOSED:
            ++failure_count_;
            if (failure_count_ >= config_.failure_threshold) {
                state_ = CircuitState::OPEN;
                open_since_ = std::chrono::steady_clock::now();
            }
            break;
        case CircuitState::HALF_OPEN:
            state_ = CircuitState::OPEN;
            open_since_ = std::chrono::steady_clock::now();
            break;
        default:
            break;
    }
}

} // namespace apex::shared::adapters
```

**테스트** (TDD — 먼저 작성):
```cpp
// test_circuit_breaker.cpp
// TC1: CLOSED 상태에서 성공 호출 — CLOSED 유지
// TC2: CLOSED -> OPEN 전이 (failure_threshold 도달)
// TC3: OPEN 상태에서 호출 거부
// TC4: OPEN -> HALF_OPEN 전이 (open_duration 경과)
// TC5: HALF_OPEN -> CLOSED 전이 (half_open_max_calls 성공)
// TC6: HALF_OPEN -> OPEN 전이 (실패 발생)
// TC7: reset() 테스트
```

**테스트 패턴 예시** (io_context + co_spawn):
```cpp
TEST_F(CircuitBreakerTest, ClosedToOpenTransition) {
    boost::asio::io_context io;
    CircuitBreaker cb(CircuitBreakerConfig{.failure_threshold = 3});

    boost::asio::co_spawn(io, [&]() -> boost::asio::awaitable<void> {
        auto fail = [&]() -> boost::asio::awaitable<apex::core::Result<void>> {
            co_return std::unexpected(apex::core::ErrorCode::AdapterError);
        };
        for (int i = 0; i < 3; ++i) co_await cb.call(fail);
        EXPECT_EQ(cb.state(), CircuitState::OPEN);
        co_return;
    }, boost::asio::detached);
    io.run();
}
```

**CMake**: `apex_shared/lib/adapters/common/CMakeLists.txt`에 `src/circuit_breaker.cpp` 추가

**커밋**: `feat(shared): CircuitBreaker 상태 머신 구현 + 단위 테스트 7TC`

### Step B-3: Redis AUTH 지원

**수정 파일**:
- `apex_shared/lib/adapters/redis/src/redis_multiplexer.cpp`

**현재 구조**:
- `RedisConfig` — `password` 필드 이미 존재 (line 13)
- `RedisConnection::create()` — 동기 팩토리, `unique_ptr<RedisConnection>` 반환
- `RedisMultiplexer::reconnect_loop()` — `RedisConnection::create()` 호출 후 바로 사용

**변경**:
1. `command()`를 사용하지 않고 별도 `authenticate()` 메서드 추가
2. `authenticate()`는 `conn_->raw_async_command("AUTH", password)`를 직접 사용
3. `reconnect_loop`에서 `conn_` 재생성 후 `authenticate()` 호출

```cpp
// redis_multiplexer.hpp — 메서드 추가
Task<Result<void>> authenticate(RedisConnection& conn);

// redis_multiplexer.cpp — authenticate() 구현
Task<Result<void>> RedisMultiplexer::authenticate(RedisConnection& conn) {
    if (config_.password.empty()) co_return {};
    // command() 대신 raw_async_command 직접 사용 — command()는 reconnecting_ 상태 체크 등
    // 내부 가드가 있어 reconnect_loop 내에서 사용 시 문제가 될 수 있음
    auto auth_result = co_await conn.raw_async_command("AUTH", config_.password);
    if (!auth_result.has_value()) {
        spdlog::warn("Redis AUTH failed");
        conn.disconnect();
        co_return auth_result.error();
    }
    co_return {};
}

// redis_multiplexer.cpp — reconnect_loop() 내부 변경
conn_ = RedisConnection::create(io_ctx_, config_);
if (conn_ && conn_->is_connected()) {
    // AUTH 수행 (password가 설정된 경우)
    auto auth = co_await authenticate(*conn_);
    if (!auth.has_value()) {
        conn_.reset();
        // backoff 후 재시도
        boost::asio::steady_timer timer(io_ctx_, backoff);
        co_await timer.async_wait(boost::asio::use_awaitable);
        backoff = std::min(backoff * 2, max_backoff);
        continue;
    }
    reconnecting_ = false;
    reconnect_attempts_ = 0;
    spdlog::info("Redis reconnected successfully{}",
                 config_.password.empty() ? "" : " (authenticated)");
    co_return;
}
```

**주의**: `authenticate()`는 `raw_async_command`를 사용하여 `command()`의 내부 가드(reconnecting_ 체크 등)를 우회한다. reconnect 완료 전에 AUTH를 수행해야 하므로 이 방식이 올바르다.

**테스트**: AUTH는 Redis 서버가 필요한 integration 테스트 영역. 단위 테스트에서는 reconnect_loop 로직만 검증.

**커밋**: `feat(redis): RedisMultiplexer AUTH 지원 (reconnect_loop 내 인증)`

### Step B-4: RedisReply ARRAY 지원

**수정 파일**:
- `apex_shared/lib/adapters/redis/include/apex/shared/adapters/redis/redis_reply.hpp`

**현재 구조** (전체 파일):
```cpp
struct RedisReply {
    std::string str;
    int64_t integer{0};
    int type{0};
    explicit RedisReply(redisReply* r) { ... }
    RedisReply() = default;
};
```

**변경 후**:
```cpp
#pragma once

#include <hiredis/hiredis.h>

#include <cstdint>
#include <string>
#include <vector>

namespace apex::shared::adapters::redis {

struct RedisReply {
    std::string str;
    int64_t integer{0};
    int type{0};
    std::vector<RedisReply> array;   // 추가: ARRAY 재귀 파싱

    explicit RedisReply(redisReply* r) {
        if (!r) return;
        type = r->type;
        if (r->str) str.assign(r->str, r->len);
        integer = r->integer;
        // ARRAY 재귀 파싱
        if (r->type == REDIS_REPLY_ARRAY && r->element) {
            array.reserve(r->elements);
            for (size_t i = 0; i < r->elements; ++i) {
                array.emplace_back(r->element[i]);
            }
        }
    }

    RedisReply() = default;

    // 타입 체크 헬퍼
    [[nodiscard]] bool is_string() const noexcept {
        return type == REDIS_REPLY_STRING || type == REDIS_REPLY_STATUS;
    }
    [[nodiscard]] bool is_integer() const noexcept { return type == REDIS_REPLY_INTEGER; }
    [[nodiscard]] bool is_array() const noexcept { return type == REDIS_REPLY_ARRAY; }
    [[nodiscard]] bool is_nil() const noexcept { return type == REDIS_REPLY_NIL; }
    [[nodiscard]] bool is_error() const noexcept { return type == REDIS_REPLY_ERROR; }
};

} // namespace apex::shared::adapters::redis
```

**테스트** (TDD):
```
// TC1: STRING 타입 파싱 — is_string() == true
// TC2: INTEGER 타입 — is_integer() == true
// TC3: ARRAY 타입 — 재귀 파싱, array.size() 검증
// TC4: NIL 타입 — is_nil() == true
// TC5: 중첩 ARRAY 파싱
```

mock `redisReply` 구조체를 수동 생성하여 테스트.

**커밋**: `feat(redis): RedisReply ARRAY 재귀 파싱 + 타입 체크 헬퍼`

### Step B-5: PgPool acquire_with_retry

**수정 파일**:
- `apex_shared/lib/adapters/pg/include/apex/shared/adapters/pg/pg_pool.hpp`
- `apex_shared/lib/adapters/pg/src/pg_pool.cpp`
- `apex_shared/lib/adapters/pg/include/apex/shared/adapters/pg/pg_config.hpp`

**현재 구조**:
- `PgPool::acquire()` — 동기, idle 큐 -> 신규 생성 -> 실패 시 `AdapterError` 반환
- `PgAdapterConfig` — `pool_size_per_core`, `max_idle_time`, `health_check_interval`

**변경**:

1. `PgAdapterConfig`에 retry 설정 추가:
```cpp
// pg_config.hpp
struct PgAdapterConfig {
    // ... 기존 필드 ...
    uint32_t max_acquire_retries = 3;
    std::chrono::milliseconds retry_backoff{100};  // 초기 backoff
};
```

2. `PgPool`에 `acquire_with_retry()` 추가:
```cpp
// pg_pool.hpp
[[nodiscard]] boost::asio::awaitable<apex::core::Result<Connection>>
acquire_with_retry();
```

3. 구현:
```cpp
// pg_pool.cpp
boost::asio::awaitable<apex::core::Result<PgPool::Connection>>
PgPool::acquire_with_retry() {
    for (uint32_t i = 0; i <= config_.max_acquire_retries; ++i) {
        auto conn = acquire();
        if (conn.has_value()) co_return std::move(conn);

        if (i < config_.max_acquire_retries) {
            auto delay = config_.retry_backoff * (1u << i);  // exponential
            boost::asio::steady_timer timer(io_ctx_, delay);
            co_await timer.async_wait(boost::asio::use_awaitable);
        }
    }
    co_return std::unexpected(apex::core::ErrorCode::PoolExhausted);
}
```

**테스트** (TDD):
```
// TC1: 풀에 idle 연결 있을 때 — 즉시 성공 (재시도 없음)
// TC2: 풀 고갈 + 재시도 후 성공
// TC3: 풀 고갈 + max_retries 초과 — PoolExhausted 반환
// TC4: exponential backoff 시간 검증 (소요 시간 범위 체크)
```

**커밋**: `feat(pg): PgPool::acquire_with_retry exponential backoff 재시도`

### Step B-6: Kafka DLQ

**수정 파일**:
- `apex_shared/lib/adapters/kafka/include/apex/shared/adapters/kafka/kafka_consumer.hpp`
- `apex_shared/lib/adapters/kafka/src/kafka_consumer.cpp`
- `apex_shared/lib/adapters/kafka/include/apex/shared/adapters/kafka/kafka_adapter.hpp`
- `apex_shared/lib/adapters/kafka/src/kafka_adapter.cpp`

**현재 구조**:
- `MessageCallback` = `std::function<void(topic, partition, key, payload, offset)>`
- `KafkaConsumer::poll_messages()` — callback 호출 후 반환값 미검사
- `KafkaConsumer` — `KafkaProducer` 참조 없음
- `KafkaAdapter` — `producer_`, `consumers_` 소유

**변경**:

1. `MessageCallback` 반환 타입 변경:
```cpp
// kafka_consumer.hpp
using MessageCallback = std::function<apex::core::Result<void>(
    std::string_view topic,
    int32_t partition,
    std::span<const uint8_t> key,
    std::span<const uint8_t> payload,
    int64_t offset)>;
```

2. `KafkaConsumer`에 Producer 참조 주입:
```cpp
// kafka_consumer.hpp
class KafkaConsumer {
public:
    explicit KafkaConsumer(const KafkaConfig& config,
                           uint32_t core_id,
                           boost::asio::io_context& io_ctx,
                           KafkaProducer* producer = nullptr);  // DLQ용
    // ...
private:
    KafkaProducer* producer_ = nullptr;  // DLQ produce용 (non-owning)
};
```

3. `poll_messages()` DLQ 라우팅:
```cpp
// kafka_consumer.cpp — poll_messages() 변경
if (message_cb_) {
    auto result = message_cb_(rd_kafka_topic_name(msg->rkt),
                               msg->partition,
                               key_span, payload_span,
                               msg->offset);
    if (!result.has_value() && producer_) {
        auto dlq_topic = fmt::format("{}-dlq",
                                      rd_kafka_topic_name(msg->rkt));
        spdlog::warn("Message processing failed (topic={}, offset={}), "
                     "routing to DLQ: {}",
                     rd_kafka_topic_name(msg->rkt),
                     msg->offset,
                     apex::core::error_code_name(result.error()));
        (void)producer_->produce(dlq_topic, key_span,
                                  payload_span);
    }
}
```

4. `KafkaAdapter::do_init()`에서 Consumer 생성 시 Producer 전달:
```cpp
// kafka_adapter.cpp — do_init() 내 Consumer 생성 변경
consumers_.push_back(std::make_unique<KafkaConsumer>(
    config_, core_id, io_ctx, producer_.get()));  // producer_ 전달
```

**기존 호출처 영향**:
- `KafkaAdapter::set_message_callback()` — 반환타입 변경에 따라 기존 callback을 사용하는 서비스 코드도 수정 필요. 현재 `apex_services/`에 실제 서비스가 없으므로 영향 없음.
- 예제 코드가 있다면 확인 필요.

**fmt 의존성**: spdlog가 fmt를 포함하므로 `spdlog/fmt/fmt.h`로 사용 가능. 또는 `std::string` 결합 사용.

**테스트** (TDD):
```
// TC1: 콜백 성공 시 DLQ produce 미호출
// TC2: 콜백 실패 시 DLQ topic 생성 + produce 호출
// TC3: producer null일 때 DLQ 스킵 (크래시 없음)
```

**커밋**: `feat(kafka): DLQ 라우팅 (MessageCallback Result<void> + Consumer->Producer 주입)`

### Step B-7: KafkaAdapter AdapterState enum

**수정 파일**:
- `apex_shared/lib/adapters/kafka/include/apex/shared/adapters/kafka/kafka_adapter.hpp`
- `apex_shared/lib/adapters/kafka/src/kafka_adapter.cpp`

**현재 구조**:
- `KafkaAdapter::draining_` — `bool` (line 97)

**변경**:

1. AdapterState enum 정의 (kafka_adapter.hpp 또는 별도 헤더):
```cpp
enum class AdapterState : uint8_t {
    RUNNING,
    DRAINING,
    CLOSED
};
```

2. `bool draining_` -> `std::atomic<AdapterState> state_{AdapterState::RUNNING}`:
```cpp
class KafkaAdapter : public apex::shared::adapters::AdapterBase<KafkaAdapter> {
    // ...
private:
    std::atomic<AdapterState> state_{AdapterState::RUNNING};

    [[nodiscard]] bool is_running() const noexcept {
        return state_.load(std::memory_order_acquire) == AdapterState::RUNNING;
    }
};
```

3. 기존 `draining_` 참조를 모두 `state_` 로 변경:
- `do_drain()` → `state_.store(AdapterState::DRAINING, std::memory_order_release)`
- `do_close()` → `state_.store(AdapterState::CLOSED, std::memory_order_release)`
- `produce()` 내 guard → `if (!is_running()) return error(...)`

**커밋**: `refactor(kafka): KafkaAdapter draining_ -> AdapterState enum + atomic`

### Bundle B 완료
모든 B 스텝 완료 후 빌드 검증.

---

## Chunk 3: Phase 1 — Bundle C-prep (선행 작업)

### 목표
Protocol concept 정의, 공유 스키마 4개, Per-session write queue.

### 터치 파일
- `apex_core/include/apex/core/protocol.hpp` (신규)
- `apex_core/include/apex/core/session.hpp` (write queue 추가)
- `apex_core/src/session.cpp` (**주의**: Bundle A도 이 파일 수정 — A는 clang-tidy만, C-prep은 write queue 추가. 병렬 시 A가 먼저 완료되어야 함. 또는 session.cpp 변경은 C-prep이 session.hpp에만 선언하고, 구현은 별도 추가로 분리)
- `apex_shared/schemas/` (신규 4개)
- `apex_shared/CMakeLists.txt` (스키마 등록)
- `apex_core/CMakeLists.txt` (protocol.hpp는 헤더 전용이라 변경 없음)

**충돌 방지**: Bundle A는 `session.cpp`의 기존 코드 clang-tidy만 수정. C-prep은 `session.hpp`에 write queue 멤버/메서드를 **추가**하고, `session.cpp`에 새 메서드 구현을 **append**. A의 clang-tidy 수정과 C-prep의 신규 함수 추가는 파일 내 다른 위치이므로 merge 시 충돌 없음. 단, **A가 먼저 커밋되는 것을 권장**.

### Step C-prep-1: Protocol concept 정의

**신규 파일**: `apex_core/include/apex/core/protocol.hpp`

**현재 구조 참조**:
- `ProtocolBase<Derived>` (CRTP) — `apex_core/include/apex/core/protocol_base.hpp`
- `TcpBinaryProtocol::try_decode_impl()` 반환: `std::expected<Frame, FrameError>`
- `Result<T>` = `std::expected<T, ErrorCode>` — FrameError와 ErrorCode는 다른 타입

**설계**: concept은 `std::expected<typename P::Frame, FrameError>`가 아닌 일반화된 형태를 사용. 스펙에서는 `Result<typename P::Frame>`을 사용하지만 현재 `try_decode`는 `std::expected<Frame, FrameError>`를 반환. concept에서는 반환 타입을 명시적으로 `Result<typename P::Frame>`으로 강제할지, 아니면 더 유연하게 할지 결정 필요.

**스펙 준수**: 스펙 문서 4.1에 `Result<typename P::Frame>` 명시. 그러나 현재 `FrameCodec::try_decode`는 `std::expected<Frame, FrameError>` 반환. Phase 2 C-main에서 TcpBinaryProtocol 마이그레이션 시 반환 타입을 `Result<Frame>`으로 통일할 것이므로, concept은 스펙대로 정의.

```cpp
// protocol.hpp
#pragma once

#include <apex/core/result.hpp>
#include <apex/core/ring_buffer.hpp>

#include <concepts>

namespace apex::core {

/// Protocol concept — core에서 정의, shared에서 구현.
/// 의존성 역전: core는 concept만, 구체 프로토콜은 shared가 제공.
///
/// 요구사항:
///   - P::Config  — 프로토콜별 설정 타입
///   - P::Frame   — 디코딩 결과 프레임 타입
///   - P::try_decode(RingBuffer&) -> Result<P::Frame>
///   - P::consume_frame(RingBuffer&, const P::Frame&) -> void
template<typename P>
concept Protocol = requires {
    typename P::Config;
    typename P::Frame;
} && requires(RingBuffer& buf, const typename P::Frame& frame) {
    { P::try_decode(buf) } -> std::same_as<Result<typename P::Frame>>;
    { P::consume_frame(buf, frame) } -> std::same_as<void>;
};

} // namespace apex::core
```

**테스트** (컴파일타임 검증):
```cpp
// Phase 2에서 TcpBinaryProtocol 마이그레이션 후 static_assert(Protocol<TcpBinaryProtocol>)
// C-prep 단계에서는 Mock 프로토콜로 concept 자체 검증:
struct MockProtocol {
    struct Config {};
    struct Frame {};
    static Result<Frame> try_decode(RingBuffer&) { return Frame{}; }
    static void consume_frame(RingBuffer&, const Frame&) {}
};
static_assert(Protocol<MockProtocol>);
```

**커밋**: `feat(core): Protocol concept 정의 (의존성 역전 기반)`

### Step C-prep-2: 공유 스키마 4개 생성

**신규 파일**:
- `apex_shared/schemas/gateway_envelope.fbs`
- `apex_shared/schemas/auth_request.fbs`
- `apex_shared/schemas/auth_response.fbs`
- `apex_shared/schemas/error_envelope.fbs`

**구현**:

```fbs
// gateway_envelope.fbs
namespace apex.shared.schemas;

table GatewayEnvelope {
    session_id: uint64;
    origin_core: uint8;
    msg_id: uint16;
    payload: [ubyte];
    timestamp_ms: uint64;
}

root_type GatewayEnvelope;
```

```fbs
// auth_request.fbs
namespace apex.shared.schemas;

table AuthRequest {
    token: string;
    request_type: uint8;  // 0=발급, 1=검증, 2=블랙리스트
}

root_type AuthRequest;
```

```fbs
// auth_response.fbs
namespace apex.shared.schemas;

table AuthResponse {
    success: bool;
    user_id: uint64;
    error_message: string;
}

root_type AuthResponse;
```

```fbs
// error_envelope.fbs
namespace apex.shared.schemas;

table ErrorEnvelope {
    source_service: string;
    error_code: uint16;
    message: string;
    timestamp_ms: uint64;
}

root_type ErrorEnvelope;
```

**CMake 수정** (`apex_shared/CMakeLists.txt`):
```cmake
find_program(FLATC flatc REQUIRED)

set(APEX_SHARED_SCHEMAS
    ${CMAKE_CURRENT_SOURCE_DIR}/schemas/gateway_envelope.fbs
    ${CMAKE_CURRENT_SOURCE_DIR}/schemas/auth_request.fbs
    ${CMAKE_CURRENT_SOURCE_DIR}/schemas/auth_response.fbs
    ${CMAKE_CURRENT_SOURCE_DIR}/schemas/error_envelope.fbs
)

set(FLATC_OUTPUT_DIR ${CMAKE_CURRENT_BINARY_DIR}/generated/apex/shared/schemas)
file(MAKE_DIRECTORY ${FLATC_OUTPUT_DIR})

foreach(SCHEMA ${APEX_SHARED_SCHEMAS})
    get_filename_component(SCHEMA_NAME ${SCHEMA} NAME_WE)
    add_custom_command(
        OUTPUT ${FLATC_OUTPUT_DIR}/${SCHEMA_NAME}_generated.h
        COMMAND ${FLATC} --cpp -o ${FLATC_OUTPUT_DIR} ${SCHEMA}
        DEPENDS ${SCHEMA}
        COMMENT "FlatBuffers: ${SCHEMA_NAME}.fbs"
    )
    list(APPEND FLATC_OUTPUTS ${FLATC_OUTPUT_DIR}/${SCHEMA_NAME}_generated.h)
endforeach()

add_custom_target(apex_shared_schemas DEPENDS ${FLATC_OUTPUTS})
```

**빌드 검증**: `flatc --cpp` 가 정상 생성하는지 확인.

**커밋**: `feat(shared): 공유 FlatBuffers 스키마 4종 + CMake flatc 빌드`

### Step C-prep-3: Per-session write queue

**수정 파일**:
- `apex_core/include/apex/core/session.hpp`
- `apex_core/src/session.cpp`

**현재 Session 구조** (session.hpp):
- `async_send()` → `awaitable<Result<void>>` (co_await 기반 직접 쓰기)
- `async_send_raw()` → `awaitable<Result<void>>`
- private 멤버: `id_`, `core_id_`, `state_`, `socket_`, `recv_buf_`, `timer_entry_id_`

**변경 — session.hpp에 추가**:

```cpp
// session.hpp — public 영역에 추가
struct WriteRequest {
    std::vector<uint8_t> data;
};

// 기존 async_send/async_send_raw는 유지 (호환성)
// 신규 API 추가:

/// 비동기 전송 큐에 적재 (동기, 즉시 반환).
/// write pump가 미실행이면 기동한다.
/// @return BufferFull if queue exceeds max_queue_depth_
[[nodiscard]] Result<void> enqueue_write(std::vector<uint8_t> data);

/// raw 데이터를 write queue에 적재.
[[nodiscard]] Result<void> enqueue_write_raw(std::span<const uint8_t> data);

// session.hpp — private 영역에 추가
std::deque<WriteRequest> write_queue_;
bool pump_running_ = false;
size_t max_queue_depth_ = 256;

boost::asio::awaitable<void> write_pump();
```

**include 추가** (session.hpp):
```cpp
#include <deque>
#include <vector>
```

**구현** (session.cpp에 append):

```cpp
Result<void> Session::enqueue_write(std::vector<uint8_t> data) {
    if (!is_open()) return error(ErrorCode::SessionClosed);
    if (write_queue_.size() >= max_queue_depth_) {
        SPDLOG_WARN("Session {} write queue full (depth={})", id_, max_queue_depth_);
        return error(ErrorCode::BufferFull);
    }
    write_queue_.push_back(WriteRequest{std::move(data)});
    if (!pump_running_) {
        pump_running_ = true;
        boost::asio::co_spawn(socket_.get_executor(), write_pump(),
                              boost::asio::detached);
    }
    return ok();
}

Result<void> Session::enqueue_write_raw(std::span<const uint8_t> data) {
    std::vector<uint8_t> copy(data.begin(), data.end());
    return enqueue_write(std::move(copy));
}

boost::asio::awaitable<void> Session::write_pump() {
    while (!write_queue_.empty() && is_open()) {
        auto& front = write_queue_.front();
        auto [ec, bytes] = co_await boost::asio::async_write(
            socket_,
            boost::asio::buffer(front.data),
            boost::asio::as_tuple(boost::asio::use_awaitable));

        write_queue_.pop_front();

        if (ec) {
            write_queue_.clear();  // 에러 시 잔여 큐 정리
            close();
            break;
        }
    }
    pump_running_ = false;
}
```

**include 추가** (session.cpp):
```cpp
#include <spdlog/spdlog.h>
```

**기존 API 유지**: `async_send()`/`async_send_raw()`는 삭제하지 않음. Phase 2 C-main에서 호출처를 `enqueue_write()`로 전환.

**테스트** (TDD — `test_session.cpp`에 추가하거나 별도 파일):

주의: C-prep은 `apex_core/tests/` 터치 가능하지만, E-1(Phase 2)과 겹치지 않도록 write queue 테스트만 추가.

```
// TC1: enqueue_write 후 상대 소켓에서 데이터 수신 확인
// TC2: 큐 가득 참 — BufferFull 반환
// TC3: 세션 닫힘 상태에서 enqueue_write — SessionClosed 반환
// TC4: 다수 enqueue_write 순서 보장 (FIFO)
```

**커밋**: `feat(core): Per-session write queue (enqueue_write + write_pump + backpressure)`

### Bundle C-prep 완료
모든 C-prep 스텝 완료 후 빌드 검증.

---

## Phase 1 게이트

**조건**: Chunk 1 + Chunk 2 + Chunk 3 모두 완료.

**게이트 절차**:
1. 메인 워크스페이스에서 빌드: `cmd.exe //c build.bat debug`
2. 테스트 실행: `bin/debug/apex_tests.exe`
3. **full auto-review (12명)** 실행
4. 리뷰 이슈 수정
5. 재빌드 + 재테스트 확인

**게이트 통과 후** Phase 2 진입.

---

## Chunk 4: Phase 2 — Bundle C-main (본작업)

### 목표
TcpBinaryProtocol 마이그레이션, ProtocolBase 삭제, Server 리팩터링 (ListenerBase + Listener<P>), ConnectionHandler<P> 템플릿화, WebSocketProtocol 구현, 기존 examples/tests 수정.

### 터치 파일
- `apex_core/include/apex/core/server.hpp` (Server + PerCoreState 리팩터링)
- `apex_core/src/server.cpp`
- `apex_core/include/apex/core/connection_handler.hpp` -> 템플릿화
- `apex_core/src/connection_handler.cpp` -> 템플릿이므로 .hpp로 통합 또는 유지
- `apex_core/include/apex/core/protocol_base.hpp` -> 삭제
- `apex_core/include/apex/core/tcp_binary_protocol.hpp` -> 삭제 (shared로 이동)
- `apex_core/include/apex/core/frame_codec.hpp` -> shared로 이동
- `apex_core/include/apex/core/wire_header.hpp` -> shared로 이동
- `apex_core/src/frame_codec.cpp` -> shared로 이동
- `apex_core/src/wire_header.cpp` -> shared로 이동
- `apex_core/CMakeLists.txt` (이동된 파일 제거)
- `apex_shared/lib/protocols/` (신규 디렉토리)
- `apex_shared/lib/protocols/tcp/` (TcpBinaryProtocol, FrameCodec, WireHeader)
- `apex_shared/lib/protocols/websocket/` (WebSocketProtocol)
- `apex_shared/CMakeLists.txt` (protocols 서브디렉토리 추가)
- `apex_core/examples/` (echo_server.cpp, chat_server.cpp, multicore_echo_server.cpp)
- `vcpkg.json` (boost-beast 추가 — WebSocket용)

### Step C-main-1: TcpBinaryProtocol + FrameCodec + WireHeader를 shared로 이동

**이동 대상**:

| 현재 위치 | 이동 위치 |
|----------|----------|
| `apex_core/include/apex/core/wire_header.hpp` | `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/wire_header.hpp` |
| `apex_core/src/wire_header.cpp` | `apex_shared/lib/protocols/tcp/src/wire_header.cpp` |
| `apex_core/include/apex/core/frame_codec.hpp` | `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/frame_codec.hpp` |
| `apex_core/src/frame_codec.cpp` | `apex_shared/lib/protocols/tcp/src/frame_codec.cpp` |
| `apex_core/include/apex/core/tcp_binary_protocol.hpp` | `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/tcp_binary_protocol.hpp` |

**네임스페이스 변경**: `apex::core` -> `apex::shared::protocols::tcp`

**호환성**: core에서 WireHeader/FrameCodec를 사용하는 곳이 많으므로, core에 forwarding header를 남기거나 include 경로를 모두 변경. **forwarding header 권장**:

```cpp
// apex_core/include/apex/core/wire_header.hpp (forwarding)
#pragma once
#include <apex/shared/protocols/tcp/wire_header.hpp>
// 호환성을 위한 namespace alias
namespace apex::core {
    using apex::shared::protocols::tcp::WireHeader;
    using apex::shared::protocols::tcp::ParseError;
    namespace wire_flags = apex::shared::protocols::tcp::wire_flags;
}
```

동일하게 `frame_codec.hpp`, `tcp_binary_protocol.hpp`도 forwarding:

```cpp
// apex_core/include/apex/core/frame_codec.hpp (forwarding)
#pragma once
#include <apex/shared/protocols/tcp/frame_codec.hpp>
namespace apex::core {
    using apex::shared::protocols::tcp::FrameCodec;
    using apex::shared::protocols::tcp::Frame;
    using apex::shared::protocols::tcp::FrameError;
}
```

```cpp
// apex_core/include/apex/core/tcp_binary_protocol.hpp (forwarding)
#pragma once
#include <apex/shared/protocols/tcp/tcp_binary_protocol.hpp>
namespace apex::core {
    using apex::shared::protocols::tcp::TcpBinaryProtocol;
}
```

**주의**: Session 클래스가 `WireHeader`를 include하므로 forwarding이 필수.

**TcpBinaryProtocol 변경** — Protocol concept 만족:

```cpp
// tcp_binary_protocol.hpp (shared)
#pragma once

#include <apex/core/protocol.hpp>
#include <apex/shared/protocols/tcp/frame_codec.hpp>

namespace apex::shared::protocols::tcp {

struct TcpBinaryProtocol {
    struct Config {
        uint32_t max_frame_size = 64 * 1024;
    };

    using Frame = ::apex::shared::protocols::tcp::Frame;

    [[nodiscard]] static apex::core::Result<Frame>
    try_decode(apex::core::RingBuffer& buf) {
        auto result = FrameCodec::try_decode(buf);
        if (result.has_value()) return std::move(*result);
        // FrameError -> ErrorCode 변환
        switch (result.error()) {
            case FrameError::InsufficientData:
                return std::unexpected(apex::core::ErrorCode::InsufficientData);  // 새 에러코드, 값 = 13
            case FrameError::HeaderParseError:
                return std::unexpected(apex::core::ErrorCode::InvalidMessage);
            case FrameError::BodyTooLarge:
                return std::unexpected(apex::core::ErrorCode::InvalidMessage);
        }
        return std::unexpected(apex::core::ErrorCode::Unknown);
    }

    static void consume_frame(apex::core::RingBuffer& buf, const Frame& frame) {
        FrameCodec::consume_frame(buf, frame);
    }
};

// Concept 검증
static_assert(apex::core::Protocol<TcpBinaryProtocol>);

} // namespace apex::shared::protocols::tcp
```

**CMake**: `apex_shared/lib/protocols/tcp/CMakeLists.txt` 생성.

빌드 + 기존 테스트 통과 확인.

**커밋**: `refactor(shared): TcpBinaryProtocol + FrameCodec + WireHeader를 shared/protocols/tcp로 이동`

### Step C-main-2: ProtocolBase 삭제

**파일**: `apex_core/include/apex/core/protocol_base.hpp`

Protocol concept이 이를 대체하므로 삭제. 기존 TcpBinaryProtocol이 이미 shared로 이동되어 참조 없음.

**액션**:
1. `protocol_base.hpp` 삭제
2. 이 파일을 include하는 곳 검색 -> 없어야 함 (TcpBinaryProtocol이 유일한 사용자였고 이미 이동)
3. 빌드 + 기존 테스트 통과 확인

**커밋**: `refactor(core): ProtocolBase CRTP 삭제 (Protocol concept으로 대체)`

### Step C-main-3: ListenerBase + Listener<P> 구현

**신규 파일**: `apex_core/include/apex/core/listener.hpp`

```cpp
// listener.hpp
#pragma once

#include <apex/core/connection_handler.hpp>
#include <apex/core/message_dispatcher.hpp>
#include <apex/core/protocol.hpp>
#include <apex/core/session_manager.hpp>
#include <apex/core/tcp_acceptor.hpp>

#include <boost/asio/awaitable.hpp>

#include <memory>
#include <vector>

namespace apex::core {

/// 의도적 virtual: CRTP 일관성 예외.
/// start/drain/stop은 서버 시작·종료 시 1회 호출로 비용 ≈ 0.
/// Hot path는 ConnectionHandler<P> 템플릿으로 zero-overhead.
class ListenerBase {
public:
    virtual ~ListenerBase() = default;
    // 현재 Server lifecycle은 동기 흐름. start()는 내부적으로 코루틴을
    // spawn하지만 자체가 코루틴일 필요 없음.
    virtual void start() = 0;
    virtual void drain() = 0;
    virtual void stop() = 0;
    [[nodiscard]] virtual uint32_t active_sessions() const noexcept = 0;
};

/// 프로토콜별 리스너. 포트 바인딩 + accept loop + per-core ConnectionHandler 관리.
template<Protocol P>
class Listener : public ListenerBase {
public:
    struct PerCoreHandler {
        MessageDispatcher dispatcher;       // handler보다 먼저 선언 (소멸 순서 보장)
        ConnectionHandler<P> handler;
    };

    Listener(uint16_t port, typename P::Config config,
             CoreEngine& engine,
             std::vector<SessionManager*> session_mgrs,
             ConnectionHandlerConfig handler_config);

    void start() override;
    void drain() override;
    void stop() override;
    [[nodiscard]] uint32_t active_sessions() const noexcept override;

private:
    void on_accept(boost::asio::ip::tcp::socket socket);

    uint16_t port_;
    typename P::Config protocol_config_;
    std::vector<PerCoreHandler> per_core_handlers_;
    std::vector<std::unique_ptr<TcpAcceptor>> acceptors_;
    CoreEngine& engine_;
    std::atomic<uint32_t> next_core_{0};
};

} // namespace apex::core
```

**Accept loop 전략**:
- `Listener<P>::start()`에서 TcpAcceptor 생성 + accept loop `co_spawn`
- reuseport 모드: Listener가 per-core acceptor 운영 (기존 `Server::setup_reuseport` 로직 이전)
- 단일 acceptor: Listener가 control io_context에서 accept 후 round-robin 분배

빌드 + 기존 테스트 통과 확인.

**커밋**: `feat(core): ListenerBase (virtual lifecycle) + Listener<P> 템플릿`

### Step C-main-4: ConnectionHandler<P> 템플릿화

**수정 파일**: `apex_core/include/apex/core/connection_handler.hpp`

**현재**: 비템플릿 `ConnectionHandler` — `TcpBinaryProtocol::try_decode()` 직접 호출 (`connection_handler.cpp` line 83)

**변경**: `ConnectionHandler<P>` 템플릿으로 변환. `process_frames()`에서 `P::try_decode()`/`P::consume_frame()` 호출.

```cpp
// connection_handler.hpp
template<Protocol P>
class ConnectionHandler {
public:
    ConnectionHandler(SessionManager& session_mgr,
                      MessageDispatcher& dispatcher,
                      ConnectionHandlerConfig config);

    void accept_connection(boost::asio::ip::tcp::socket socket,
                           boost::asio::io_context& io_ctx);

    [[nodiscard]] uint32_t active_sessions() const noexcept;

private:
    boost::asio::awaitable<void> read_loop(SessionPtr session);
    boost::asio::awaitable<void> process_frames(SessionPtr session);

    SessionManager& session_mgr_;
    MessageDispatcher& dispatcher_;
    ConnectionHandlerConfig config_;
    std::atomic<uint32_t> active_sessions_{0};
};
```

`process_frames` 구현에서 `TcpBinaryProtocol` 대신 `P`:
```cpp
template<Protocol P>
boost::asio::awaitable<void>
ConnectionHandler<P>::process_frames(SessionPtr session) {
    auto& recv_buf = session->recv_buffer();
    for (;;) {
        auto decode_result = P::try_decode(recv_buf);
        if (!decode_result.has_value()) {
            if (decode_result.error() == ErrorCode::InsufficientData) {
                break;  // 더 읽기
            }
            // 그 외 에러 (InvalidMessage 등) → 연결 종료
            session->close();
            co_return;
        }
        auto& frame = *decode_result;
        // ... dispatch + consume_frame ...
        P::consume_frame(recv_buf, frame);
        // ...
    }
}
```

**주의**: 템플릿이므로 구현이 헤더에 있어야 함. `connection_handler.cpp`를 삭제하고 `connection_handler.hpp`에 인라인 구현. 또는 `.ipp` 파일 분리.

빌드 + 기존 테스트 통과 확인.

**커밋**: `refactor(core): ConnectionHandler<P> 템플릿화 (Protocol concept 기반)`

### Step C-main-5: Server 리팩터링

**수정 파일**: `apex_core/include/apex/core/server.hpp`, `apex_core/src/server.cpp`

**PerCoreState 변경**:
```cpp
struct PerCoreState {
    uint32_t core_id;
    SessionManager session_mgr;
    // MessageDispatcher 제거 — Listener<P>가 소유
    // ConnectionHandler 제거 — Listener<P>가 소유
    std::vector<std::unique_ptr<ServiceBaseInterface>> services;
    BumpAllocator bump_allocator;
    ArenaAllocator arena_allocator;

    explicit PerCoreState(uint32_t id, uint32_t heartbeat_timeout_ticks,
                          size_t timer_wheel_slots, size_t recv_buf_capacity,
                          size_t bump_capacity, size_t arena_block, size_t arena_max);
};
```

**Server 변경**:
```cpp
class Server {
public:
    // ... 기존 API 유지 ...

    /// 프로토콜별 리스너 등록. 포트 분리.
    template<Protocol P>
    Server& listen(uint16_t port, typename P::Config config = {});

    // ... 기존 add_service, add_adapter 유지 ...

private:
    // acceptors_ 제거 — Listener가 소유
    std::vector<std::unique_ptr<ListenerBase>> listeners_;
    // ...
};
```

`listen<P>()` 구현:
```cpp
template<Protocol P>
Server& Server::listen(uint16_t port, typename P::Config config) {
    std::vector<SessionManager*> mgrs;
    for (auto& state : per_core_) {
        mgrs.push_back(&state->session_mgr);
    }
    ConnectionHandlerConfig handler_config{.tcp_nodelay = config_.tcp_nodelay};
    auto listener = std::make_unique<Listener<P>>(
        port, std::move(config), *core_engine_, std::move(mgrs), handler_config);
    listeners_.push_back(std::move(listener));
    return *this;
}
```

**ServerConfig::port 마이그레이션**:
- `ServerConfig::port` 필드 제거 — 각 `listen<P>(port, config)` 호출로 대체
- `Server` 생성자에서 acceptor 바인딩 로직 제거 — `Listener::start()`에서 수행
- `Server::port()` 메서드 제거 — 여러 포트를 가질 수 있으므로 의미 없음
- `listen<P>()` 바인딩 실패 시 `std::system_error` 예외 throw (포트 충돌 등)

`run()` 변경: `acceptors_` 대신 `listeners_` 순회.
`stop()` 변경: listener drain/stop.
`total_active_sessions()` 변경: listener에서 집계.

빌드 + 기존 테스트 통과 확인.

**커밋**: `refactor(core): Server 비템플릿 + listen<P>() + ListenerBase vector + ServerConfig::port 제거`

### Step C-main-6: WebSocketProtocol 구현

**신규 파일**:
- `apex_shared/lib/protocols/websocket/include/apex/shared/protocols/websocket/websocket_protocol.hpp`
- `apex_shared/lib/protocols/websocket/src/websocket_protocol.cpp`
- `apex_shared/lib/protocols/websocket/CMakeLists.txt`

**vcpkg.json 수정**: `boost-beast` 추가

```cpp
// websocket_protocol.hpp
namespace apex::shared::protocols::websocket {

struct WebSocketProtocol {
    struct Config {
        std::string path = "/ws";
        size_t max_message_size = 1 * 1024 * 1024;  // 1MB
    };

    struct Frame {
        std::vector<uint8_t> payload;
        bool is_text = false;
        bool is_binary = true;
    };

    [[nodiscard]] static apex::core::Result<Frame>
    try_decode(apex::core::RingBuffer& buf);

    static void consume_frame(apex::core::RingBuffer& buf, const Frame& frame);
};

static_assert(apex::core::Protocol<WebSocketProtocol>);

} // namespace apex::shared::protocols::websocket
```

**WebSocketProtocol try_decode 전략**:
- Beast의 `websocket::stream`은 자체 버퍼링으로 완성된 메시지 단위 수신
- `WebSocketProtocol::try_decode()`는 Beast 내부 버퍼에서 메시지 추출, RingBuffer는 사용하지 않을 수 있음
- Protocol concept에 optional `handshake()` static method 추가 고려:
  ```cpp
  // concept에 optional: 없으면 handshake 생략
  if constexpr (requires { P::handshake(socket, config); }) {
      co_await P::handshake(socket, config);
  }
  ```
- 또는: WebSocketProtocol은 `ConnectionHandler` 수준에서 Beast stream으로 소켓을 래핑한 후 사용
- **핵심 방향**: Beast가 framing 담당, `try_decode`는 메시지 추출만
- **구현 시 결정**: Beast API 학습 후 RingBuffer 우회 또는 브릿지 방식 확정

빌드 + 기존 테스트 통과 확인.

**커밋**: `feat(shared): WebSocketProtocol 구현 (Boost.Beast 기반)`

### Step C-main-7: 기존 examples + tests 수정

**수정 대상 (examples)**:
- `apex_core/examples/echo_server.cpp` — `Server` + `listen<TcpBinaryProtocol>()` 패턴으로 변경
- `apex_core/examples/chat_server.cpp` — 동일
- `apex_core/examples/multicore_echo_server.cpp` — 동일
- `apex_core/examples/echo_loadtest.cpp` — 클라이언트이므로 변경 최소화
- include 경로 업데이트

```cpp
// 변경 전 (기존):
Server server({.port = 9000});
server.add_service<EchoService>().run();

// 변경 후:
Server server({.num_cores = 4});
server.listen<TcpBinaryProtocol>(9000, TcpBinaryProtocol::Config{
    .max_frame_size = 64 * 1024
});
server.add_service<EchoService>().run();
```

**수정 대상 (기존 테스트)**:
- `test_frame_codec.cpp`, `test_wire_header.cpp`, `test_tcp_binary_protocol.cpp` 등
- 네임스페이스/include 경로를 forwarding header 기준으로 업데이트
- 빌드 + 테스트 통과 확인

**커밋**: `refactor(examples,tests): Server::listen<P>() API 전환 + 기존 테스트 네임스페이스·경로 업데이트`

### Step C-main-8: CMake 정리

**수정 파일**:
- `apex_core/CMakeLists.txt` — 이동된 소스(wire_header.cpp, frame_codec.cpp) 제거
- `apex_shared/CMakeLists.txt` — `add_subdirectory(lib/protocols)` 추가
- `apex_shared/lib/protocols/CMakeLists.txt` (신규) — tcp/, websocket/ 서브디렉토리
- `apex_shared/lib/protocols/tcp/CMakeLists.txt` (신규)
- `apex_shared/lib/protocols/websocket/CMakeLists.txt` (신규)

**커밋**: `build: protocols 라이브러리 CMake 구성 + core에서 이동된 소스 제거`

### Bundle C-main 완료
모든 C-main 스텝 완료 후 빌드 + 전체 테스트 검증.

---

## Chunk 5: Phase 2 — Bundle E-1 (단위 테스트)

> **C-main 선행 필수**: E-1은 C-main이 완료된 API를 대상으로 테스트한다. C-main과 E-1은 Phase 2 내에서 순차 실행(C-main → E-1).

### 목표
test_mocks.hpp 작성, ConnectionHandler/PgTransaction/RedisMultiplexer/Session/Server 테스트.

### 터치 파일
- `apex_core/tests/test_mocks.hpp` (신규)
- `apex_shared/tests/test_mocks.hpp` (신규)
- `apex_core/tests/unit/test_connection_handler.cpp` (신규)
- `apex_core/tests/unit/test_session_write_queue.cpp` (신규)
- `apex_core/tests/unit/test_server_error_paths.cpp` (신규)
- `apex_shared/tests/unit/test_pg_transaction.cpp` (신규)
- `apex_shared/tests/unit/test_redis_multiplexer.cpp` (신규)
- `apex_core/tests/unit/CMakeLists.txt` (테스트 타겟 추가)
- `apex_shared/tests/CMakeLists.txt` 또는 `apex_shared/tests/unit/CMakeLists.txt`

### Step E-1-1: test_mocks.hpp (Core)

**신규 파일**: `apex_core/tests/test_mocks.hpp`

```cpp
#pragma once

#include <apex/core/session.hpp>
#include <apex/core/message_dispatcher.hpp>

#include <cstdint>
#include <vector>

namespace apex::test {

/// Mock SessionManager — 세션 생성/삭제 추적
class MockSessionManager {
public:
    std::vector<apex::core::SessionId> created_sessions;
    std::vector<apex::core::SessionId> destroyed_sessions;

    apex::core::SessionPtr create_session(boost::asio::ip::tcp::socket socket) {
        // 간단한 구현 — 실제 SessionManager 로직 없이 Session만 생성
        auto id = next_id_++;
        auto session = new apex::core::Session(id, std::move(socket), 0);
        intrusive_ptr_add_ref(session);
        created_sessions.push_back(id);
        return apex::core::SessionPtr(session, false);
    }

    void remove_session(apex::core::SessionId id) {
        destroyed_sessions.push_back(id);
    }

    void touch_session(apex::core::SessionId) {}

private:
    apex::core::SessionId next_id_ = 1;
};

/// Mock MessageDispatcher — dispatch 호출 추적
class MockDispatcher {
public:
    struct DispatchRecord {
        apex::core::SessionId session_id;
        uint16_t msg_id;
        std::vector<uint8_t> payload;
    };

    std::vector<DispatchRecord> dispatched;

    boost::asio::awaitable<apex::core::Result<void>>
    dispatch(apex::core::SessionPtr session, uint16_t msg_id,
             std::span<const uint8_t> payload) {
        dispatched.push_back({session->id(), msg_id,
                              {payload.begin(), payload.end()}});
        co_return apex::core::ok();
    }
};

} // namespace apex::test
```

**커밋**: `test(core): test_mocks.hpp (MockSessionManager, MockDispatcher)`

### Step E-1-2: test_mocks.hpp (Shared)

**신규 파일**: `apex_shared/tests/test_mocks.hpp`

```cpp
#pragma once

#include <apex/core/result.hpp>
#include <boost/asio/awaitable.hpp>

#include <string>
#include <vector>

namespace apex::test {

/// Mock PgConnection — 쿼리 결과 stub
class MockPgConnection {
public:
    bool connected = true;
    bool poisoned = false;
    std::vector<std::string> executed_queries;

    [[nodiscard]] bool is_connected() const { return connected; }
    [[nodiscard]] bool is_valid() const { return connected && !poisoned; }
    [[nodiscard]] bool is_poisoned() const { return poisoned; }
    void close() { connected = false; }
};

} // namespace apex::test
```

**커밋**: `test(shared): test_mocks.hpp (MockPgConnection 등)`

### Step E-1-3: ConnectionHandler 테스트 (5~8 TC)

**신규 파일**: `apex_core/tests/unit/test_connection_handler.cpp`

**테스트 전략**: MockSessionManager/MockDispatcher 직접 주입은 concrete type이라 불가. 대신 **integration-style unit test** 방식을 사용:
- 실제 SessionManager 사용 (lightweight)
- 실제 MessageDispatcher에 `dispatcher.register_handler(msg_id, test_lambda)`로 동작 제어
- `make_socket_pair()`로 소켓 쌍 생성하여 제어된 입력 전달

```
TC1: accept_connection — 세션 생성 확인
TC2: 정상 프레임 수신 — dispatch 호출 확인 (register_handler로 검증)
TC3: 불완전 프레임 — 추가 읽기 대기
TC4: 유효하지 않은 프레임 — 세션 종료
TC5: 클라이언트 연결 끊김 — 세션 정리
TC6: dispatch 실패 — 에러 응답 전송
TC7: 다중 프레임 연속 처리
TC8: active_sessions 카운트 정확성
```

CMake 테스트 타겟 등록: 테스트 파일 추가 시 함께 수행.

**커밋**: `test(core): ConnectionHandler 단위 테스트 8TC`

### Step E-1-4: PgTransaction 테스트 (4~5 TC)

**신규 파일**: `apex_shared/tests/unit/test_pg_transaction.cpp`

```
TC1: begin -> execute -> commit 정상 흐름
TC2: begin -> execute -> rollback 정상 흐름
TC3: 소멸자에서 commit/rollback 미호출 시 poisoned 마킹
TC4: 중첩 begin 방어 (이미 begun 상태에서 begin 재호출)
TC5: execute 실패 시 에러 전파
```

CMake 테스트 타겟 등록: 테스트 파일 추가 시 함께 수행.

**커밋**: `test(shared): PgTransaction 단위 테스트 5TC`

### Step E-1-5: RedisMultiplexer 테스트 (5~6 TC)

**신규 파일**: `apex_shared/tests/unit/test_redis_multiplexer.cpp`

```
TC1: command 성공 — RedisReply 반환
TC2: command 타임아웃 — Timeout 에러
TC3: 연결 끊김 — 모든 pending command 에러 반환
TC4: reconnect 성공 확인
TC5: pipeline 순서 보장
TC6: close 후 command 거부
```

**주의**: hiredis mock이 복잡하므로 실제 서버 없이 가능한 범위로 제한. 필요 시 integration 테스트로 이동.

CMake 테스트 타겟 등록: 테스트 파일 추가 시 함께 수행.

**커밋**: `test(shared): RedisMultiplexer 단위 테스트 6TC`

### Step E-1-6: Session write queue 회귀 확인 + 추가 엣지 케이스

C-prep-3에서 작성한 write queue 테스트가 Phase 2 API 변경 후에도 유효한지 **회귀 확인**한다. 추가 엣지 케이스만 작성:

```
TC1: (회귀 확인) C-prep-3 테스트 4건 전부 통과 확인
TC2: 프로토콜 마이그레이션 후 enqueue_write 호환성 확인
```

CMake 테스트 타겟 등록: 테스트 파일 추가 시 함께 수행.

**커밋**: `test(core): Session write queue 회귀 확인 + 프로토콜 마이그레이션 호환 TC`

### Step E-1-7: Server 에러 경로 테스트 (4~5 TC)

**신규 파일**: `apex_core/tests/unit/test_server_error_paths.cpp`

```
TC1: 이미 사용 중인 포트에 listen — 에러
TC2: run() 이중 호출 — logic_error
TC3: graceful shutdown — drain 후 세션 0 확인
TC4: drain timeout — 강제 종료
TC5: stop() 재진입 안전성
```

CMake 테스트 타겟 등록: 테스트 파일 추가 시 함께 수행.

**커밋**: `test(core): Server 에러 경로 단위 테스트 5TC`

### Step E-1-8: (삭제 — 각 테스트 스텝에서 CMake 등록 병행)

> CMake 테스트 타겟 등록을 별도 스텝으로 분리하지 않고, 각 테스트 파일 추가 시(E-1-3, E-1-4, E-1-5, E-1-6, E-1-7) 함께 수행한다.

### Bundle E-1 완료
모든 E-1 스텝 완료 후 전체 테스트 통과 확인.

---

## Phase 2 게이트

**조건**: Chunk 4 + Chunk 5 모두 완료.

**게이트 절차**:
1. 빌드: `cmd.exe //c build.bat debug`
2. 전체 테스트: `bin/debug/apex_tests.exe`
3. **auto-review (12명)** 실행
4. 리뷰 이슈 수정
5. 문서 갱신 (docs/Apex_Pipeline.md, CLAUDE.md 로드맵, README.md)
6. PR 생성 -> main 머지

---

## 의존성 요약

```
                     Phase 1                          Phase 2
                 ┌─────────────┐                 ┌─────────────┐
     ┌──────────>│  Chunk 1 A  │────────────────>│             │
     │           └─────────────┘                 │             │
     │           ┌─────────────┐                 │  Chunk 4    │
 독립 │──────────>│  Chunk 2 B  │────────────────>│  C-main     │
     │           └─────────────┘   Phase 1 Gate  │             │
     │           ┌─────────────┐   (auto-review) │             │
     └──────────>│  Chunk 3    │────────────────>│             │
                 │  C-prep     │                 └─────────────┘
                 └─────────────┘                 ┌─────────────┐
                                                 │  Chunk 5    │
                                                 │  E-1        │
                                                 └─────────────┘
```

- Phase 1: Chunk 1/2/3 병렬 (충돌 없음)
- Phase 1 -> Phase 2: full auto-review 게이트
- Phase 2: Chunk 4/5 병렬 가능 (C-main은 API 변경, E-1은 테스트 — 단, E-1은 C-main의 최종 API가 확정된 후 작성하는 것이 효율적이므로 **C-main 완료 후 E-1 시작 권장**)

---

## 커밋 히스토리 예상

### Phase 1
```
fix(core): server.cpp clang-tidy 경고 2건 수정
fix(core): session.cpp clang-tidy 경고 5건 수정
fix(core): arena_allocator.cpp clang-tidy 경고 8건 수정
fix(test): make_socket_pair 반환 순서 호출처 통일
docs(adr): ADR-04 모노레포 구조 현행화 + protocols 디렉토리 반영
docs(backlog): MoveConstruction 오타 아님 확인, 항목 정리
feat(core): ErrorCode::PoolExhausted(2001) 추가
feat(shared): CircuitBreaker 상태 머신 구현 + 단위 테스트 7TC
feat(redis): RedisMultiplexer AUTH 지원 (reconnect_loop 내 인증)
feat(redis): RedisReply ARRAY 재귀 파싱 + 타입 체크 헬퍼
feat(pg): PgPool::acquire_with_retry exponential backoff 재시도
feat(kafka): DLQ 라우팅 (MessageCallback Result<void> + Consumer->Producer 주입)
refactor(kafka): KafkaAdapter draining_ -> AdapterState enum + atomic
feat(core): Protocol concept 정의 (의존성 역전 기반)
feat(shared): 공유 FlatBuffers 스키마 4종 + CMake flatc 빌드
feat(core): Per-session write queue (enqueue_write + write_pump + backpressure)
```

### Phase 2
```
refactor(shared): TcpBinaryProtocol + FrameCodec + WireHeader를 shared/protocols/tcp로 이동
refactor(core): ProtocolBase CRTP 삭제 (Protocol concept으로 대체)
feat(core): ListenerBase (virtual lifecycle) + Listener<P> 템플릿
refactor(core): ConnectionHandler<P> 템플릿화 (Protocol concept 기반)
refactor(core): Server 비템플릿 + listen<P>() + ListenerBase vector
feat(shared): WebSocketProtocol 구현 (Boost.Beast 기반)
refactor(examples): Server::listen<P>() API로 전환
build: protocols 라이브러리 CMake 구성 + core에서 이동된 소스 제거
test(core): test_mocks.hpp (MockSessionManager, MockDispatcher)
test(shared): test_mocks.hpp (MockPgConnection 등)
test(core): ConnectionHandler 단위 테스트 8TC
test(shared): PgTransaction 단위 테스트 5TC
test(shared): RedisMultiplexer 단위 테스트 6TC
test(core): Session write queue 단위 테스트 4TC
test(core): Server 에러 경로 단위 테스트 5TC
build(test): 신규 테스트 파일 CMake 등록
```
