# Full Auto-Review v0.5.7.0

**일시**: 2026-03-19 21:14:03
**모드**: full (전체 코드베이스)
**대상**: 275 소스 파일 + 193 문서 파일
**리뷰어**: 7명 (systems, logic, design, test, infra-security, docs-spec, docs-records)

---

## 리뷰 통계

| 리뷰어 | CRITICAL | MAJOR | MINOR | 수정 |
|--------|----------|-------|-------|------|
| systems | 0 | 3 | 3 | 0 |
| logic | 0 | 2 | 7 | 0 |
| design | 2 | 7 | 6 | 0 |
| test | 0 | 1 | 4 | 0 |
| infra-security | 2 | 6 | 11 | 0 |
| docs-spec | 0 | 4 | 7 | 0 |
| docs-records | 0 | 3 | 4 | 0 |
| **합계** | **4** | **26** | **42** | **0** |

## 수정 완료 (메인 오케스트레이터)

### CRITICAL (1건)

1. **chat_svc.toml Redis 포트 교차 수정** — `redis_data` 6381→6382, `redis_pubsub` 6382→6383. docker-compose 인스턴스 매핑과 일치하도록 교정. E2E 설정(`chat_svc_e2e.toml`)은 이미 올바른 값이었으나 프로덕션 설정이 잘못되어 있었음.

### MAJOR (6건)

2. **Gateway 시스템 메시지 FlatBuffers Verifier 추가** — `AUTHENTICATE_SESSION`, `SUBSCRIBE_CHANNEL`, `UNSUBSCRIBE_CHANNEL` 3개 핸들러에 `VerifyTableStart()` + `VerifyString()` 검증 추가. 악의적 클라이언트의 malformed payload로 인한 buffer overread 방어. (`gateway_service.cpp`)

3. **system_msg_ids uint16_t → uint32_t 통일** — WireHeader의 `msg_id`가 `uint32_t`인데 system_msg_ids가 `uint16_t`로 선언되어 타입 불일치. `uint32_t`로 통일. (`gateway_service.hpp`)

4. **SlidingWindowCounter zero division 방어** — `window_size.count() == 0`으로 생성 시 `advance_window()`에서 정수 나눗셈 0 UB 발생 가능. 생성자에서 0이면 Duration{1}로 클램핑. (`sliding_window_counter.cpp`)

5. **ResponseDispatcher payload offset 경계 검증** — `envelope_payload_offset()`이 `payload_copy->size()`보다 큰 값을 반환할 경우 unsigned underflow 방지. 경계 검사 추가. (`response_dispatcher.cpp`)

6. **버전 번호 5곳 통일** — CMakeLists.txt(루트 0.4.0→0.5.7, apex_core 0.4.0→0.5.7), vcpkg.json(루트 0.5.5→0.5.7, apex_core 0.4.0→0.5.7, apex_shared 0.4.0→0.5.7) 모두 v0.5.7로 동기화.

7. **CI path filter에 `cmake/**` 추가** — `cmake/ApexWarnings.cmake` 등 경고 설정 변경 시 CI가 트리거되지 않는 문제 해결. (`.github/workflows/ci.yml`)

### 문서 수정 (5건)

8. **BACKLOG #71 타이포 수정** — `타ип`(키릴 문자 혼입) → `타입`. (`BACKLOG.md`)

9. **BACKLOG_HISTORY ID 누락 15건 보정** — ID 없이 기록된 15개 히스토리 항목에 #74-#88 할당. (`BACKLOG_HISTORY.md`)

10. **핸드오프 문서 미이전 백로그 정리** — `20260319_115806_post-e2e-review-handoff.md` 섹션 4의 MINOR 10건을 BACKLOG #97 + 기존 #68로 이전 완료, 원본에서 제거.

11. **신규 백로그 항목 9건 등록** — 리뷰에서 발견된 설계 부채/문서 이슈를 BACKLOG #89-#97으로 등록. (상세 아래)

12. **BACKLOG "다음 발번" 갱신** — 74 → 98.

## 백로그 등록 (수정 불가 — 설계 변경 필요)

| ID | 등급 | 이슈 | 리뷰어 |
|----|------|------|--------|
| #89 | CRITICAL | core → shared 역방향 의존 (forwarding header + kafka_envelope) | design |
| #90 | MAJOR | ErrorCode에 Gateway 전용 에러 코드 혼입 | design |
| #91 | MAJOR | SessionId 강타입 부재 (uint64_t typedef) | design |
| #92 | MAJOR | WebSocket msg_id 바이트오더 미지정 | design, logic |
| #93 | MAJOR | config.hpp → server.hpp 순환 include 비용 | design |
| #94 | MAJOR | outstanding_coros_ fetch_add 메모리 오더 비대칭 | design |
| #95 | MAJOR | apex_core/README.md 전면 갱신 필요 | docs-spec |
| #96 | MAJOR | post_init_callback 문서 vs 코드 괴리 | docs-spec, logic |
| #97 | MINOR | 서비스 레이어 코드 위생 일괄 정리 (8건 묶음) | docs-records |

## 기존 백로그 해당 (신규 등록 불필요)

- Redis AUTH 미설정 → 기존 #8
- PG 크레덴셜 평문 → 기존 #5, #6
- Grafana 기본 비밀번호 → 기존 #5
- ServiceRegistry API 가이드 불일치 → 기존 #70
- Shutdown 시퀀스 가이드 갱신 → 기존 #69
- set_default_handler 캡슐화 우회 → 기존 #68
- 테스트 커버리지 갭 (Listener, API 등) → 기존 #13, #14, #67

## 리뷰어별 상세

### systems (메모리·동시성)

전체 평가: **양호**. 심각한 결함 없음. 커스텀 할당기 3종 정합성 높음. shared-nothing io_context-per-core 아키텍처 일관 적용. cross_core_call CAS 기반 timeout race 해결 올바름. FlatBuffers co_await 전 필드 복사 패턴 일관 적용. Shutdown 시퀀스 역순 파괴 원칙 부합.

- MAJOR-1: write_pump pump_running_ race window — 코루틴 단일 스레드 모델에서 안전. 주석 보강 권장
- MAJOR-2: GatewayService dynamic_cast — D1 완전 구현 시 해소 예정. ServiceRegistry 기반 조회 전환 권장
- MAJOR-3: KafkaAdapter wire_services this capture lifetime — shutdown 시퀀스에서 안전 확인

### logic (비즈니스 로직)

전체 평가: **양호**. 핵심 자료구조 경계 조건 처리 철저. WireHeader/KafkaEnvelope 직렬화 strict aliasing 위반 없음.

- **MAJOR: Gateway FlatBuffers Verifier 미적용** → 수정 완료
- **MAJOR: write_pump 에러 시 silent 메시지 드랍** → 설계 의도이나 로깅 보강 검토 필요
- MINOR 7건: SlidingWindowCounter zero div(수정 완료), WebSocket msg_id endian(#92), account lock 시간 비교, ResponseDispatcher offset(수정 완료), spawn 예외 전파 등

### design (API·아키텍처)

전체 평가: **설계 품질 높음**. Protocol/Transport concept, ServiceBase CRTP + Phase 분리, Gateway 서비스 독립성 완벽.

- **CRITICAL 2건: core → shared 역방향 의존** → #89 등록
- MAJOR 7건: ErrorCode 혼입(#90), SessionId 강타입(#91), system_msg_ids 타입(수정 완료), ServiceRegistry 가이드 불일치(#70), outstanding_coros_ 오더(#94), WebSocket endian(#92), config.hpp 순환(#93)

### test (테스트 품질)

전체 평가: **양호**. 350+ 테스트 케이스, Arrange-Act-Assert 일관 적용, test_helpers 인프라 우수.

- MAJOR: reinterpret_cast<Server*>(0x1) UB 패턴 — 기술적 UB이나 현재 동작. aligned storage 패턴 전환 권장
- 커버리지 갭 10건: set_default_handler/kafka_route API, Listener, Gateway 컴포넌트(broadcast_fanout 등), 어댑터 에러 경로

### infra-security (인프라·보안)

전체 평가: **빌드 시스템 양호**. apex_set_warnings() 전 타겟 적용, SQL 파라미터 바인딩 완벽.

- **CRITICAL: chat_svc.toml Redis 포트 교차** → 수정 완료
- **CRITICAL: Redis AUTH 미설정** → 기존 #8
- MAJOR 6건: 크레덴셜 평문(기존 #5,#6), 버전 불일치(수정 완료), 0.0.0.0 바인딩, Grafana 기본값, num_cores DoS
- MINOR 11건: CI path filter(수정 완료), PII 로깅, 하드코딩 테스트 패스워드 등

### docs-spec (원천 문서 정합성)

전체 평가: **핵심 버전 동기화는 양호** (CLAUDE.md, Apex_Pipeline.md, README.md 간 v0.5.7.0 일치). 서브 문서 갱신 지연이 주요 문제.

- MAJOR 4건: apex_core/README.md 오래된 정보(#95), 가이드 버전 v0.5.5.2 체류, post_init_callback 문서/코드 괴리(#96), vcpkg.json 버전(수정 완료)
- MINOR 7건: 가이드 D1-D7 면책조항, ServiceRegistry API 예시(#70), Gateway "HTTP" 오기재, BACKLOG 타이포(수정 완료) 등

### docs-records (기록 문서 품질)

전체 평가: **plans/progress/review 3단계 추적 양호**. 빈 껍데기 문서 없음.

- MAJOR 3건: BACKLOG_HISTORY ID 누락 15건(수정 완료), 핸드오프 미이전 백로그(수정 완료), 반올림 타임스탬프 3건(과거 이력, #59에서 방지 예정)
- MINOR 4건: review 내 "향후 과제" 잔류, progress 내 "남은 백로그" 포인터 등

## 빌드 검증

- MSVC `/W4 /WX` debug 빌드 성공
- 71/71 유닛 테스트 통과 (72.22초)
- clang-format 검증 통과
