# Auto-Review: FSD 백로그 소탕 (#139, #140, #141, #143, #144, #145)

**날짜**: 2026-03-23
**브랜치**: feature/fsd-backlog-20260323_011850
**PR**: #117

## 리뷰 구성

| 리뷰어 | 담당 | 결과 |
|--------|------|------|
| logic | #139 SQL 전환, #145 Lua 원자화 | 이슈 0건 |
| test | #143 테스트 추가, tcp_acceptor 테스트 변경 | 이슈 0건 |
| systems | #144 atomic, #140 bind_address, #141 max_connections | MAJOR 1 + MINOR 4 |

## 발견 이슈

### MAJOR

| # | 이슈 | 파일 | 조치 |
|---|------|------|------|
| 1 | make_address 예외 시 불친절한 에러 메시지 | tcp_acceptor.cpp:29 | **수정**: error_code overload + spdlog::critical 로깅 |

### MINOR

| # | 이슈 | 파일 | 조치 |
|---|------|------|------|
| 2 | IPv6 dual-stack 미고려 (Windows IPV6_V6ONLY) | tcp_acceptor.cpp:30 | 문서화만 (향후) |
| 3 | TOCTOU check-then-accept | listener.hpp:107,216 | 의도적 tradeoff, 수정 불필요 |
| 4 | 로그에서 active_sessions() 이중 호출 | listener.hpp:109,218 | **수정**: 결과 캐싱 |
| 5 | socket.close() throwing overload | listener.hpp:111,219 | **수정**: error_code overload |

## 상세 분석

### logic 리뷰

- **#139 SQL 전환**: `locked_until > NOW()` 결과의 NULL/boolean 처리 정확. libpq 텍스트 프로토콜에서 boolean은 항상 "t"/"f". refresh_token 패턴과 일관.
- **#145 Lua EVAL**: SISMEMBER+SREM 원자화 정확. join_room Lua 패턴과 일관된 에러 핸들링. Redis Lua EVAL은 원자 실행 보장.

### systems 리뷰

- **#144 relaxed ordering 안전성**: `intrusive_ptr_release`의 `acq_rel`이 cross-thread 소멸자 접근의 happens-before를 보장. relaxed는 hot-path(implicit strand)에서 zero-cost.
- **#140 bind_address**: `make_address` 파싱 후 `is_v4()`로 protocol 자동 결정. IPv6 dual-stack은 향후 과제.
- **#141 max_connections**: `active_sessions()` 캐싱으로 로그 정확성 개선. TOCTOU는 "대략적 제한"으로 허용 가능 (최대 num_cores-1개 초과).

### test 리뷰

- **#143 테스트 3건**: MockAdapter의 `try_spawn()` wrapper로 protected 메서드 테스트. `outstanding_adapter_coros() == 0` assertion이 실용적으로 충분. 테스트 격리성 양호.
