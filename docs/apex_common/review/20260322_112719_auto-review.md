# Auto-Review — FSD Backlog Sweep (BACKLOG-26,27,38,39,128,129)

**브랜치**: feature/fsd-backlog-20260322_105023
**일시**: 2026-03-22 11:27
**리뷰어**: systems, logic, design, infra-security (4명 병렬)

---

## 리뷰 요약

| 리뷰어 | 담당 | 이슈 수 | 수정 |
|--------|------|---------|------|
| systems | #129 RedisMultiplexer lifetime | 4건 (MAJOR 3, MINOR 1) | revert — 어댑터 인터페이스 선행 필요 |
| logic | #26 ReplyTopicHeader, #27 FrameError | 0건 로직 결함, 2건 테스트 누락 | 테스트 2건 추가 |
| design | API 일관성, 모듈 경계 | 0건 설계 결함, 1건 MockProtocol 누락 | MockProtocol 동기화 |
| infra-security | CMake, vcpkg | 0건 | — |

---

## 상세

### [systems] RedisMultiplexer — revert 판단

systems 리뷰어가 `inflight_coros_` 카운터 + `loop_done_` 동기화 + `closed_` 플래그 + assert를 도입.
그러나 `RedisAdapter::do_close()`가 `per_core_.clear()`로 RedisMultiplexer를 동기적으로 파괴하여 `close()` 미호출 → assert 발동.
어댑터 인터페이스(`do_close()` 코루틴화) 변경이 선행 필요하므로 원래 TcpAcceptor 패턴 유지.
BACKLOG-132로 등록.

pipeline() 내 "ownership transferred" 주석 오류는 유효했으나 스코프 외로 미수정.

### [logic] 로직 결함 0건, 테스트 누락 2건

1. **build_full_envelope + TopicTooLong**: 65536바이트 topic 시 reply 섹션 생략 + HAS_REPLY_TOPIC 해제 동작 미테스트 → `TopicTooLongOmitsReplySection` 테스트 추가
2. **UnsupportedProtocolVersion 경로**: frame_codec→tcp_binary_protocol 변환 경로 미테스트 → `UnsupportedVersionReturnsError` 테스트 추가

EnvelopeBuilder는 ReplyTopicHeader::serialize()를 사용하지 않고 자체 구현 — 정합성 확인됨.
ParseError→FrameError default 분기: InsufficientData는 선체크에서 걸려 parse()까지 도달 불가 — 안전.

### [design] MockProtocol 동기화

test_mocks.hpp의 MockProtocol이 FrameError::UnsupportedProtocolVersion case 누락.
`-Wswitch` 경고 + 실 프로토콜과 동작 불일치 → case 추가.
동시에 BodyTooLarge→BufferFull 매핑도 TcpBinaryProtocol과 일치시킴.

BodyTooLarge→BufferFull 의미 불일치(backpressure vs 프로토콜 위반) 지적 있었으나,
ErrorCode에 전용 코드가 없고 connection_handler 동작(close)은 유지되므로 현 상태 유지.

### [infra-security] 이슈 없음

vcpkg.json $comment 필드, APEX_CORE_BIN_DIR 변수 스코프, standalone/CI 빌드 모두 검증 통과.

---

## 신규 백로그

- **BACKLOG-132**: RedisAdapter::do_close()에서 RedisMultiplexer::close() 미호출 — 어댑터 인터페이스 설계 변경 선행 필요
