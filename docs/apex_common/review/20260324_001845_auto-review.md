# Auto-Review: BACKLOG-150,152 백로그 소탕

- **브랜치**: `feature/fsd-backlog-20260323_235625`
- **PR**: #127
- **일시**: 2026-03-24 00:18

## 변경 범위

| 파일 | 변경 내용 |
|------|-----------|
| `apex_services/gateway/src/jwt_verifier.cpp` | `std::stoull` → `std::from_chars` + 에러 분기 |
| `apex_services/auth-svc/src/jwt_manager.cpp` | `std::stoull` → `std::from_chars` + 에러 분기 |
| `apex_core/include/apex/core/service_base.hpp` | CRTP/virtual 비대칭 설계 의도 주석 |

## 리뷰 결과

소규모 변경 (3파일, 26줄)이므로 메인 에이전트가 직접 리뷰.

### 발견 이슈

| # | 심각도 | 파일 | 이슈 | 조치 |
|---|--------|------|------|------|
| 1 | MINOR | `test_jwt_verifier.cpp` | `from_chars` 에러 경로 테스트 누락 — 비숫자 uid 문자열에 대한 검증 없음 | `MalformedUidClaim` 테스트 추가 (즉시 수정) |

### 확인 항목 (이슈 없음)

- `from_chars` 패턴이 기존 코드(`auth_service.cpp`, `chat_service.cpp`)와 일관됨
- 에러 시 `ServiceError` 반환 — 기존 catch 경로와 동일한 에러 코드
- `spdlog::debug` 로그 레벨 적절 (운영 환경에서 불필요한 노이즈 방지)
- `service_base.hpp` 주석이 영어로 작성 — 코드 주석 관례와 일치
- `<charconv>` include 위치가 알파벳 순서 정렬 준수

## 빌드 검증

- 로컬 MSVC `/W4 /WX` debug: 경고 0건, 84/84 테스트 통과
- CI (GCC, UBSAN, ASAN, TSAN, E2E): 전체 통과
