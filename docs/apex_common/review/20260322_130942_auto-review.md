# Auto-Review: BACKLOG-131,130,24,29 백로그 소탕

- **일시**: 2026-03-22 13:09:42
- **브랜치**: feature/fsd-backlog-20260322_122946
- **PR**: #100
- **리뷰어**: design, systems, infra-security (3명 병렬)

---

## 리뷰 대상

| BACKLOG | 항목 | 변경 |
|---------|------|------|
| #131 | KafkaSecurityConfig | kafka_config.hpp, kafka_producer.cpp, kafka_consumer.cpp |
| #130 | TransportContext | transport.hpp, tls/plain_tcp_transport.hpp, CMakeLists.txt, vcpkg.json |
| #24 | AdapterState enum | adapter_base.hpp, kafka_adapter.hpp/cpp, pg_adapter.cpp |
| #29 | drain/stop 분리 | listener.hpp |

14파일 변경, 125+/45-.

---

## 발견 이슈 및 처리

### 수정 완료 (5건)

| # | 등급 | 리뷰어 | 이슈 | 처리 |
|---|------|--------|------|------|
| 1 | CRITICAL | infra-security | `sasl.password` 로그 평문 노출 — `rd_kafka_conf_set` 실패 시 val 포함 로그 | 마스킹 처리 (`"****"`) |
| 2 | HIGH | infra-security | 보안 설정(SSL/SASL) 실패 시 silent fallback to PLAINTEXT | `sec_set` 헬퍼 도입, 실패 시 init 즉시 중단 |
| 3 | MINOR | design | AdapterState enum 값 순서가 라이프사이클과 불일치 | CLOSED=0→RUNNING=1→DRAINING=2 재배치 |
| 4 | LOW | infra-security | `kafka_consumer.cpp`에 `<cstring>` include 누락 | 추가 |
| 5 | LOW | systems | `KafkaConsumer::consuming_` 비원자적 — TSAN false positive 가능 | `std::atomic<bool>` 전환 |

### 백로그 등록 (3건)

| BACKLOG | 등급 | 리뷰어 | 이슈 |
|---------|------|--------|------|
| #133 | MAJOR | design | TransportContext의 `ssl::context*` → apex_core에 OpenSSL 직접 의존. concept associated type으로 타입 소거 필요 |
| #134 | MAJOR | design | AdapterState가 AdapterInterface에 미노출. Server에서 어댑터 상태 조회 불가 |
| #135 | MINOR | design+infra | KafkaSecurityConfig::sasl_password 평문 메모리 저장. v0.6에서 secure secret 처리 필요 |

### 이슈 없음 확인 (systems 리뷰어)

- AdapterBase memory_order (release/acquire) 선택: 올바름
- drain/close의 "state-first, then cleanup" 패턴: 의도적 비대칭, 시맨틱 올바름
- `is_running()` → `is_ready()` 전환: 경쟁 조건 악화 없음
- TransportContext raw pointer lifetime: 현재 호출 경로 미존재, 향후 주의 필요
- wire_services shared_ptr 체인: 코루틴 프레임이 수명 보장

---

## 빌드 검증

- 로컬 MSVC debug: 81/81 테스트 통과 (수정 전후 모두)
- CI (GCC + ASAN + TSAN + UBSAN): 확인 대기
