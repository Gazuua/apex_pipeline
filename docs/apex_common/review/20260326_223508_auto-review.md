# Auto-Review: Whisper O(1) Core Routing (BACKLOG-149)

- **일시**: 2026-03-26 22:35
- **모드**: task (변경 파일만)
- **대상 커밋**: `a544599` feat(services): Whisper O(1) core routing

## 변경 파일 (6개)

| 파일 | 변경 | 역할 |
|------|------|------|
| `auth-svc/session_store.hpp` | 시그니처 변경 | `core_id` 파라미터 추가 |
| `auth-svc/session_store.cpp` | 값 합성 | `"sid:core_id"` 포맷 |
| `auth-svc/auth_service.cpp` | 호출부 수정 | on_login, on_refresh에서 `meta.core_id` 전달 |
| `chat-svc/chat_service.cpp` | 파싱+라우팅 | `parse_session_core` 헬퍼 + 실제 core_id 사용 |
| `gateway/response_dispatcher.cpp` | 단일 코어 post | corr_id==0 경로 O(1) 분기 |
| `docs/apex_common/plans/` | 설계 문서 | 설계 스펙 |

## 리뷰 결과

### Logic 관점

| # | 심각도 | 파일 | 이슈 | 조치 |
|---|--------|------|------|------|
| L1 | MINOR | chat_service.cpp:618 | whisper 로그에 target_core_id 미포함 — 디버깅 시 O(1) 라우팅 추적 불가 | **수정**: debug 레벨 로그 추가 |

**검증 완료 항목:**
- `parse_session_core` 엣지 케이스: 빈 문자열, `:` 없음(레거시), `:` 끝(빈 core_id), 다중 `:` → 모두 에러 경로 처리됨
- Gateway 폴백: core_id >= num_cores 시 전수 검색으로 안전하게 폴백
- stale core_id: 세션 종료 후 재접속 시 단일 코어에서 find_session 실패 → 조용히 드롭 (정상 동작)
- 롤링 배포 호환: 구버전 Auth가 쓴 레거시 값(`:` 없음)은 core_id=0 폴백

### Systems 관점

| # | 결과 | 항목 |
|---|------|------|
| S1 | OK | 스레드 안전성: deliver_to_core 람다 캡처 전부 값 복사 (shared_ptr 포함) |
| S2 | OK | Lifetime: Kafka 콜백 스코프 payload → shared_ptr 복사로 수명 보장 |
| S3 | OK | 변수 섀도잉: session_store.cpp의 파라미터 core_id vs 로컬 core_id → current_core로 리네임 완료 |
| S4 | OK | core_id validation 순서: push 경로에서 자체 범위 검증, request-response 경로에서 별도 검증 |

## 통계

- 리뷰 이슈: 1건 (MINOR 1)
- 수정: 1건
- 미수정: 0건
- 백로그 등록: 0건
