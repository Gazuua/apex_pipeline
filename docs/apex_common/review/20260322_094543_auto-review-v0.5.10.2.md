# Auto-Review v0.5.10.2 (Full, Docs 제외)

**날짜**: 2026-03-22
**범위**: 전체 코드베이스 (305파일, ~43K 라인) — 문서 리뷰어 제외
**리뷰어**: 5명 (logic, systems, test, design, infra-security)

## 요약

| 등급 | 발견 | 수정 | 미수정 |
|------|------|------|--------|
| CRITICAL | 0 | — | — |
| MAJOR | 7 | 3 | 4 (BACKLOG 등록) |
| MINOR | 16 | 1 | 15 (보고만) |

## 수정 완료 (코드 변경 3건 + 인프라 3건)

### [MAJOR] finalize_shutdown 인프라 코루틴 drain 누락 (systems)
- **파일**: `apex_core/src/server.cpp:426`
- **수정**: step 4.5에 `core_engine_->outstanding_infra_coroutines()` 합산 추가. KafkaDispatchBridge 등 spawn_tracked 코루틴이 adapter close 전에 완료되도록 보장. 이 수정이 없으면 payload_pool UAF 가능성 존재.

### [MAJOR] CI 워크플로우 timeout-minutes 미설정 (infra-security)
- **파일**: `.github/workflows/ci.yml`, `nightly.yml`, `docker-build.yml`
- **수정**: 모든 잡에 적절한 timeout-minutes 추가 (format: 5분, build: 30분, e2e: 15분, valgrind: 60분, docker: 45분)

### [MAJOR] CI 워크플로우 concurrency 미설정 (infra-security)
- **파일**: `.github/workflows/ci.yml`
- **수정**: `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }` 추가

### [MINOR] PerCoreState 소멸 순서 주석 불일치 (systems)
- **파일**: `apex_core/include/apex/core/server.hpp:70-82`
- **수정**: 실제 선언 순서와 일치하도록 주석 갱신, Server::~Server()와의 관계 설명 추가

### [MINOR] .dockerignore에 build.sh 누락 (infra-security)
- **파일**: `.dockerignore`
- **수정**: `!build.sh` 추가

## 미수정 → BACKLOG 등록 (MAJOR 4건)

### #128. AuthService::on_login locked_until 시간 비교 누락 (logic)
- **파일**: `apex_services/auth-svc/src/auth_service.cpp:162`
- **사유**: `locked_until`이 과거 시각이어도 영구 잠금 처리. SQL 쿼리 변경 + DB 마이그레이션 영향도 확인 필요

### #129. RedisMultiplexer reconnect_loop this 캡처 생존 보장 불완전 (systems)
- **파일**: `apex_shared/lib/adapters/redis/src/redis_multiplexer.cpp:36-48`
- **사유**: reconnect_loop 코루틴의 cancellation 설계 변경 필요

### #130. TlsTcpTransport::make_socket() static SSL context 멀티코어 unsafe (design)
- **파일**: `apex_shared/lib/protocols/tcp/include/apex/shared/protocols/tcp/tls_tcp_transport.hpp:33-38`
- **사유**: Transport concept 확장이 선행되어야 함

### #131. Kafka 통신 PLAINTEXT (infra-security)
- **사유**: v0.6 운영 인프라 스코프. KafkaAdapter에 security.protocol 설정 메커니즘 추가 필요

## 미수정 MINOR (보고만, 15건)

### logic (3건)
1. auth_exempt 엔드포인트에 Per-User/Per-Endpoint rate limit 미적용 (IP rate limit만) — 설계 판단 영역
2. PubSubListener Redis AUTH 실패 시 silent continue — SUBSCRIBE 실패 시 재연결으로 복구되나 진단 어려움
3. ResponseDispatcher whisper 유니캐스트 전달 경로 미연결 가능성 — 아키텍처 레벨 확인 필요

### systems (1건)
4. Session::state_ 비원자적이지만 cross-thread 소멸 경로 존재 — 성능 트레이드오프, shutdown 순서로 현재 안전

### test (5건)
5. CircuitBreakerTest run_coro 헬퍼가 detached로 예외 삼킴
6. CountingService static atomic 카운터 격리 패턴 — 주석 경고 존재
7. TC3 부재 증명 sleep 기반 — 본질적 한계
8. E2E ServiceRecoveryAfterTimeout 2초 하드코딩 — flaky 위험 요소
9. mock_kafka_adapter produced() lock 없이 벡터 참조 반환

### design (2건)
10. WebSocketProtocol try_decode max_message_size static 고정 — MVP, TODO 존재
11. apex_core에 deprecated tcp_binary_protocol.hpp 포워딩 헤더 잔존

### infra-security (3건)
12. Kafka PLAINTEXT — #131로 BACKLOG 등록
13. TcpAcceptor listen backlog 미지정 — ServerConfig 확장 필요
14. E2E config Redis 비밀번호 하드코딩 — 테스트 전용, 문제 없음
15. ci.Dockerfile ubuntu:24.04 태그 가변성 — 보안 패치 수신 트레이드오프

## 전체 평가

코드 품질이 전반적으로 높다. CRITICAL 이슈 0건. 가장 실질적인 수정은 `finalize_shutdown()`의 인프라 코루틴 drain 누락(payload_pool UAF 방지)이다. CI timeout/concurrency 추가로 운영 안정성도 개선됐다.
