# Auto-Review: 보안 시크릿 관리 + Blacklist 정책

- **브랜치**: `feature/security-secrets-hardening`
- **백로그**: #5, #6, #8, #100
- **리뷰어**: infra-security, logic, test
- **일시**: 2026-03-21 23:04:20

---

## 리뷰 결과 요약

| 리뷰어 | CRITICAL | MAJOR | MINOR | 수정 |
|--------|----------|-------|-------|------|
| infra-security | 2 | 3 | 3 | 전부 수정/등록 |
| logic | 0 | 0 | 0 | — |
| test | 1 | 2 | 1 | 전부 수정/등록 |

---

## Infra-Security 리뷰 상세

### [CRITICAL-1] Redis healthcheck requirepass 미호환 → 수정 완료
- **파일**: `docker-compose.yml`, `docker-compose.e2e.yml`
- **문제**: `redis-cli ping`이 requirepass 환경에서 `NOAUTH` 반환 → healthcheck 영구 unhealthy → 의존 컨테이너 기동 불가
- **해결**: 각 Redis 서비스에 `REDISCLI_AUTH` 환경 변수 추가. redis-cli가 자동으로 인증하여 healthcheck 정상 동작

### [CRITICAL-2] SQL injection 경로 (heredoc 환경 변수 확장) → 수정 완료
- **파일**: `001_create_schema_and_role.sh`
- **문제**: `${POSTGRES_AUTH_ROLE_PASSWORD}`가 SQL heredoc 내에서 직접 확장되어 single quote 포함 시 SQL injection 가능
- **해결**: single quote 포함 여부 검증 추가 (포함 시 에러 종료)

### [MAJOR-1] gateway_e2e_valgrind.toml Redis password 누락 → 수정 완료
- **파일**: `gateway_e2e_valgrind.toml`
- **해결**: 3개 Redis 섹션에 `password = "dev_redis_pass"` 추가

### [MAJOR-2] docker-compose.yml env_file 미설정 → 의도된 설계
- **판단**: 모든 변수에 `:-default` fallback이 있어 `.env` 파일 없이도 동작. 기능상 문제 없음. prod는 `.env.prod.example` 참조.

### [MAJOR-3] PgBouncer 2단계 인증 문서화 → 수정 완료
- **해결**: `.env.prod.example`에 PGBOUNCER_PASSWORD와 POSTGRES_PASSWORD의 관계 주석 추가

### [MINOR-1] init.sql auth_schema 고아 스키마 → BACKLOG-125 등록
### [MINOR-2] TOML expand_env 의존 → 정상 (설계 의도)
### [MINOR-3] Kafka PLAINTEXT → prod 시 SASL/TLS 필요 (v0.6 스코프)

---

## Logic 리뷰 상세

이슈 0건. 검증 완료:
- jwt_blacklist.cpp: `Result<bool>` ↔ `std::unexpected(AdapterError)` 타입 호환 정상
- gateway_pipeline.cpp: fail-open/fail-close 분기 설정 기반 동작 정상
- expand_env(): regex 패턴 정확, 엣지 케이스 처리 완전, thread-safe
- BlacklistCheckFailed 에러 경로: 클라이언트까지 정상 전파 확인
- blacklist_fail_open: hot-reload 범위 밖 (config reloader가 교체하지 않음) → 안전

---

## Test 리뷰 상세

### 직접 수정 (4건)
- `test_config_utils.cpp`: 엣지 케이스 테스트 4건 추가 (DollarWithoutBraces, EmptyVarName, DefaultValueEmpty, VarEmbeddedInText)

### 보고 → 수정 (2건)
- `test_gateway_pipeline.cpp`: `AuthExemptDefaults`에 `blacklist_fail_open` 기본값 검증 추가
- `test_config_reloader.cpp`: `kUpdatedToml`에 `blacklist_fail_open = true` 추가 + 파싱 검증 assertion 추가

### 보고 → BACKLOG 등록 (2건)
- BACKLOG-123: blacklist fail-open/fail-close 분기 단위 테스트 (코루틴+Redis mock 인프라 필요)
- BACKLOG-124: jwt_blacklist is_valid_jti 입력 검증 테스트
