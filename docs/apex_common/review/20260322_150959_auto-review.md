# Auto-Review — v0.5.10.5 FSD 백로그 소탕

**브랜치**: feature/fsd-backlog-20260322_145014
**일시**: 2026-03-22 15:09:59

---

## 리뷰 대상

| 항목 | 타입 | 변경 |
|------|------|------|
| BACKLOG-102+127 | test | GatewayPipeline template policy 리팩토링 + 에러 경로 테스트 16건 |

8 files changed, 713 insertions(+), 255 deletions(-)

---

## 인라인 리뷰

### 1. Template 파라미터화 정확성

- 원본 `GatewayPipeline` 클래스의 모든 메서드가 `GatewayPipelineBase<V,B,L>` template으로 정확히 이전됨
- `using GatewayPipeline = GatewayPipelineBase<JwtVerifier, JwtBlacklist, RateLimitFacade>` — 프로덕션 타입이 동일
- 기존 test_gateway_pipeline.cpp의 10개 테스트가 변경 없이 통과 → 동작 보존 확인

### 2. 헤더 분리 전략

- `gateway_pipeline.hpp` — template class만, 프로덕션 의존성 없음 (mock 테스트용)
- `gateway_pipeline_production.hpp` — concrete includes + using alias (프로덕션용)
- `gateway_service.hpp`와 `main.cpp`는 production 헤더 사용 → 올바름

### 3. 에러 테스트 품질

- MSVC 코루틴 제약: coroutine lambda 내 `ASSERT_*` (return 생성) 사용 불가 → 모두 `EXPECT_*`로 교체. 이는 MSVC C++23 코루틴 구현의 알려진 제약
- `io_context::run()` + `restart()` 패턴으로 코루틴 테스트 하네스 구현
- 16건의 테스트가 IP RL, JWT, blacklist, user/endpoint RL의 모든 에러 경로를 커버

### 4. 드롭: BACKLOG-132

구현 시도 중 UAF 위험 발견. 단순 shutdown 재배치(close→stop)하면 io_context가 살아있는 동안 detached 코루틴이 파괴된 multiplexer에 접근 가능. 현재 순서(stop→close)가 안전. cancellation 인프라 선행 필요.

---

## 빌드 검증

- 83/83 테스트 전체 통과 (신규 test_gateway_pipeline_errors 포함)
- Zero Warning (MSVC /W4 /WX)

## 최종 판정

이슈 0건. 머지 가능.
