# Auto-Review: v0.6.1 퀄리티 강화 (BACKLOG-185, 186, 173)

- **브랜치**: `feature/v0.6.1-quality-hardening`
- **PR**: #137
- **리뷰 일시**: 2026-03-24 15:51

## 변경 요약

| 백로그 | 제목 | 파일 수 |
|--------|------|---------|
| BACKLOG-186 | MetricsHttpServer read timeout (beast::tcp_stream + expires_after) | 2 |
| BACKLOG-185 | MetricsHttpServer in-flight session drain | 2 |
| BACKLOG-173.1 | SFINAE → requires절 전환 | 1 |
| BACKLOG-173.2 | RouteTable::Entry / RouteEntry 타입 통일 | 2 |
| BACKLOG-173.3 | ErrorCode enum 값 순서 재배치 | 1 |
| BACKLOG-173.4 | wire_services/register_metrics CRTP 통일 | 7 |

총 13파일, +135/-61

## 리뷰어 디스패치

| 리뷰어 | 도메인 | 결과 |
|--------|--------|------|
| systems | 메모리 + 동시성 | 5/5 PASS |
| design | API + 아키텍처 | 5/5 PASS |
| logic | 비즈니스 로직 | 7/7 PASS |

## 상세 결과

### systems 리뷰 (5건)

1. **MetricsSessionTracker lifetime** — shared_ptr 체인이 코루틴 프레임까지 연결. 서버 소멸 후에도 코루틴이 tracker를 안전하게 참조. **PASS**
2. **cancel 안전성** — socket().cancel()이 operation_aborted를 발생시키고 catch 블록에서 처리. control_io_ 단일 스레드에서 실행 확인. **PASS**
3. **control_io_ single-threaded 보장** — do_accept, handle_session, stop 모두 control_io_ 스레드에서 실행. mutex 불필요 판단 올바름. **PASS**
4. **beast::tcp_stream move + SessionGuard raw ptr** — 코루틴 프레임 내 파라미터 주소 안정성 확인. SessionGuard 소멸 순서(guard → stream)도 올바름. **PASS**
5. **CRTP dispatch 기본 no-op** — name lookup이 Derived → AdapterBase 순으로 탐색. protected 기본 구현이 올바르게 호출됨. **PASS**

### design 리뷰 (5건)

1. **CRTP 일관성** — do_ prefix 패턴이 모든 dispatch 메서드에 일관 적용. **PASS**
2. **requires절 시맨틱** — 기존 SFINAE와 동일한 제약. 가독성 개선. **PASS**
3. **RouteTable 타입 통일** — Entry 완전 제거, RouteEntry 단일 타입. 외부 참조 없음 확인. **PASS**
4. **AdapterWrapper 호출 체인** — 이중 dispatch 없음. adapter_.wire_services() → CRTP dispatch → do_wire_services() 정상 체인. **PASS**
5. **do_close_per_core 패턴** — 기존부터 CRTP dispatch (name hiding 아님). 일관성 확인. **PASS**

**수정**: AdapterBase Doxygen 주석에 `do_wire_services`/`do_register_metrics`를 선택적 override 목록에 추가.

### logic 리뷰 (7건)

1. **ErrorCode 값 보존** — uint16_t 값 동일, 선언 순서만 변경. **PASS**
2. **ErrorCode switch case 완전성** — 전체 24개 case(22 named + 2 adapter + default) 빠짐 없음. **PASS**
3. **route_table.cpp validation→move→sort 순서** — 기존 대비 의미적으로 동일하되 validation과 데이터 이동이 분리되어 더 안전. **PASS**
4. **route_table.cpp Entry→RouteEntry 의미 동치** — 필드 동일, 외부 참조 없음. **PASS**
5. **handle_session 4개 엔드포인트 동작** — /metrics, /health, /ready, 404 모두 기존과 동일. **PASS**
6. **handle_session tcp_stream 전환** — stream.socket().shutdown() 패턴 정확. **PASS**
7. **ErrorCode wire format 의존성** — FlatBuffers는 uint16 직렬화, 값 미변경이므로 호환성 유지. **PASS**

## 발견 이슈

**수정 필요 이슈: 0건**

**참고 사항 (수정 불필요):**
- `AdapterBase::close()` spin-wait busy loop (adapter_base.hpp L107-116) — control_io_ 블로킹 가능성. shutdown 경로 전용이므로 긴급하지 않음. 기존 코드, 이번 변경과 무관.
