# Auto-Review — Batch B 코어/공유 단위 테스트 소탕

- **브랜치**: `feature/backlog-67-101-13-14-16-core-shared-unit-tests`
- **백로그**: BACKLOG-67, BACKLOG-101, BACKLOG-13, BACKLOG-14, BACKLOG-16
- **일시**: 2026-03-21 18:15:55

---

## 리뷰 구성

| 리뷰어 | 도메인 | 결과 |
|--------|--------|------|
| test | 테스트 품질, 커버리지, TC 설계 | Fix 5건, Info 4건 |
| design | 코드 설계, 미사용 멤버, 안전성 | Fix 3건 (MAJOR 2, MINOR 1) |
| docs-records | 문서/기록 정합성 | Fix 2건 |

---

## test 리뷰어 — Fix 5건

### T-1. run_coro 헬퍼 예외 안전성 (Fix)
- **파일**: 테스트 유틸리티 `run_coro()` 헬퍼
- **내용**: `io.run()` 도중 테스트 코루틴이 예외를 던지면 io_context가 멈추지 않고 무한 대기할 수 있음. `io.run()` 래핑에 try-catch + `io.stop()` 호출 추가.
- **수정**: run_coro에 예외 전파 안전 래퍼 적용

### T-2. 신규 TC 5건 추가 (Fix)
- **파일**: 각 테스트 파일
- **내용**: 리뷰어가 커버리지 갭을 식별하여 엣지 케이스 TC 추가 권고. ConsumerPayloadPool 이중 release, wire_services 빈 레지스트리, ErrorSender 0값 코드, Listener 재시작 시나리오, WebSocket 불완전 프레임.
- **수정**: 식별된 엣지 케이스 TC 추가 구현

### T-3. Assertion 정밀도 향상 (Fix)
- **파일**: `test_pg_transaction.cpp`
- **내용**: PgTransaction 테스트에서 `EXPECT_TRUE(result.has_error())` 같은 불투명 단언 사용. 실패 시 어떤 에러인지 디버깅 정보 부족.
- **수정**: `EXPECT_EQ(result.error(), PgError::TransactionFailed)` 등 구체적 에러 값 비교로 교체

### T-4. TC 네이밍 일관성 (Fix)
- **파일**: 전체 신규 테스트
- **내용**: 일부 TC가 `Test_XXX` 형식, 일부가 `XXX_YYY` 형식으로 불일치. 기존 코드베이스의 `PascalCase` 패턴과 혼재.
- **수정**: 전체 신규 TC를 기존 프로젝트 컨벤션(PascalCase)으로 통일

### T-5. ConsumerPayloadPool 빈 payload 엣지 케이스 (Fix)
- **파일**: `test_consumer_payload_pool.cpp`
- **내용**: payload 크기 0인 acquire 호출 시 동작 미검증. 빈 메시지가 Kafka에서 도착하는 실제 시나리오 존재.
- **수정**: 크기 0 payload acquire/release TC 추가

---

## test 리뷰어 — Informational 4건

### T-I1. MockPgConn 확장성 (Info)
- **내용**: MockPgConn이 현재 PgTransaction 테스트에 최적화되어 있음. 향후 PgPool 테스트 추가 시 prepared statement mock 확장 필요. 현재 스코프에서는 충분.

### T-I2. Listener 테스트 포트 충돌 가능성 (Info)
- **내용**: 테스트가 고정 포트 번호를 사용. CI 병렬 실행 시 포트 충돌 가능. 현재 CTest가 순차 실행이므로 문제 없음.

### T-I3. WebSocket 대용량 프레임 테스트 미포함 (Info)
- **내용**: 64KB 이상 extended payload length 프레임 테스트 미포함. 현재 프로덕션에서 대용량 프레임 미사용이므로 우선순위 낮음.

### T-I4. wire_services 서비스 순서 의존성 (Info)
- **내용**: wire_services가 반환하는 서비스 목록의 순서가 레지스트리 내부 구현에 의존. 현재 순서 무관 설계이므로 문제 없음.

---

## design 리뷰어 — Fix 3건

### D-1. PgTransaction pool_ 미사용 멤버 제거 (MAJOR → 수정)
- **파일**: `pg_transaction.hpp`
- **내용**: `pool_` 멤버가 선언만 되고 어디에서도 사용되지 않음. 생성자에서 인자로 받아 저장하지만 접근하는 코드 없음. 불필요한 의존성 + 메모리 낭비 + 리더 혼동 유발.
- **수정**: `pool_` 멤버 제거, 생성자 시그니처에서 pool 파라미터 제거

### D-2. PgTransaction finished_ 가드 추가 (MAJOR → 수정)
- **파일**: `pg_transaction.hpp`
- **내용**: `commit()`/`rollback()` 이후 재호출 시 이미 완료된 트랜잭션에 대해 중복 SQL 전송. `begun_` 체크는 있으나 완료 상태 체크 없음. 이중 commit은 PostgreSQL 에러를 유발하고, 이중 rollback은 silent 실패.
- **수정**: `finished_` bool 멤버 추가, commit/rollback 진입 시 finished_ 체크 + early return

### D-3. pg_pool.hpp 불필요 include 제거 (MINOR → 수정)
- **파일**: `pg_transaction.hpp`
- **내용**: pool_ 멤버 제거 후 `pg_pool.hpp` include가 불필요해짐. 불필요 헤더 include는 빌드 시간 증가 + 순환 의존 위험.
- **수정**: `#include "pg_pool.hpp"` 제거

---

## docs-records 리뷰어 — Fix 2건

### R-1. 설계 문서 파일 카운트 오류 (Fix)
- **파일**: `docs/apex_common/plans/` 내 설계 문서
- **내용**: 설계 문서에 기재된 변경 파일 수가 실제 변경 파일 수와 불일치. "5 new, 3 modified" → 실제 "7 new, 5 modified".
- **수정**: 정확한 파일 카운트로 수정

### R-2. 설계 문서 작성일 누락 (Fix)
- **파일**: `docs/apex_common/plans/` 내 설계 문서
- **내용**: 설계 문서 헤더에 작성일 필드 누락.
- **수정**: 작성일 추가

---

## 최종 상태

- **수정 적용**: Fix 10건 모두 수정 완료 (test 5 + design 3 + docs-records 2)
- **Informational**: 4건 (조치 불필요)
- **테스트**: 79/79 통과
- **잔여 이슈**: 0건
