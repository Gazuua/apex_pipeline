# Auto-Review: CI 파이프라인 확장 (v0.5.8.0)

**브랜치**: `feature/7-9-ci-pipeline`
**작성일**: 2026-03-20 04:12:34
**리뷰 대상**: BACKLOG-7, BACKLOG-9 구현 (25개 파일, +3093/-393)

---

## 리뷰어 구성

| 리뷰어 | 도메인 | 담당 파일 수 |
|--------|--------|-------------|
| infra-security | CI/Docker/CMake | 9 |
| test | E2E 테스트 코드 | 7 |
| docs-records | 문서 정합성 | 9 |

---

## 발견 이슈 통계

| 등급 | 수정 완료 | 보고 (수정 불필요/불가) |
|------|----------|----------------------|
| CRITICAL | 4 | 1 (BACKLOG 후보) |
| MAJOR | 6 | 1 (BACKLOG 후보) |
| MINOR | 6 | 5 |
| INFO | 0 | 3 |
| **합계** | **16** | **10** |

---

## 수정 완료 항목

### CRITICAL (4건)

**1. nightly.yml 아티팩트 경로 전면 오류** (infra-security)
- `build/Linux/debug/bin/` 경로가 존재하지 않음 (Ninja 빌드는 타겟별 binary dir에 출력)
- 실제 경로 패턴(`build/Linux/debug/apex_services/...`)으로 전면 수정

**2. nightly.yml valgrind-e2e Docker 네트워크 이슈** (infra-security)
- Docker 컨테이너 내 서비스가 TOML의 `localhost` 주소로 다른 컨테이너에 접근 불가
- CI E2E와 동일하게 호스트 직접 실행 구조로 변경, Valgrind은 호스트에서 래핑

**3. Dockerfile COPY 경로 불일치** (infra-security)
- `PREBUILT_BIN` fallback 경로와 실제 빌드 출력 경로 불일치 + `.dockerignore` 차단
- builder stage에서 `/out/`으로 복사 후 `COPY --from=builder /out/...`으로 전달

**4. e2e_stress_protocol.cpp 포트 오류** (test)
- `connect_raw()`가 WebSocket 포트(8444)에 연결 → TCP 포트(8443)로 수정
- WireHeader v2 바이너리 프로토콜 테스트가 잘못된 포트로 가면 의미 없는 테스트

### MAJOR (6건)

**5. CMakePresets.json CMAKE_C_FLAGS 누락** (infra-security)
- asan/tsan/ubsan preset에 C++ 플래그만 설정, C 소스(`apex_bcrypt`)가 sanitizer 미적용
- 3개 preset 모두에 `CMAKE_C_FLAGS` 추가

**6. ci.yml E2E sleep→healthcheck** (infra-security)
- `sleep 20` 고정 대기 → `timeout 60 bash -c 'until nc -z ...'` + sleep 5

**7-8. 스트레스 테스트 환경변수 기본값 + get_env_int 중앙화** (test)
- 4개 파일에 복사된 `get_env_int()` → fixture 헤더로 중앙화
- 기본값 drift 방지 (50→10, 100→20)

**9. MaxSizeMessage 테스트 assertion 부재** (test)
- 서버 거부 검증 없이 전송만 수행 → `EXPECT_TRUE` 검증 추가

**10. ConcurrentLogin SCOPED_TRACE thread-safety** (test)
- GTest `SCOPED_TRACE`는 thread-safe하지 않음 → 제거

### MINOR (6건)

**11-13.** 문서 수정 3건: BACKLOG 연관 참조 형식, progress cron 시각, 설계 문서 작성일 정밀화
**14.** DisconnectDuringResponse 검증 강화 (connect→login)
**15.** `using namespace wire_flags` 위치 이동 (가독성)
**16.** E2E CMakeLists TIMEOUT 추가 (120초/300초)

---

## 보고 항목 (수정 불필요/불가)

### BACKLOG 후보

**B1. .dockerignore 서비스 Dockerfile 충돌** — `*` 전체 차단으로 서비스 Dockerfile의 `COPY . /src`가 동작 불가. 서비스 독립 빌드(`docker build`) 시 .dockerignore 확장 필요.

**B2. TOML localhost 하드코딩** — Docker 컨테이너 내 서비스 실행 시 인프라 접근 불가. `expand_env()` 패턴으로 환경변수 주입 지원 필요.

### 기타 보고

- warm-up 실패 시 경고만 출력 — 의도적 설계, 수정 불필요
- `std::atoi()` 변환 실패 시 0 — E2E 환경에서 리스크 낮음
- UBSAN 더미 스토리지 strict aliasing — 테스트에서 미접근, 실용적 안전
- Redis AUTH 미설정 — E2E 전용, 운영 환경 별도 설정
- Docker 이미지 rolling tag — ci.Dockerfile과 일관, 현행 유지
