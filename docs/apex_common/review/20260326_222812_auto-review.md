# Auto-Review — 보안 소탕 (BACKLOG-247,248,249,253)

- **PR**: #207
- **모드**: task (diff 기준)
- **리뷰어**: 메인 직접 (변경 범위 집중적)

## 리뷰 결과: 이슈 0건

### 검토 항목

| 영역 | 파일 | 판정 | 근거 |
|------|------|------|------|
| Kafka manual commit | kafka_consumer.cpp | OK | async commit(non-blocking) + DLQ 3회 재시도 + 실패 시 미커밋. at-least-once 보장 |
| DLQ retry | kafka_consumer.cpp | OK | 동기 재시도 루프는 실패 경로(cold path)에서만 실행, 성능 영향 미미 |
| Auth exempt rate limit | gateway_pipeline.hpp | OK | FNV-1a IP hash → uint64_t 키. 2^64 공간에서 충돌 확률 무시 가능 |
| System message auth | gateway_service.cpp | OK | auth_states_ 조회 + pipeline_ null 가드. AUTHENTICATE_SESSION은 정당하게 면제 |
| NetworkPolicy | 4× networkpolicy.yaml | OK | Deny-All + 서비스별 최소 allow. DNS egress(UDP+TCP 53) 포함 |
| NetworkPolicy prod | values-prod.yaml | OK | gateway/auth-svc/chat-svc enabled=true, log-svc는 서비스 자체가 disabled |

### 부가 관찰 (수정 불필요)

1. **Prometheus 메트릭 ingress 소스 미제한**: NetworkPolicy에서 메트릭 포트(8081-8084)는 소스 무제한. 프로덕션에서 Prometheus namespace로 제한 가능하나 현재는 모니터링 스택 미구성이므로 OK.
2. **per-IP 연결 제한 미구현**: BACKLOG-256으로 등록 완료. 프레임워크(Listener/ConnectionHandler) 레벨 변경 필요.
