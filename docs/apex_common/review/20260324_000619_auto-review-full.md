# Full Codebase Auto-Review — 2026-03-24

**범위**: 전체 코드베이스 (314 C++ 소스 파일 + CMake/CI/Docker/문서)
**브랜치**: `review/auto-review-20260323_235120`
**리뷰어**: 6명 병렬 디스패치 (systems, logic, design, test, infra-security, docs-spec)

---

## 통계

| 리뷰어 | 수정 | 보고 | CRITICAL | MAJOR | MINOR | INFO |
|--------|------|------|----------|-------|-------|------|
| systems | 0 | 8 | 0 | 0 | 5 | 3 |
| logic | 0 | 7 | 0 | 1 | 5 | 1 |
| design | 0 | 14 | 0 | 1 | 9 | 4 |
| test | 0 | 5+커버리지 | 0 | 0 | 3 | 2 |
| infra | 0 | 10 | 0 | 0 | 4 | 6 |
| docs | 0 | 9 | 1 | 4 | 4 | 0 |
| **합계** | **0** | **53** | **1** | **6** | **30** | **16** |

---

## 직접 수정한 이슈 (6건)

### F1. [CRITICAL] README.md 버전 미갱신
- **원인**: v0.5.10.7 머지 시 README.md 갱신 누락
- **수정**: `## 현재 상태 — v0.5.10.6` → `v0.5.10.7`, v0.5.10.7 + BACKLOG-163 + BACKLOG-164 + PR #125 완료 항목 추가

### F2. [MAJOR] apex_core_guide.md 버전 + ServerConfig 필드 누락
- **원인**: v0.5.10.7 코어 변경 시 가이드 갱신 누락
- **수정**: 버전 v0.5.10.6 → v0.5.10.7, ServerConfig에 `bind_address`, `max_connections` 필드 추가

### F3. [MAJOR] Hook 개수 불일치 (문서 전체)
- **원인**: validate-backlog hook 추가 이후 관련 문서 일괄 갱신 누락
- **수정**: CLAUDE.md "hook 7개" → "6종 hook", README.md "5 강제 게이트" → "6 강제 게이트", Apex_Pipeline.md "5개 hook 게이트" → "6종 hook 게이트" + validate-backlog 추가

### F4. [MAJOR] Apex_Pipeline.md 도구 이력 미반영
- **원인**: BACKLOG-163, BACKLOG-164, PR #125 완료 후 로드맵 트리 갱신 누락
- **수정**: 활성 로드맵 트리에 3개 항목 추가

### F5. [MAJOR] deprecated 역방향 의존 헤더 삭제
- **파일**: `apex_core/include/apex/core/tcp_binary_protocol.hpp`
- **원인**: apex_core → apex_shared 역방향 include. C++ 소스에서 미사용 확인
- **수정**: 파일 삭제

### F6. [MINOR] CLAUDE.md PR/백로그 번호 혼용
- **수정**: `(#126)` → `(PR #126)`, `(#163)` → `(BACKLOG-163)`, `(#164)` → `(BACKLOG-164)`

---

## 백로그 등록 이슈 (3건)

### BACKLOG-171: Gateway 내부 컴포넌트 단위 테스트 추가 [MAJOR/TEST]
- broadcast_fanout, pubsub_listener, response_dispatcher, gateway_service, gateway_config_parser
- E2E 테스트에만 의존 중, 단위 테스트 부재

### BACKLOG-172: deprecated 포워딩 헤더 참조 문서 정리 [MINOR/DOCS]
- 삭제된 헤더를 참조하는 설계 문서 9건 (히스토리 문서이므로 낮은 우선순위)

### BACKLOG-173: Design 개선 모음 [MINOR/DESIGN_DEBT]
- SFINAE→requires절, RouteTable 타입 통일, ErrorCode 선언 순서, AdapterBase CRTP 통일

---

## 리뷰어별 상세

### Systems (메모리 + 동시성)
**양호**: Session lifetime(intrusive_ptr + core_executor_ 분리), write_pump implicit strand, MpscQueue/SpscQueue memory ordering, TimingWheel tick 3단계 분리, Server 소멸자 파괴 순서, cross_core_call 3-state CAS 프로토콜, SharedPayload refcount 관리

**보고 MINOR**: LegacyCrossCoreFn raw pointer 패턴 3곳 중복, TimingWheel per-schedule 힙 할당, Server shutdown 폴링, ArenaAllocator malloc 정렬 제한

### Logic (비즈니스 로직)
**양호**: FlatBuffers co_await 전 복사 준수, PG/Redis/Kafka 에러 처리 완전성, Gateway MSA 독립성, Refresh Token Reuse Detection, TOCTOU 방지(Redis Lua), PendingRequests 타임아웃, CircuitBreaker 상태 전이, Redis Key Injection 방어

**보고 MAJOR**: ChatDbConsumer stub (DB persist 미구현, placeholder 상태) — 이미 알려진 E2E phase 작업
**보고 MINOR**: SessionStore/AuthService int 변환 클램프 부재, pubsub_listener null 체크 누락, response_dispatcher N코어 브로드캐스트 최적화

### Design (API + 아키텍처)
**양호**: Concept 설계(Protocol/Transport/CoreAllocator/PoolLike), 의존성 방향 준수, Gateway 독립성, 강타입(enum class), [[nodiscard]] 일관성, CRTP + AdapterWrapper 타입 소거, 계층별 에러 타입 분리, const correctness

**보고 MAJOR**: deprecated 역방향 의존 헤더 (→ F5로 수정)
**보고 MINOR**: FrameError/ErrorCode 에러 타입 중복, AdapterKeyHash 약한 결합, SFINAE→requires 현대화, CircuitBreaker [[nodiscard]] 중복 등

### Test (테스트 품질)
**양호**: 테스트 헬퍼 통합(run_coro, timeout_multiplier), 격리성(독립 io_context), assertion 적절성, 에러 경로 커버리지, 엣지케이스(경계값), 동시성 테스트(4-producer FIFO), Mock 설계, 어댑터 라이프사이클 일관성, CTest 타임아웃

**커버리지 갭 MAJOR**: Gateway 5개 내부 컴포넌트 (→ BACKLOG-171), Server adapter init 실패 복구 경로
**보고 MINOR**: sleep 기반 부재 증명, 로컬 run_coro 재정의, TSAN 빌드 CTest TIMEOUT 미적용

### Infra/Security (CMake + CI + Docker + 보안)
**양호**: apex_set_warnings() 전 타겟 적용, 명시적 소스 나열(GLOB 미사용), vcpkg 3매니페스트 일관성, CI 최소 권한(permissions: {}), 4잡 매트릭스+E2E+Go, 서비스 non-root 유저, 멀티스테이지 빌드, SQL 파라미터 바인딩, WireHeader 크기 제한, TSAN suppressions

**보고 MINOR**: Kafka PLAINTEXT(개발용), .env.dev 평문 비밀번호, Grafana admin 기본값
**보고 INFO**: max_connections 무제한 기본값, bind_address 0.0.0.0 기본값, CI 이미지 vcpkg shallow clone 최적화

### Docs/Spec (문서 정합성)
**양호**: CLAUDE.md ↔ Apex_Pipeline.md 버전 체계 일치, 모노레포 구조 일치, CLAUDE.md 포인터 6개 전체 유효, ConfigureContext/WireContext/Phase 순서 문서-코드 일치, v0.6→v1.0.0.0 로드맵 3곳 일관

**수정**: README 버전(F1), 가이드 버전+필드(F2), hook 개수(F3), 도구 이력(F4), 번호 형식(F6) — 전부 이번 리뷰에서 직접 수정

---

## 종합 평가

전체 코드베이스가 매우 견고한 상태. CRITICAL 이슈는 문서 버전 미갱신 1건뿐이고, 코드 자체의 CRITICAL/MAJOR 버그는 0건. 메모리 관리, 동시성, 비즈니스 로직, 인프라/보안 모두 높은 수준.

주요 개선 영역:
1. **Gateway 단위 테스트 커버리지** — 가장 큰 기술적 갭 (BACKLOG-171)
2. **문서 갱신 프로세스** — 버전 업 시 모든 문서 동시 갱신 체크리스트 필요
3. **프로덕션 보안 강화** — v0.6 운영 인프라 단계에서 해결 예정 항목
