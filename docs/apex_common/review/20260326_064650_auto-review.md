# Auto-Review — BACKLOG-218 + BACKLOG-172

- **날짜**: 2026-03-26
- **PR**: #177
- **브랜치**: feature/backlog-218-172-metrics-test-docs-cleanup
- **모드**: task (변경 파일만)
- **리뷰어**: 메인 직접 (소규모 변경)

## 변경 범위

| 파일 | 변경 | 내용 |
|------|------|------|
| `apex_core/tests/unit/test_metrics_http_server.cpp` | +53 | InFlightSessionCancelledOnStop 테스트 추가 |
| `docs/apex_common/plans/` 5건 | 경로 치환 | deprecated 포워딩 헤더 참조를 신경로로 갱신 |
| `docs/apex_common/progress/` 1건 | 문맥 추가 | backlog-116-117 progress에 "이후 삭제됨" 보충 |

## 리뷰 항목

### 1. [PASS] InFlightSessionCancelledOnStop 테스트 설계

- **slow client 패턴**: TCP 연결만 맺고 HTTP 요청 미전송 → handle_session의 async_read가 대기 상태 진입
- **cancel 경로**: stop() → cancel_all() → socket::cancel() → operation_aborted → SessionGuard 소멸
- **검증**: 클라이언트 read_some에서 EOF/에러 관찰로 세션 종료 확인
- **크로스플랫폼**: `ec || n == 0` 조건으로 Windows(EOF)/Linux(connection_reset) 양쪽 커버

### 2. [PASS] sleep_for 타이밍 안전성

- 50ms (accept→async_read 진입) + 100ms (cancel 처리 drain) = 150ms 총 대기
- server_thread가 io_ctx_.run()을 구동 중이므로 cancel 핸들러는 즉시 처리됨
- 150ms 마진은 CI 환경에서도 충분 (실제 처리 시간 < 1ms)
- read_some 블로킹 최악 케이스: 서버의 `expires_after(5s)` 타임아웃이 상한선 — 무한 행 불가

### 3. [PASS] TearDown 이중 호출 안전성

- stop() 내부에서 `acceptor_ && acceptor_->is_open()` 체크 후 close → 재호출 시 no-op
- 테스트에서 stop() 호출 후 TearDown에서 재호출해도 안전

### 4. [PASS] 문서 경로 치환 정합성

- 활성 설계 문서 5건: `apex/core/tcp_binary_protocol.hpp` → `apex/shared/protocols/tcp/tcp_binary_protocol.hpp`
- 히스토리 문서 3건: 미수정 (원칙 준수)
- 구경로 잔존 grep 결과: 수정된 문서 내에서만 문맥설명으로 남아있음 (의도적)

## 이슈 요약

| 심각도 | 건수 |
|--------|------|
| CRITICAL | 0 |
| MAJOR | 0 |
| MINOR | 0 |
| NOTE | 0 |

**결론**: 수정 불필요. 코드+문서 모두 적절.
