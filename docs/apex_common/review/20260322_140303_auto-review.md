# Auto-Review — v0.5.10.4 FSD 백로그 소탕

**브랜치**: feature/fsd-backlog-20260322_134217
**일시**: 2026-03-22 14:03:03
**리뷰어**: design, logic, test (3명 병렬 디스패치)

---

## 리뷰 대상

| 항목 | 타입 | 변경 |
|------|------|------|
| BACKLOG-134 | design-debt | AdapterState enum → apex_core 이동, AdapterInterface::state() 추가 |
| BACKLOG-19 | test | Auth/Chat 비즈니스 로직 순수 함수 추출 + 단위 테스트 24건 |

14 files changed, 439 insertions(+), 37 deletions(-)

---

## 리뷰 결과

### Design (reviewer-design) — 이슈 0건

1. AdapterState core 이동이 모듈 경계 원칙(shared→core 단방향 의존)에 부합
2. `using` alias로 shared 코드 호환성 유지 — 한 파일 내 사용이므로 전염 위험 없음
3. 순수 함수 헤더 네이밍(`{domain}_logic.hpp`)이 기존 패턴과 일관
4. chat_logic.hpp constexpr 적용 적절 (순수 정수 비교, 컴파일 타임 평가 가능)
5. AdapterInterface에 state() 추가가 기존 is_ready() 상위 집합으로 자연스러운 확장

### Logic (reviewer-logic) — 이슈 0건

1. is_account_locked: 원본 인라인 코드와 추출 함수의 시간 비교 로직 정확히 일치
2. validate_message_content: `content ? content->size() : 0` null 처리가 원본 `!content || content->size() == 0`과 의미론적 동치
3. interpret_join_result: Lua 반환값 -1/0/양수 매핑이 원본과 일치
4. 관찰: on_global_broadcast에 max_length 검증 미적용 (기존 동작, 이번 PR 범위 아님)

### Test (reviewer-test) — 이슈 4건 (전부 수정 완료)

| # | 수정 | 내용 |
|---|------|------|
| 1 | test_auth_logic.cpp | EmptyString_NotLocked 엣지 케이스 추가 |
| 2 | test_chat_logic.cpp | OneCharName 경계값 테스트 추가 |
| 3 | test_chat_logic.cpp | UnexpectedNegativeValue_TreatedAsJoined (-2, -100) 추가 |
| 4 | test_chat_logic.cpp | ZeroMaxLength 경계값 테스트 추가 (message + room_name) |

---

## 빌드 검증

- 리뷰 수정 후 재빌드: **82/82 테스트 전체 통과**

---

## 최종 판정

리뷰 이슈 0건 (테스트 보강 4건은 수정 완료). 머지 가능.
