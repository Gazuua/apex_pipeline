# Auto-Review: 전체 코드베이스 (apex-agent Go 제외)

- **일시**: 2026-03-23 01:24:59
- **모드**: full (제외: 7be74bda — apex-agent Go 백엔드)
- **브랜치**: review/auto-review-20260323_010503
- **리뷰어**: systems, logic, design, test, infra-security, docs-spec (6명)

## 통계

| 구분 | 건수 |
|------|------|
| 발견 이슈 총계 | 55건 |
| 직접 수정 | 21건 (서브에이전트 19 + 메인 2) |
| 백로그 등록 | 5건 (#136 복원, #146-#149 신규) |
| HISTORY 이동 | 1건 (#138) |
| 보고만 (기존 백로그 포함/설계 결정) | 28건 |

## 수정 항목 상세

### Critical (1건)

| ID | 파일 | 내용 |
|----|------|------|
| L1 | `gateway/src/response_dispatcher.cpp` | 서버 push 메시지(corr_id=0) silent drop 수정 — 모든 코어 순회하여 session_id로 대상 세션 탐색 후 전달. Whisper 메시지 배달 장애 해소 |

### High (4건)

| ID | 파일 | 내용 |
|----|------|------|
| L2 | `chat-svc/src/chat_service.cpp` | on_leave_room: Redis SISMEMBER 에러를 NOT_IN_ROOM으로 오보 → 에러 분리 |
| L3 | `chat-svc/src/chat_service.cpp` | on_send_message: 동일 패턴 수정 |
| L4 | `chat-svc/src/chat_service.cpp` | on_chat_history: 동일 패턴 수정 |
| DS1 | `docs/Apex_Pipeline.md` | EchoService 예제 msg_id 타입 uint16_t→uint32_t |

### Medium (11건)

| ID | 파일 | 내용 |
|----|------|------|
| L5 | `gateway/src/pubsub_listener.cpp` | start() 이중 호출 시 std::terminate 방어 |
| S5 | `gateway/src/gateway_service.cpp` | FlatBuffers magic number 4 → kPrimaryStringField named constant |
| S6 | `shared/adapters/adapter_base.hpp` | AdapterBase::init() 이중 호출 방어 guard |
| D5 | `shared/adapters/adapter_base.hpp` | wire_services() CRTP 비적용 사유 주석 추가 |
| D7 | `shared/adapters/circuit_breaker.hpp` | CircuitCallable concept에 has_value()/error() 타입 제약 추가 |
| T1 | `tests/mocks/mock_kafka_adapter.hpp` | fail_produce_ bool→atomic<bool> (TSAN) |
| T2 | `tests/mocks/mock_pg_pool.hpp` | fail_queries_ bool→atomic<bool> (TSAN) |
| T3 | `tests/mocks/mock_redis_multiplexer.hpp` | fail_commands_ bool→atomic<bool> (TSAN) |
| I1 | `apex_infra/docker-compose.yml`, `docker-compose.e2e.yml` | redis-pubsub maxmemory-policy allkeys-lru 추가 |
| DS3 | `docs/Apex_Pipeline.md` | Graceful Shutdown 시퀀스 부정확 수정 |
| DS4 | `docs/BACKLOG_HISTORY.md` | apex-agent LOC 수치 19,000→14,000+ 통일 |

### Low (5건)

| ID | 파일 | 내용 |
|----|------|------|
| S1 | `shared/adapters/redis/redis_multiplexer.cpp` | pipeline() FreeGuard RAII 무력화 정리 |
| L6 | `gateway/src/broadcast_fanout.cpp` | substr→starts_with 개선 |
| I2 | `shared/protocols/kafka/CMakeLists.txt` | target_compile_features cxx_std_23 추가 |
| DS2 | `docs/Apex_Pipeline.md` | WebSocket 버전 주석 업데이트 |
| DS5 | `docs/BACKLOG.md` | 연관 항목 (HISTORY) 표기 누락 4건 수정 |

### 문서 수정 (메인 직접)

| ID | 파일 | 내용 |
|----|------|------|
| D1 | `docs/apex_core/apex_core_guide.md` | build_frame() 존재하지 않는 API → async_send() 올바른 패턴으로 교정 |

## 백로그 변동

### 신규 등록 (4건)
- **#146** (MAJOR, AUTH_SVC, PERF): bcrypt 동기 블로킹 — thread pool offload 필요
- **#147** (MAJOR, INFRA, SECURITY): Docker 이미지에 테스트용 RSA 키 번들링
- **#148** (MAJOR, CORE/SHARED, TEST): 테스트 커버리지 갭 9건 일괄
- **#149** (MINOR, CHAT_SVC/GATEWAY, DESIGN_DEBT): Whisper core_id=0 하드코딩

### 복원 (1건)
- **#136** (MAJOR, SHARED, DESIGN_DEBT): HiredisAsioAdapter [this] sentinel 패턴 전환

### HISTORY 이동 (1건)
- **#138** (MAJOR, GATEWAY, DESIGN_DEBT): ResponseDispatcher push 경로 부재 → L1 수정으로 FIXED

## 보고만 된 주요 이슈 (수정 불필요 또는 설계 결정)

- **S2**: HiredisAsioAdapter dangling — 현재 short-circuit으로 안전, #136으로 추적
- **L9**: bcrypt 코어 블로킹 — #146으로 추적
- **L11**: ChatDbConsumer placeholder — 의도적 미구현 (Plan 5)
- **D2**: FrameCodec/Protocol 에러 타입 이원화 — 코어 API 변경 필요
- **I11**: Docker 테스트 키 — #147으로 추적
- **I5/I23**: Kafka PLAINTEXT/무인증 — 개발 환경 전용, 프로덕션은 KafkaSecurityConfig 활용

## 코드베이스 양호 사항

- **메모리/동시성**: intrusive_ptr Session 수명 관리, per-core shared-nothing, write_pump implicit strand 모두 올바름
- **보안**: TLS 1.3 기본, RS256 JWT only, 파라미터화 쿼리, FlatBuffers Verifier, 16MB body 제한
- **설계**: 의존성 방향 완벽 (core→shared→services), Gateway MSA 독립성 준수, 강타입 SessionId/ErrorCode
- **테스트**: 84개 테스트 전체 PASS, timeout_multiplier TSAN/ASAN 고려, 에러 경로 커버리지 두터움
- **인프라**: CI 최소 권한, Docker 비root 실행, 멀티스테이지 빌드, Redis 인증 적용
