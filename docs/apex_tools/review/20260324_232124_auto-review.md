# Auto-Review: 큐 히스토리 이벤트 로그 + 백로그 updated_at 버그 수정

- **브랜치**: feature/queue-history-backlog-fix
- **PR**: #145
- **리뷰어**: logic, test, design (3명 병렬)
- **리뷰 일시**: 2026-03-24

## 리뷰 결과 요약

| 리뷰어 | 이슈 수 | 수정 | 판정 |
|--------|---------|------|------|
| logic | 1 + 5 참고 | 1건 수정 | PASS |
| design | 1 | 1건 수정 (logic과 동일) | PASS |
| test | 3 | 3건 테스트 추가 | PASS |

## 수정 내역 (4건)

### 1. offset/limit 파라미터 클램핑 (logic)
- **파일**: `internal/httpd/handler_queue.go`
- **이슈**: `handlePartialQueueHistory`에서 음수 offset, 0/음수 limit, 과도한 limit 미처리
- **수정**: `offset < 0 → 0`, `limit < 1 → 50`, `limit > 500 → 500` 클램핑 추가

### 2. historyUrl URL 이스케이프 (logic + design)
- **파일**: `internal/httpd/render.go`
- **이슈**: 템플릿 함수 `historyUrl`에서 channel/from/to 파라미터를 URL에 직접 연결
- **수정**: `url.QueryEscape` 적용, 변수명 shadowing 해소 (`url` → `u`)

### 3. TestUpdateFromImport_NotFound 추가 (test)
- **파일**: `internal/modules/backlog/manage_test.go`
- **이슈**: UpdateFromImport의 item-not-found 에러 경로 미테스트
- **수정**: 존재하지 않는 ID에 대한 에러 반환 테스트 추가

### 4. DashboardHistory 추가 테스트 2건 (test)
- **파일**: `internal/modules/queue/manager_test.go`
- **이슈**: 빈 히스토리 + from/to 필터 경로 미테스트
- **수정**: `TestDashboardHistory_Empty`, `TestDashboardHistory_FromToFilter` 추가

## 참고 사항 (수정 불요)

1. **CleanupStale 히스토리 누락**: 죽은 PID 정리 시 queue_history에 기록 안 함. 설계 결정 사항 (DONE 없는 레코드가 비정상 종료를 의미).
2. **insertHistory non-tx 미사용**: 현재 모든 호출이 insertHistoryTx 사용. 향후 확장용으로 유지.
3. **UpdateFromImport 트랜잭션 부재**: SyncImport에서만 호출 + SQLite 단일 writer로 실질적 race 없음.

## CI 결과

- **Go (apex-agent)**: PASS (1m57s) — go test -race, go vet, linux/windows 빌드
- **전체 런**: success
