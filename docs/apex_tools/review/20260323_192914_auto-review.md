# Auto-Review — backlog 정합성 강화

**브랜치**: `bugfix/backlog-sync-fixes` (BACKLOG-160)
**리뷰어**: logic, design, test, infra-security (4명 병렬)
**일시**: 2026-03-23 19:29

---

## 수정 이슈 (5건, 전부 해결)

### 1. ExportHistory off-by-one panic [logic, confidence 85%]
- **파일**: `export.go:160`
- **원인**: 마커 뒤 `\n` 없으면 `insertPos`가 문자열 길이 초과 → panic
- **수정**: 마커 끝 위치에서 `\n` 존재 여부 확인 후 조건부 증가
- **테스트**: `TestExportHistory_MarkerNoTrailingNewline` 추가

### 2. Update() scope 검증 누락 [logic 95%, design 90%, infra 85%]
- **파일**: `manage.go:398-412`
- **원인**: severity/timeframe/type은 검증하지만 scope 빠짐 — Add()와 불일치
- **수정**: `ValidateScope()` 호출 추가
- **테스트**: `TestUpdate_InvalidScope` 추가

### 3. autoCommitExport git add 에러 무시 [logic 90%, design 80%, infra 75%]
- **파일**: `pipeline.go:107-108`
- **원인**: `exec.Command(...).Run()` 반환값 무시 → export 미커밋 시 silent 통과
- **수정**: `CombinedOutput()` + 에러 반환

### 4. SyncExport history 이중 읽기 [logic 75%, design 85%]
- **파일**: `sync.go:74`
- **원인**: SafeExport용 historyData와 별도로 ExportHistory용 2차 ReadFile
- **수정**: `string(historyData)` 재사용 + 주석 설명

### 5. ValidateBacklog 대소문자 우회 [test 80%, infra 70%]
- **파일**: `gate.go:139-141`
- **원인**: Windows NTFS 대소문자 무관 → `backlog.md`로 우회 가능
- **수정**: `strings.ToUpper` 정규화 후 매칭
- **테스트**: `TestValidateBacklog_CaseInsensitiveBlocks` 추가

---

## 이슈 없음 확인 (설계 검증)

| 검증 항목 | 결과 | 리뷰어 |
|-----------|------|--------|
| SQL Injection (Update 동적 SQL) | 안전 — allowedUpdateFields 화이트리스트 + ? 바인딩 | infra |
| Command Injection (autoCommitExport) | 안전 — exec.Command 쉘 미경유, 인자 하드코딩 | infra |
| Path Traversal (ValidateBacklog) | 안전 — HasSuffix + 역슬래시 정규화 | infra |
| 모듈 경계 (autoCommitExport 위치) | pipeline.go 적절 — 범용 git.go와 분리 | design |
| 의존성 방향 | cli→workflow→backlog 단방향 정상 | design |
| Breaking change (export CLI) | 의도된 변경, CLAUDE.md 갱신 완료 | design |
| MergePipeline Phase 순서 | 실패 시 전체 중단 의도적 설계 | design |
| Hook 구조 (settings.json) | 충돌 없음, 독립 프로세스 실행 | infra |
| map 순회 비결정성 | SQL SET 순서 무관, 기능 이슈 아님 | design |

---

## 추가 테스트 (서브에이전트 작성)

| 테스트 | 파일 | 커버 경로 |
|--------|------|-----------|
| TestUpdate_EmptyFields | manage_test.go | len(fields)==0 early return |
| TestUpdate_UnknownField | manage_test.go | allowedUpdateFields miss |
| TestUpdate_ScopeField | manage_test.go | scope 필드 갱신 |
| TestUpdate_RelatedField | manage_test.go | related 필드 갱신 |
| TestExport_FixingItemIncluded | export_test.go | FIXING 상태 출력 확인 |
| TestExportHistory_EmptyExistingHistory | export_test.go | 빈 history 입력 |
| TestExportHistory_MarkerOnlyHistory | export_test.go | marker만 있는 입력 |
| TestUpdate_PositionReorder | manage_test.go | position 재배치 동작 |
| TestUpdate_PositionInvalid | manage_test.go | position 검증 |
