# Auto-Review: BACKLOG-126 apex-agent Go 백엔드 — Full Review

**대상**: `apex_tools/apex-agent/` 전체 (14K LOC, 14 패키지)
**리뷰어**: logic, design, systems, test, infra-security (5명 병렬)
**일시**: 2026-03-23

---

## 발견 요약

| Severity | 발견 | 수정 | 백로그 |
|----------|:----:|:----:|:------:|
| CRITICAL | 2 | 2 | 0 |
| MAJOR | 13 | 13 | 0 |
| MINOR | 19 | 18 | 1 |
| **합계** | **34** | **33** | **1** |

미수정 1건: MAJOR-7 CLI 레이어 테스트 부재 (17파일 2000+ LOC 대규모 작업 → 별도 백로그)

---

## 수정 완료 (이번 리뷰에서 직접 수정)

### [CRITICAL-1] queue.TryAcquire TOCTOU 레이스 (수정 완료)
- **파일**: `queue/manager.go`
- **문제**: Status 체크 → INSERT 사이에 트랜잭션 없음. 동시 요청 시 동일 채널에 두 개의 ACTIVE 엔트리 가능
- **수정**: TryAcquire 전체를 `RunInTx`로 래핑, 트랜잭션 바운드 헬퍼 함수 추가
- **검증**: logic, systems, design 3명이 독립 지적

### [CRITICAL-2] queue.Acquire 폴링 TOCTOU + 취소 시 불완전 정리 (수정 완료)
- **파일**: `queue/manager.go`
- **문제**: promote 시 check-then-act 비원자적 + 취소 시 ACTIVE 엔트리 미정리 + DELETE 에러 무시
- **수정**: `tryPromote()` CAS 패턴 도입 (트랜잭션 + `WHERE status=?`), 취소 시 ACTIVE도 삭제, 에러 로깅
- **검증**: logic, systems, design 3명이 독립 지적

### [MAJOR-1] handoff.NotifyStart — MERGE_NOTIFIED 잔존으로 새 작업 차단 (수정 완료)
- **파일**: `handoff/manager.go`
- **문제**: 머지 완료 후 핸드오프 DB 항목이 남아서 같은 workspace에서 새 작업 시작 불가
- **수정**: A안 채택 — 모든 상태의 기존 항목을 자동 교체. 비-MERGE_NOTIFIED 상태 시 연결된 FIXING 백로그를 OPEN으로 복귀
- **테스트**: `TestNotifyStart_ReplaceMergeNotified`, `TestNotifyStart_ReplaceImplementing_AbandonedWork`

### [MAJOR-2] ipc.Client versionChecked 비원자적 (수정 완료)
- **파일**: `ipc/client.go`
- **문제**: `versionChecked bool` 필드에 동기화 없이 접근
- **수정**: `sync.Once` 패턴으로 전환
- **검증**: systems, design 2명이 독립 지적

### [MAJOR-3] backlog.Release branch_backlogs DELETE 에러 무시 (수정 완료)
- **파일**: `backlog/manage.go`
- **문제**: `_, _ = m.store.Exec(...)` 로 에러 무시. 삭제 실패 시 merge gate에서 false positive 차단 가능
- **수정**: 에러 로깅 추가
- **검증**: logic, systems, design 3명이 독립 지적

---

## 미수정 — 백로그 등록 권장

### [MAJOR-4] handoff FIXING 중복 체크 TOCTOU
- **파일**: `handoff/manager.go:78-94`
- **문제**: FIXING 검사가 트랜잭션 외부에서 수행됨
- **위험**: 동시 세션에서 같은 backlog로 NotifyStart 시 양쪽 모두 FIXING 진입 가능
- **권장**: Check를 트랜잭션 내부로 이동 또는 SetStatusWith에 WHERE status != 'FIXING' 추가

### [MAJOR-5] migrator 비트랜잭션 실행
- **파일**: `store/migrator.go:31-73`
- **문제**: 마이그레이션 실행 + _migrations INSERT가 트랜잭션으로 묶이지 않음
- **위험**: 부분 실패 시 DB inconsistent
- **권장**: per-migration RunInTx 도입

### [MAJOR-6] openBacklogStore() 이중 Manager 생성
- **파일**: `cli/backlog_cmd.go:197-213`
- **문제**: `backlog.New(s)` + `backlog.NewManager(s)` 별도 호출로 두 Manager 인스턴스 생성
- **현재 영향 없음**: Module은 스키마 등록에만 사용
- **권장**: `mod.Manager()` 사용으로 통일

### [MAJOR-7] CLI 레이어 테스트 부재
- **파일**: `internal/cli/` (17파일, ~2000 LOC)
- **문제**: CLI 인자 파싱, 에러 처리 단위 테스트 전무
- **권장**: cobra 테스트 패턴 활용한 CLI 단위 테스트 추가

### [MAJOR-8] daemon 모듈 OnStart 롤백 테스트 부재
- **파일**: `daemon/daemon.go:100-109`
- **문제**: 부분 실패 시 역순 정리 로직 검증 안 됨
- **권장**: 실패 시나리오 테스트 추가

### [MAJOR-9] queue benchmark 명령 인젝션 방어 부재 (infra-security)
- **파일**: `cli/queue_cmd.go:277-289`
- **문제**: `exec.Command(exe, ...)` 에 실행 파일 경로 검증 없음. hook 레이어가 방어하지만 CLI 단독 실행 시 무방비
- **권장**: 실행 파일 경로에 화이트리스트 (`bench_` prefix) 또는 프로젝트 루트 하위 확인

### [MAJOR-10] hook fail-open 미로깅 (infra-security)
- **파일**: `cli/hook_cmd.go:33-37`
- **문제**: stdin JSON 파싱 실패 시 `return nil`만 하고 로그 없음. 프로토콜 변경 시 모든 게이트 무력화
- **권장**: 파싱 에러 시 stderr 경고 + 로그 기록 추가

### [MAJOR-11] Named Pipe 과도한 권한 (infra-security)
- **파일**: `ipc/transport_windows.go:16`
- **문제**: `IU` (Interactive Users)에게 `GA` (Generic All) 부여. 공유 서버에서 다른 사용자가 데몬 조작 가능
- **권장**: 현재 사용자 SID로 교체 또는 `GR+GW`로 최소 권한 부여

---

## 미수정 — MINOR (향후 개선)

1. backlog.ValidateScope 빈 토큰 허용 ("CORE,,SHARED" 통과)
2. hook.EnforceRebase git fetch 에러 무시 (네트워크 단절 시 stale 판단)
3. hook.ValidateBuild strings.Contains 부분문자열 매칭
4. backlog params PascalCase 불일치 (프로토콜 전체 snake_case)
5. handoff handleAck `"ok": true` 중복
6. daemon stop Windows Kill (graceful shutdown 미지원)
7. Client checkVersion 500ms 하드코딩
8. Release가 handoff branch_backlogs 직접 조작 (모듈 경계 위반)
9. notification type 상수 lowercase (다른 열거형 UPPER_SNAKE_CASE)
10. gate_test setupGateTestDB t.Cleanup 미등록
11. cleanup 함수별 단위 테스트 부족
12. backlog.Add LastInsertId 에러 무시
13. queue.CleanupStale rows.Close 이중 호출
14. E2E testenv.Restart 소켓 경쟁 (Linux CI 추가 시 주의)
15. context.Generate context.Background() 무한 대기
16. Unix 소켓 TOCTOU: Remove→Listen 사이 경합 (infra-security)
17. run-hook 임시파일 예측 가능한 fallback 경로 (infra-security)
18. config 기본 log level "debug" → "info" 권장 (infra-security)

---

## 긍정적 평가

- **모듈 아키텍처 우수**: Module interface + DIP(BacklogOperator) + CLI-데몬 분리가 깔끔
- **RunInTx 패턴**: 트랜잭션 바운드 Store 복사본 설계가 안전하고 사용하기 쉬움
- **Source of Truth 분리**: MD(메타데이터) vs DB(상태) 명확한 책임 분리
- **Hook fail-open 전략**: 데몬 장애 시 개발 워크플로우 차단 방지
- **Queue stale cleanup**: PID 기반 자동 lock 해제로 영구 점유 방지
- **테스트 격리**: :memory: SQLite + testenv 독립 DB/소켓으로 순서 무관 실행
- **E2E 인프라**: 인프로세스 데몬 기동 + polling readiness + t.Cleanup 자동 정리
