# Auto-Review (Full): Session Server Polish

> **브랜치**: feature/session-polish
> **모드**: full (apex-agent Go 코드 전체)
> **리뷰어**: logic (full scope)
> **일시**: 2026-03-26

## 수정된 이슈 (3건)

| # | 심각도 | 파일 | 내용 |
|---|--------|------|------|
| 1 | **HIGH** | `server.go` | ReadTimeout/WriteTimeout 30s가 WebSocket 연결을 강제 종료. 0(unlimited)으로 변경 — localhost 전용이므로 slowloris 위험 없음 |
| 2 | **HIGH** | `session_cmd.go` | startSessionProcess에서 health check 실패 시 고아 프로세스 누수. child.Process.Kill() 추가 |
| 3 | **MEDIUM** | `session_cmd.go` | force kill 후 프로세스 미종료 시 silent success → 에러 반환으로 수정 |

## 보고만 한 이슈 (미수정, 모두 LOW)

| # | 내용 | 사유 |
|---|------|------|
| 4 | Watchdog onUpdate가 SessionLog 경로를 빈 문자열로 리셋 | 프로세스 사망 시 세션 무효화는 합리적 설계 |
| 5 | Shutdown 중 새 세션 생성 race window | 발생 확률 극히 낮음, httpSrv.Shutdown이 진행 중 요청 대기 |
| 6 | resume 모드 sessionID 명령줄 직접 삽입 | 자체 생성 ID + CreateProcess 직접 호출로 위험 없음 |

## 검증 완료 (이슈 없음)

- DashboardBranchesList SQL JOIN 정확성 ✅
- DashboardBlockedBacklogs SQL injection 안전성 ✅
- goroutine 누수 방어 (readPump done, Subscribe/unsub idempotent) ✅
- path traversal 방어 ✅
- WebSocket ReadLimit 64KB/1MB ✅
- RingBuffer 동시성 ✅
- CI: go vet + go test -race + linux/windows 빌드 PASS ✅
