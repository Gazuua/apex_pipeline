# Auto-Review: BACKLOG-126 apex-agent Go 백엔드 (Round 1)

- **브랜치**: feature/backlog-126-apex-agent-go-backend
- **리뷰 범위**: task 모드 (Go 백엔드 + 연관 파일만)
- **리뷰어**: logic, design, test, infra-security (4명 병렬)
- **변경 파일**: 45개, +1,197 / -615 라인

## 리뷰어별 발견 건수

| 리뷰어 | Critical | Major | Minor | 합계 |
|--------|:--------:|:-----:|:-----:|:----:|
| design | 1 | 7 | 4 | 12 |
| logic | 0 | 2 | 7 | 9 |
| test | 0 | 6 | 10 | 16 |
| infra-security | 0 | 4 | 14 | 18 |
| **합계 (중복 제거)** | **1** | **13** | **26** | **40** |

## 수정 완료 항목 (40/40건 — 백로그 이관 0건)

### Critical (1건)

| # | 이슈 | 파일 | 조치 |
|---|------|------|------|
| 1 | Store.RunInTx 동시성 — tx를 struct 필드에 저장하여 goroutine race | store/store.go | RunInTx가 tx-bound Store 복제본을 콜백에 전달하는 패턴으로 리팩토링 |

### Major (13건)

| # | 이슈 | 파일 | 조치 |
|---|------|------|------|
| 2 | handoff NotifyStart FIXING 전이가 트랜잭션 밖 | handoff/manager.go | RunInTx 패턴 전환 + BacklogStatusSetter.SetStatusWith 인터페이스 확장 |
| 3 | nullableString 중복 정의 (backlog/handoff) | store/store.go, manage.go, manager.go | store.NullableString 공개 헬퍼로 통합 |
| 4 | 열거형 네이밍 불일치 (handoff/queue 소문자 vs backlog UPPER_SNAKE) | state.go, manager.go, module.go | Migration v4(handoff)/v2(queue)로 DB 데이터 + 상수 UPPER_SNAKE 통일 |
| 5 | Named Pipe SecurityDescriptor 미설정 | transport_windows.go | SDDL `D:P(A;;GA;;;SY)(A;;GA;;;BA)(A;;GA;;;IU)` 설정 |
| 6 | IPC server 연결 deadline 미설정 | server.go | conn.SetDeadline(30s) + WriteMessage 에러 로깅 |
| 7 | Unix Socket 0600 권한 미설정 | transport_unix.go | os.Chmod(addr, 0o600) 추가 |
| 8 | IPC Client.Send context 무시 | client.go | context deadline 적용 + 기본 10초 timeout |
| 9 | gitCurrentBranch 함수 충돌 (cli 패키지 내 동명 이종) | handoff_cmd.go | currentGitBranch() 삭제, gitCurrentBranch("") 통합 |
| 10 | context 레거시 YAML 의존 | context/context.go | daemon IPC(handoff.get-branch) 전환 + graceful degradation |
| 11 | NormalizeResolution 오염 데이터 미처리 | enums.go | LastIndex 기반 재작성 — "→" 패턴 정확 처리 |
| 12 | Migration v3 rows.Scan/Exec 에러 무시 | module.go | 에러 체크 + 전파 추가 |
| 13 | ValidateMergeGate/GetBranch rows.Scan 에러 무시 | gate.go, manager.go | 에러 체크 + 전파 추가 |

### Minor (26건)

| # | 이슈 | 파일 | 조치 |
|---|------|------|------|
| 14 | Version 변수 4곳 분산 | version/version.go (신규), main.go, root.go, daemon.go, client.go | internal/version 패키지로 중앙화 |
| 15 | IPC 헬퍼 패턴 불일치 | (보고) | 구조적 차이 문서화, 향후 통합 시 참조 |
| 16 | WriteMessage 에러 무시 | server.go | 에러 로깅 추가 |
| 17 | env.go 파일명 불일치 | platform/ | env.go+path.go → paths.go 병합, 테스트도 병합 |
| 18 | UpdateFromImport 주석-코드 불일치 | manage.go | 주석 수정 — status 전달 동작 명시 |
| 19 | removeWorktree 항상 nil 반환 | cleanup.go | 디렉토리 잔존 시 에러 반환 |
| 20 | handleAcquire context.Background() 무한 블록 | queue/module.go | 30분 timeout context 추가 |
| 21 | SafeExport 트랜잭션 없이 실행 | export.go | RunInTx 내부 호출로 원자성 보장 |
| 22 | PID 파일 0644 권한 | daemon.go | 0o600으로 변경 |
| 23 | 설계서-코드 괴리 (cleanup/context Module 미구현) | (설계 문서) | CLI-only 유틸리티로 재분류 예정 |
| 24-26 | CI go mod verify, run-hook 에러 메시지, post-merge 경고 | ci.yml, run-hook, post-merge | 각각 수정 |
| 27-40 | 테스트 커버리지 갭 14건 | 8개 테스트 파일 | enums_test(신규), RunInTx/NullableString, SetStatus/Release, SafeExport, ResolveBranch, NotifyStart mock, gate FIXING, Acquire blocking, 4MB boundary, polling fix, t.Setenv, rebase false positive |

## 검증 결과

- `go build` 성공
- `go vet ./...` 통과
- `go test ./...` 전체 PASS (unit 14패키지 + E2E)

## 통계

- 발견: 40건 (Critical 1, Major 13, Minor 26)
- 수정: 40건
- 미수정: 0건
- 백로그 등록: 0건
