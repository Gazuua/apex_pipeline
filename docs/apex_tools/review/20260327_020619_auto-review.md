# Auto-Review: 세션 서버 + Branches 페이지 + local_branches 제거 (BACKLOG-260)

- **PR**: #213
- **브랜치**: `feature/dashboard-observe-only`
- **리뷰어**: 메인 에이전트 직접 (소규모 제거 작업)
- **일시**: 2026-03-27

## 변경 요약

41 files changed, +95 / -3,508 lines. 대시보드 관찰 전용화를 위한 대규모 제거.

### 제거 대상
| 카테고리 | 파일 수 | 삭제 라인 |
|----------|---------|-----------|
| session/ (ConPTY, WebSocket, watchdog) | 9 | ~1,300 |
| workspace/ (local_branches, scanner) | 4 | ~1,000 |
| httpd (branches, proxy, terminal) | 5 | ~400 |
| CLI (session_cmd) | 1 | ~250 |
| E2E 테스트 (session, workspace) | 2 | ~200 |
| 기타 수정 (config, routes, daemon 등) | 20 | ~350 |

## 리뷰 항목

### ISSUE-1: gofmt struct 필드 정렬 (FIXED)
- **파일**: `internal/httpd/server.go`
- **심각도**: LOW
- **내용**: Server struct의 `queueMgr`, `httpSrv` 필드 정렬이 gofmt 규칙과 불일치
- **조치**: `gofmt -w` 실행하여 수정 완료

### 검증 완료 항목 (이슈 없음)
1. **잔여 참조**: session, workspace, BranchInfo, BlockedBacklogInfo, WorkspaceQuerier, SessionProxy 등 삭제된 심볼에 대한 참조 0건 확인
2. **인터페이스 정합성**: `httpd.New()` 시그니처 변경 (7인자 → 4인자)이 daemon_cmd.go, testenv/env.go, daemon.go, server_test.go 4곳 모두 정확히 반영
3. **settings.json**: SessionEnd hook 제거, SessionStart의 plugin setup은 유지 (데몬 자동 기동 + 플러그인 등록)
4. **plugin teardown**: no-op으로 전환 — 기존 config에 SessionEnd hook이 남아있는 사용자도 에러 없이 동작
5. **테스트 이전**: `TestBacklog_BlockedReason_E2E`가 `workspace_test.go`에서 `backlog_test.go`로 정상 이전
6. **gorilla/websocket**: go.mod에서 정상 제거 (go mod tidy)
7. **CSS**: branches 관련 스타일 + blocked-badge 스타일 전체 제거
8. **layout.html**: Branches 네비 링크 + blocked-badge HTMX 폴링 제거

## 빌드/테스트
- `go build ./...`: 성공
- `go test ./... -count=1`: 20 패키지 전체 통과 (E2E 포함)
- CI (첫 번째 런): 성공
