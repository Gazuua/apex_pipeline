# apex-agent Go 코드베이스 Full Auto-Review (2라운드)

**일시**: 2026-03-26
**대상**: `apex_tools/apex-agent/` 전체 (115개 Go 파일, ~22K LOC)
**모드**: full (전체 코드베이스)
**리뷰어**: logic, design, test, infra-security, systems (5명 병렬)
**PR**: #184

---

## 리뷰 통계

| 항목 | 수 |
|------|:--:|
| 수정한 이슈 | 17 |
| 담당 밖 보고 | 3 |
| DESIGN_DECISION | 2 (1건 스킵, 1건 BACKLOG-236) |
| 수정 파일 | 15 |
| 변경량 | +128 / -49 |
| **클린 통과 영역** | **infra-security, systems** |

---

## 수정 이슈 상세

### MAJOR (2건)

#### M-1. NotifyStart RESOLVED 백로그 early reject 누락
- **파일**: `internal/modules/handoff/manager.go`
- **문제**: FIXING만 early reject하고 RESOLVED를 통과시킴. TX 내부 `SetStatusWith(FIXING)`이 결국 거부하나, 불필요한 TX 진입(active_branches+junction 삽입 후 rollback) + 에러 메시지가 모호.
- **수정**: RESOLVED 상태 early reject 추가 — 명확한 에러 메시지로 즉시 거부.

#### M-2. RebaseOnMain staged changes 검사 누락
- **파일**: `internal/workflow/git.go`
- **문제**: unstaged(`git diff --quiet`)만 체크하고 staged(`git diff --cached`)를 무시. EnforceRebase hook 경로에서 staged changes가 있는 상태로 rebase 진입 시, staged 변경이 conflict와 섞여 데이터 손실 가능.
- **수정**: unstaged 검사 전에 `git diff --cached --quiet` 검사 추가.

### MINOR (8건)

| # | 파일 | 설명 |
|---|------|------|
| m-1 | `cli/backlog_cmd.go` + `handoff_cmd.go` | ID 파싱 보일러플레이트 6곳 → `parseID` 헬퍼 통합 |
| m-2 | `cli/backlog_cmd.go` | `backlog show` json.Number → int 검증 통일 |
| m-3 | `cli/config_cmd.go` | `config show` 3/6 섹션만 표시 → 전체 표시 |
| m-4 | `cli/daemon_cmd.go` | stop 후 500ms 고정 sleep → 프로세스 종료 폴링 |
| m-5 | `config/config.go` | QueueConfig Raw 필드 TOML 제약 주석 추가 |

### MEDIUM (테스트 — 9건)

json.Unmarshal/Marshal 에러 미체크로 인한 거짓 통과 위험 수정:

| # | 파일 | 설명 |
|---|------|------|
| t-1 | `backlog/import_test.go` | Migrate(), Get() 에러 체크 3건 |
| t-2 | `e2e/e2e_test.go` | json.Unmarshal 에러 체크 |
| t-3 | `daemon/router_test.go` | json.Marshal/Unmarshal 에러 체크 |
| t-4 | `ipc/client_test.go` | json.Unmarshal 에러 체크 |
| t-5 | `workflow/sync_test.go` | os.ReadFile + json.Unmarshal 에러 체크 |
| t-6 | `httpd/server_test.go` | json Decode 에러 체크 |
| t-7~9 | `e2e/http_test.go` | 4개 HTTP API 테스트 json Decode 에러 체크 |

### TRIVIAL (2건)

| # | 파일 | 설명 |
|---|------|------|
| tr-1 | `cli/queue_cmd.go` | `sendQueueRequest` dead code + unused import 삭제 |
| tr-2 | `config/config.go` | QueueConfig Raw 필드 의도 주석 |

---

## 보고만 이슈 (미수정)

| # | 영역 | 심각도 | 설명 | 미수정 사유 |
|---|------|--------|------|------------|
| 1 | backlog/module.go | MINOR | handleAdd에서 Add+Fix 비원자적 (고아 OPEN 가능) | 구조 변경 규모 큼, 에러 반환으로 인지 가능, OPEN은 정상 상태 |
| 2 | handoff manager.go | MINOR | Branch struct JSON 태그 PascalCase (1라운드 재보고) | CLI 소비자 동시 변경 필요 |
| 3 | 전체 모듈 | MINOR | IPC 응답 이중 패턴 (1라운드 재보고) | 현재 규모에서 통합 ROI 낮음 |

## DESIGN_DECISION

| # | 결정 | 결과 |
|---|------|------|
| 1 | DashboardList scope 필터 multi-value 불일치 | **BACKLOG-236 등록** (현행 유지, 데이터 클렌징으로 해결 예정) |
| 2 | IPC 응답 처리 패턴 통합 | **현행 유지** (리뷰어 권장, 모듈 수 증가 시 재검토) |

---

## 라운드 비교

| 항목 | 1라운드 | 2라운드 |
|------|:------:|:------:|
| 수정 건수 | 21 | 17 |
| CRITICAL | 1 | 0 |
| MAJOR | 8 | 2 |
| 클린 통과 영역 | 1 (systems) | 2 (infra-security, systems) |

심각도 분포가 CRITICAL/MAJOR → MINOR/MEDIUM으로 이동. 핵심 안전성 이슈는 1라운드에서 해결되었고, 2라운드는 방어 로직 보강과 코드 품질 개선에 집중.
