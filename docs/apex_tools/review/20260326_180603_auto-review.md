# Auto-Review: Session Server Phase 2-3

> **브랜치**: feature/session-server-conpty
> **대상**: `git diff main...HEAD` (Go 코드 ~4500줄)
> **리뷰어**: logic, design, infra-security (3명 병렬)
> **일시**: 2026-03-26

## 수정된 이슈 (15건)

### HIGH (3건)

| # | 리뷰어 | 파일 | 내용 |
|---|--------|------|------|
| L-1 | logic | `manager.go` Remove() | Watchdog 경로에서 subscriber 채널 미닫기 + readPump 종료 미대기 → goroutine/logFile 누수. term.Close(), done 대기, 채널 close 추가 |
| L-4 | logic | `websocket.go` + `manager.go` | unsub()이 채널을 close하지 않아 write pump goroutine 누수. Subscribe의 unsub에 delete+close 원자적 포함, idempotent 수정 |
| D-1 | design | `proxy.go` | WS 프록시 클라이언트 종료 시 백엔드 conn 명시적 Close 누락 → goroutine 대기. Close + done 대기 추가 |

### MEDIUM (9건)

| # | 리뷰어 | 파일 | 내용 |
|---|--------|------|------|
| L-2 | logic | `manager.go` Register() | os.MkdirAll 에러 silent 무시 → Warn 로깅 추가 |
| L-5 | logic | `manager.go` readPump() | logFile.Write 에러 silent 무시 → Warn 로깅 추가 |
| L-7 | logic | `conpty_windows.go` | int→int16 캐스팅 범위 검증 없음 → NewConPTY, Resize 양쪽 bounds check 추가 |
| L-9 | logic | `server.go` handleSendInput() | r.Body 크기 무제한 → io.LimitReader(1MB) 추가 |
| L-11 | logic | `server.go` Run() | 취소된 ctx를 StopAll에 전달 → context.WithTimeout(Background, 5s) 사용 |
| S-1 | infra | `websocket.go` + `proxy.go` | WebSocket ReadLimit 미설정 → 64KB/1MB 제한 추가 |
| S-2 | infra | `manager.go` | workspace_id에 `..`/`/`/`\` 포함 시 path traversal → 검증 추가 |
| D-2 | design | `session_cmd.go` | workspace_id URL PathEscape 적용 |
| D-3 | design | `branches.go` | DashboardBranchesList 에러 로깅 추가 |

### LOW (3건)

| # | 리뷰어 | 파일 | 내용 |
|---|--------|------|------|
| L-13 | logic | `watchdog.go` | interval ≤ 0이면 time.NewTicker panic → 기본값 1초 fallback |
| D-1b | design | `proxy.go` | url.Parse 에러 처리 (panic with message) |
| D-4 | design | `routes.go` | 프록시 메서드 미제약 의도 주석 추가 |

## 보고만 한 이슈 (미수정)

| # | 심각도 | 내용 | 사유 |
|---|--------|------|------|
| L-3 | MEDIUM | server.go Get/Register TOCTOU 레이스 | Register 내부 중복 체크 + cleanup으로 기능적 안전. 동시 요청 빈도 낮음 |
| D-A | LOW | session server가 workspace.Manager 자체 생성 (DB 동시 접근) | SQLite WAL로 safe. 향후 전용 wrapper 분리 고려 |
| D-B | LOW | DashboardBlockedCount가 workspace 모듈에 위치 (크로스 모듈 쿼리) | 기능 동작에 문제 없음. 리팩토링 시 이동 고려 |
| D-C | MEDIUM | BranchInfo의 HandoffStatus/BacklogIDs/BlockedBacklogs 미채움 | 어댑터에서 JOIN 쿼리 필요. 별도 백로그로 추적 |
| S-4 | LOW | PID 파일 TOCTOU (동시 session start) | 포트 바인딩이 자연스럽게 중복 방지 |
| S-5 | MEDIUM | Windows SIGTERM 미지원 → Kill 강제 종료 | graceful shutdown HTTP 엔드포인트 추가 필요. 별도 백로그 |
| S-6 | LOW | xterm.js CDN에 SRI 미사용 | localhost 전용. CDN→로컬 번들 전환 시 함께 처리 |
| S-I5 | LOW | HTTP 타임아웃 30s가 WebSocket에 부적절할 수 있음 | gorilla/websocket Hijack 이후 적용 안 됨 (확인 필요) |

## 검증

- Go 빌드: PASS
- 전체 테스트 (22개 패키지): PASS
- CI (Go 잡): PASS (go vet + go test -race + linux/windows 빌드)
