# Auto-Review: workflow 공유 레이어 + BACKLOG-157

**PR**: #119 (`feature/apex-agent-enhancements`)
**일시**: 2026-03-23 16:53

## 리뷰어 구성

| 리뷰어 | 도메인 | 이슈 |
|--------|--------|------|
| logic | 파이프라인 로직, 에러 처리, 상태 전이 | CRITICAL 2, MAJOR 5, MINOR 5 |
| test | 테스트 커버리지, assertion 품질, 격리성 | MAJOR 11, MINOR 7 |

## 수정된 이슈 (4건)

### 1. MergePipeline: SyncExport 후 커밋 없이 CheckoutMain (logic MAJOR-5)
- **문제**: SyncExport가 docs/BACKLOG.md를 디스크에 쓴 뒤, 커밋 없이 CheckoutMain을 호출하면 uncommitted changes 상태에서 브랜치 전환
- **수정**: MergePipeline에서 CheckoutMain 제거. 호출자가 export 결과를 커밋+푸시한 뒤 별도 처리
- **파일**: `pipeline.go` Phase 5 삭제, `handoff_cmd.go` 안내 메시지 추가

### 2. projectRoot 폴백 "." (logic MAJOR-2)
- **문제**: `projectRoot()` 실패 시 `"."` 폴백 → SyncExport가 잘못된 경로에 파일 쓰기 가능
- **수정**: 에러 반환으로 변경 — 3개 커맨드(start, merge, drop) 모두
- **파일**: `handoff_cmd.go` 3곳

### 3. RebaseOnMain rev-list 실패 시 silent skip (logic MAJOR-1)
- **문제**: `git rev-list` 실패 시 에러 삼킴, rebase 스킵으로 간주
- **수정**: `ml.Warn` 경고 로그 추가
- **파일**: `git.go`

### 4. 중복 assertion (test MINOR)
- **문제**: `TestMergePipeline_RebaseFails`에서 동일 assertion 2회 반복 (복사-붙여넣기 잔여물)
- **수정**: 중복 제거
- **파일**: `pipeline_test.go`

## 기존 동작 유지 (2건)

### logic CRITICAL-1: StartPipeline IPC 성공 후 git 실패 시 DB-git 불일치
- **판단**: 기존 `doNotifyStart`에도 동일한 동작. `Manager.NotifyStart()`가 재등록을 허용하므로 재시도 가능. 보상 트랜잭션은 향후 과제. (→ 백로그 등록 대상)

### logic CRITICAL-2: CreateAndPushBranch push 실패 무시
- **판단**: 기존 코드도 경고만 출력. push 실패를 fatal로 만들면 네트워크 일시 장애 시 브랜치 생성+DB 등록이 롤백되어 더 나쁜 상태.

## 스코프 외 (백로그 등록)

### logic MAJOR-4: SyncExport BACKLOG_HISTORY.md 미출력
- HISTORY 자동화는 별도 기능. SafeExport()가 OPEN+FIXING만 출력하는 건 설계 의도.

### test MAJOR 11건: 테스트 커버리지 갭
- 리모트 브랜치 검증, CreateAndPushBranch 실패, abort 실패 경로, history import, 에러 메시지 검증 등
- 핵심 경로는 테스트됨. 엣지 케이스 보강은 향후 테스트 커버리지 작업에서 처리.
