# auto-review v1.1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** auto-review 플러그인에 2-tier 멀티에이전트 리뷰, confidence 하향, smart re-review skip 적용

**Architecture:** 기존 5개 에이전트 md 파일 + 오케스트레이터 md 파일을 수정. reviewer-code/test는 내부에서 superpowers:code-reviewer 서브에이전트를 병렬 호출하는 2-tier 구조로 전환. 오케스트레이터에 파일 타입 매핑 + 의미적 판단 기반 smart skip 로직 추가.

**Tech Stack:** Claude Code 플러그인 (markdown 에이전트/커맨드), superpowers 플러그인 연동

**Design doc:** `docs/apex_tools/plans/20260308_135041_auto-review-v1.1-design.md`

---

### Task 1: reviewer-code.md — 2-tier 구조 + confidence 하향

**Files:**
- Modify: `apex_tools/claude-plugin/agents/reviewer-code.md`

**Step 1: 현재 파일 읽기**

Read `apex_tools/claude-plugin/agents/reviewer-code.md` 전체 확인.

**Step 2: Confidence 섹션 수정**

기존:
```
**Confidence ≥ 80인 이슈만 보고**
```

변경:
```
**Confidence ≥ 40인 이슈만 보고**
```

Confidence Scoring 체계도 업데이트:
```markdown
각 이슈에 0-100 confidence 점수 부여:
- **0-25**: false positive 가능성 높음 → 제외
- **26-39**: 불확실하지만 가능성 있음 → 제외
- **40-60**: 유효하지만 저영향 → 보고 (Minor 추천)
- **61-80**: 주의 필요 → 보고
- **81-100**: 치명적 버그 또는 명시적 규칙 위반 → 보고
```

**Step 3: 2-tier 멀티에이전트 섹션 추가**

"## 작업 지침" 앞에 새 섹션 추가:

```markdown
## 2-Tier 멀티에이전트 병렬 리뷰

### 우선 전략: superpowers:code-reviewer 활용

리뷰 범위가 파악되면, 다음 절차로 병렬 리뷰를 수행한다:

1. **모듈 분할**: 리뷰 대상 파일을 모듈/디렉토리 단위로 분할
   - 예: `include/apex/core/` / `src/` / `examples/`
   - 파일이 적으면 (5개 이하) 분할 없이 단일 서브에이전트

2. **서브에이전트 병렬 디스패치**: 각 분할 단위마다 `superpowers:code-reviewer` 서브에이전트를 Agent 도구로 동시 호출
   - task 모드: git diff range (`BASE_SHA..HEAD_SHA`) + 해당 모듈 파일 목록 전달
   - full 모드: 해당 모듈 전체 파일 목록 전달 (diff 없이 전체 리뷰 지시)
   - 서브에이전트에게는 "가능한 많이 보고하라 (confidence 제한 없음)" 지시

3. **결과 취합**: 서브에이전트 결과를 수집하여:
   - 중복 이슈 제거 (동일 파일:라인의 동일 이슈)
   - Confidence < 40 이슈 필터링
   - 최종 보고서 형식으로 변환

### Fallback: 자율 리뷰

superpowers:code-reviewer 서브에이전트 호출이 실패하면 (플러그인 미설치 등), 기존 방식대로 자율 분할 리뷰를 수행한다. 이 경우에도 파일이 많으면 모듈/디렉토리 단위로 서브에이전트를 분할하여 병렬 처리한다.
```

**Step 4: 작업 지침의 기존 "자율 병렬 분할" 항목 수정**

기존:
```
4. **자율 병렬 분할** — 파일이 많으면 모듈/디렉토리 단위로 서브에이전트 분할
```

변경:
```
4. **2-tier 병렬 리뷰 적극 활용** — 위 "2-Tier 멀티에이전트 병렬 리뷰" 섹션에 따라 superpowers:code-reviewer를 우선 사용. 코드 품질은 최우선 가치이므로 서브에이전트 분할을 적극 권장한다.
```

**Step 5: Confidence 관련 작업 지침 수정**

기존:
```
3. **false positive 최소화** — 확실하지 않으면 confidence를 낮게 매겨서 제외
```

변경:
```
3. **낮은 confidence도 보고** — Confidence ≥ 40이면 보고. 40-60 구간은 Minor로 분류 추천. false positive보다 이슈 누락이 더 위험하다.
```

**Step 6: 커밋**

```bash
git add apex_tools/claude-plugin/agents/reviewer-code.md
git commit -m "feat(auto-review): reviewer-code 2-tier 병렬 리뷰 + confidence ≥ 40"
```

---

### Task 2: reviewer-test.md — 2-tier 구조 + confidence 하향

**Files:**
- Modify: `apex_tools/claude-plugin/agents/reviewer-test.md`

**Step 1: 현재 파일 읽기**

Read `apex_tools/claude-plugin/agents/reviewer-test.md` 전체 확인.

**Step 2: Confidence threshold 수정**

기존:
```
3. **Confidence ≥ 80인 이슈만 보고** — 불확실한 이슈는 제외
```

변경:
```
3. **Confidence ≥ 40인 이슈만 보고** — 40-60 구간은 Minor 추천. 이슈 누락보다 낮은 confidence 보고가 안전하다.
```

**Step 3: 2-tier 멀티에이전트 섹션 추가**

"## 작업 지침" 앞에 새 섹션 추가 (reviewer-code와 동일 구조, 테스트 관점으로 조정):

```markdown
## 2-Tier 멀티에이전트 병렬 리뷰

### 우선 전략: superpowers:code-reviewer 활용

테스트 리뷰도 코드 품질의 핵심이므로, superpowers:code-reviewer를 적극 활용한다:

1. **분할 기준**: 테스트 파일을 단위별로 분할
   - unit tests / integration tests 분리
   - 또는 대응 소스 모듈별 분리 (예: network 관련 테스트 / engine 관련 테스트)

2. **서브에이전트 병렬 디스패치**: 각 분할 단위마다 `superpowers:code-reviewer` 서브에이전트를 Agent 도구로 동시 호출
   - task 모드: git diff range + 해당 테스트 파일 + 대응 소스 파일 전달
   - full 모드: 해당 테스트 파일 + 대응 소스 파일 전체 목록 전달
   - 서브에이전트에게는 "테스트 커버리지, assertion 품질, 누락 테스트에 집중하라. 가능한 많이 보고하라" 지시

3. **결과 취합**: 중복 제거 → Confidence < 40 필터링 → 최종 보고

### Fallback: 자율 리뷰

superpowers:code-reviewer 호출 실패 시 기존 방식대로 자율 분할 리뷰 수행.
```

**Step 4: 작업 지침 수정**

기존 항목 4:
```
4. **자율 병렬 분할** — 테스트 파일이 많으면 모듈별로 서브에이전트 분할
```

변경:
```
4. **2-tier 병렬 리뷰 적극 활용** — 위 "2-Tier 멀티에이전트 병렬 리뷰" 섹션에 따라 superpowers:code-reviewer를 우선 사용. 테스트 품질은 코드 품질만큼 중요하므로 서브에이전트 분할을 적극 권장한다.
```

**Step 5: 커밋**

```bash
git add apex_tools/claude-plugin/agents/reviewer-test.md
git commit -m "feat(auto-review): reviewer-test 2-tier 병렬 리뷰 + confidence ≥ 40"
```

---

### Task 3: reviewer-docs / structure / general — confidence 하향

**Files:**
- Modify: `apex_tools/claude-plugin/agents/reviewer-docs.md`
- Modify: `apex_tools/claude-plugin/agents/reviewer-structure.md`
- Modify: `apex_tools/claude-plugin/agents/reviewer-general.md`

**Step 1: 3개 파일 읽기**

모두 읽어서 현재 confidence 관련 문구 확인.

**Step 2: reviewer-docs.md 수정**

기존 (107행):
```
3. **Confidence ≥ 80인 이슈만 보고** — 불확실한 이슈는 제외
```

변경:
```
3. **Confidence ≥ 40인 이슈만 보고** — 40-60 구간은 Minor 추천. 이슈 누락보다 낮은 confidence 보고가 안전하다.
```

**Step 3: reviewer-structure.md 수정**

기존 (113행):
```
3. **Confidence ≥ 80인 이슈만 보고** — 불확실한 이슈는 제외
```

동일하게 변경.

**Step 4: reviewer-general.md 수정**

기존 (127행):
```
2. **Confidence ≥ 80인 이슈만 보고** — 불확실한 이슈는 제외
```

변경:
```
2. **Confidence ≥ 40인 이슈만 보고** — 40-60 구간은 Minor 추천. 이슈 누락보다 낮은 confidence 보고가 안전하다.
```

**Step 5: 커밋**

```bash
git add apex_tools/claude-plugin/agents/reviewer-docs.md apex_tools/claude-plugin/agents/reviewer-structure.md apex_tools/claude-plugin/agents/reviewer-general.md
git commit -m "feat(auto-review): 3개 에이전트 confidence threshold ≥ 80 → ≥ 40"
```

---

### Task 4: 오케스트레이터 — Smart Re-review Skip 로직

**Files:**
- Modify: `apex_tools/claude-plugin/commands/auto-review.md`

**Step 1: 현재 파일 읽기**

Read `apex_tools/claude-plugin/commands/auto-review.md` 전체 확인.

**Step 2: Phase 2 "재리뷰" 섹션에 Smart Skip 로직 추가**

기존 Phase 2의 4번 항목:
```markdown
4. **재리뷰 (Phase 1로 돌아감)**
   - 라운드 카운터 증가
   - Phase 1 재실행
   - Clean(0건) 달성까지 반복
```

변경:
```markdown
4. **재리뷰 (Phase 1로 돌아감) — Smart Re-review**
   - 라운드 카운터 증가

   **스킵 판단 절차:**

   a) 수정된 파일 목록 추출: `git diff HEAD~1..HEAD --name-only`

   b) 파일 타입 매핑으로 "최소 필수 리뷰어" 결정:

   | 수정 파일 타입 | 영향받는 리뷰어 |
   |--------------|--------------|
   | `.cpp`, `.hpp` (소스/헤더) | code, test, structure |
   | `test_*.cpp`, `test_helpers.hpp` | test |
   | `CMakeLists.txt`, `vcpkg.json`, `build.*`, `CMakePresets*` | structure, general |
   | `*.md` (문서) | docs |
   | `.gitignore`, hooks, scripts | general |

   c) 수정 내용의 **의미적 영향** 추가 판단 (오케스트레이터 자율):
   - 코드 로직 변경 (if/for/while 등 제어 흐름) → docs 추가 (설계 정합성 영향)
   - 의존성 변경 (vcpkg, include) → general 추가
   - 디렉토리/파일 구조 변경 → 전원 재리뷰
   - **판단이 애매하면 → 해당 리뷰어를 포함** (보수적으로)

   d) 스킵 조건 (AND):
   - 직전 라운드에서 **Clean (0건)**
   - 위 판단에서 "영향받는 리뷰어"에 **미포함**
   - 두 조건 모두 만족 → 해당 에이전트 스킵

   e) Phase 1 재실행 시 **스킵 대상이 아닌 에이전트만 디스패치**

   f) 리뷰 보고서에 참여 현황 기록:
   ```markdown
   ## Round {N} 참여 현황
   - reviewer-docs: 스킵 (Round {N-1} Clean + 수정 무관)
   - reviewer-code: 리뷰 수행
   - ...
   ```

   - Clean(0건) 달성까지 반복
```

**Step 3: Phase 1의 에이전트 디스패치 설명에 스킵 참조 추가**

기존 Phase 1의 2번 항목:
```markdown
2. **5개 리뷰어 에이전트 병렬 디스패치**

   Agent 도구로 **반드시 5개를 동시에** 하나의 메시지에서 호출:
```

변경:
```markdown
2. **리뷰어 에이전트 병렬 디스패치**

   Agent 도구로 **참여 대상 에이전트를 동시에** 하나의 메시지에서 호출:
   - Round 1: 5개 전원 디스패치
   - Round 2+: Phase 2 Smart Re-review 스킵 판단에 따라 참여 에이전트만 디스패치
```

**Step 4: 커밋**

```bash
git add apex_tools/claude-plugin/commands/auto-review.md
git commit -m "feat(auto-review): 오케스트레이터 Smart Re-review Skip 로직 추가"
```

---

### Task 5: plugin.json 버전 업데이트 + 최종 커밋

**Files:**
- Modify: `apex_tools/claude-plugin/.claude-plugin/plugin.json`

**Step 1: 버전 업데이트**

기존:
```json
"version": "1.0.0"
```

변경:
```json
"version": "1.1.0"
```

**Step 2: 커밋**

```bash
git add apex_tools/claude-plugin/.claude-plugin/plugin.json
git commit -m "chore(auto-review): v1.1.0 버전 범프"
```

---

### Task 6: 검증

**Step 1: 변경 파일 목록 확인**

```bash
git log --oneline main..HEAD
git diff --stat main..HEAD
```

변경된 파일이 다음 7개인지 확인:
- `apex_tools/claude-plugin/agents/reviewer-code.md`
- `apex_tools/claude-plugin/agents/reviewer-test.md`
- `apex_tools/claude-plugin/agents/reviewer-docs.md`
- `apex_tools/claude-plugin/agents/reviewer-structure.md`
- `apex_tools/claude-plugin/agents/reviewer-general.md`
- `apex_tools/claude-plugin/commands/auto-review.md`
- `apex_tools/claude-plugin/.claude-plugin/plugin.json`
(+ 설계/계획 문서, 이전 라운드 수정 파일들)

**Step 2: 각 에이전트 파일에서 confidence 확인**

5개 에이전트 파일 모두에서 `≥ 40` 문구 존재 확인.

**Step 3: reviewer-code/test에서 2-tier 섹션 확인**

"2-Tier 멀티에이전트 병렬 리뷰" 섹션과 "superpowers:code-reviewer" 언급 존재 확인.

**Step 4: 오케스트레이터에서 Smart Skip 로직 확인**

Phase 2에 "스킵 판단 절차" 섹션과 파일 타입 매핑 테이블 존재 확인.

---

### Task 7: auto-review task 실행 (자동 리뷰)

> 구현이 완료되면 `/auto-review task`를 실행하여 변경사항을 자동 리뷰한다.
> executing-plans 완료 시 이 Task를 자동으로 수행할 것.

**Step 1: auto-review task 실행**

```bash
# 현재 브랜치에서 main 대비 변경분 리뷰
/auto-review task
```

auto-review가 Round 1 → 수정 → 재리뷰 → Clean 달성까지 자동 진행.
이때 v1.1에서 개선한 기능(2-tier 리뷰, confidence ≥ 40, smart skip)이 적용되는지도 함께 검증된다.

**Step 2: 리뷰 결과 확인**

- Clean 달성 시: 완료
- 이슈 발견 시: auto-review가 자동 수정 → 재리뷰 반복
