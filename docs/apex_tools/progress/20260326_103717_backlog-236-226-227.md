# BACKLOG-236, 226, 227 — scope 필터 + cleanup 테스트 + 소켓 경쟁

**일시**: 2026-03-26
**브랜치**: `feature/apex-agent-minor-cleanup`
**PR**: #188

## 수정 내용

### BACKLOG-236: DashboardList scope 필터 (MINOR/DESIGN_DEBT)
- `addInFilter("scope", ...)` → scope 전용 `LIKE '%val%'` OR 패턴으로 변경
- multi-value scope ("CORE, SHARED")에서 단일 scope 필터("CORE") 시 정상 매칭

### BACKLOG-226: cleanup 모듈 단위 테스트 (MINOR/TEST)
- 8개 테스트 함수, 43개 케이스 추가
- parseWorktreeLine, protectedBranches, isSubPath, cleanResidualWorktreeDirs, processEmptyWorktreeDirs 등
- git/외부 명령 없이 순수 로직만 테스트

### BACKLOG-227: E2E testenv.Restart 소켓 경쟁 (MINOR/BUG)
- Restart()에서 Unix 소켓 선제 삭제 (`os.Remove`) 추가
- `runtime.GOOS != "windows"` 조건으로 Windows Named Pipe 무영향
- 이중 방어: transport_unix.go의 listenPlatform도 os.Remove 수행
