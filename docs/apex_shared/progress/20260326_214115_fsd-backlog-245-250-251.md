# FSD Backlog Sweep: BACKLOG-245,250,251

**일시**: 2026-03-26
**브랜치**: feature/fsd-backlog-20260326_212408
**PR**: #204

## 완료 항목

### BACKLOG-245: SecureString constant_time_equal()
- `constant_time_equal(const SecureString&)` + `constant_time_equal(std::string_view)` 추가
- XOR 누적 + volatile 패턴 (OpenSSL CRYPTO_memcmp 동등)
- operator==는 비보안 컨텍스트용으로 현행 유지
- 테스트 6건 추가

### BACKLOG-250: Gateway RL RedisAdapter 라이프사이클 통합
- standalone unique_ptr → Server::add_adapter("ratelimit") 이관
- GatewayService 생성자에서 어댑터 파라미터 제거
- on_configure에서 Server 레지스트리를 통해 어댑터 참조 획득
- Server의 drain()/close() 시퀀스에 자동 포함 → graceful shutdown 코루틴 누수 방지

### BACKLOG-251: GatewayPipeline rate_limiter_ atomic 제거
- `std::atomic<LimiterT*>` → `LimiterT*` (초기화 후 불변)
- set_rate_limiter() doc에 "initialization only" 명시
- `<atomic>` include 제거
- ConfigReloader hot-reload은 RateLimitFacade::update_endpoint_config()로 통일 (포인터 교체 금지)

## FSD 스캔 드롭 항목

| ID | 사유 |
|----|------|
| 246 | SSO 버퍼 제로화 UB 경계, 설계 결정 필요 |
| 247 | Kafka consumer 의미론 전체 변경, 전용 작업 |
| 248+249 | Gateway 보안 대규모 변경, 전용 작업 |
| 252 | AWS IAM 인프라 연동, 설계 결정 필요 |
| 253 | Helm 템플릿 다수 작업, 전용 작업 |
| 149 | 3개 서비스 변경, FSD 규모 초과 |

모든 드롭 항목에 `[FSD 분석 2026-03-26]` 메모 추가 완료.
