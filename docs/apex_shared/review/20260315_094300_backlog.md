# Auto-Review 에스컬레이션 백로그 — v0.5 Wave 1 Phase 1

## Critical (후속 마일스톤에서 해결)

### cancel_all_pending UAF (redis_multiplexer.cpp:262-277)
- **발견**: cross-cutting 리뷰어, confidence 85%
- **설명**: resolver.cancel() 후 즉시 slab_.destroy() 호출 시 코루틴 resume에서 dangling PendingCommand 참조 가능
- **권장 해결 시점**: v0.5 Phase 2 또는 v0.6
- **관련 코드**: apex_shared/lib/adapters/redis/src/redis_multiplexer.cpp

### CircuitBreaker HALF_OPEN 코루틴 인터리빙 (circuit_breaker.hpp:51-63)
- **발견**: logic + concurrency 리뷰어, confidence 65%
- **설명**: should_allow()에서 카운트 미증가 -> co_await 윈도우에서 max_calls 초과 가능
- **현재 상태**: 미사용 코드 (어댑터 통합 미완료)
- **권장 해결 시점**: 어댑터 통합 시점 (v0.5 Phase 2)

## Important (후속 개선)

### CircuitBreaker::call() Result<void> 타입 제한
- **설명**: 현재 call()이 Result<void>만 반환. 통합 시 Result<T> 제네릭 확장 필요
- **권장 해결 시점**: 어댑터 통합 시점

### 어댑터 상태 관리 불일치
- **설명**: KafkaAdapter는 자체 AdapterState, 나머지는 AdapterBase::ready_ 사용. 통일 검토 필요
- **권장 해결 시점**: v0.6 (운영 인프라)

## Minor (품질 개선)

- test run_coro detached->use_future 불일치
- test_pg_pool_retry 타이밍 flaky (sanitizer multiplier 미적용)
- test_session.cpp 매직 넘버 256 하드코딩
- test_redis_reply.cpp 매직 넘버 0
- write_pump 에러 경로 미테스트 (non-deterministic)
- HALF_OPEN max_calls 거부 미테스트
- RedisReply 빈 배열 미테스트
- PgPool retries=0 경계값 미테스트
- plan/spec 파일명 형식 docs/CLAUDE.md 네이밍 규칙 미준수

---

## Phase 2 에스컬레이션

### Critical
1. **Protocol concept Frame 내부 구조 미제약** — ConnectionHandler<P>가 frame.header.msg_id, frame.payload에 직접 접근하지만 concept이 이를 제약하지 않음. WebSocketProtocol 인스턴스화 시 컴파일 실패. → 어댑터 통합 시점에서 concept에 accessor 요구 추가 또는 Frame trait 도입
2. **WebSocketProtocol 테스트 완전 부재** — MVP 스켈레톤이므로 v0.5.1+ Beast 통합 시 함께 작성

### Important
3. **FrameError→ErrorCode 매핑 구분 불가** — BodyTooLarge/HeaderParseError가 모두 InvalidMessage. MessageTooLarge 등 세분화 필요
4. **drain_timeout 만료 시 Server 멤버 소멸 순서** — listeners_(dispatcher)가 core_engine_(코루틴) 전에 소멸 → UB 가능. 선언 순서 변경 또는 명시적 정리 필요
5. **Server 예외 경로 소멸 순서** — run()에서 listener->start() 실패 시 dangling dispatcher 참조
6. **Listener<P> 단위 테스트 부재** — 149줄 신규 코드, E2E 간접 커버만

### Minor
7. cross-thread acceptor close
8. boost-beast 조기 추가 (vcpkg.json 미사용 의존성)
9. 하드코딩된 상대 경로 apex_core/CMakeLists.txt:81
10. drain()/stop() 동일 구현
11. Acceptor control_io_ → core 0 이동 부하 불균형
