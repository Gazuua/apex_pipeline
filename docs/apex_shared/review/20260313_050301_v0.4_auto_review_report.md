# v0.4 External Adapters — Auto-Review Report

**Date**: 2026-03-13 05:03 KST
**Branch**: `feature/v0.4-external-adapters`
**Reviewer**: apex-auto-review (5 specialized agents × 3 rounds)

## Summary

| Round | Critical | Important | Minor | Result |
|-------|----------|-----------|-------|--------|
| 1     | 7        | 13        | 9     | Fixed  |
| 2     | 0        | 1         | 0     | Fixed  |
| 3     | 0        | 0         | 1*    | Clean  |

*Round 3 Minor (confidence 45%): 쿼리 에러 시 커넥션 무조건 폐기 → 재사용 가능한 커넥션 낭비. PgBouncer 환경에서는 커넥션 재생성 비용이 낮아 실질적 영향 미미. 향후 최적화 후보로 남김.

## Round 1 Fixes (commit 7064465)

### Critical (7건)
1. **Shutdown 순서 오류**: adapter close가 CoreEngine stop 전에 실행 → use-after-free 위험. drain→services stop→CoreEngine stop/join/drain→adapter close로 재배치.
2. **KafkaProducer data race**: `topic_cache_` (std::vector) 동기화 없이 멀티스레드 접근 → `std::mutex` + `std::lock_guard` 추가.
3. **KafkaConsumer pipe fd double-close (Linux)**: stream_descriptor가 이미 관리하는 fd를 수동 close → `pipe_desc_.reset()` 후 `pipe_fds_[1]`만 수동 close.
4. **KafkaSink 테스트 SUCCEED()**: 실제 검증 없이 항상 PASS → produce count before/after 비교로 교체.
5. **PoolStats 테스트 부재**: 6개 단위 테스트 + 2개 discard() 테스트 추가.
6. **KafkaProducer rd_kafka_conf_set 반환값 미검사**: 실패 시 spdlog::warn 로깅 추가.
7. **PgConnection 예외 안전성**: `use_awaitable` → `redirect_error(use_awaitable, ec)` 전환 (shutdown 시 예외 대신 에러코드).

### Important (13건)
- ConnectionPool discard() 메서드 추가 (에러 경로 카운터 누수 방지)
- PgAdapter 커넥션 스트링 패스워드 마스킹
- `connection_pool.hpp` acquire()에서 null 커넥션 방어
- docker-compose PgBouncer DATABASE_URL 환경변수
- CI cache key에 apex_shared/vcpkg.json 포함
- ci.Dockerfile에 apex_shared/vcpkg.json pre-install
- vcpkg.json builtin-baseline 추가
- 버전 0.3.0 → 0.4.0 일괄 업데이트
- Server::add_adapter_with_args 테스트 추가
- AdapterWrapper LifecycleDelegation 테스트 추가
- KafkaProducer 테스트 EXPECT_GE → before/after 비교
- MultipleAdapters 테스트 추가
- Progress 파일명 HHMMSS 추가

### Minor (9건)
- 주석 개선, 코드 정리, 로그 메시지 개선 등

## Round 2 Fixes (commit 95c88d1)

### Important (1건)
1. **PgAdapter/PgPool 에러 경로 discard() 누락**: query/execute 4개 메서드 + acquire_connected() connect 실패 시 커넥션이 release도 discard도 되지 않아 풀 카운터(active_count_, total_count_) 영구 누적. `discard(std::move(conn))` 호출 추가.

## Round 3 Result

**Clean** — Critical 0 / Important 0 / Minor 1 (confidence 45%, 최적화 후보)

## Reviewer Coverage

| Reviewer | Round 1 | Round 2 | Round 3 |
|----------|---------|---------|---------|
| reviewer-code | C:4 I:5 M:3 | I:1 | M:1 (45%) |
| reviewer-test | C:3 I:3 M:2 | Clean | — |
| reviewer-docs | I:2 M:2 | Clean | — |
| reviewer-structure | I:2 M:1 | Clean | — |
| reviewer-general | I:1 M:1 | Clean | — |
