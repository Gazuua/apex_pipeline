# Auto-Review: SecureString ZeroizesOnDestruction 테스트 UB 제거

- **브랜치**: `bugfix/secure-string-valgrind-ub`
- **PR**: #222
- **일시**: 2026-03-28

## 변경 요약

`test_secure_string.cpp` 1파일 변경 (24 ins / 25 del).
`ZeroizesOnDestruction` 테스트의 freed memory 접근(UB)을 placement new 기반 SSO 버퍼 검증으로 교체.

## 근본 원인 분석

1. CI `nightly.yml`에서 `apt-get install -y valgrind` → 실행 도구만 설치, `valgrind-dev`(헤더) 미포함
2. 빌드 **후** Valgrind 설치 → 컴파일 타임 `__has_include(<valgrind/valgrind.h>)` 항상 false
3. `APEX_RUNNING_ON_VALGRIND` 매크로 항상 0 → 런타임 가드 무효
4. MSVC debug 빌드에서 `~string()` → `_Tidy_deallocate()` → `_Myres = 15` 재기록으로 전체 바이트 검증 불가

## 리뷰 결과

| 관점 | 결과 | 비고 |
|------|------|------|
| test | PASS | SSO 임계값 내 12바이트 문자열, ASSERT 가드로 비SSO 플랫폼 보호, 0xCC prefill |
| logic | PASS | explicit destructor 1회 호출, pointer arithmetic 안전 |
| systems | PASS | alignas 정렬, ptrdiff_t→size_t 캐스트 전 ASSERT_GE 검증 |
| design | N/A | 테스트 전용 변경 |
| infra-security | N/A | 프로덕션 코드 변경 없음 |

**발견 이슈: 0건**
