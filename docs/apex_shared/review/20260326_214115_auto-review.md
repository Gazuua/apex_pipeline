# Auto-Review: FSD Backlog Sweep (BACKLOG-245,250,251)

**일시**: 2026-03-26 21:41
**모드**: task (diff 기반)
**브랜치**: feature/fsd-backlog-20260326_212408
**리뷰어**: 메인 직접 (소규모 변경)

## 변경 범위

| 파일 | 변경 | 목적 |
|------|------|------|
| secure_string.hpp/cpp | +25 | constant_time_equal() 추가 |
| test_secure_string.cpp | +48 | 테스트 6건 |
| gateway_pipeline.hpp | -8/+7 | atomic→plain pointer, set_rate_limiter 초기화 전용 |
| gateway_service.hpp | -2/+1 | 생성자 파라미터 제거 |
| gateway_service.cpp | -3/+3 | add_adapter 레지스트리 통합, 수동 init 제거 |
| main.cpp | -7/+4 | standalone adapter → add_adapter chain |

## 리뷰 항목

### 1. SecureString::constant_time_equal() (BACKLOG-245)

**패턴**: XOR 누적 + volatile unsigned char accumulator
- `volatile unsigned char acc = 0` → 컴파일러 최적화 방지
- 바이트별 XOR 누적 → early-return 없이 전체 비교
- 길이 불일치 시 early return → 길이 정보 유출이나, 토큰 길이는 비밀이 아님 (업계 표준)
- OpenSSL CRYPTO_memcmp, libsodium sodium_memcmp과 동일 접근

**결론**: 이슈 없음

### 2. Gateway RedisAdapter add_adapter() 이관 (BACKLOG-250)

**변경**: standalone unique_ptr → Server::add_adapter("ratelimit") 레지스트리
- Server가 init/drain/close 라이프사이클 자동 관리
- on_configure(Phase 1)에서 어댑터 참조 획득 → 어댑터 초기화 이후이므로 안전
- 수동 `init()` 호출 제거 (이중 초기화 방지)
- 기존 Gateway 유닛 테스트는 PipelineBase<Mock> 사용 → 서비스 생성자 변경 무영향

**결론**: 이슈 없음

### 3. rate_limiter_ atomic 제거 (BACKLOG-251)

**변경**: `std::atomic<LimiterT*>` → `LimiterT*`
- set_rate_limiter()는 on_wire(main thread)에서 1회 호출, 이후 읽기 전용
- Server::run()이 on_wire ↔ core thread 간 happens-before 보장
- atomic memory ordering 불필요
- doc comment에 "initialization only" 명시 → 잘못된 hot-reload 사용 방지

**결론**: 이슈 없음

## 발견 이슈

| # | 심각도 | 파일 | 내용 | 조치 |
|---|--------|------|------|------|
| 1 | TRIVIAL | gateway_service.cpp:384 | init() 제거 후 빈 줄 잔존 | 즉시 수정 완료 |

## 요약

- 리뷰 이슈: 0건 (스타일 1건 즉시 수정)
- 백로그 신규: 0건
- 빌드+테스트: 97/97 통과
