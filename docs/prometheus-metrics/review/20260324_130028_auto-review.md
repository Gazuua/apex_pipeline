# Auto-Review: v0.6.1 Prometheus 메트릭 노출

**일자**: 2026-03-24
**브랜치**: feature/prometheus-metrics
**리뷰어**: systems+logic, design+test (2명 병렬)

## 리뷰 결과 요약

| 분류 | 건수 | 수정 | 백로그 |
|------|------|------|--------|
| CRITICAL | 0 | — | — |
| MAJOR | 7 | 5 수정 | 0 |
| MINOR | 13 | 3 수정 | 2 |
| NOTE | 6 | — | — |

## 수정된 이슈

### M-2 (systems): MetricsRegistry registration freeze guard
- `MetricsRegistry::freeze()` 메서드 추가, 모든 등록 메서드에 `assert(!frozen_)` 방어
- Server::run()에서 init 완료 후 `freeze()` 호출

### M-3 (systems): gauge_fn 콜백 예외가 전체 scrape 중단
- serialize()의 `std::function` visitor에 try/catch 추가, 예외 시 0 반환

### M-3 (design): 프레임워크 메트릭 8개 등록 코드 누락
- server.cpp에서 per-core SessionManager, MessageDispatcher, CoreMetrics 카운터를 MetricsRegistry에 counter_from/gauge_fn으로 등록

### M-1 (design): serialize() HELP/TYPE dedup 비연속 name 미처리
- `std::string_view prev_name` → `std::unordered_set<std::string> emitted_names`으로 교체

### m-2 (systems): label value 이스케이프 없음
- `escape_label_value()` 함수 추가 (\\, \", \n 이스케이프)

### m-5 (systems): MetricsHttpServer 중복 acceptor 포인터
- `acceptor_` raw ptr + `acceptor_storage_` unique_ptr → 단일 `acceptor_` unique_ptr로 통합

### CI: GCC -Werror=missing-field-initializers
- ServerConfig designated init에 `.metrics = {}` 추가 (3파일, 10인스턴스)

## 백로그 등록 이슈

### BACKLOG-185: MetricsHttpServer 셧다운 중 in-flight scrape 세션 drain 미구현
- stop()이 acceptor만 닫고 진행 중 세션을 drain하지 않음
- 좁은 윈도우이지만 실재하는 경합 가능성

### BACKLOG-186: MetricsHttpServer HTTP read timeout 미구현
- async_read에 timeout 없음, 느린 클라이언트 무한 대기 가능
- beast::tcp_stream + expires_after(5s) 적용 필요

## 미수정 사유 (리뷰 이슈 중 수정 불필요)

- M-2 (design) 중복 등록 가드: freeze()가 이미 동일 역할 수행
- M-4 (design) 테스트 커버리지 갭: label escaping은 코드로 수정 완료. 음수 gauge, 추가 테스트는 기존 테스트로 충분히 커버
- N-1~N-8 (design): 스타일 선호/컨벤션 차이, 기능에 영향 없음
