# apex-agent Full Auto-Review

- **날짜**: 2026-03-24
- **브랜치**: review/auto-review-20260324_153429
- **범위**: apex-agent Go 코드베이스 전수 리뷰 (19,000줄)
- **리뷰어**: logic, infra-security, test, design, docs-spec (5개 병렬)

## 통계

| 카테고리 | 발견 | 수정 | 보류 |
|----------|------|------|------|
| 코드 수정 | 16 | 16 | 0 |
| 테스트 개선 | 11 | 10 | 1 (E2E 범위 밖) |
| 문서 정합성 | 14 | 13 | 1 (LOW) |
| 설계/정책 | 7 | 0 | 7 (결정 대기) |
| **합계** | **48** | **39** | **9** |

## 코드 수정 상세 (16건)

### HIGH

1. **isGitBranchCreate flag 우회** — `git branch -v feature/foo` 같은 flag+non-flag 조합이 게이트 우회. scan 로직을 전체 인자 순회로 변경 + rename(-m/-c)/copy 차단 추가
2. **hasGoToolCommand 과도한 허용** — 첫 토큰 `go` fallback 제거. `go generate`(임의 명령 실행 가능) 등 차단. 명시적 서브커맨드만 허용
3. **HTTP 에러 메시지 내부 정보 노출** — `err.Error()` → generic "internal error" + 로그 기록
4. **Windows IsProcessAlive PID ≤ 0 가드 누락** — Unix와 동일한 `pid <= 0` 가드 추가

### MEDIUM

5. **IPC Client 기본 deadline 10초** — 30초로 증가 (export 등 무거운 요청 대응)
6. **Queue.Release RowsAffected 미확인** — 이중 release 시 warning 로깅 추가
7. **Queue Acquire cancel 시 ACTIVE 삭제** — WAITING만 삭제하도록 수정 (race condition 방지)
8. **cleanup 현재 브랜치 보호 누락** — `*` 접두사 줄 skip
9. **runGitCtx 불필요한 -C 분기** — 항상 `-C dir` append 사용
10. **backlog Update map 비결정성** — sort.Strings로 결정적 SQL 생성
11. **Timeframe 빈 문자열 CLI 우회** — Add()에서 빈 timeframe 거부
12. **NotifyStart backlog OPEN 복귀 warn** — 에러로 승격, 트랜잭션 롤백
13. **config addr 바인딩 주소 미검증** — localhost 외 주소 시 보안 경고
14. **SQL Sort Column 주석** — allowlist 안전성 주석 강화
15. **Daemon shutdown 이중 요청** — already_shutting_down 응답 구분
16. **IPC Client checkVersion 주석** — auto-start 책임 명확화

## 테스트 개선 상세 (10건)

1. cleanup_test: 테스트 이름/주석 명확화 + Run 에러 체크
2. daemon_test: time.Sleep → PID 파일 polling loop
3. ipc server_test: Write/ReadMessage 에러 t.Fatal
4. config_test: HTTP 설정 기본값/TOML 테스트 추가
5. ipc protocol_test: Response 에러 체크
6. backlog manage_test: TestAdd_InvalidEnum 추가
7. queue manager_test: TestAcquire_Timeout 추가
8. log_test: TestWarn/Error_WritesToBuffer 추가
9. store migrator_test: TestMigrate_FailureRollback 추가
10. (E2E http_test env.Stop() 이중 호출 — 범위 밖 스킵)

## 문서 정합성 상세 (13건)

| # | 문서 | 이슈 |
|---|------|------|
| 1 | apex-agent CLAUDE.md | `handoff check` 미존재 커맨드 삭제 |
| 2 | apex-agent CLAUDE.md | 상태 머신 kebab-case → UPPER_SNAKE_CASE |
| 3 | apex-agent CLAUDE.md | 마이그레이션 테이블 handoff v4,v5 추가 |
| 4 | apex-agent CLAUDE.md | 마이그레이션 테이블 queue v1~v3 추가 |
| 5 | apex-agent CLAUDE.md | 모듈 등록 순서 hook+queue 추가 |
| 6 | apex-agent CLAUDE.md | notify start job --branch-name/--scopes 추가 |
| 7 | apex-agent CLAUDE.md | --scopes 필수 표기 수정 |
| 8 | apex-agent CLAUDE.md | validate-build 허용 목록 gh pr/run 추가 |
| 9 | 루트 CLAUDE.md | 상태 머신 UPPER_SNAKE_CASE |
| 10 | 루트 CLAUDE.md | --backlog 복수 지원 표기 |
| 11 | 루트 CLAUDE.md | IsSourceFile 확장자 목록 .go 추가 |
| 12 | 루트 CLAUDE.md | git 차단 패턴 switch -c, worktree, -m/-c 추가 |
| 13 | apex_tools CLAUDE.md | BACKLOG_HISTORY CLI 경유로 변경 |

## 보류: 설계/정책 결정 필요 (7건)

1. NotifyDrop시 FIXING 백로그 자동 OPEN 복귀 여부
2. Hook fail-open vs fail-close 범위 (stdin 파싱 실패 시)
3. backlog→handoff 모듈 경계 위반 (cross-table 직접 접근) 리팩터링
4. httpd가 모듈 DB 직접 쿼리 리팩터링
5. CLI가 데몬과 별도 DB 연결 리팩터링
6. 미등록 브랜치에서 git commit 허용 정책
7. ctx 파라미터 전파 범위
