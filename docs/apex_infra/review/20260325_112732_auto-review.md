# Auto-Review — v0.6.3 Helm minikube 실배포 검증

> PR: #158 | 브랜치: feature/helm-minikube-validation
> 리뷰어: infra-security, docs-records (메인 취합)

## 직접 수정 (7건)

| # | 리뷰어 | 심각도 | 파일 | 이슈 | 수정 |
|---|--------|--------|------|------|------|
| 1 | docs | MINOR | progress 문서 | "신규 백로그" 섹션 잔류 (규칙 위반) | 섹션 삭제 — BACKLOG-216 이미 등록 |
| 2 | docs | MINOR | progress 문서 | "미해결 사항" 섹션 잔류 | TODO 성격 항목 삭제, 환경 제약으로 재구성 |
| 3 | docs | MINOR | 설계서 | 산출물 경로 `docs/v0.6.3-k8s-helm/` 오류 | `docs/apex_infra/`로 수정 |
| 4 | infra | MINOR | ci.yml | `setup-minikube@latest` 비고정 | `@v0.0.21` 버전 고정 |
| 5 | infra | MINOR | pgbouncer/values.yaml | probe 주석 `pg_isready` 잔류 | `tcpSocket port check`로 갱신 |
| 6 | infra | MINOR | local-setup.sh | `DOCKER_MEM_MB` 감지 실패 시 음수 메모리 | 기본값 2048 가드 추가 |
| 7 | main | MAJOR | pgbouncer/values.yaml | `tag: "latest"` — 배포 재현성 미보장 | ECR에서 `1.25.1` 태그 확인 후 고정 |

## 보고만 (2건)

| # | 리뷰어 | 심각도 | 파일 | 이슈 | 비고 |
|---|--------|--------|------|------|------|
| 8 | docs | MINOR | progress 문서 | S4-S5와 S8-S10에 동일 이슈 중복 기술 | 역참조 추가 권고 — 가독성 개선 |
| 9 | infra | MINOR | ci.yml + docker-bake.hcl | workflow_dispatch에서 feature 브랜치 bake 시 `:latest` 오염 가능 | BACKLOG-217 등록 |

## PASS 항목

- docker-bake.hcl context 경로 정합성
- `{{-` → `{{` 수정 일관성 (gateway 전용 `{{- if`는 정상 유지)
- PgBouncer tcpSocket probe 합리성
- namespace 충돌 해결 (`namespaces.create=false`)
- helm-validation 잡 구조 (2-release 순서, 디버그 로그, 타임아웃)
- ECR registry + allowInsecureImages (로컬 전용, 프로덕션 무영향)
- 크레덴셜 관리 (평문=로컬, existingSecret=프로덕션)
- 파일명 타임스탬프, progress 실질성, 설계-결과 정합성
