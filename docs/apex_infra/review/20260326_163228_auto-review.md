# Auto-Review: smoke test Chat Service 타임아웃 수정 (BACKLOG-241)

- **브랜치**: `bugfix/smoke-test-chat-timeout`
- **PR**: #197
- **일시**: 2026-03-26

## 변경 범위

| 파일 | 변경 | 목적 |
|------|------|------|
| `service.Dockerfile` | +1 패키지 | `curl` CLI 추가 (healthcheck용) |
| `docker-compose.smoke.yml` | +14줄 | gateway/auth-svc/chat-svc `/ready` healthcheck |
| `docker-compose.e2e.yml` | +14줄 | 동일 healthcheck 적용 |
| `auth_svc_docker.toml` | +4줄 | `[metrics]` 활성화 |
| `chat_svc_docker.toml` | +4줄 | `[metrics]` 활성화 |
| `e2e_test_fixture.cpp` | +85줄 | Chat Service warmup 프로브 |

## 리뷰 결과

### infra-security

| # | 항목 | 판정 | 비고 |
|---|------|------|------|
| 1 | `/ready` 엔드포인트가 서버 초기화 완료(`running_`) 기반 | OK | `metrics_http_server.cpp:36-42` |
| 2 | CMD-SHELL healthcheck — 포트 외부 노출 불필요 | OK | 컨테이너 내부 실행 |
| 3 | `curl` ↔ `libcurl4` 패키지 호환성 | OK | `curl` ⊃ `libcurl4` (Ubuntu 24.04) |
| 4 | metrics port 8081 서비스 간 충돌 | OK | 독립 컨테이너, 내부 포트 |
| 5 | Gateway healthcheck TCP→`/ready` 업그레이드 | OK | 초기화 완료 보장 강화 |

### logic / test

| # | 항목 | 판정 | 비고 |
|---|------|------|------|
| 1 | warmup 프로브 타임아웃 에러 처리 | OK | msg_id≠2008 → 재시도 |
| 2 | 독립 deadline (`chat_deadline`) | OK | login 프로브와 별도 30s |
| 3 | 예외 처리 (연결 실패, recv 타임아웃) | OK | catch-all + sleep 2s |
| 4 | recv timeout 8s > request_timeout 5s | OK | 타임아웃 응답 수신 보장 |

### 발견 이슈

없음.

### 참고 사항

- `libcurl4`는 `curl` 패키지의 의존성이므로 명시적 설치가 중복이지만, 기존 코드와 호환성 유지를 위해 그대로 둠
- Chat warmup 프로브는 Login → Authenticate → ListRooms 전체 파이프라인을 검증하므로, Auth Service와 Chat Service 양쪽 Kafka 컨슈머 준비 상태를 한 번에 확인
