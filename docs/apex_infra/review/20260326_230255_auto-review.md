# Auto-Review: ESO SecretStore Per-Service RBAC (BACKLOG-252)

- **브랜치**: `feature/eso-per-service-rbac`
- **PR**: #209
- **리뷰 모드**: task (변경 파일만)
- **리뷰어**: infra-security, docs-records (메인 직접)
- **서브에이전트**: 없음 (소규모 변경)

## 변경 요약

단일 SecretStore + SA 구조에서 서비스별 독립 SecretStore + ServiceAccount로 전환.
각 SA에 EKS IRSA용 IAM role annotation 지원.

| 파일 | 변경 |
|------|------|
| `templates/external-secret-store.yaml` | secrets map loop → per-service SA + SecretStore 생성 |
| `templates/external-secrets.yaml` | secretStoreRef 동적 참조 |
| `values-prod.yaml` | 글로벌 serviceAccount 제거, 서비스별 iamRoleArn 추가 |
| `docs/apex_infra/plans/` | 설계 문서 |

## 리뷰 항목

### Infra-Security

| # | 검토 항목 | 판정 | 상세 |
|---|----------|------|------|
| 1 | SA↔SecretStore 1:1 매핑 | PASS | `{{ $name }}-secrets-sa` ↔ `{{ $name }}-secret-store` 이름 일관성 확보 |
| 2 | IAM role annotation 조건부 생성 | PASS | `iamRoleArn` 빈 문자열 시 annotation 생략 — fail-closed (보안 안전) |
| 3 | automountServiceAccountToken | PASS | ESO가 SA projected token으로 IRSA 인증, 기본값(true) 정확 |
| 4 | Helm 업그레이드 전이 | PASS | 동일 release에서 원자적 적용, 구 SecretStore 자동 제거 |
| 5 | 로컬 환경 호환 | PASS | `externalSecrets.enabled: false` 시 모든 리소스 생성 스킵 |
| 6 | values-prod.yaml 주석 표기 | INFO | `{{ name }}` 표기가 YAML values에서 약간 혼란 가능하나 의도 전달에 문제 없음 |

### Docs-Records

| # | 검토 항목 | 판정 |
|---|----------|------|
| 1 | 설계 문서 포맷 | PASS — 문제/선택/변경범위/결과 구조 완비 |
| 2 | 옵션 비교표 | PASS — 3개 옵션 비교 + 채택 근거 명시 |

## 결과

- **발견 이슈**: 0건
- **수정**: 없음
- **신규 백로그**: 없음
