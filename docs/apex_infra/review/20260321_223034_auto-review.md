# Auto-Review: CI Docker Hardening

**브랜치**: `feature/ci-docker-hardening`
**모드**: task (변경 파일만)
**리뷰어**: infra-security (1명)
**일시**: 2026-03-21

---

## 리뷰 범위

| 항목 | 값 |
|------|---|
| 변경 파일 | 11개 (인프라 설정 전용, 소스 코드 없음) |
| 백로그 | BACKLOG-49, BACKLOG-122, BACKLOG-121 |
| 영역 | Docker, CI workflow, compose, .dockerignore |

---

## 발견 이슈 (7건)

### CRITICAL — 1건

**C-1. `.dockerignore`에서 `CMakePresets.json` 누락**
- **파일**: `.dockerignore`
- **내용**: `*` + whitelist 방식인데 `!CMakePresets.json`이 빠져있어 서비스 Dockerfile(`COPY . /src` → `cmake --preset debug`)이 빌드 실패
- **조치**: ✅ 수정 — `!CMakePresets.json` 추가

### MAJOR — 2건

**M-1. `docker-build.yml` ci-image-tag 갱신이 PR 트리거에서 실패**
- **파일**: `.github/workflows/docker-build.yml:38-46`
- **내용**: `workflow_call`이 PR에서도 호출되는데, PR의 `actions/checkout`은 detached HEAD(`refs/pull/N/merge`)라서 `git push` 실패. PR에서 ci-image-tag 갱신 자체가 무의미
- **조치**: ✅ 수정 — `if: github.event_name == 'push' && github.ref == 'refs/heads/main'` 조건 추가

**M-2. bitnami pgbouncer 타임아웃/pool 설정 누락 (DoS 벡터)**
- **파일**: `apex_infra/docker-compose.yml:150-155`
- **내용**: edoburu → bitnami 전환 시 `min_pool_size`, `max_db_connections`, `query_wait_timeout`, `server_idle_timeout`, `client_idle_timeout` 환경변수 매핑 누락. bitnami 기본값이 0(무제한)이라 유휴 연결 미해제 → pool 고갈 위험
- **조치**: ✅ 수정 — 이전 pgbouncer.ini 값과 동일한 환경변수 5개 추가

### MINOR — 3건

**m-1. bitnami/pgbouncer 버전 다운그레이드 (1.23.1→1.23.0)**
- **조치**: ✅ 수정 — `bitnami/pgbouncer:1.23.1` 존재 확인 후 버전 올림

**m-2. pgbouncer.ini + userlist.txt 고아 파일**
- **조치**: ✅ 수정 — 삭제 (bitnami 전환으로 미사용, 크레덴셜 위생)

**m-3. ubuntu:24.04 rolling 태그**
- **조치**: 무시 — 보안 패치 자동 수신과 재현성 trade-off. 현재 접근 합리적

### INFO — 1건

**i-1. Redis 4인스턴스 AUTH 미설정**
- **조치**: 무시 — 기존 이슈, BACKLOG-8에서 다룸

---

## 통계

| 구분 | 건수 |
|------|------|
| 발견 | 7 |
| 수정 | 5 (C-1, M-1, M-2, m-1, m-2) |
| 무시 | 2 (m-3, i-1) |
| 백로그 | 0 (기존 항목으로 커버) |
