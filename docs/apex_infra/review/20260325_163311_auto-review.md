# Auto-Review: helm-validation CreateContainerConfigError 수정

- **일시**: 2026-03-25 16:33
- **브랜치**: `bugfix/helm-securitycontext-fix`
- **백로그**: BACKLOG-230
- **모드**: task (변경 파일만)
- **리뷰어**: 메인 직접 (3파일/5줄 소규모 변경)

## 변경 요약

PR #163에서 추가된 `securityContext(runAsNonRoot: true)`가 CI helm-validation에서
`CreateContainerConfigError`를 유발. `minikube image load` 시 Docker IMAGE USER 메타데이터가
불완전하게 보존되어 kubelet이 root로 간주, runAsNonRoot 검증 실패.

## 변경 파일

| 파일 | 변경 내용 |
|------|-----------|
| `apex_infra/docker/service.Dockerfile` | `groupadd --gid 10001` + `useradd --uid 10001` 고정 |
| `apex_infra/k8s/apex-pipeline/charts/apex-common/templates/_helpers.tpl` | `runAsUser: 10001`, `runAsGroup: 10001` 추가 |
| `.github/workflows/ci.yml` | Debug 스텝에 `kubectl describe pod` 추가 |

## 리뷰 항목

| # | 관점 | 판정 | 상세 |
|---|------|------|------|
| 1 | Dockerfile UID 범위 | PASS | 10001은 시스템(100-999)/일반(1000+) UID와 비충돌 |
| 2 | GID/UID 일관성 | PASS | groupadd --gid 10001 + useradd --uid 10001 --gid apex |
| 3 | Helm YAML 정합성 | PASS | helm template 렌더링 3서비스 모두 정상 |
| 4 | values 오버라이드 | PASS | `.Values.securityContext` 분기로 서비스별 커스텀 가능 |
| 5 | CI debug 안전성 | PASS | `2>/dev/null \|\| true`로 실패 시 워크플로우 중단 방지 |
| 6 | Valgrind stage 호환 | PASS | USER root → USER apex 순서 유지, UID 변경 무영향 |
| 7 | docker-compose.e2e 호환 | PASS | Docker 직접 사용, Helm 미관여 |
| 8 | values-prod.yaml 영향 | PASS | 기본값 적용, 프로덕션 오버라이드 가능 |

## 결론

이슈 0건. 코드 수정 불필요.
