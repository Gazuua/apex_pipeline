# v0.6.2 Docker 멀티스테이지 빌드 — Auto-Review

**일자**: 2026-03-24
**브랜치**: feature/docker-multistage-build
**리뷰어**: infra-security, docs-spec

## 리뷰 통계

| 지표 | 값 |
|------|---|
| 리뷰어 수 | 2 |
| 직접 수정 | 5건 |
| 백로그 등록 | 1건 (BACKLOG-190) |
| 허용 판정 | 4건 |

## 직접 수정 이슈

### 1. [보안-HIGH] Trivy action `@master` → `@0.31.0` 핀닝
- **파일**: `.github/workflows/ci.yml`
- **문제**: 공급망 공격 벡터. 다른 모든 action은 버전 핀닝인데 Trivy만 `@master`
- **수정**: `@0.31.0`으로 핀닝

### 2. [보안-LOW] .dockerignore key/crt 파일 제외
- **파일**: `.dockerignore`
- **문제**: `!apex_services/` 허용으로 로컬 개발 시 생성된 key/crt 파일이 빌드 컨텍스트에 유입 가능
- **수정**: `gateway/certs/`, `**/*.key`, `**/*.crt` 제외 규칙 추가

### 3. [문서] README 완료 이력 누락
- **파일**: `README.md`
- **문제**: v0.6.1.0, v0.6.2.0 등 5개 완료 항목 미기재
- **수정**: 완료 이력 추가

### 4. [문서] README "다음" 섹션 구체화
- **파일**: `README.md`
- **문제**: v0.6 전체를 가리키고 있었으나 v0.6.1/v0.6.2 완료 상태
- **수정**: v0.6.3 → v0.6.4 → v1.0.0.0으로 구체화

### 5. [최소권한] scan 잡 불필요 permission 제거
- **파일**: `.github/workflows/ci.yml`
- **문제**: `security-events: write` 권한이 있으나 SARIF 업로드 미사용
- **수정**: 해당 권한 라인 제거

## 백로그 등록

### BACKLOG-190: docker-compose.e2e.yml RSA 키 volume 경로 불일치
- **등급**: MINOR / IN_VIEW
- **내용**: volume mount `/app/keys/`와 config.toml의 상대경로 불일치. 호스트 실행 CI에는 미영향, Docker 기반 E2E 시 문제

## 허용 판정 (수정 불필요)

| # | 이슈 | 판단 |
|---|------|------|
| 1 | MSVC `continue-on-error: true` | 설계 의도 (안정화 후 제거 예정) |
| 2 | tools-image-tag 초기값 `latest` | 첫 docker-images.yml 실행 시 자동 갱신 |
| 3 | 스펙 RSA 경로 `../` vs `../../` | 스펙은 참조용, 실제 구현 정확 |
| 4 | ci.Dockerfile BuildKit cache mount 미적용 | Docker 레이어 캐시로 충분히 커버 |

## 양호 사항

- non-root 실행 (`USER apex`), 최소 권한 워크플로우 (`permissions: {}`)
- 멀티스테이지 빌드로 소스/빌드 아티팩트 runtime 미포함
- RSA 키 이미지에서 완전 제거, 런타임 volume mount `:ro`
- ENTRYPOINT `exec` PID 1 처리, apt 캐시 정리
- sccache 프리셋 3종(configure/build/test) 정합성 확인
- vcpkg 캐시 경로 CI 프리셋과 일치
