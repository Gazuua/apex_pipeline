#include <apex/chat_svc/chat_service.hpp>

#include <spdlog/spdlog.h>

#include <cstdlib>

int main([[maybe_unused]] int argc, [[maybe_unused]] char* argv[]) {
    spdlog::info("Chat Service starting...");

    // TODO: TOML config loading
    // TODO: Server instance + adapter registration + ChatService start
    // Scaffolding -- build verification only

    spdlog::info("Chat Service placeholder -- exiting.");
    return EXIT_SUCCESS;
}
