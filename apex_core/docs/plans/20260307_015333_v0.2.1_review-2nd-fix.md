# v0.2.1 2차 코드 리뷰 수정 계획서

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 2차 코드 리뷰 43건 전체 수정 (Critical 4 + Important 21 + Suggestion 18)

**Architecture:** 4개 병렬 에이전트가 레이어별로 분담. 코드 수정 에이전트 3개 + 테스트 에이전트 1개.

**Tech Stack:** C++23, Boost.Asio, FlatBuffers, GTest, MSVC 19.44, Ninja

---

## Task 1: 코어 인프라 수정 (8건)

**Files:**
- Modify: `include/apex/core/timing_wheel.hpp`
- Modify: `src/timing_wheel.cpp`
- Modify: `include/apex/core/mpsc_queue.hpp`
- Modify: `include/apex/core/slab_pool.hpp`
- Modify: `include/apex/core/detail/math_utils.hpp`
- Modify: `include/apex/core/ring_buffer.hpp`

### C-1: TimingWheel schedule() 범위 검증
`timing_wheel.cpp:53` — schedule()에 assert 추가:
```cpp
TimingWheel::EntryId TimingWheel::schedule(uint32_t ticks_from_now) {
    assert(ticks_from_now < num_slots_ && "ticks_from_now must be < num_slots");
```

### I-6: TimingWheel tick() 벡터 힙 할당 → 멤버 변수
`timing_wheel.hpp` — private에 `std::vector<Entry*> expired_buf_;` 추가
`timing_wheel.cpp:113` — 지역 `std::vector<Entry*> expired` → `expired_buf_.clear()` + `expired_buf_` 사용

### I-7: TimingWheel schedule() new Entry → 재사용 불가 시 문서화
현재 reschedule이 주 사용 패턴(하트비트)이므로, new/delete 대신 주석으로 성능 특성을 문서화.
`timing_wheel.hpp:37` schedule() 주석에 "매 호출 시 힙 할당 발생. 빈번한 타임아웃 갱신은 reschedule()을 사용할 것." 추가.

### I-8: MpscQueue Slot — 문서화 (벤치마크 전 패딩 추가 보류)
`mpsc_queue.hpp:63` Slot 구조체에 주석 추가:
```cpp
// NOTE: 작은 T에서 인접 슬롯 간 false sharing 가능성 있음.
// 성능 크리티컬 시 벤치마크 후 alignas(64) 패딩 고려.
struct Slot {
```

### I-12: SlabPool double-free — 문서화
`slab_pool.hpp:41` deallocate() 주석 보강:
```cpp
/// @pre ptr은 반드시 이 풀에서 allocate()로 할당된 포인터여야 한다.
/// @warning Release 빌드에서는 소유권 검증이 비활성화됨 (assert 기반).
/// double-free 시 정의되지 않은 동작.
```

### S-1: math_utils overflow 반환 계약 주석
`math_utils.hpp:7` 함수 주석 추가:
```cpp
/// @return overflow 시 0 반환. 호출자가 반드시 0 검사 후 throw/reject 해야 한다.
```

### S-2: TypedSlabPool destroy() noexcept static_assert
`slab_pool.hpp:98`:
```cpp
void destroy(T* ptr) noexcept {
    static_assert(std::is_nothrow_destructible_v<T>,
        "T destructor must be noexcept for TypedSlabPool::destroy");
```

### S-3: RingBuffer linear_buf_ 초기화 일관성
`ring_buffer.hpp:65` — `uint8_t* linear_buf_{nullptr};` (in-class initializer로 통일)

---

## Task 2: 프레임워크 수정 (9건)

**Files:**
- Modify: `include/apex/core/service_base.hpp`
- Modify: `include/apex/core/server.hpp`
- Modify: `src/core_engine.cpp`
- Modify: `src/error_sender.cpp`
- Modify: `src/wire_header.cpp`
- Modify: `include/apex/core/message_dispatcher.hpp`

### C-2: ServiceBase self pointer — 수명 제약 주석 명시 + stop()에서 핸들러 ID 추적
`service_base.hpp:72` handle()/route() 앞에 주석 추가:
```cpp
/// @warning 핸들러 람다가 this(Derived*) raw pointer를 캡처함.
/// 서비스 수명이 디스패처 수명보다 길거나 같아야 한다.
/// Server 사용 시 자동 보장됨 (서비스와 디스패처를 동시 소유).
```
또한 stop()에서 등록된 핸들러를 unregister하도록 `registered_msg_ids_` 벡터 추가:
```cpp
private:
    std::vector<uint16_t> registered_msg_ids_;
```
handle()/route()에서 `registered_msg_ids_.push_back(msg_id)` 추가.
stop()에서:
```cpp
void stop() override {
    started_ = false;
    for (auto id : registered_msg_ids_) {
        dispatcher_->unregister_handler(id);
    }
    registered_msg_ids_.clear();
    on_stop();
}
```

### C-3: Server::Config clang 호환 — 클래스 외부로 이동
`server.hpp` — `Config`를 `Server` 클래스 밖(namespace apex::core 내)으로 이동:
```cpp
struct ServerConfig {
    uint16_t port = 9000;
    uint32_t heartbeat_timeout_ticks = 300;
    uint32_t tick_interval_ms = 100;
    size_t recv_buf_capacity = 8192;
    size_t timer_wheel_slots = 1024;
};

class Server {
public:
    using Config = ServerConfig;
    explicit Server(boost::asio::io_context& io_ctx, Config config = {});
```

### I-9: ServiceBase owned_dispatcher_ → unique_ptr (lazy)
`service_base.hpp:106`:
```cpp
std::unique_ptr<MessageDispatcher> owned_dispatcher_;
MessageDispatcher* dispatcher_{nullptr};
```
생성자에서 `owned_dispatcher_` 생성, `dispatcher_ = owned_dispatcher_.get()`.
bind_dispatcher에서 `owned_dispatcher_.reset()`.

### I-10: CoreEngine run() 중복 호출 방지
`core_engine.cpp:52`:
```cpp
void CoreEngine::run() {
    if (running_.exchange(true, std::memory_order_acq_rel)) {
        throw std::logic_error("CoreEngine::run() already running");
    }
```

### I-11: ErrorSender WireHeader version 명시
`error_sender.cpp:31`:
```cpp
WireHeader header{
    .version = WireHeader::CURRENT_VERSION,
    .msg_id = original_msg_id,
```

### S-4: WireHeader byte-swap 주석
`wire_header.cpp:10` 앞:
```cpp
// NOTE: 호스트가 little-endian임을 가정 (x86/x64 전용).
// big-endian 포팅 시 std::endian 체크 추가 필요.
```

### S-5: ServiceBase route() FlatBuffers 검증 실패 시 로깅 TODO
`service_base.hpp:98`:
```cpp
if (!verifier.VerifyBuffer<FbsType>()) {
    // TODO: 로깅 프레임워크 도입 후 검증 실패 로그 추가
    co_return;
}
```

### S-6: CoreEngine drain 콜백 ctx 섀도잉 제거
`core_engine.cpp:130` — `auto& ctx` → `auto& core_ctx`:
```cpp
auto& core_ctx = *cores_[core_id];
while (auto msg = core_ctx.inbox->dequeue()) {
```

### S-10: MessageDispatcher move(session) 의도 주석
`message_dispatcher.cpp:26` 앞 (이미 있는 코드에 주석만 추가):
```cpp
// session은 값으로 받았으므로 move해도 호출자에 영향 없음
co_await handler(std::move(session), msg_id, payload);
```

---

## Task 3: 네트워크/세션 수정 (10건)

**Files:**
- Modify: `include/apex/core/session.hpp`
- Modify: `include/apex/core/tcp_acceptor.hpp`
- Modify: `include/apex/core/server.hpp`
- Modify: `src/server.cpp`
- Modify: `src/tcp_acceptor.cpp`
- Modify: `src/session_manager.cpp`
- Modify: `src/frame_codec.cpp`
- Modify: `examples/echo_server.cpp`
- Modify: `examples/chat_server.cpp`

### I-1: Server stop()/start_tick_timer() this 캡처 — 수명 제약 주석
`server.cpp:58`:
```cpp
// NOTE: post된 핸들러가 실행되기 전 Server가 소멸하면 UB.
// 전제 조건: io_ctx.run()이 완료된 후에만 Server를 소멸시킬 것.
void Server::stop() {
```
`server.cpp:151`:
```cpp
// NOTE: [this] 캡처 — Server가 tick_timer_보다 오래 살아야 함.
// 소멸자에서 tick_timer_.cancel()로 보장됨.
void Server::start_tick_timer() {
```

### I-2: Session set_state() → private + friend
`session.hpp:61`:
```cpp
private:  // set_state를 private으로 이동
    void set_state(State s) noexcept { state_ = s; }
    friend class SessionManager;
```

### I-3: TcpAcceptor/Server running_ → atomic<bool>
`tcp_acceptor.hpp:45`: `bool running_{false}` → `std::atomic<bool> running_{false}`
`server.hpp:84`: `bool running_{false}` → `std::atomic<bool> running_{false}`
`server.hpp` 상단에 `#include <atomic>` 추가
관련 메서드에서 `.load()`/`.store()` 사용으로 변경.

### I-4: FrameCodec try_decode() — 헤더를 로컬 배열로 복사
`frame_codec.cpp:14-25`:
```cpp
// 헤더를 로컬 배열로 복사 — 이후 linearize() 호출이 이 데이터를 무효화하지 않음
std::array<uint8_t, WireHeader::SIZE> hdr_buf;
auto hdr_span = buf.linearize(WireHeader::SIZE);
std::memcpy(hdr_buf.data(), hdr_span.data(), WireHeader::SIZE);
auto parse_result = WireHeader::parse(hdr_buf);
```

### I-5: Server TMP_BUF_SIZE vs recv_buf_capacity 런타임 검증
`server.cpp:18` 생성자에 assert 추가:
```cpp
Server::Server(boost::asio::io_context& io_ctx, Config config)
    : io_ctx_(io_ctx)
    , config_(config)
    ...
{
    assert(config_.recv_buf_capacity >= TMP_BUF_SIZE &&
           "recv_buf_capacity must be >= TMP_BUF_SIZE");
}
```

### I-13: SessionManager on_timer_expire close() 순서 주석
`session_manager.cpp:106`:
```cpp
// 콜백에서 세션에 마지막 작업(로깅 등)을 수행할 수 있도록
// close()는 콜백 이후에 호출한다.
if (timeout_callback_) {
```

### S-7: process_frames payload span SAFETY 주석 보강
`server.cpp:135-136`:
```cpp
// SAFETY: 단일 io_context 스레드에서 read_loop → process_frames가 순차 실행되므로,
// co_await 중에도 이 세션의 recv_buf에 새 데이터가 쓰이지 않는다.
// 따라서 linearize() 결과인 frame->payload span은 consume_frame() 전까지 유효하다.
```

### S-8: async_send payload span @pre 주석
`session.hpp:38-39`:
```cpp
/// @pre payload 데이터는 co_await 완료 시점까지 유효해야 한다.
///      (코루틴 프레임이 복사를 보장하지 않음)
```

### S-9: TcpAcceptor accept 에러 재시도 TODO 주석
`tcp_acceptor.cpp:48`:
```cpp
// TODO: 일시적 에러(EMFILE 등) 시 exponential backoff 추가 고려
continue;
```

### S-11: echo/chat server std::atoi → 안전한 파싱
`echo_server.cpp:62`, `chat_server.cpp:79`:
```cpp
if (argc >= 2) {
    int p = std::atoi(argv[1]);
    if (p > 0 && p <= 65535) port = static_cast<uint16_t>(p);
}
```

---

## Task 4: 테스트 추가 (16건)

**Files:**
- Create: `tests/test_helpers.hpp`
- Modify: `tests/unit/test_message_dispatcher.cpp`
- Modify: `tests/unit/test_session.cpp`
- Modify: `tests/unit/test_session_manager.cpp`
- Modify: `tests/unit/test_slab_pool.cpp`
- Modify: `tests/unit/test_core_engine.cpp`
- Modify: `tests/unit/test_service_base.cpp`
- Modify: `tests/unit/test_timing_wheel.cpp`
- Modify: `tests/unit/test_ring_buffer.cpp`
- Modify: `tests/unit/test_frame_codec.cpp`
- Modify: `tests/unit/test_tcp_acceptor.cpp`
- Modify: `tests/unit/test_flatbuffers_dispatch.cpp`
- Modify: `tests/integration/test_pipeline_integration.cpp`

### TI-7/TI-8: run_coro + make_socket_pair 헬퍼 추출
`tests/test_helpers.hpp` 생성. 4개 파일에서 중복 제거.

### TC-1: HandlerFailed 테스트
### TI-1: async_send_raw 테스트
### TI-2: 이중 close() 테스트
### TI-3: for_each 테스트
### TI-4: SlabPool initial_count=0 예외 테스트
### TI-5: CoreEngine io_context/post_to 경계 테스트
### TI-6: ServiceBase bind_dispatcher 외부 바인딩 테스트
### TS-1: TimingWheel 큰 타임아웃 테스트
### TS-2: TimingWheel 콜백 재진입 테스트
### TS-3: RingBuffer full/capacity=1 테스트
### TS-4: FrameCodec encode wrap-around 테스트
### TS-5: TcpAcceptor IPv6/이중 start 테스트
### TS-6: (보류) Server E2E sleep → event-based 전환은 별도 이터레이션으로
### TS-7: SessionManager invalid id 안전성 테스트

---

## 빌드 및 검증

```bash
build.bat debug
```
Expected: 빌드 성공, 테스트 전체 통과

## 코드 리뷰

빌드+테스트 통과 후 코드 리뷰 에이전트 실행. Clean 판정까지 반복.
