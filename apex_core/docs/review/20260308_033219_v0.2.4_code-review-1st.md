# v0.2.4 코드 리뷰 1차 보고서

**날짜:** 2026-03-08
**리뷰 범위:** feature/core-integration 브랜치 전체 (16 files, +817/-357)
**리뷰 방식:** 병렬 3-에이전트 (Core Engine / Server & Network / Docs & Examples)

## 리뷰 결과 요약

### Critical (2건 → 모두 수정)

| # | 이슈 | 파일 | 수정 |
|---|------|------|------|
| C-1 | cross_core_call result가 CAS 이후 기록 → 데이터 레이스 | cross_core_call.hpp | ✅ result를 CAS 이전에 기록 |
| C-1' | IOCP 소켓 바인딩 — accept_io_ 병목 | tcp_acceptor.cpp | ⏳ TODO 주석 (성능 이슈, 정확성 OK) |

**C-1' 분석:** `use_awaitable`이 `this_coro::executor`로 코루틴 resume을 dispatch하므로 correctness 문제 없음. 다만 모든 I/O completion이 accept_io_를 경유하는 extra hop 발생. `async_accept(target_executor)` 적용은 TcpAcceptor API 변경이 필요하므로 별도 태스크로 추적.

### Important (7건 → 모두 수정)

| # | 이슈 | 수정 |
|---|------|------|
| I-2 (Core) | 타임아웃 후 task 실행 → 댕글링 참조 | ✅ early status check 추가 |
| I-3 (Core) | void 오버로드도 동일 순서 문제 | ✅ 동일 패턴 적용 |
| I-1 (Server) | stop()/running_ 의미 불명확 | ✅ 주석 추가 |
| I-2 (both) | ServerConfig.bind_address 누락 | ✅ deferred 주석 |
| I-3 (both) | chat_server placeholder → 미동작 | ✅ add_service_factory로 복구 |
| M-1 (Server) | tick_interval_ms 미사용 | ✅ 필드 제거 |
| I-4 (Core) | shutdown 중 cross_core_call 보호 부재 | ⏳ 현재 테스트 커버리지로 충분, 향후 보강 |

### Minor (미수정, 인지)

- M-2: next_core_ uint32 overflow (40억+ 연결에서만)
- M-5: CoreEngine run() deprecated 표기 누락
- M-6: 테스트 sleep 기반 동기화 (Server ready 이벤트 필요)
- M-3 (Docs): EchoService 중복 (예제 자기 완결성 의도)
- M-4 (Docs): multicore_echo_server <thread> include (교육적 가치)

## 테스트 결과

```
20/20 tests passed, 0 tests failed
Total Test time: ~9s
```

## 설계 문서 정합성

대부분 일치. 주요 불일치:
- async_accept(target_executor) → TODO로 추적
- bind_address → deferred 주석
- tick_interval_ms → 제거 (drain_interval에 통합)
- cross_core_call 소유권 → shared_ptr 기반 (설계 대비 개선)
